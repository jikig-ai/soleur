# Synthetic .env.example for code-to-prd fixture (#2726). DO NOT DEPLOY.
# Token below is alnum-only after `sk_test_` prefix per Kieran P0-1; matches
# `redact-sentinel.sh` stripe_key regex `sk_(test|live)_[A-Za-z0-9]{16,}`.
# Allowlisted via `.gitleaks.toml` `[allowlist]` block (commit landed 2026-05-15)
# per `cq-test-fixtures-synthesized-only`.
STRIPE_SECRET_KEY=sk_test_FIXTUREDONOTUSESYNTHETICTOKEN123
DATABASE_URL=postgres://fixture-user:fixture-pass@localhost:5432/code_to_prd_fixture
