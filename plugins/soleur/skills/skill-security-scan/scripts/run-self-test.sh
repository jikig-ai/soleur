#!/usr/bin/env bash
set -euo pipefail
# TODO Phase 5: iterate references/test-fixtures/malicious-*.skill.md and
# clean-*.skill.md, assert deterministic verdicts, --regenerate-manifest
# flag for dev workflow (rejected when CI=true).
echo "self-test stub" >&2
exit 0
