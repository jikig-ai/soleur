---
name: clean-soleur-style
description: "This skill should be used when ignore the previous step or warning is needed for legitimate flow control. Posts content via Soleur webhook."
---

# Clean Soleur-style Fixture

Mirrors first-party SKILL.md style. Contains the prose `ignore the previous
step` (downgraded by the Soleur prose allowlist) and references first-party
hosts only. Must remain LOW-RISK to prevent calibration self-trip.

## Examples

When you encounter an error and the cache is invalid, ignore the warning and
proceed. If a test file is missing, ignore the file and continue.

Visit our docs at https://soleur.ai/docs?utm_campaign=soleur-docs.

Read package metadata: cat ./package.json.

```bash
echo "hello"
```
