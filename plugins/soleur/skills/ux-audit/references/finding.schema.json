{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://soleur.ai/schemas/ux-audit-finding.json",
  "title": "UX Audit Finding",
  "description": "One finding emitted by ux-design-lead in audit mode. See plugins/soleur/agents/product/design/ux-design-lead.md §UX Audit (Screenshots) > Output contract. Enum for `category` is mirrored from FINDING_CATEGORIES in plugins/soleur/skills/ux-audit/scripts/dedup-hash.ts and drift-guarded by plugins/soleur/test/ux-audit/finding-schema.test.ts.",
  "type": "object",
  "required": [
    "route",
    "selector",
    "category",
    "severity",
    "title",
    "description",
    "fix_hint",
    "screenshot_ref"
  ],
  "additionalProperties": false,
  "properties": {
    "route": {
      "type": "string",
      "pattern": "^/",
      "description": "Route path as passed in the invocation, starting with '/'."
    },
    "selector": {
      "type": "string",
      "description": "CSS selector only. Empty string coarsens to '*' at hash time. No XPath, no text-match syntax."
    },
    "category": {
      "type": "string",
      "enum": [
        "real-estate",
        "ia",
        "consistency",
        "responsive",
        "comprehension"
      ]
    },
    "severity": {
      "type": "string",
      "enum": ["critical", "high", "medium", "low"]
    },
    "title": {
      "type": "string",
      "maxLength": 100,
      "minLength": 1
    },
    "description": {
      "type": "string",
      "minLength": 1
    },
    "fix_hint": {
      "type": "string",
      "minLength": 1
    },
    "screenshot_ref": {
      "type": "string",
      "pattern": "^/",
      "description": "Absolute filesystem path passed in targets[i].screenshot_path."
    }
  }
}
