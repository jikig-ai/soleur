---
component: skills
updated: 2026-08-06
primary_location: src/skills/
---

# Skills

Synthesized fixture in Soleur's own prose shape — this one omits the
`**Internal**` line from `## Dependencies` entirely, matching the real
`ci-guards.md` case the plan calls out (a doc with no `**Internal**` line at all,
which is why doc-count does not imply connected-element count).

## Dependencies

- **External**: none
