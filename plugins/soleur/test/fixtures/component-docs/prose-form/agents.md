---
component: agents
updated: 2026-08-06
primary_location: src/agents/
---

# Agents

Synthesized fixture reproducing Soleur's OWN component-doc shape: no
`dependencies:` frontmatter, and an `**Internal**` line whose content is prose
rather than links. There is nothing here for either parse path to recover, which
is the corpus AC2 requires — elements render, relationships are zero, and the run
must report **degraded** rather than success.

## Dependencies

- **Internal**: None (agents are standalone)
- **External**: none
