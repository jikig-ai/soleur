---
component: commands
updated: 2026-08-06
primary_location: src/commands/
---

# Commands

Synthesized fixture in Soleur's own prose shape. The `**Internal**` line names
other components in plain prose with no markdown links, so the fallback link
extractor recovers nothing from it.

## Dependencies

- **Internal**: Skills (workflow stages), Agents
- **External**: none
