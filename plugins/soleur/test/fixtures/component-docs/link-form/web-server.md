---
component: web-server
updated: 2026-08-06
primary_location: src/web/
---

# Web Server

Synthesized fixture in the PRE-CONTRACT shape: no `dependencies:` frontmatter,
internal dependencies expressed only as markdown links in the prose line. The
parser's fallback path must recover these.

## Dependencies

- **Internal**: [database](database.md), [cache-layer](cache-layer.md)
- **External**: fastify
