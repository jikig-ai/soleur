---
component: legacy-worker
updated: 2026-08-06
primary_location: src/legacy/
status: deprecated
dependencies:
  - job-queue
---

# Legacy Worker

Synthesized fixture. Removed component, retained per sync's contract ("Removed
components: Add `status: deprecated` to frontmatter (do not delete)"). Without a
frontmatter filter the element set accretes deleted components forever, and this
doc's own declared dependency creates an edge from a component that no longer
exists.

## Dependencies

- **Internal**: job-queue
- **External**: none
