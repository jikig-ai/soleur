# rule-weakening-acks.txt — CODEOWNERS-owned WORM ack log (ADR-092).
#
# Append-only. Every change or deletion of an `hr-*`/`wg-*` rule BODY line in
# AGENTS.rules.md must be accompanied by a matching ack here, or the
# required `rule-body-lint` CI check BLOCKS the merge.
#
# Format (one per line; `#` comments + blank lines ignored):
#   <id>|<sha256>|<date>|<PR>|<reason>
# where <sha256> is the NEW normalized body hash from .claude/rule-body-hashes.txt
# after running `python3 scripts/lint-rule-bodies.py --write`. For a DELETION the
# sha256 field is the literal token `DELETED`.
#
# NEVER edit or remove an existing line (WORM). To weaken a rule:
#   1. edit the body line in the sidecar
#   2. python3 scripts/lint-rule-bodies.py --write   # regenerate the manifest
#   3. append the ack line below with the new hash + a real reason
hr-when-in-a-worktree-never-read-from-bare|c6e8f0c198680f7e0b7ccbedf03d962b6b7fae861f04c1dcb2a9027ac0745a07|2026-07-07|6183|Added non-bare Concierge caveat + ADR-099 pointer (clarification, not a weakening — the rule's bare-root guidance is unchanged; the caveat prevents mis-applying it to the non-bare agent workspace)
wg-at-session-start-run-bash-plugins-soleur|eed77e1cfbd5678fb364995abec0c83bd0f6abaa5382d3ea9cc825e1a601a302|2026-07-07|6183|Added non-bare Concierge caveat + ADR-099 pointer (clarification, not a weakening — session-start hygiene guidance unchanged)
hr-pipeline-skills-never-inline-after-go-route|b9e25473eb36c56dfc0aa07e7f09ccbaa4b936e73915aaa1288f2bf40b29fd1e|2026-07-11|6347|consolidated lifecycle/merge/BEHIND into harness rule (workflow-fidelity.ts) — clarification not weakening
wg-block-pr-ready-on-undeferred-operator-steps|9739a528c692e513a2e4e2f3248aecf6339e516a22582e1de917fe97cdd136fc|2026-07-11|6347|demoted wg-* core→rest for B_ALWAYS headroom; ship Phase 5.5 gate unchanged
hr-prod-host-config-change-immutable-redeploy|cc3b752f99ec2d39e31fbb886178f5a2b530e2db7cce008512e3ccad8539001b|2026-07-15|6457|Named the no-rollback danger: -replace destroys before it creates, so a failed create (DC stock) strands the fleet. Strengthening, not a weakening (ADR-092 Decision 2 requires the ack regardless). Trimmed prose to stay under PER_RULE_CAP; rule id and existing guidance unchanged.
wg-when-an-audit-identifies-pre-existing|f7664416f6cf7eb36ce7d2e5977f904069e1811c29928a9853f81df425634980|2026-07-27|7006|Added a precedence clause for the DEFER branch — NOT a weakening: the unconditional "file them" mandate is unchanged, and the clause narrows an overlap where this rule and wg-when-deferring-a-capability-create-a gave an agent opposite instructions (file vs document-in-place) for a pre-existing finding it elects to defer. Routes that branch to wg-defer-only-after-inline-triage while requiring a durable artifact (plan Non-Goals, register, code comment), never conversation-only. AGENTS.rest.md is excluded from B_ALWAYS so this costs zero always-loaded bytes; body 437 B against the 600 B per-rule cap.
wg-block-pr-ready-on-undeferred-operator-steps|c171187443a446c1020bc06932c93b6b36a44344ec038ed211b6c87d8caacdcc|2026-08-02|7161|Added the [mandates-filing] marker. NOT a weakening of this rule: its mandate (a tracking issue before gh pr ready) is unchanged word for word. The marker is read by net-issue-flow.sh, which until now blocked the very filing this rule requires, leaving the blanket gate-override marker as the only exit -- an exit whose own help text called itself an architectural-pivot deferral, so complying with this rule required mis-describing the escape. The exemption is narrow and fail-closed: the issue must name this id, be OPEN, and be corroborated by a Tracks/Refs companion in the PR body. See ADR-155.
wg-when-deferring-a-capability-create-a|d0a0ee4bb8476c53d17585b1c6b0b640de744852d32cf142129502f29e739129|2026-08-02|7161|Added the [mandates-filing] marker. NOT a weakening: the documenting-in-place default and the ONLY-when-the-triple-test-passes condition are both unchanged. The marker covers the conditional branch where the triple test DOES pass and a GitHub issue is therefore required; in that branch this rule mandates a filing that net-issue-flow.sh would otherwise block. Three reviewers challenged including this rule (it is default-inline, so the mandate is conditional and not mechanically checkable); operator direction said at minimum these two, and the challenge is recorded unapplied in specs/feat-one-shot-net-issue-flow-mandated-filing-exemption/decision-challenges.md DC-1. Untagging is a one-line corpus edit under this same gate. See ADR-155.
wg-when-deferring-a-capability-create-a|0bd0bc73f6535aeb97e9d0f07812bad5631231473b62d6c704c39b1acd3d969b|2026-08-03|7194|Removed the [mandates-filing] marker added 2026-08-02. NOT a weakening of this rule: the documenting-in-place default and the ONLY-when-the-triple-test-passes condition are unchanged word for word; this narrows a CROSS-GATE exemption (net-issue-flow.sh), not this rule's mandate. Operator decision reversing the earlier "at minimum these two" direction, siding with the three reviewers recorded in specs/feat-one-shot-net-issue-flow-mandated-filing-exemption/decision-challenges.md DC-1. Decisive evidence is NOT "the gate becomes advisory" -- that framing was REFUTED by measurement on 2026-08-03 (442 merged PRs / 729 issues over 30d; of 256 PRs with closing-or-filing activity, 138 block at NET>0; tagging both rules flips 8 (5.8%) strict / 32 (23.2%) upper-bound vs 2 (1.4%) / 10 (7.2%) for the first rule alone, so 77-94% of blocked PRs still block either way and the issue's own ">half" bar was not met). The decisive point is that NOTHING WRITES THE CLAIM: every writer emits the first rule id literally (ship/SKILL.md Phase 5.5 snippet, work/SKILL.md operator-only deferral row); no writer anywhere emits Mandated-By: wg-when-deferring-a-capability-create-a, so citing it would be hand-authored free-form text -- the shape the closed vocabulary exists to prevent. Secondary: this rule's default is document-in-place, so its filing mandate is one conditional branch gated on a triple test the gate cannot observe. The blanket gate-override still covers capability deferrals. See ADR-155.
