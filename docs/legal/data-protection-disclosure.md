---
title: "Data Protection Disclosure"
type: data-protection-disclosure
jurisdiction: FR, EU
generated-date: 2026-02-20
---

# Data Protection Disclosure

**Effective Date:** February 20, 2026

**Last Updated:** July 16, 2026 (Ref #6538 (chore-retire-web-2-fsn1-orphan) — **factual accuracy correction; no change to the substance of processing.** The hosting locative is now stated at the **EU** level, with the current data centres named in the Section 4.2 processor table (and Sections 6.4 / 8.1(e)). The host that serves `app.soleur.ai` and stores workspace data is in **Helsinki, Finland (`hel1`)**; a second web host (**web-2**) has been in **Falkenstein, Germany (`fsn1`)** since July 13, 2026 (Ref #6393), and the prior "Helsinki, EU-only / same region" wording was inaccurate from that date. web-2 has **never served user traffic and has never stored user data** (no load balancer exists; `app.soleur.ai` resolves solely to the Helsinki host; its workspace volume is empty) and its retirement is decided. Both data centres are **EU**, under the **same Hetzner account and the same signed AVV** → **no new sub-processor, no third-country transfer, GDPR Chapter V not engaged**, and **no change to lawful bases, retention, or data-subject rights**. Output marked draft-requiring-professional-review). Previous: July 5, 2026 (Ref #6007 (feat-severity-ranked-inbox) — added the `inbox_item` table (migration 122), an **operational-notification store** for the founder's severity-ranked attention inbox (ADR-085). It holds **server-generated notification metadata only** — `workspace_id` (FK), an optional `user_id` (the recipient), `severity`, `source`, a **server-generated `title`**, ids-only `source_ref`, and read/act/archive timestamps — and **no agent output, message body, or email content**. Personally-targeted rows (`user_id` set) are **INCLUDED in the Article 15 self-serve export** — their `created_at`/`read_at`/`acted_at`/`archived_at` notification-interaction history is personal data held in no other exported table; workspace-broadcast rows (`user_id` NULL) are not personal to a subject and are excluded by the per-user export filter (Article 20 portability does not apply — the `title`/`source_ref` are controller-generated, not user-provided). **Article 17 erasure** is satisfied by `ON DELETE CASCADE` from `public.users` and `public.workspaces` (non-WORM operational data; no anonymise RPC). **No new processing activity** (no Article 30 amendment), **no new sub-processor, recipient, or third-country transfer.** Output marked draft-requiring-professional-review). Previous: July 2, 2026 (Ref #5274 Phase 3 (feat-multi-host-phase3d-cutover) — extended the Section 4.2 Hetzner processor row and the Section 6.4 Hetzner international-transfers entry to reflect the multi-host `/workspaces` GA setup: a **second EU web host (web-2)** and a dedicated per-workspace **git-data host**, all on the **same Hetzner account and Helsinki (EU) region** — **no new sub-processor and no international data transfer**. Stored workspace git data now sits on a **LUKS-encrypted (encryption-at-rest)** volume; traffic between hosts is **encrypted in transit with TLS**; and access to a workspace's git data is **authorized per workspace** (re-verified on proxied sessions). No new processing activity, personal-data category, lawful basis, or recipient. Output marked draft-requiring-professional-review). Previous: June 30, 2026 (Ref #5274 (feat-phase2-git-data-lease-fencing) — added the `worktree_write_lease` table (migration 116), an **operational write-lease coordination** record for the multi-host `/workspaces` layer (epic #5274, ADR-068 §2). It holds infrastructure-coordination fields only — `workspace_id` (FK), `worktree_id`, `host_id` (a host-**stable infrastructure server identity**, NEVER an `auth.uid()`; it identifies a machine, not a data subject), `lease_generation`, and two timestamps — and **no user-provided content or personal-profile data**, so it is **excluded from the Article 15/20 self-serve export** (DSAR exclusion: no personal data to disclose, no new disclosure section). **Article 17 erasure** is satisfied by `ON DELETE CASCADE` from `public.workspaces` (the row has zero audit lineage — no anonymise RPC needed; proven by the AC5 cascade integration test). **No new processing activity** (no Article 30 amendment), **no new sub-processor, recipient, or third-country transfer.** Output marked draft-requiring-professional-review). Previous: June 16, 2026 (Ref #5345 (feat-routines-management) — added Section 2.3(ac) "Routine run-log" (`routine_runs`, migration 107): an append-only WORM operational audit row written by the run-log Inngest middleware (via the service-role-only `write_routine_run` RPC) on each terminal run of an `EXPECTED_CRON_FUNCTIONS` cron — recording routine id, run id, completed/failed status, trigger source (scheduled/manual/agent), actor_class, started/ended/duration, and a scrubbed+truncated error_summary for failures. On the single-operator tenant the only data subject is Soleur itself — `actor_id`/`delegating_principal` (FKs to `public.users(id)` `ON DELETE RESTRICT`) carry the operator's own id on off-schedule human/agent runs and are NULL for scheduled system runs. **Legal basis:** legitimate interest (Article 6(1)(f) GDPR) — balancing test in GDPR Policy Section 3.7. **Retention:** indefinite (low-volume audit log). **Article 17:** `anonymise_routine_runs(p_user_id)` SECURITY DEFINER RPC NULLs both actor columns under the `app.worm_bypass` GUC carve-out, preserving the append-only audit row, called in `server/account-delete.ts` BEFORE `auth.admin.deleteUser` — mirrors `anonymise_workspace_activity` (Section 2.3(x)). **DSAR (Articles 15/20):** excluded from the self-serve export at present (operational system-audit log, not personal-profile data; canonical rationale in `apps/web-platform/server/dsar-export-allowlist.ts`); promote when a non-Soleur tenant exists or the `dsar-export.ts` chain is wired. **Article 22:** negative determination. **No new sub-processor, recipient, or third-country transfer** (substrate internal to the existing Supabase row — the Section 4.2 processor table and its "consistent with Sections" line are therefore unchanged). Article 30 register Processing Activity 29; Privacy Policy Section 4.12; GDPR Policy Section 3.7. Output marked draft-requiring-professional-review. Previous: June 15, 2026 (Ref #5370 (feat-reasoning-chat-boxes) — extended Section 2.3(i) "Web Platform conversation management" to disclose the durable per-turn plain-language **turn summary** the Concierge (`/soleur:go`) runtime persists on successful turns: one additional `messages` row (`message_kind='turn_summary'`, migration 105 — additive + nullable; `messages_message_kind_chk` pins it to `role='assistant'`) authored by the assistant under the user's own `user_id`, holding a short, redacted plain-language description of what the turn accomplished. It is the **user's own data**, inheriting the existing conversation lawful basis (Article 6(1)(b)), retention, Article 17 erasure cascade, and Article 15 + 20 export (un-redacted in the user's own bundle — the Article 15(4) author-redaction keys on `user_id`, not message role). The transient `narrate` live status line is **not stored** (live-only). **No new sub-processor, recipient, or third-country transfer; no new processing activity** (Article 30 register Processing Activity 2 amended, not minted). **Article 22 negative determination:** descriptive narration of requested work — no automated decision producing legal or similarly significant effects on the data subject. TOM: emit-boundary redaction + length caps applied before buffering/persistence (a surviving secret shape drops the whole narration); the residual cross-tenant-prose risk is controlled by a system-prompt directive + the solo-pinned tenant client + PR-time `user-impact-reviewer` (recorded in `knowledge-base/legal/compliance-posture.md`). Article 30 register Processing Activity 2 (amended). Output marked draft-requiring-professional-review. Previous: June 15, 2026 (Ref #5325 (feat-agent-native-outbound-email) — added Section 2.3(ab) "Agent-assisted, human-approved outbound cold email" (`email_suppression` + `outbound_sends`, `outbound.soleur.ai`): the **outbound counterpart** to Section 2.3(aa), overturning the 2026-06-11 inbound LIA's send-authority deferral. `gated`-tier tools (`email_send` / `email_reply` / `email_suppress`) surface the exact recipient + body to the operator → body-hash-bound single-use approval → single compliance chokepoint `sendCompliantOutbound` runs an ordered refuse-to-send gate (domain-verified → CLO C1–C5 → RFC-5322 header-injection guard → recipient allow-list → body-hash match → in-transaction suppression recheck → Resend → WORM audit row) → send from the dedicated `outbound.soleur.ai` subdomain (a separate sender-reputation stream, distinct from the `soleur.ai` apex transactional sender `notifications@soleur.ai`; same registrable domain, distinct subdomain). **No LLM in the send path.** Two tables (migration 104), keyed-pseudonymised only — the **plaintext recipient address and the body are NEVER persisted** (only `recipient_hash = HMAC-SHA-256(EMAIL_HASH_PEPPER, normalize(email))` with the pepper in Doppler, plus body hashes); `email_suppression` permanent / monotonic (retention IS the opt-out guarantee), `outbound_sends` append-only WORM accountability audit (Art. 5(2)). Recipients are **involuntary** third-party data subjects whose contact details were obtained NOT from them (a published professional byline), so **Article 14 applies** and the disclosure is carried **in-line in the first EU/UK communication** via CLO condition C3 (six discrete present element predicates) — **no reliance on the Article 14(5)(b) disproportionate-effort exemption** (contrast (aa)). Lawful basis Art. 6(1)(f) / Recital 47 — LIA `2026-06-15-outbound-email-authority-lia.md` (channel-level ePrivacy / UK PECR / FR CPCE-CNIL flagged as a pending counsel item). Article 17 via service-role-only RPCs (`anonymise_email_suppression`, `anonymise_outbound_sends` — NULL `owner_id` + scrub `recipient_hash`) with an Art. 17(3)(b) accountability carve-out for the WORM audit. Section 4.2 Web Platform Processors table amended — the Resend row gains the outbound sending-custody role (30-day retention, NO deletion API — same Chapter V finding as the inbound role); **no new sub-processor surface** (Resend already documented; Supabase EU + Doppler existing; **no LLM recipient on the outbound leg**). The "consistent with Sections" line extended to include 2.3(ab). Article 30 register Processing Activity 28; DPIA screened at single-operator scale with the channel-level ePrivacy analysis as the named accepted residual; counsel review pending. Previous: June 11, 2026 (Ref #5103 (feat-operator-inbox-delegation) — added Section 2.3(aa) "AI-assisted triage of inbound operational email" (`email_triage_items`, ops@soleur.ai): Proton Sieve forward-and-keep → Resend Inbound (US) → svix-signed metadata-only webhook → Inngest pipeline with a deterministic, rule-based statutory fast-path (no LLM for DSAR / breach / service-of-process / regulator mail) and a read-only Anthropic summarizer for the rest; persisted data = summary + envelope metadata only (the table has NO body column — parse-and-discard is schema-enforced); involuntary data subjects informed via Privacy Policy Section 4.13 under Article 14(5)(b); Article 17 via gated SECURITY DEFINER RPCs (`purge_email_triage_items`, `anonymise_email_triage_items`) with an Article 17(3)(b) accountability carve-out for statutory rows; retention probe 7d / non-statutory 365d / statutory per accountability period; Section 4.2 Web Platform Processors table amended — Resend row gains the inbound raw-mail custody role (30-day retention on all plans, NO deletion API — Chapter V finding) and a new Anthropic PBC row covers the email-triage summarization scope (non-statutory mail only, truncated + sanitized, 30-day default retention until the Zero-Retention amendment; DPA snapshot at `knowledge-base/legal/data-processing-agreements/anthropic.md`); Article 30 register Processing Activity 27; LIA `2026-06-11-operator-inbox-triage-lia.md`; DPIA screening memo `2026-06-11-dpia-screening-operator-inbox-triage.md` (three named accepted residuals); counsel review pending as a single bundle). Previous: June 10, 2026 (PR #5124 (#4046) — re-pointed the LinkedIn Company Page publication surface from `linkedin.com/company/jikigai/` to the new dedicated Soleur Company Page `linkedin.com/company/soleur/` (org 129094054) per the operator's 2026-06-10 brand-separation decision; Jikigai SARL remains controller and Page admin; no new processing activity, personal-data category, lawful basis, recipient, sub-processor, or retention change — only the Page identity changed; zero posts had been published to the prior surface so no data subjects migrate; LIA amendment banner + Article 30 PA15 amendment note land in lockstep; counsel-review audit at `knowledge-base/legal/audits/2026-06-counsel-review-5124.md`). Previous: June 8, 2026 (PR #5014 (#5005) — converged the DSAR export's workspace-files enumeration root (`server/dsar-export.ts` `resolveDsarWorkspacePath`) off the stale `users.workspace_path` column onto the workspace-id resolver (`workspacePathForWorkspaceId(expectedUserId)`, solo/N2), fixing an Art. 15+20 completeness gap whereby data subjects provisioned after the ADR-044 `users → workspaces` relocation had their OWN workspace files silently omitted from the self-serve export (the legacy per-user column was empty/stale). No new processing activity, personal-data category, lawful basis, recipient, or sub-processor — the export now delivers the subject's own workspace files it always intended to; the solo-keyed resolver (single-arg, no active-workspace claim) structurally prevents the inverse over-export of a shared-workspace owner's files into a member's export. No Article 30 amendment (read-path completeness remediation, not a new processing purpose). Previous: PR #4627 (#4625) — extended Section 2.3(w) "Delegated-credential prompt routing" with the consent-ENFORCEMENT layer: recorded in-app acceptance is now the source of truth gating the BYOK key lease (migration 083 adds an acceptance-EXISTS clause to `resolve_byok_key_owner` plus a server-owned `current_byok_side_letter_version()`), and a gate-side withdrawal path (migration 084, `byok_delegation_withdrawals` WORM ledger + `withdraw_byok_delegation_consent` RPC) satisfies Art. 7(3) "as easy to withdraw as to give" — it blocks new leases and stops in-flight billing within one turn. The withdrawal ledger is added to DSAR Art. 15+20 coverage and to the Art. 17 cascade (`anonymise_byok_delegation_withdrawals` step 5.12, after the acceptances step 5.11). No new sub-processor — substrate is internal to the existing Supabase rows; gated by `BYOK_DELEGATIONS_ENABLED` (default OFF). Cross-references Article 30 register PA-23, AUP Section 5.6, the Delegation Consent Side Letter template. Previous: PR #4559 (#4558) — extended Section 2.3(u) "Workspace co-member data category" to disclose that, post-ADR-044 (migrations 079/080/081 relocating repo-connection state from the per-user `users` table to the `workspaces` table), JOINING a workspace as a Co-Member grants access to that workspace's connected GitHub repository AND the knowledge-base derived from it — the Workspace Owner's repo/KB content is now processed for the workspace's Co-Members at the workspace-ownership grain (previously a joined member could not reach the owner's repo, the #4543 brand-survival defect). No new sub-processor — GitHub Inc. (Microsoft) remains the upstream source already disclosed in Section 4.2; the new recipients are the workspace's own Co-Members already named in Section 2.3(u). Lawful basis for the Co-Member's processing of the Owner's repo/KB data: the Owner's Art. 6(1)(a) consent in the migration-058 `workspace_member_attestations` invite attestation (amended to consent to Co-Member access to the connected repository + knowledge-base) plus the existing Art. 6(1)(f) legitimate-interest basis for repo-derived signals (PA-17). Credential gate (Art. 32): `github_installation_id` (a GitHub App token grant) is column-level REVOKED from the `authenticated` role and readable only via the membership-checked SECURITY DEFINER RPC `resolve_workspace_installation_id(p_workspace_id)` (NULL to non-members); repo reads resolve from `workspaces` only (single source of truth — no per-user fallback). Cross-references ADR-044, Article 30 register Processing Activity 17 sub-clause (c), Privacy Policy Section 4.11, GDPR Policy Section 5.3. Previous: PR-B #4508 (#4232) — added Section 2.3(w) "Delegated-credential prompt routing" for `byok_delegations` + `byok_delegation_acceptances` consent table (migration 074); Art. 26 joint controllership between Grantor and Grantee; Art. 7 consent evidence via acceptance rows; Art. 17 cascade step 5.11; no new sub-processor. Previous: PR #4491 (#4358) — extended DSAR conversations fetch to include participated workspace conversations for Art. 15 completeness; subject-authored messages in co-member-owned conversations now included in the bundle. Previous: (PR #4486 — resolved co-uploader DSAR enumeration (#4445) and Storage cleanup (#4444): manifest schema 1.2.0 adds co-uploader manifest entries with `redacted: true`, `redaction_reason: "art-15-co-uploader"`, `uploader_pseudonym: "member_<hex12>"`; bytes NOT included in the ZIP per Art. 15(4). Account-delete Storage cleanup step 3.9015 added. Previous: May 25, 2026 (PR-1 of umbrella #4456 / PR #4455 — added Bullet Train Limited (Flagsmith) row to Section 4.2 "Web Platform Processors" table disclosing the new sub-processor for runtime feature-flag identity-trait evaluation. UK-based controller (England and Wales no. 12353266) → AWS-hosted global edge (`edge.api.flagsmith.com`); region not pinned on managed-SaaS tier. Only pseudonymised `identifier` + traits `{role, orgId}` egress (no message content, no attachments, no BYOK keys, no email). `transient: true` MANDATORY on every `getIdentityFlags(...)` call as the data-minimisation lever (opts out of Flagsmith server-side identity persistence). Dual-control gating: every flag flip is the AND of Flagsmith boolean AND server-side env-allowlist. Transfer mechanism: UK IDTA + EU SCCs Modules 2+3 belt-and-suspenders to EC adequacy decision (EU) 2021/1772 for the UK; Flagsmith's onward US sub-processors covered by SCCs Module 3 + DPF where available. DPA snapshot at `knowledge-base/legal/data-processing-agreements/flagsmith.md`; sub-processor list at `https://www.flagsmith.com/gdpr-sub-processor-list` (last updated 13 October 2025). Mirror (`plugins/soleur/docs/pages/legal/data-protection-disclosure.md`) updated in lockstep with the same Section 4.2 row content. Note: pre-existing structural drift between root and mirror on LinkedIn / Microsoft Ireland rows in the §4.2 table (drift origin not bisected as part of this PR; subsequent commits to one file without the other are the most likely cause — PR #4081 / issue #4051 LinkedIn legal track did touch both files in lockstep per commit 32cd8198) is documented as out of scope of this PR; both files agree on the Flagsmith addition. Previous: #4290 — added `byok_delegations` WORM ledger to `DSAR_TABLE_ALLOWLIST` per PR-A of feat-byok-delegations (#4232). Migration 064 introduces the substrate that lets a grantor workspace co-member fund a grantee co-member's BYOK runs inside a shared workspace under a per-day + per-hour USD cap, with revoke + Art. 17 cascade + cap-update flow; the row IS the Art. 6(1)(b) contract between grantor and grantee per the migration's LAWFUL_BASIS header. 7-year retention per joint-controllership audit trail. Production exposure at PR-A merge is internal-only — gated by the `byok-delegations` feature flag (default-off in `lib/feature-flags/server.ts`) — but the DSAR worker is wired now so future tenant-onboarding flips do not silently drop Art. 15 completeness. OR-semantics over five actor columns (grantor_user_id, grantee_user_id, created_by_user_id, revoked_by_user_id, cap_updated_by_user_id) per per-row-WHERE lint AC30; the DSAR worker reads via one explicit `.eq()` chain per column in `apps/web-platform/server/dsar-export.ts` with id-set merge/dedupe so a row in which the same user appears in multiple positions (e.g. grantor == created_by) is not double-counted. The `art_17_anonymise` value is reserved in the migration's `revoked_reason` CHECK enum alongside `grantor_revoke`, `grantee_decline`, `member_departed`, `admin_revoke` — the erasure cascade RPC ships in PR-B alongside the cost-writer route through `check_and_record_byok_delegation_use`. Service-role allowlist extended for `apps/web-platform/server/byok-resolver.ts` (workspace + organization lookup BEFORE the BYOK lease opens, where the caller's tenant JWT is not yet established; `callerUserId` is server-derived per the SS F3 invariant; the `resolve_byok_key_owner` RPC is SECURITY DEFINER search_path-pinned per cq-pg-security-definer-search-path-pin-pg-temp). Migration 064 carries `to_regclass` preconditions for `public.users` + `public.workspaces` per the #4338 schema-vs-ledger drift class. No new sub-processor engaged — substrate is internal to the existing Supabase row. Previous: May 22, 2026 (#4353 — added Section 2.3(v) "Workspace member removal audit ledger" disclosing the `workspace_member_removals` WORM substrate introduced by migration 062 / PR #4294 (Article 30 register Processing Activity 19 — distinct from sibling PA-20 `workspace_member_actions` covered immediately below); 36-month retention floor per Art. 82(2) shortest-applicable-jurisdiction limitations period; Art. 17 cascade via `anonymise_workspace_member_removals(p_user_id)` SECURITY DEFINER RPC step 3.905 in `server/account-delete.ts` BEFORE step 3.91 cascade DELETE, BEFORE step 3.93 PA-20 anonymise, and BEFORE `auth.admin.deleteUser` (step 4); RPC NULL-sets both PII columns `removed_user_id` AND `removed_by_user_id`; lineage columns `id` and `removed_at` preserved post-erasure unconditionally for Art. 5(2) accountability, `workspace_id` preserved while the parent workspace exists (may transition to NULL via its `ON DELETE SET NULL` FK at step 3.92 orphan-org cleanup per ADR-039 §Invariants); WORM trigger bypass is structural-shape detection at the trigger body itself (PII columns transition NOT NULL → NULL only, lineage columns unchanged) — NOT a `SET LOCAL session_replication_role` GUC bypass (post-#4294-review deliberate divergence from mig 037 / mig 051 precedent per learning 2026-05-18, because pg_cron runs as `postgres` not `service_role`); departed Co-Members read PA-19 via service-role DSAR worker — `is_workspace_member()` RLS helper returns false post-removal so the standard self-serve surface omits the row; `workspace_member_removals` is queried single-arm on `removed_user_id` (the row's owner-of-record; a Workspace Owner's own DSAR returns rows where they were removed, not rows where they performed removals on others); PA-19 ⊥ PA-20 — distinct retention floors (36-month vs 7-year), distinct anonymisation RPCs; no new sub-processor engaged; cross-references Privacy Policy Section 8.1 "Departed workspace members" paragraph block and GDPR Policy Section 5.3 "Workspace member removal audit ledger (PA-19)" bullet; closes legal-doc-cross-document-gate lockstep gap from PR #4294 (#4333); previous: #4330 — Section 2.1b(a) clarifying carve-out for the team-workspace sub-case; cross-references Section 2.3(u) and the Section 4.2 footer carve-out shipped by PR #4289; no new sub-processor engaged and no new processing activity (Article 30 register Processing Activity 2 already covers workspace co-member processing); customer-facing Data Processing Agreement pre-drafted pending publication at first customer-DPA execution per Section 8.1(a) commitment; previous: #4287 — cross-referenced Article 30 register Processing Activity 20 covering the new `workspace_member_actions` append-only audit log introduced by migration 063 / PR #4231: AFTER trigger on `public.workspace_members` writes one row per add / remove / role-change capturing `actor_user_id` (via the `workspace_audit.actor_user_id` session GUC set by `invite_workspace_member` + `remove_workspace_member` RPCs) and `target_user_id`; owner-only read surface via `list_workspace_member_actions` SECURITY DEFINER RPC scoped through `organizations.owner_user_id`; 7-year retention with daily pg_cron purge wrapper; Article 17 erasure via `anonymise_workspace_member_actions(p_user_id)` step 3.93 in `server/account-delete.ts` (between `anonymise_organization_membership` and `auth.admin.deleteUser`); cascade DELETE at step 3.91 does NOT create new audit rows because mig 063 re-CREATEs `anonymise_workspace_members` to bypass the AFTER trigger via `SET LOCAL session_replication_role='replica'`; `workspace_member_actions` added to `DSAR_TABLE_ALLOWLIST` with OR-semantics (`actor_user_id` + `target_user_id`); Section 2.3(u) "Workspace co-member data category" covers the parent membership substrate — this audit log is the Article 5(2) accountability evidence layer on top; no new sub-processor engaged; internal jikigai workspaces only at v1 (gated by `FLAG_TEAM_WORKSPACE_INVITE`); previous: May 22, 2026 added Section 2.3(u) "Workspace co-member data category" for the team-workspace feature gated by `FLAG_TEAM_WORKSPACE_INVITE` per AC-LEGAL-FLIP and added a Section 4.2 footer carve-out clarifying that workspace co-members are NOT processors under Article 28 — access is contract-mediated under the Anthropic Commercial Terms §C "authorized users" framework; the Workspace Owner is the controller per Terms & Conditions Section 3b and the SECURITY DEFINER `is_workspace_member()` helper is the load-bearing Article 32 TOM; no new sub-processor engaged; cross-references Privacy Policy Section 4.11 and Article 30 register Processing Activity 2 (already amended by PR #4225); previous: May 21, 2026 (extended Section 2.3(r) to disclose the workspace-synchronization sub-surface under #4224 — webhook-triggered `git pull --ff-only` into operator workspace clone, manual `POST /api/kb/sync` endpoint, heterogeneous `kb_sync_history` ledger; no new sub-processor; cross-referenced to Article 30 register Processing Activity 17 sub-purpose (b)(ii); added Section 2.3(t) for the `template_authorizations` per-template authorization ledger introduced by PR-I #4078: first-send-IS-authorization pattern, NOT NULL bounded columns with provisional defaults 100 sends / 30-day soft reconfirm / 90-day hard expiry, 8-value paired-null revocation_reason CHECK, append-only WORM trigger with session_replication_role bypass at the SECURITY DEFINER RPC layer, semantic-ordering Article 17 cascade between `anonymise_action_sends` and `anonymise_scope_grants` in `server/account-delete.ts`; folds the un-revocability + Article 5(2) attribution rationale formerly proposed as ADR-036; no new sub-processor engaged; previous: May 19, 2026 added Section 2.3(r) and Section 2.3(s) for the `action_sends` per-send signature ledger, the typed-confirm gate at Send-time for the brand-critical action classes, the `auto_with_digest` tier value, and the DB-level enum-absence CHECK on `scope_grants.action_class` + `action_sends.action_class` -- no new sub-processor engaged, outbound delivery integrations defer to PR-I [#4077]; added Section 2.3(p) "LinkedIn Company Page publication" activity row disclosing Page operation + aggregate Page Insights consumption + one-time K-bis transfer; extended Section 4.2 Web Platform Processors table with LinkedIn Ireland Unlimited Company and Microsoft Ireland Operations Ltd rows; extended Section 6.4 international-transfers with LinkedIn EU + Microsoft EUDB rows; extended Section 10.3 with a "LinkedIn-published content carve-out" paragraph for the Article 17 cascade limitation per EDPB Guidelines 5/2019 [#4051]; previous: May 18, 2026 extended Section 2.3(o) to disclose the per-tenant `scope_grants` ledger, the `/dashboard/audit` viewer, and the Article 22(3) human-review affordance shipped in PR-G #3947; re-confirmed Inngest remains self-hosted and no new sub-processor is engaged; May 16, 2026 extended Section 2.3(d) and added Section 2.3(n) for the off-site CLA evidence archive)

This Data Protection Disclosure ("DPD") describes the data processing relationship between:

- **Jikigai** ("Provider," "we," "us," or "our"), the operator and maintainer of the Soleur Claude Code plugin, accessible at [https://soleur.ai](https://soleur.ai) and the GitHub repository [jikig-ai/soleur](https://github.com/jikig-ai/soleur); and

- **You** ("User," "Controller," or "you"), the individual or entity using the Soleur plugin.

This DPD supplements our [Terms and Conditions](terms-and-conditions.md) and [Privacy Policy](privacy-policy.md) and transparently describes the data processing relationship under the General Data Protection Regulation (EU) 2016/679 ("GDPR"). Because Soleur is not a data processor (see Section 2), this is not a Data Processing Agreement under Article 28. It is a disclosure document that clarifies data handling responsibilities.

Soleur is a source-available project maintained by Jikigai, a company incorporated in France, with its registered office at 25 rue de Ponthieu, 75008 Paris, France.

---

## 1. Definitions

**1.1** "Personal Data" means any information relating to an identified or identifiable natural person as defined in Article 4(1) of the GDPR.

**1.2** "Processing" means any operation or set of operations performed on Personal Data, as defined in Article 4(2) of the GDPR.

**1.3** "Controller" means the natural or legal person which, alone or jointly with others, determines the purposes and means of the Processing of Personal Data, as defined in Article 4(7) of the GDPR.

**1.4** "Processor" means a natural or legal person which processes Personal Data on behalf of the Controller, as defined in Article 4(8) of the GDPR.

**1.5** "Sub-processor" means any Processor engaged by a Processor to carry out Processing activities on behalf of the Controller.

**1.6** "Plugin" means the Soleur Claude Code plugin, including all agents, skills, commands, and the knowledge base it provides.

**1.7** "Local Data" means all files, knowledge-base entries, brainstorms, plans, specs, code, and other data generated or stored on the User's local filesystem through use of the Plugin.

**1.8** "Docs Site" means the Soleur documentation website hosted at [https://soleur.ai](https://soleur.ai) via GitHub Pages.

---

## 2. Data Processing Relationship Classification

### 2.1 The Soleur Plugin Is Not a Data Processor

**This section is critical to understanding the data processing relationship for the Plugin.**

The Soleur Plugin operates entirely on the User's local machine. It is installed via CLI and runs as a local extension within the User's development environment.

As a result:

- **(a)** The Plugin does **not** process Personal Data on behalf of the User within the meaning of Article 28 of the GDPR.
- **(b)** The Plugin does **not** have access to, collect, store, transmit, or otherwise process any Local Data created or managed through the Plugin.
- **(c)** All knowledge-base files, plans, brainstorms, specs, generated code, and other artifacts remain exclusively on the User's local filesystem under the User's sole control.
- **(d)** The Plugin does **not** act as an intermediary for any API calls made by the User to third-party services (including, but not limited to, the Anthropic Claude API). Users authenticate directly with third-party services using their own API keys and credentials.

**Therefore, Soleur is neither a Controller nor a Processor with respect to the data processed locally through the Plugin.**

### 2.1b Web Platform Data Processing

The Soleur Web Platform at [app.soleur.ai](https://app.soleur.ai) is a cloud-hosted service operated by Jikigai. Unlike the Plugin (Section 2.1), the Web Platform involves server-side processing of User data on Jikigai-operated infrastructure.

For the Web Platform:

- **(a)** Jikigai acts as the **data controller** for User account data, workspace data, and subscription data processed through the Web Platform — **EXCEPT where an organization has invited additional natural persons ("Co-Members") to access its workspace under the team-workspace feature**, in which case the Workspace Owner is the controller per Section 2.3(u) and the Section 4.2 footer carve-out, and Jikigai acts as the processor for the Workspace Owner. When this exception applies, a customer-facing Data Processing Agreement governs the Jikigai-as-processor relationship with the Workspace Owner; the DPA is pre-drafted and will be published at this site at first execution.
- **(b)** Jikigai engages the following **data processors** under Article 28 of the GDPR: Supabase (authentication and database), Stripe (payment processing), Hetzner (infrastructure hosting), and Cloudflare (CDN/proxy). See Section 4.2 for the full processor table.
- **(c)** Data processed includes: email addresses, hashed passwords, authentication tokens, session data, encrypted API keys, subscription metadata, conversation metadata, message content, and technical data (IP addresses, request headers).
- **(d)** The legal basis for this processing is **contract performance** (Article 6(1)(b) GDPR) -- processing is necessary to provide the Web Platform service the User signed up for. For Cloudflare CDN/proxy processing of unauthenticated traffic (visitors who have not signed up), the legal basis is **legitimate interest** (Article 6(1)(f) GDPR) -- see Section 4.2 for the full dual-basis disclosure.

This section fulfills the commitment made in Section 8.1(a) to update this DPD with Article 28-compliant terms when cloud features are introduced.

### 2.2 User's Responsibilities as Controller

The User is solely responsible for:

- **(a)** All Personal Data processed on their local machine through use of the Plugin;
- **(b)** Ensuring a lawful basis for any processing of Personal Data that occurs through their use of the Plugin;
- **(c)** Compliance with GDPR and other applicable data protection laws with respect to data processed locally;
- **(d)** Securing their local environment, including filesystem permissions, encryption, and access controls;
- **(e)** Managing API keys and credentials used to interact with third-party services; and
- **(f)** Any data shared with third-party services (e.g., Anthropic Claude API) through the Plugin's functionality, including compliance with those services' own data processing terms.

### 2.3 Limited Processing by Soleur

Soleur's data processing activities are limited to:

- **(a)** **Docs Site hosting and analytics:** The Soleur documentation website is hosted on GitHub Pages. GitHub may collect standard web server logs, including IP addresses, browser user-agent strings, and page request data. This processing is governed by [GitHub's Privacy Statement](https://docs.github.com/en/site-policy/privacy-policies/github-general-privacy-statement). Additionally, the Docs Site uses **Plausible Analytics** ([plausible.io](https://plausible.io)) for privacy-respecting website analytics. Plausible is cookie-free, does not store IP addresses, and collects only aggregated, anonymous data (page URLs, referrer URLs, country, device type, browser type).
- **(b)** **GitHub repository interaction:** Users who submit issues, pull requests, or participate in discussions on the Soleur GitHub repository interact with GitHub's platform. This processing is governed by GitHub's terms and privacy policies.
- **(c)** **Plugin distribution:** The Plugin is distributed via GitHub and npm. Download and installation telemetry is handled by those respective platforms under their own privacy policies.
- **(d)** **Contributor License Agreement (CLA) signatures:** Contributors who submit pull requests to the Soleur repository are asked to sign a CLA via the CLA Assistant integrated into GitHub. This processing collects the contributor's GitHub username, signature timestamp, and associated pull request reference. Signature data is stored in the Soleur GitHub repository on a dedicated branch (`cla-signatures`) and is publicly visible. The legal basis is legitimate interest (Article 6(1)(f) GDPR) in maintaining an enforceable record of contributor IP license grants. Signature data on the public branch is retained indefinitely as a contributor-facing receipt; an off-site evidence archive of the same record is retained for ten (10) years per Section 2.3(n).
- **(e)** **Newsletter subscription management:** Visitors who subscribe to the Soleur newsletter via the Docs Site provide their email address, which is transmitted to and processed by **Buttondown** ([buttondown.com](https://buttondown.com)), a third-party newsletter platform. Buttondown also automatically collects IP address, referrer URL, subscription timestamp, and browser/device metadata during the subscription request. Buttondown acts as a data processor on behalf of Jikigai. Buttondown's sub-processor list is maintained at [buttondown.com/legal/subprocessors](https://buttondown.com/legal/subprocessors). The legal basis for email address processing is consent (Article 6(1)(a) GDPR), verified through a double opt-in confirmation email. The legal basis for technical metadata is legitimate interest (Article 6(1)(f) GDPR) -- service operation and abuse prevention. Email addresses are retained until the subscriber unsubscribes. Technical metadata retention is governed by Buttondown's data retention practices.
- **(f)** **Web Platform account management:** The Web Platform (app.soleur.ai) processes email addresses, authentication tokens, and session data for user account management and authentication. Users may authenticate via magic link or OAuth providers (Google, Apple, GitHub, Microsoft). OAuth sign-in additionally processes provider user IDs, display names, and profile picture URLs. Accounts with matching verified email addresses are automatically linked. OAuth identity data is managed by Supabase. Legal basis: contract performance (Article 6(1)(b) GDPR). Retention: while account is active; deleted on account deletion request.
- **(g)** **Web Platform payment processing:** The Web Platform processes customer email addresses and subscription metadata via Stripe Checkout. Card data is handled exclusively by Stripe and never reaches Jikigai servers (PCI SAQ-A). Legal basis: contract performance (Article 6(1)(b) GDPR). Retention: subscription records retained for 10 years per French tax law (Code de commerce Art. L123-22).
- **(h)** **Web Platform infrastructure hosting:** The Web Platform hosts user workspaces, encrypted API keys (AES-256-GCM), and Docker containers on Hetzner servers in the EU (EU-only); workspace data is stored in Helsinki, Finland (`hel1`). Legal basis: contract performance (Article 6(1)(b) GDPR). Retention: while account is active.
- **(i)** **Web Platform conversation management:** The Web Platform stores conversation metadata and message content associated with user accounts. Data processed: conversation status, domain leader assignment, user messages, assistant responses, tool call metadata, and -- when usage telemetry is enabled per operator configuration -- per-message `usage` jsonb (token consumption and cost metadata). Legal basis: contract performance (Article 6(1)(b) GDPR). Retention: while account is active; deleted on account deletion request (cascade delete). **Turn summaries (feat-reasoning-chat-boxes #5370, migration 105):** on the Concierge (`/soleur:go`) runtime, each turn that completes successfully may add one further `messages` row holding a short plain-language description of what the turn accomplished (`message_kind='turn_summary'`, `role='assistant'`, associated with the user via the user's own `user_id`; the additive, nullable `message_kind` discriminator is pinned to `role='assistant'` by the `messages_message_kind_chk` CHECK). The summary is agent-authored but is the **user's own data** — a record of the work the user requested — and inherits this activity's lawful basis, retention, Article 17 erasure cascade (FK on conversation delete and account delete), and Article 15 + 20 self-serve export (returned un-redacted in the user's own bundle because the Article 15(4) author-redaction keys on `user_id`, not message role; `message_kind` is classified in `MESSAGE_NON_REDACT_ALLOWLIST` as a structural row-shape discriminator and is gated by the CI sentinel `apps/web-platform/test/dsar-message-redact-fields-sweep.test.ts`, while the summary content lives in the already-redact-classified `content` column). It is written **only on successful turns** (dropped on Stop/abort). A separate, **transient** live status line (`narrate`) shown during the turn is **never persisted**. **No new sub-processor, recipient, or third-country transfer; no new processing activity** (Article 30 register Processing Activity 2 amended). **Article 22:** none in the Article 22 sense — descriptive narration of requested work, no decision producing legal or similarly significant effects on the data subject. **TOM (Article 32):** emit-boundary redaction + length caps applied before the string is buffered or stored (a surviving secret shape drops the whole narration, fail-loud); the same redacted string feeds both the persisted row and the transient frame; the residual risk of another tenant's data named in agent prose persisting into an exportable record is controlled by the solo-pinned tenant client, a system-prompt directive forbidding naming entities outside the user's own context, and the PR-time `user-impact-reviewer` (recorded in `knowledge-base/legal/compliance-posture.md`). Article 30 register entry: existing Processing Activity 2 (amended for the turn-summary sub-purpose).
- **(j)** **Web Platform push notification subscriptions:** The Web Platform stores push notification subscription data (endpoint URL, encryption keys) when users enable browser push notifications. Data processed: push subscription endpoint, p256dh and auth encryption keys, timestamps. Legal basis: consent (Article 6(1)(a) GDPR) -- subscriptions are created only after explicit browser permission grant. Retention: while account is active; expired subscriptions (HTTP 410 Gone) deleted automatically; all data deleted on account deletion (cascade delete).
- **(k)** **Web Platform transactional email notifications:** The Web Platform sends email notifications via Resend when an AI agent requires user input and the user has no active push subscriptions. Data processed: recipient email address, notification content (agent name, question summary, conversation deep link). Legal basis: legitimate interest (Article 6(1)(f) GDPR) -- transactional notifications are necessary to inform users of pending decisions that block AI agent progress. Retention: email delivery logs retained per Resend's data retention policy.
- **(l)** **DSAR (Articles 15 + 20) self-serve export:** The Web Platform packages a user's data into a ZIP archive on request at `/dashboard/settings/privacy`. Data processed: the categories enumerated in Section 5.3 below (account profile, conversations, messages, message attachments, KB share links, team / agent names, BYOK credentials and usage audit, workspace files). The bundle is delivered as a one-time signed URL bound to the requesting session and IP prefix, valid for 7 days. An audit row (`dsar_export_audit_pii`) recording requester IP, user agent, and event timestamp is written for each lifecycle event (enqueue, download_complete, reissue, expire, fail). **Art. 15(4) author-only redaction (#4319, manifest schema 1.1.0):** in shared workspace conversations, all free-text and namespace-leaking columns on messages authored by a non-subject are nulled (canonical list maintained at `apps/web-platform/server/dsar-export.ts` constant `MESSAGE_REDACT_FIELDS`; currently covers `content`, `tool_calls`, `usage`, `draft_preview`, `action_class`, `tier`, `source`, `owning_domain`, `urgency`, `trust_tier`, `source_ref`, `leader_id`, `template_id`). Raw author `user_id` of the non-subject is replaced with a per-bundle salt-scoped pseudonym (`member_<hex12>`); attachments cascade via an allowlist on subject-authored message IDs (orphan / foreign-author parents → fail-closed). The bundle's `manifest.redactions[]` field discloses the path, reason (`art-15-4-rights-of-others`), and count of redacted rows per table. Future migrations adding free-text or namespace-leaking columns to `messages` are gated by a CI sentinel test (`apps/web-platform/test/dsar-message-redact-fields-sweep.test.ts`) requiring each new column to be classified as redact-or-allowlist before the migration can merge. Legal basis for the bundle generation: legal obligation (Article 6(1)(c) GDPR) under Articles 15 + 20. Legal basis for the audit row: legal obligation (Article 6(1)(c)) under Article 5(2) accountability. Retention: bundle Storage object 7 days hard-cap (whichever first: download or pg_cron sweep); audit row 24 months then automatic delete; both anonymised on account erasure via the Article 17 cascade. Sub-processors: Supabase Storage (bundle ZIP), Resend (delivery email containing the download link).
- **(m)** **Operational telemetry & breach detection:** The Web Platform emits two operational telemetry streams to support service reliability and breach-detection obligations under GDPR Articles 32 and 33. (i) **Structured application logs** are written to standard output by the application server on Hetzner infrastructure in the EU -- the user-serving application server is in Helsinki, Finland (EU-only) -- and retained in a rolling Docker log buffer (capacity-bounded; no off-host log shipping is configured). (ii) **Error and breadcrumb events** are sent to Sentry (Functional Software GmbH, DE region; Standard Contractual Clauses) for error monitoring. In both streams, user identifiers are pseudonymised at the emission boundary by replacing the raw `userId` with a keyed cryptographic hash (`userIdHash`) computed using a server-resident secret pepper. Under GDPR Recital 26, the controller cannot re-identify a data subject from the hash alone without the pepper. Legal basis: legitimate interest (Article 6(1)(f) GDPR) in service reliability, security, and abuse prevention, balanced against the pseudonymisation safeguard; together with legal obligation (Article 6(1)(c) GDPR) for compliance with the Article 33 breach-notification timeline (see Section 7.2). Retention: Sentry events retained for 90 days (rolling); pino stdout retained in a fixed-capacity Hetzner-local rolling buffer (no off-host copies). Right to erasure (Article 17 GDPR): hashed identifiers age out per the rolling retention windows; the controller cannot perform processor-side targeted erasure of a pseudonym whose subject cannot be re-identified, consistent with Recital 26. **Sentry monitor classes processed:** issue alerts and cron monitor check-ins (vendor-hosted heartbeat for scheduled GitHub Actions jobs); both carry no application log content and only structural metadata (job slug, status, timestamp, pseudonymous identifier where applicable). **Sentry log ingestion (Logs product) is NOT enabled and no application log content is forwarded to Sentry**; if a future change introduces a Sentry log channel, the disclosure must be re-extended in advance and the scrub boundary at `apps/web-platform/server/sentry-scrub.ts` must be widened to cover it. **Better Stack s.r.o. (EU) — two recipient roles:** **(i) Inngest server liveness heartbeat (unchanged):** the self-hosted Inngest durable-trigger server (bullet (o)) emits a 60-second loopback-fed heartbeat to Better Stack; the payload is an opaque ping + timestamp + HTTP status code and carries no personal data, user identifiers, or application content — for this role only, Better Stack is not a sub-processor of personal data under GDPR Article 28 and no SCC/DPA is required. **(ii) Operational log aggregation (added 2026-05-21, PR #4279; disclosed 2026-05-22, PR #4293):** the Vector agent (`apps/web-platform/infra/vector.toml`) reads journald (which mirrors pino stdout from `inngest-server.service`) and ships journald + host_metrics to Better Stack Logs (source ID `2457081`, name `soleur-inngest-vector-prd`, EU region — Hetzner Falkenstein cluster `eu-fsn-3`, pinned at `https://s2457081.eu-fsn-3.betterstackdata.com/`). Log lines incidentally include pseudonymous user identifiers (`userIdHash`), conversation IDs, request metadata, and error stack traces. **The Vector boundary applies three VRL transforms (`pii_scrub_drop_userdata` + `pii_scrub_structured` + `pii_scrub_string`) before egress:** the first drops top-level user-content keys (`body`, `content`, `message`, `userMessage`, `prompt`, `chat_message`, `userInput`, `user_input`) on parsed pino payloads to mitigate Article 9 special-category-data exposure; the second renames top-level `userId` / `user_id` to `userIdHash` via HMAC-SHA256 + base16 (reusing the Doppler-held `SENTRY_USERID_PEPPER` per ADR-029 §I4 — bit-for-bit equivalent to the TS `hashUserId` primitive, asserted by CI fixture); the third applies regex backstops (userid= substring, email, Authorization Bearer/Basic, line-injection chars) on non-pino / kernel-oops branches and is skipped when the structured branch parsed (`/api/auth/callback/...` paths preserved). **For this role, Better Stack IS a sub-processor of personal data under GDPR Article 28.** The Better Stack DPA is available for execution under standard EU-region terms (SCCs incorporated as belt-and-braces); the Vendor DPA Status table in `knowledge-base/legal/compliance-posture.md` tracks the signing state, an open operator action under AC15 of PR #4293. The Doppler-held `SENTRY_USERID_PEPPER` is not shared with Better Stack — same Recital 26 pseudonymisation properties as the Sentry plane. **Legal basis:** legitimate interest (Article 6(1)(f) GDPR) in service reliability and long-tail diagnostic recall beyond the 30 MB Hetzner Docker json-file rolling buffer (purpose-(vi) of Article 30 PA8); balanced against the VRL boundary controls. **Retention:** Better Stack paid-tier default. **CI gate:** `validate-vector-config` in `.github/workflows/apply-web-platform-infra.yml` enforces `vector validate` (schema) + `vector vrl` fixture tests (transform-body — BLOCKING) on every `vector.toml` or fixture change. **No Art. 33 / Art. 34 notification was warranted by this role addition** — data flowed only post-2026-05-21 (#4279 merge) under processor-DPA terms; this disclosure is the Art. 30(1)(d) record-keeping discharge. Strategic single-provider observability consolidation question tracked in #4296 (60-day re-decision).
- **(o)** **Web Platform autonomous-draft runtime (Inngest CFO) and per-tenant scope grants:** The Web Platform runs a server-side trigger substrate (self-hosted Inngest OSS on Hetzner, eu-central, bound to `127.0.0.1` with SQLite state on the same host -- no external sub-processor) that converts a Stripe `invoice.payment_failed` event into a CFO-domain customer-response **draft** persisted to the user's `messages` table and surfaced on `/dashboard` Today. Data processed: `founderId` (UUID), `invoiceId` (Stripe identifier, non-PII), `customerEmailHash` (sha256 of customer email -- the **cleartext customer email is hashed at the webhook boundary BEFORE the Inngest envelope is signed**; `payment_method` is dropped entirely), `amount`, `currency`, `failureCode`, plus the generated draft text. **Drafts only, sends nowhere** -- enforced at the DB level by the `messages_external_tier_status_check` CHECK constraint (migration 046); the user decides Send / Edit / Discard for each draft. Per-tenant hourly Anthropic-API cost cap (default $20/hr, configurable per founder) enforced atomically by `record_byok_use_and_check_cap()` plpgsql RPC with leading `SELECT ... FOR UPDATE`. A page-level disclosure banner above the Today section displays the `RUNTIME_COST_DISCLOSURE` notice (Article 13 transparency).

  **Per-tenant scope grants (PR-G, #3947).** As of May 18, 2026, the substrate's `inngest.send` call is gated by a per-tenant `scope_grants` ledger (`public.scope_grants`, migration 048): the webhook produces no Inngest event for an action class unless the founder has an active grant for that class via `/dashboard/settings/scope-grants`. The ledger is append-only WORM (grant, tier change, revocation all preserved); RLS self-select (`auth.uid() = founder_id`); writes routed through three SECURITY DEFINER RPCs (`grant_action_class`, `revoke_action_class`, `anonymise_scope_grants`). Data processed in the ledger: `founder_id` (UUID), `action_class` (text), `tier` (one of `auto` / `draft_one_click` / `approve_every_time`), `granted_at`, `revoked_at`, `revoked_reason`. The grant tier active at the moment of each Inngest event is recorded on the event envelope so the audit ledger pins the consent that was in force when the action ran.

  **Audit viewer (`/dashboard/audit`).** Founders can inspect their own per-tenant runtime ledger via `/dashboard/audit`: (1) BYOK invocations from `audit_byok_use` (RLS + belt-and-suspenders founder filter), (2) Inngest run history via a server-only proxy at `/api/dashboard/runs` (signing-key never reaches the client; raw customer identifier masked server-side; `authorizing_event` rendered only as the redacted summary). Each row inlines a "Request human review →" (`mailto:legal@jikigai.com`) affordance and a "Change authorization →" link to the scope-grants page, satisfying Article 22(3).

  **Legal basis:** contract performance (Article 6(1)(b) GDPR) for the draft generation feature, the scope grants ledger, and the audit viewer; legitimate interest (Article 6(1)(f) GDPR) for the cost-telemetry rows in `audit_byok_use` that evidence per-tenant cap enforcement. **Retention:** draft rows controlled by the user via Send / Edit / Discard; scope grants ledger retained indefinitely as an append-only consent record (the substrate of the user's authorization to act on their behalf), anonymised on account erasure via the Article 17 cascade (`anonymise_scope_grants` runs before the `anonymise_tc_acceptances` step in `server/account-delete.ts`); Inngest event store in the operator-managed SQLite state directory (corrected June 11, 2026: the self-hosted Inngest server performs no automatic deletion of stored events — the previously stated 30-day rolling window was not configuration-backed; not personal-data-bearing because customer email is hashed before persistence). **Sub-processors:** none new (Inngest remains self-hosted; Anthropic receives BYOK API calls under the user's own API key, governed by the user's bilateral Anthropic relationship via BYOK). ADR-030 records the trigger-substrate decision; ADR-031 records the per-tenant scope grants substrate decision.
- **(n)** **CLA evidence archive (off-site):** Jikigai maintains an off-site, tamper-evident archive of CLA signature evidence at a Cloudflare R2 bucket (`soleur-cla-evidence`, region `weur` -- Western Europe). For each signature recorded under Section 2.3(d), a content-addressed evidence record is written to the bucket containing: signer GitHub username, signature timestamp, verbatim sign-comment body, pull-request-of-record, SHA-256 hash of the CLA document text at the PR base SHA, and capture method. Bypass records for allowlisted bot accounts (`dependabot[bot]`, `renovate[bot]`, `claude[bot]`) are recorded once per principal per quarter; the upstream CLA action filters `github-actions[bot]` (DB-id 41898282) so no record is written for that actor. R2 Lock Rules enforce an **age-based ten (10) year retention floor** providing write-once-read-many (WORM) semantics; administrator override is reserved for GDPR Article 17 erasure cases and writes a permanent tombstone (`tombstones/<sha>.deleted.json`) included in the next monthly RFC 3161 manifest, preserving the integrity of the timestamp chain. A monthly cron submits the SHA-256 of the bucket-state manifest to **FreeTSA** ([freetsa.org](https://freetsa.org)) -- an RFC 3161 Time Stamp Authority -- and stores the returned `.tsr` at `timestamps/<yyyy-mm>/manifest.tsr` in the bucket and as a paper trail on the `cla-signatures` branch. FreeTSA receives only the SHA-256 hash and no contributor-identifying data. **Legal basis:** legitimate interest (Article 6(1)(f) GDPR) -- defense of legal claims regarding contributor IP grants under Article 17(3)(e); the three-part balancing test is documented in the GDPR Policy §3.4. **Retention:** ten (10) years on the bucket; monthly RFC 3161 timestamps retained indefinitely as part of the evidentiary chain. **Sub-processors:** Cloudflare R2 (storage, EU region) and FreeTSA TSA (timestamping authority, DE -- EU).
- **(r)** **Action-sends ledger -- low-stakes external sends (`external_low_stakes` class):** The Web Platform records a cryptographic signature for every founder click on Send for an `external_low_stakes` action class (customer status updates, vendor support tickets, personal-handle Bluesky replies, standard Slack DMs). One row per click in the `action_sends` table. Data processed: `user_id` (UUID, nullable for Article 17 anonymise), `message_id` (FK to `messages`), `action_class` (literal from the 11-entry registry), `tier_at_send`, `template_hash` (SHA-256), `per_send_body_sha256` (SHA-256 of the exact outbound body), `recipient_id_hash` (SHA-256 of the recipient identifier -- the raw value is NOT persisted), `clicked_at`, `confirmed_typed` (false for this class -- the 1-click is the consent), `grant_id` (FK to `scope_grants`). **Drafts only, sends nowhere until clicked.** Append-only WORM trigger (`action_sends_no_mutate`, pure-reject) on UPDATE/DELETE. RLS owner-only (`action_sends_owner_select` / `_insert`). **Legal basis:** contract performance (Article 6(1)(b) GDPR) -- the signature is Article 5(2) accountability evidence for the founder's per-send authorization granted by the active row in `scope_grants` (Section 2.3(o)). **Retention:** indefinite as an append-only audit record; Article 17 erasure cascade via `anonymise_action_sends(user_id)` SECURITY DEFINER RPC sets `user_id = NULL` and `recipient_id_hash = '__anonymised__'`, runs BEFORE `anonymise_scope_grants` in `server/account-delete.ts` (FK ordering: both `action_sends.user_id` and `action_sends.grant_id` are `ON DELETE RESTRICT`). **Sub-processors:** none new; outbound delivery integrations for `external_low_stakes` action classes defer to PR-I (#4078). **Workspace synchronization (#4224):** workspace synchronization runs outside the operator's session on receipt of a GitHub `push` webhook for the operator's default branch — the Soleur backend pulls the connected repository's `knowledge-base/` content into the operator's workspace clone at `userData.workspace_path` via installation-scoped credentials (`git pull --ff-only`, Inngest-coalesced per `installation_id`). This is a controller-side filesystem write side-effect — distinct from the display-only signal ingestion under Section 2.3(o); see Article 30 register Processing Activity 17 sub-purpose (b)(ii).
- **(s)** **Action-sends ledger -- brand-critical external sends (`external_brand_critical` class) and digest tier (`auto_with_digest`):** Same `action_sends` table as Section 2.3(r) with two additions. **(i)** Brand-critical classes (marketing email blasts, public X threads, Soleur-handle Bluesky replies, enterprise-tier Slack DMs) require a typed-confirm modal at click-time: the founder types `SEND` verbatim before the server-side route handler accepts the signature row (`confirmed_typed = true`, `approval_signature_sha256 = sha256(canonical-JSON({founder_id, message_id, typed_value, ts}))`). The typed-confirm gate is server-side re-validated case-sensitively (no `.trim()` / `.normalize()` -- the load-bearing TOM per Article 32). Article 22(2)(c) explicit consent applies. **(ii)** The `auto_with_digest` tier records `tier_at_send='auto_with_digest'` rows for autonomous `infra.*` action classes (dependency bumps, log rotations). The daily-digest aggregator that surfaces these rows in the dashboard ships in PR-I (#4078); PR-H ships the tier value and the signature substrate so disclosure is forward-honest about the tier semantics that founders see in the `/dashboard/settings/scope-grants` UI starting at PR-H merge. Article 22(3) right to human review for `auto_with_digest`: founders may revoke the grant at any time at `/dashboard/settings/scope-grants`, and the next-business-day digest review window (PR-I) IS the human-review path. **Defense-in-depth at the DB level:** both `scope_grants` and `action_sends` carry CHECK constraints (`scope_grants_action_class_not_locked`, `action_sends_action_class_not_locked`) rejecting any `action_class` matching `^(payment|legal|auth)\.` so a 5th-class action (refunds, legal signatures, credential rotation) cannot be persisted through indirect routes -- enforcing the "per-command-ack" hard rule at the DB. **Legal basis, retention, sub-processors:** identical to Section 2.3(p); outbound delivery integrations for the brand-critical and `auto_with_digest` action classes defer to PR-I.

- **(p)** **LinkedIn Company Page publication:** Jikigai operates the Soleur LinkedIn Company Page at `https://www.linkedin.com/company/soleur/` (Page slug `soleur`) as a marketing, community, and case-study distribution channel for Soleur content. The processing has three sub-surfaces. **(i) Page operation:** posts published by `scripts/content-publisher.sh` (cron) via the LinkedIn Community Management API (developer app 229658411) on the `w_organization_social` scope. Content is operator-authored — no Soleur-user personal data is included in posts, no `@mention` of LinkedIn members, no follower-list extraction. Operational secrets: `LINKEDIN_ORG_ACCESS_TOKEN` (60-day OAuth bearer, Doppler `prd` + GitHub Actions secret) and `LINKEDIN_ORG_ID` (organisation URN). **(ii) Page Insights consumption (aggregate only):** LinkedIn Page Insights API surfaces aggregate engagement metrics (impressions, reactions, comments, shares, follower growth) to the Page admin. Soleur consumes these in aggregate to measure marketing effectiveness; no per-individual decisioning, no profiling on Soleur infrastructure, no cross-referencing against Soleur user records. Under CJEU C-210/16 *Wirtschaftsakademie* and EDPB Guidelines 07/2020, the Page admin (Jikigai) and LinkedIn Ireland are **joint controllers** for Page Insights analytics — the standard joint-controller arrangement provided by LinkedIn in its Page Admin terms governs. **(iii) K-bis business-verification transfer (one-time):** the K-bis extract (gérant full legal name, Jikigai SARL registered address, French commerce-registry number (RCS Paris 927 585 729), capital structure metadata) plus tax registration and registered-address proof is transmitted once to Microsoft Ireland Operations Ltd via the LinkedIn Page admin upload UI at the appeal-flow step, satisfying Microsoft's KYC-equivalent business-verification requirement under the LinkedIn Platform Terms. **Legal bases:** sub-surfaces (i) and (ii) — legitimate interest (Article 6(1)(f) GDPR); the full three-part purpose / necessity / balancing test is documented at `knowledge-base/legal/legitimate-interest-assessments/2026-05-19-linkedin-org-page-lia.md`. Sub-surface (iii) — legal obligation (Article 6(1)(c) GDPR) under the LinkedIn Platform Terms. **Retention:** posts and engagement metadata controlled by LinkedIn under its Page-data retention policies (Soleur does not set the envelope); `LINKEDIN_ORG_ACCESS_TOKEN` rotates on 60-day vendor-mandated cadence; K-bis documents retained by Microsoft Ireland under its EU Data Boundary commitments (no Soleur-side persistence beyond operator's local file system at the upload moment); internal rolling-tracker GitHub Issue (#4046) closed on completion. **Sub-processors / recipients:** LinkedIn Ireland Unlimited Company (Section 4.2 LinkedIn row) and Microsoft Ireland Operations Ltd (Section 4.2 Microsoft row). **Right of erasure (Article 17) — LinkedIn-published content carve-out:** see Section 10.3 below. Article 30 register entry: Processing Activity 15 (`knowledge-base/legal/article-30-register.md`).

- **(u)** **Workspace co-member data category (team-workspace feature, PR #4289):** Where the team-workspace feature is enabled for a Workspace Owner's organization (gated by `FLAG_TEAM_WORKSPACE_INVITE` and the per-organization allowlist `TEAM_WORKSPACE_ALLOWLIST_ORG_IDS`), the Web Platform admits additional natural persons ("Co-Members") to access the Workspace Owner's account. Data processed in this category: (i) `workspace_members.user_id` (FK to `auth.users` — the Co-Member's account identifier); (ii) `workspace_members.workspace_id` and `workspace_members.organization_id` (the owner's container identifiers); (iii) cross-member visibility of `messages`, `conversations`, `kb_files`, `kb_chunks`, `scope_grants`, `action_sends`, and `template_authorizations` rows scoped to the same `workspace_id`; (iv) `workspace_member_attestations` WORM records of the invite + accept events (Article 5(2) accountability evidence). **Legal basis:** contract performance (Article 6(1)(b) GDPR) — workspace participation is mediated by the employment, contractor, or consultancy agreement between the Workspace Owner and the Co-Member (attestation recorded at the invite endpoint per AUP Section 5.5). **Visibility scope:** Co-Members are recipients (Article 13(1)(e) GDPR) of one another's workspace-scoped metadata for in-workspace operations they participate in; each Co-Member retains independent Article 15 through 22 rights against Jikigai for rows identifiable to that Co-Member. The load-bearing technical measure (Article 32 GDPR) is the SECURITY DEFINER `is_workspace_member(workspace_id, user_id)` helper (migration 053, `search_path = pg_temp`) wired into the RLS predicates on the six affected tables. **Retention:** until each Co-Member's Article 17 erasure cascade fires; the `anonymise_workspace_member_attestations` → `anonymise_workspace_members` → `anonymise_organization_membership` RPC chain in `server/account-delete.ts` steps 3.90–3.92 runs BEFORE `auth.admin.deleteUser` (per Article 30 register PA-2 amendment). **Sub-processors:** none new — co-members are NOT processors under Article 28 (see Section 4.2 carve-out below); access is contract-mediated under the Anthropic Commercial Terms §C "authorized users" framework. **Connected repository + derived knowledge-base (ADR-044, migrations 079/080/081, #4543 / #4558).** As of ADR-044, GitHub repo-connection state (`repo_url`, `repo_provider`, `github_installation_id`, `repo_status`, `repo_last_synced_at`) is workspace-scoped — relocated from the per-user `users` table to the `workspaces` table — rather than per-user. The user-facing consequence is that **joining a workspace as a Co-Member grants the Co-Member access to that workspace's connected GitHub repository and the knowledge-base derived from it**: the Workspace Owner's repository content and repo-derived knowledge-base are processed for the workspace's Co-Members at the workspace-ownership grain (previously a joined member could not reach the owner's repo). No new third-party recipient results — GitHub Inc. (Microsoft) remains the upstream source already disclosed under Section 4.2, and the new recipients are the workspace's own Co-Members already named in this Section. **Lawful basis** for the Co-Member's processing of the Owner's repo/KB data: the Owner's Article 6(1)(a) consent captured in the migration-058 `workspace_member_attestations` invite attestation (the inviter attests the invited member will access this workspace's connected repository and knowledge-base) together with the existing Article 6(1)(f) legitimate-interest basis for repo-derived signals (Article 30 register PA-17). **Credential gate (Article 32 GDPR):** the `github_installation_id` is a GitHub App token grant (a credential) and is **column-level REVOKED** from the `authenticated` role; it is readable only via the membership-checked SECURITY DEFINER RPC `resolve_workspace_installation_id(p_workspace_id)`, which returns NULL to non-members. Repo reads resolve from `workspaces` only (single source of truth — no per-user fallback). See ADR-044 (`knowledge-base/engineering/architecture/decisions/ADR-044-workspace-repo-ownership.md`) and Article 30 register Processing Activity 17 sub-clause (c). **Co-Member responsibility framework:** the Workspace Owner is responsible for ensuring each invited Co-Member is bound by appropriate confidentiality and intellectual-property assignment terms covering Terms & Conditions Section 3b obligations. The Owner may satisfy this by any sufficient means, including reliance on the Co-Member's click-through acceptance of the Soleur Terms & Conditions, reliance on an existing employment/contractor/consultancy agreement that already contains equivalent terms, or execution of the optional Soleur Side Letter template (`knowledge-base/legal/side-letter-template.md`) as a separate bilateral instrument. The Side Letter is offered as a belt-and-braces reference document; Jikigai does not require its execution. **IP-assignment note:** click-through Terms acceptance is sufficient for confidentiality reliance but does NOT by itself effect intellectual-property assignment from the Co-Member to the Workspace Owner; Owners who require enforceable IP assignment should rely on an instrument (existing engagement or Side Letter) that names the Owner as the assignee. Article 30 register entry: existing Processing Activity 2 (`knowledge-base/legal/article-30-register.md`, amended for "workspace co-member" data category in PR #4225). See Terms & Conditions Section 3b and Privacy Policy Section 4.11 for the user-facing surface.

- **(t)** **Template-authorization ledger (`template_authorizations`, PR-I #4078):** The Web Platform records a per-(founder, template_hash) authorization row keyed off the canonical pre-personalisation template hash (SHA-256 over `body_template` from the code-static template registry at `apps/web-platform/server/templates/template-registry.ts`). One row per (founder, template) pair captures the consent envelope: `expires_at` (NOT NULL DEFAULT now()+90d — provisional, calibration #4217), `soft_reconfirm_at` (NOT NULL DEFAULT now()+30d), `max_sends` (NOT NULL DEFAULT 100), `grant_id` (FK to `scope_grants(id)` ON DELETE RESTRICT — pins the parent grant), `revoked_at`, `revocation_reason`. **Paired-null invariant:** `(revoked_at IS NULL) = (revocation_reason IS NULL)` at the DB CHECK layer. **8-value revocation_reason enum** (`founder_revoked`, `quota_exhausted`, `expired`, `dsr_erasure`, `regulator_ordered`, `vendor_tos_revoked`, `policy_violation`, `quarantine_retroactive`) at the DB CHECK layer; the over-provision (vs. cutting to 5) preserves Article 5(2) audit attribution distinguishability for future revocation drivers (three values — `regulator_ordered`, `vendor_tos_revoked`, `policy_violation` — have no v1 producer; `quarantine_retroactive` reserved for PR-I+1 #4216). Cheaper to add at mig 053 than ALTER later. **First-send-IS-authorization:** the Send click on a labeled `draft_one_click` button — with PR-H's typed-confirm context for higher-friction tiers — IS the Article 7(3) "specific" + "informed" consent act; the send route auto-calls `authorize_template` RPC on no-existing-row, then writes `action_sends` in the same request. Subsequent sends gate on the bounds via the `isTemplateAuthorized` predicate (single-conceptual-query two-probe AFTER `isGranted`; fail-closed `PredicateException` on DB error → 500 + Sentry capture; auto-revoke best-effort side effect on expired / quota-exhausted detection keeps the scope-grants UI honest). **Append-only WORM trigger** (`template_authorizations_no_mutate`, mig 053 — mirrors mig 051 pattern) on UPDATE/DELETE; all founder revoke + Article 17 anonymise paths route through SECURITY DEFINER RPCs that bypass via `SET LOCAL session_replication_role='replica'`. RLS owner-only (`template_authorizations_owner_select` / `_insert`). **Legal basis:** contract performance (Article 6(1)(b) GDPR); the row is the Article 5(2) accountability evidence for the founder's per-template consent. Article 7(3) "as easily withdrawable as given" satisfied by the per-row Revoke button at `/dashboard/settings/scope-grants` (component `apps/web-platform/components/scope-grants/template-authorization-row.tsx`). **Retention:** indefinite as an append-only audit record; Article 17 erasure cascade via `anonymise_template_authorizations(p_user_id)` SECURITY DEFINER RPC sets `founder_id = NULL`, `revoked_at = COALESCE(revoked_at, now())`, `revocation_reason = COALESCE(revocation_reason, 'dsr_erasure')`. Cascade ordering in `server/account-delete.ts`: BETWEEN `anonymise_action_sends` (Section 2.3(r)+(s)) and `anonymise_scope_grants` (Section 2.3(o)). The ordering is **semantic, not FK-driven** — `anonymise_*` performs UPDATE so the ON DELETE RESTRICT on `template_authorizations.grant_id → scope_grants.id` does not fire; the load-bearing invariant is that `dsr_erasure` must be set on these child rows BEFORE the parent grant's `founder_id` is nulled, otherwise Article 5(2) attribution breaks. **Sub-processors:** none new (substrate is internal to the existing Supabase row). Article 30 register entry: Processing Activity 18 (`knowledge-base/legal/article-30-register.md`). See also ADR-035 at `knowledge-base/engineering/architecture/decisions/ADR-035-template-registry-code-static.md` for the code-static template registry decision; this entry captures the un-revocability + Article 5(2) attribution rationale formerly proposed as ADR-036 (folded here per plan §Phase 10 v2 review).

- **(v)** **Workspace member removal audit ledger (`workspace_member_removals`, PR #4294, distinct from PA-20 `workspace_member_actions` covered in the byline above):** The Web Platform records a write-once-read-many (WORM) audit row in the `workspace_member_removals` ledger whenever a Co-Member is removed from a Workspace Owner's organization (introduced by migration 062 / PR #4294 — Article 30 register Processing Activity 19, distinct from PA-20 which covers the broader `workspace_member_actions` add/remove/role-change ledger from migration 063 / PR #4231). **Data processed:** `id` (UUID, PK — strictly immutable lineage), `workspace_id` (UUID, NULLable — FK to `public.workspaces(id)` `ON DELETE SET NULL`; may transition to NULL post-erasure if the parent workspace and organization are deleted as part of the orphan-org cleanup at cascade step 3.92, per ADR-039 §Invariants workspace_id carve-out — once the workspace and org are gone, no remaining co-member can read the row via RLS anyway), `removed_user_id` (UUID, NULLable — FK to `public.users(id)` `ON DELETE RESTRICT`; the departed Co-Member's account identifier; preserved as identifiable for Article 15 access until the Article 17 cascade fires), `removed_by_user_id` (UUID, NULLable — FK to `public.users(id)` `ON DELETE RESTRICT`; the actor, typically the Workspace Owner), `removed_at` (timestamptz, NOT NULL, strictly immutable lineage). No free-text removal reason, no IP hash, no user-agent — the removal event is purely structural-relational, deliberately lower-severity than the sibling `workspace_member_attestations` ledger (which carries `attestation_text` + `ip_hash` + `user_agent`). **Legal basis:** contract performance (Article 6(1)(b) GDPR) — the row is Article 5(2) accountability evidence for the workspace-membership lifecycle event, mediated by the same employment/contractor/consultancy agreement between the Workspace Owner and the Co-Member that established the membership under Section 2.3(u). **Retention:** at least 36 months (shortest-applicable-jurisdiction Article 82(2) limitations floor — see ADR-039 for the retention-rationale derivation, including the per-jurisdiction comparative-limitations table); ledger rows beyond the floor remain on the substrate as append-only history. **Article 17 erasure cascade:** `anonymise_workspace_member_removals(p_user_id)` SECURITY DEFINER RPC sets `removed_user_id = NULL` AND `removed_by_user_id = NULL` (both PII columns) for every row where either FK matched the departing user; lineage columns (`id`, `removed_at`) are preserved post-erasure unconditionally for Article 5(2) accountability, and `workspace_id` is preserved while the parent workspace exists (it may transition to NULL via the FK's `ON DELETE SET NULL` cascade once the parent workspace + organization are themselves deleted at step 3.92 — see Data processed above). Cascade ordering in `server/account-delete.ts`: step 3.905 — runs BEFORE step 3.91 (`anonymise_workspace_members` cascade DELETE), BEFORE step 3.93 (`anonymise_workspace_member_actions`, PA-20), and BEFORE step 4 (`auth.admin.deleteUser`). **WORM trigger bypass mechanism:** the `workspace_member_removals_no_mutate` trigger admits UPDATEs only when (a) lineage columns (`id`, `workspace_id` while non-NULL, `removed_at`) are unchanged AND (b) PII columns (`removed_user_id`, `removed_by_user_id`) transition NOT NULL → NULL only — i.e. structural-shape detection at the trigger body itself, not a GUC bypass. No `SET LOCAL session_replication_role` is issued by the anonymise RPC; the column-state transition IS the authorization. Trigger-DELETE path is analogously row-state-gated (`OLD.removed_at < now() - interval '36 months'`), enabling the pg_cron retention sweep without role gating. This is the post-#4294-review deliberate divergence from the mig 037 / mig 051 `session_replication_role` precedent per ADR-039 §Invariants and learning `2026-05-18-worm-trigger-bypass-role-check-fails-under-postgrest-routing.md` (pg_cron runs as `postgres`, not `service_role`, so role-gated bypasses silently fail). **PA-19 ⊥ PA-20 disambiguation:** PA-19 (`workspace_member_removals`) and PA-20 (`workspace_member_actions`) are sibling but distinct ledgers — different substrates, different retention floors (PA-19 = 36 months under Art. 82(2) shortest-applicable-jurisdiction limitations period; PA-20 = 7 years under the workspace-actions accountability rationale recorded in #4231), different anonymisation RPCs. A single Co-Member removal writes one row to PA-19 (per-removal event class, used for departed-Co-Member DSAR Art. 15 retrieval after the workspace RLS predicate ceases to grant access) AND one row to PA-20 (full membership-lifecycle change-log including add / remove / role-change, used for Workspace-Owner-side regulator-inquiry answering under Art. 5(2) accountability). The duplication is deliberate — each ledger serves a distinct DSAR access-pattern and retention envelope; PA-19's shorter floor reflects the departed-user-rights-centric purpose, PA-20's longer floor reflects the in-workspace audit-trail purpose. Disclosures that name "the workspace_member_* ledger" without qualification are ambiguous and MUST be avoided. **DSAR access (Article 15):** departed Co-Members cannot read their PA-19 row via the workspace's RLS predicate after removal (the SECURITY DEFINER `is_workspace_member()` helper returns false post-removal); Article 15 requests grounded on PA-19 are fulfilled via the service-role DSAR worker. The allowlist entry `workspace_member_removals: { ownerField: "removed_user_id", article: "15" }` (in `apps/web-platform/server/dsar-export-allowlist.ts`) keys on the **owner-of-record** — the departed Co-Member — and the export query in `dsar-export.ts` filters single-arm on `.eq("removed_user_id", expectedUserId)` with `assertReadScope` enforcing per-row ownership match before any row enters the bundle. A Workspace Owner exercising their own Art. 15 will see rows where THEY were removed (e.g., from a workspace they previously joined as a Co-Member), but **not** the rows where they performed removals on others — those rows belong to the removed user under Art. 15 owner-of-record semantics and surface to that user's own DSAR. Within the live workspace, the Owner sees their outgoing removals via the cookie-scoped RLS path (`removals_select_for_members` policy), which is the working-controller audit surface (not the DSAR surface). The `/dashboard/settings/privacy` self-serve ZIP export includes the PA-19 row identically to other allowlisted tables when the requester is the departed Co-Member. **Sub-processors:** none new — substrate is internal to the existing Supabase row; PA-19 disclosure is the Article 30(1)(g) recipient-disclosure discharge. **Cross-references:** Section 2.3(u) for the parent workspace-membership substrate; Privacy Policy Section 8.1 "Departed workspace members" paragraph block for the data-subject-facing surface; GDPR Policy Section 5.3 "Workspace member removal audit ledger (PA-19)" bullet for the rights-exercise surface. Article 30 register entry: Processing Activity 19 (`knowledge-base/legal/article-30-register.md`). See also ADR-039 (`knowledge-base/engineering/architecture/decisions/ADR-039-departed-member-removal-ledger.md`) for the WORM-ledger invariants, retention rationale, cascade ordering, and the RLS deviation (departed members CANNOT read their own removal row via `is_workspace_member` — DSAR service-role read only).

- **(w)** **Delegated-credential prompt routing (`byok_delegations` + `byok_delegation_acceptances` + `byok_delegation_withdrawals`, PR-B #4232 + consent enforcement #4625):** When the `BYOK_DELEGATIONS_ENABLED` feature flag is ON for a given organization, a Workspace Owner ("Grantor") may fund a Co-Member's ("Grantee") AI agent runs by delegating access to the Grantor's Anthropic API key, subject to daily and hourly USD spending caps enforced by `check_and_record_byok_delegation_use` (migration 064, PR-A #4290). **Data processed:** delegation rows (`grantor_user_id`, `grantee_user_id`, `workspace_id`, `daily_cap_cents`, `hourly_cap_cents`, `created_at`, `revoked_at`, `revocation_reason`, cap-update timestamps and actor IDs); acceptance rows in `byok_delegation_acceptances` (migration 074, PR-B #4232: `user_id`, `delegation_id`, `accepted_at`, `side_letter_version`, `ip_hash` SHA-256 of client IP, `user_agent`); cost telemetry via `audit_byok_use.delegation_id` (token count, cost in cents, agent role — NO prompt content, NO response content). The Grantor receives itemized cost telemetry only; the Grantee's prompt and response content is NOT exposed to the Grantor. **Joint controllership:** Article 26 GDPR between Grantor and Grantee for the Grantee's prompt content routed through the Grantor's API key. Anthropic PBC remains the processor under the Grantor's existing DPA; no new sub-processor relationship is created. **Legal basis (dual, non-interchangeable):** the Grantee's processing of their prompt content under the Grantor's key — and the Grantor's visibility of the Grantee's cost telemetry — rests on the **Grantee's Article 6(1)(a) consent**, recorded in `byok_delegation_acceptances` (the acceptance row is the Article 7(1) demonstrability evidence: `side_letter_version`, `ip_hash`, `user_agent`, `accepted_at`). The separate **Article 6(1)(b) contract** basis covers ONLY the funding records in `byok_delegations` (the delegation row as the grantor↔grantee funding arrangement); it is NOT the basis for, and does not substitute for, the consent that authorises prompt-content processing. **Consent is enforced, not merely evidenced:** `resolve_byok_key_owner` leases the Grantor's key only while a current-version acceptance exists (migration 083), so processing cannot begin before consent, and a server-owned `current_byok_side_letter_version()` fail-closes stale-version acceptances. **Withdrawal (Article 7(3), "as easy to withdraw as to give"):** the Grantee may withdraw at any time via the grantee-only `withdraw_byok_delegation_consent` RPC, which appends a write-once row to `byok_delegation_withdrawals` (migration 084); withdrawal blocks new key leases at the resolver AND stops any in-flight run's billing within one turn (the run's remaining cost is debited to the Grantee, not the Grantor). Withdrawal is non-terminal — re-accepting reactivates. **Retention:** 7 years (financial audit trail, matching `tc_acceptances`). **Article 17 erasure cascade:** `anonymise_byok_delegations(p_user_id)` (step 5.10 in `server/account-delete.ts`) + `anonymise_byok_delegation_acceptances(p_user_id)` (step 5.11) + `anonymise_byok_delegation_withdrawals(p_user_id)` (step 5.12). All run BEFORE `auth.admin.deleteUser` per ON DELETE RESTRICT FK ordering. **Sub-processors:** none new. **Cross-references:** Delegation Consent Side Letter template (`knowledge-base/legal/delegation-consent-side-letter-template.md`); AUP Section 5.6; Article 30 register Processing Activity 23 (`knowledge-base/legal/article-30-register.md`).

- **(x)** **Team activity feed (`workspace_activity`, PR-B #4521):** Where the team-workspace feature is enabled, the Web Platform records workspace activity events when members join or leave, or when conversations are shared with the workspace. **Data processed:** `workspace_id` (UUID, FK to `workspaces`), `actor_user_id` (UUID, NULLable, FK to `auth.users` ON DELETE SET NULL), `event_type` (text: `member_join`, `member_leave`, `conversation_shared`), `metadata` (JSONB — conversation_id for shared events), `created_at` (timestamptz). **Legal basis:** contract performance (Article 6(1)(b) GDPR) — necessary to provide the shared-workspace collaboration features the user activated. **Retention:** 90 days, enforced by pg_cron `workspace_activity_purge` (daily at 03:00 UTC). **Article 17 erasure cascade:** `anonymise_workspace_activity(p_user_id)` SECURITY DEFINER RPC sets `actor_user_id = NULL` and `metadata = '{}'` for every row referencing the departing user; runs at `server/account-delete.ts` step 3.935 BEFORE `auth.admin.deleteUser`. **Visibility scope:** workspace members only (RLS `workspace_activity_member_select` policy via `is_workspace_member()`). JTI deny RESTRICTIVE policy applied. No client INSERT — all writes via service_role `record_workspace_activity` RPC. **Sub-processors:** none new. Article 30 register entry: Processing Activity 24 (`knowledge-base/legal/article-30-register.md`).

- **(y)** **Knowledge-base file metadata (`kb_files`, PR-C #4521):** The Web Platform records metadata for files uploaded to the shared knowledge base. **Data processed:** `workspace_id` (UUID, FK to `workspaces`), `user_id` (UUID, NULLable, FK to `auth.users` ON DELETE SET NULL — the uploader), `file_path` (text), `filename` (text), `visibility` (text: `private` or `workspace`, DEFAULT `workspace`), `uploaded_at` (timestamptz), `updated_at` (timestamptz, auto-set via trigger). **Legal basis:** contract performance (Article 6(1)(b) GDPR) — necessary to provide uploader attribution and file visibility controls in the shared knowledge base. **Retention:** lifetime of the workspace (no automated purge; files deleted manually by workspace owner). **Article 17 erasure cascade:** `user_id` SET NULL via FK cascade on `auth.admin.deleteUser`. **Visibility scope:** owner sees own files + workspace members see `visibility = 'workspace'` files (RLS `kb_files_owner_or_shared` policy). Column-level REVOKE on `visibility` and `workspace_id` — mutations via `set_kb_file_visibility` SECURITY DEFINER RPC only (owner-only). JTI deny RESTRICTIVE policy applied. **Sub-processors:** none new. Article 30 register entry: Processing Activity 25 (`knowledge-base/legal/article-30-register.md`).

- **(z)** **Workspace logo image (`workspace-logos` bucket, #4916):** A Workspace Owner may upload a custom square logo image (PNG or WebP, re-encoded server-side to WebP, ≤1 MB) that renders in the workspace chrome (the `WorkspaceIdentityTile`). **Data processed:** an image file stored as a binary in the **private** Supabase Storage bucket `workspace-logos`, keyed by `workspace_id`. The logo is a **shared workspace branding asset, NOT personal data about the uploader** — no uploader-attribution column is stored in v1, and EXIF metadata is stripped at re-encode. **Legal basis:** contract performance (Article 6(1)(b) GDPR) — performance of the workspace-collaboration contract — together with legitimate interest (Article 6(1)(f) GDPR) in retaining a shared workspace asset. **Visibility scope:** members of the workspace only (owner-only RLS write + member-only RLS read), served via a short-TTL signed-URL proxy with `X-Content-Type-Options: nosniff`. **Security (Article 32 GDPR):** raster-only ingest (SVG rejected); server-side `sharp` decode-and-re-encode to WebP neutralises polyglot / decode-bomb payloads and strips EXIF; private bucket; ≤1 MB cap. **Retention:** lifetime of the workspace (Article 5(1)(e)) — purged when the sole-owned workspace is torn down on account deletion (the account-delete cascade calls `purgeWorkspaceLogoObjects`); **NOT** purged on shared-workspace member removal (it is a shared asset, not the departing member's personal data). **DSAR (Article 15 / 20):** **excluded from the automated per-user DSAR export in v1**, defensible under Article 15(4) (rights and freedoms of others) — the logo is a workspace-keyed shared asset with no uploader-attribution in v1, not personal data of a single user; the manual Article 15 path covers any edge case. **Sub-processors:** **none new** — same region (eu-west-1) and sub-processor (Supabase Inc) as the existing `chat-attachments` (Section 4.7 of the Privacy Policy) and `dsar-exports` (Section 2.3(l)) buckets. Article 30 register entry: Processing Activity 26 (`knowledge-base/legal/article-30-register.md`).

- **(aa)** **AI-assisted triage of inbound operational email (`email_triage_items`, ops@soleur.ai, #5103):** Inbound email to the company operational address `ops@soleur.ai` is kept in the company Proton mailbox (forward-and-keep — the durable original) and forwarded to Resend Inbound (US), which delivers a svix-signed **metadata-only** webhook to the Web Platform; an Inngest pipeline then runs a deterministic, rule-based statutory fast-path (data-subject requests / breach notifications / service of process / regulator correspondence — detected from subject, sender, and attachment filenames BEFORE any body fetch and with **no LLM on that path**), and for other mail transiently fetches the body (hard-truncated to 64 KiB, sanitized) and produces a 1–3 sentence summary plus category via one Anthropic API call. **Data processed at rest** (`email_triage_items` — the table has NO body column; parse-and-discard is schema-enforced): `subject`, `sender` (raw From header value), `message_id`, `resend_email_id`, `summary`, `mail_class`, `statutory_class`, `rule_id`, `status` / `status_changed_at` / `acknowledged_at`, `received_at` (+ source provenance), `created_at`, owner `user_id`. Attachment content is never downloaded (filename + content type only). **Data subjects:** inbound senders and persons mentioned in mail — **involuntary data subjects**; information is provided via Privacy Policy Section 4.13 under the Article 14(5)(b) disproportionate-effort posture. **Legal basis:** legitimate interest (Article 6(1)(f) GDPR) — LIA at `knowledge-base/legal/legitimate-interest-assessments/2026-06-11-operator-inbox-triage-lia.md`. **Retention:** synthetic-probe items 7 days; non-statutory items 365 days; statutory items retained for the accountability period (Article 17(3)(b) carve-out — the retention sweep's predicate excludes rows carrying a statutory class); Resend retains the forwarded raw mail **30 days on all plans with no deletion API** (vendor-fixed window); Anthropic retains API request content up to 30 days by default (Zero-Retention amendment tracked in `knowledge-base/legal/data-processing-agreements/anthropic.md`); subject + sender persist in the self-hosted, operator-managed Inngest event store (the same localhost-bound SQLite store described in Section 2.3(o); the self-hosted Inngest server performs no automatic deletion of stored events, so this copy persists until operator-side store maintenance; a PII surface, not a third-party recipient). **Erasure (Article 17):** the triage ledger is write-once (WORM trigger, column-frozen); deletion and anonymisation run exclusively through gated SECURITY DEFINER RPCs (`purge_email_triage_items`, `anonymise_email_triage_items` — the latter NULLs `user_id` + `sender`), making every erasure auditable; statutory rows may be retained under Article 17(3)(b), applied per-request and documented. **TOMs (Article 32):** deterministic pre-LLM statutory fast-path; structural no-body-column guarantee; WORM mutation matrix; svix webhook signature verification (fail-closed on unset secret); RLS owner-only read scoping; closed AI output allowlist (the model structurally cannot write a statutory class or the probe class); observability scrub (`subject` / `sender` / `from` / `to` in the sensitive-key set) so third-party PII never reaches error-monitoring or log sinks; volume bounds (60/hour throttle + 200/day AI-call ceiling). **Automated decision-making:** none in the Article 22 sense — output is an internal summary and category for one human operator. **Sub-processors:** Resend Inc (US, DPF + SCCs — see the Section 4.2 row), Anthropic PBC (US, DPF + SCCs Modules 2+3 — non-statutory mail only; see the Section 4.2 row), Supabase Inc (EU — existing). Article 30 register entry: Processing Activity 27 (`knowledge-base/legal/article-30-register.md`); DPIA screening memo at `knowledge-base/legal/audits/2026-06-11-dpia-screening-operator-inbox-triage.md`.
- **(ab)** **Agent-assisted, human-approved outbound cold email (`email_suppression` + `outbound_sends`, `outbound.soleur.ai`, #5325):** The **outbound counterpart to (aa)** — and the activity that overturns the 2026-06-11 inbound LIA's send-authority deferral. A founder- or agent-drafted body is surfaced to the operator by a `gated`-tier tool (`email_send`, `email_reply`, `email_suppress`) showing the **exact** recipient + body; the operator approves (the approval is **body-hash-bound and single-use**); the single compliance chokepoint `server/email-triage/outbound.ts` `sendCompliantOutbound` runs an ordered, refuse-to-send gate (domain-verified precondition → CLO conditions C1–C5 → RFC-5322 header-injection guard → recipient allow-list → body-hash approval match → in-transaction suppression recheck → Resend → WORM audit row) and sends from the dedicated subdomain `outbound.soleur.ai` via Resend (eu-west-1, SPF/DKIM/DMARC p=quarantine→reject) — a separate sender-reputation stream, distinct from the `soleur.ai` apex transactional sender (`notifications@soleur.ai`); same registrable domain, distinct subdomain. **No LLM in the send path.** **Data processed at rest** — two tables (migration 104), keyed-pseudonymised only; the **plaintext recipient address is NEVER persisted** and **bodies are NEVER persisted** (only hashes): (1) `email_suppression` — `owner_id`, `recipient_hash`, `reason`, `added_at` (per-founder permanent opt-out set; **monotonic — no un-suppress**); (2) `outbound_sends` — `owner_id`, `recipient_hash`, `approved_body_sha256`, `per_send_body_sha256`, `resend_id`, `action_class`, `sent_at` (append-only WORM cold-send audit). **`recipient_hash = HMAC-SHA-256(EMAIL_HASH_PEPPER, normalize(email))`** — deterministic keyed pseudonymisation (Article 32); the pepper is held in Doppler (`EMAIL_HASH_PEPPER`), not in-DB, so the hash is not reversible by a DB reader. **In transit:** the plaintext recipient address and the founder/agent-drafted body transit the operator review gate (in-app) and Resend (sending); **no body is sent to any LLM** on the outbound leg. **Data subjects:** outbound recipients — **involuntary, third-party data subjects** whose contact details were obtained **NOT from them** (e.g. a journalist's published professional byline) → **Article 14 applies**; plus the founder/operator (`owner_id`). Article 14 information is provided **in-line in the first EU/UK communication** via CLO condition **C3** (six discrete present element predicates — identity, purpose, legal-basis = legitimate-interest, data source/category, retention, rights); **no reliance on the Article 14(5)(b) disproportionate-effort exemption** (contrast (aa) above) — the first contact is itself an email to the subject, so in-line disclosure is required and feasible. **Legal basis:** legitimate interest (Article 6(1)(f) GDPR; Recital 47) — LIA at `knowledge-base/legal/legitimate-interest-assessments/2026-06-15-outbound-email-authority-lia.md` (channel-level ePrivacy / UK PECR / FR CPCE-CNIL analysis flagged as a pending counsel item). **Retention:** `email_suppression` permanent / monotonic (retention IS the opt-out guarantee); `outbound_sends` retained for the accountability period (Article 5(2)) as third-party-facing evidence each cold send was human-approved and carried C1–C5 (Article 17(3)(b) override — same logic as (aa)'s statutory rows); Resend retains the sent content **30 days on all plans with no deletion API** (vendor-fixed window). **Erasure (Article 17):** `outbound_sends` is write-once (WORM trigger, owner-RLS); deletion/anonymisation run through service-role-only RPCs (`anonymise_email_suppression`, `anonymise_outbound_sends` — NULL `owner_id` + scrub `recipient_hash`), called by `account-delete.ts` before auth deletion; no self-service erasure of the audit (third-party-facing accountability evidence, Article 5(2)); the less-intrusive partial measure is the `recipient_hash` scrub that preserves the compliance-evidence shape. **TOMs (Article 32):** permanent per-send human-approval gate (no auto-send for cold mail, ever — the decisive control); body-hash-bound single-use approval; single compliance chokepoint, ordered refuse-to-send (every gate throws before Resend); CLO conditions C1–C5 (postal footer, opt-out, Article 14 six-predicate disclosure, FTC material-connection disclosure, permanent opt-out suppression with a send-time refuse-to-send corollary — mechanical presence enforced, semantic correctness owned by the human approver); in-transaction suppression recheck (suppression set re-read inside the send transaction, so a same-instant opt-out wins); anti-exfiltration recipient controls (`email_reply`'s recipient derived **server-side** from the persisted inbound item's sender, never from agent args; internal/own-domain `*@soleur.ai`, `*@outbound.soleur.ai`, `*@jikigai.com` and role/bare addresses rejected by the allow-list); EU/UK-strict jurisdiction default on low confidence; RFC-5322 header-injection guard; keyed-HMAC pseudonymisation at rest (pepper segregated in Doppler); WORM `outbound_sends` (append-only pure-reject trigger, owner-RLS); PII-free observability (the chokepoint emits an op + **hashed** userId only, and passes a **synthetic Error** so a raw Resend error cannot echo the recipient/body to error-monitoring or log sinks); domain isolation (the dedicated `outbound.soleur.ai` subdomain — a separate sender-reputation stream isolating cold-outreach reputation from the `soleur.ai` apex transactional sender `notifications@soleur.ai`; same registrable domain, distinct subdomain). **Automated decision-making:** none in the Article 22 sense — output is an approved message; there is no profiling or scoring of recipients. **Sub-processors:** Resend Inc (US, DPF + SCCs — outbound sending custody; see the Section 4.2 row), Supabase Inc (EU — existing); Doppler (secrets manager — existing) holds `EMAIL_HASH_PEPPER`. **NO LLM recipient on the outbound leg** (distinct from (aa)). Article 30 register entry: Processing Activity 28 (`knowledge-base/legal/article-30-register.md`).

- **(ac)** **Routine run-log (`routine_runs`, #5345):** When a scheduled platform routine (one of the `EXPECTED_CRON_FUNCTIONS` Inngest crons) finishes — completed or failed — the run-log middleware writes a single append-only operational audit row via the service-role-only `write_routine_run` SECURITY DEFINER RPC. "Running" is a transient UI state, not a DB fact; only terminal runs are recorded (~one row per run). **Data processed** (`routine_runs`, migration 107): `id` (UUID PK), `routine_id` (text — the cron fnId), `run_id` (text — Inngest run id), `status` (text: `completed` / `failed`), `trigger_source` (text: `scheduled` / `manual` / `agent`), `actor_class` (text: `system` / `human` / `agent`), `actor_id` (UUID, NULLable — FK to `public.users(id)` `ON DELETE RESTRICT`), `delegating_principal` (UUID, NULLable — FK to `public.users(id)` `ON DELETE RESTRICT`), `started_at` / `ended_at` (timestamptz), `duration_ms` (integer), `error_summary` (text, NULLable — for failed runs only: scrubbed + truncated by the middleware), `created_at` (timestamptz). On the **single-operator tenant the only data subject is Soleur itself**: `actor_id` / `delegating_principal` carry the operator's own id when a human or agent triggers a run off-schedule, and are NULL for scheduled system runs. No message content, no third-party data. **Legal basis:** legitimate interest (Article 6(1)(f) GDPR) — operational audit of operator-/agent-triggered routine runs, service reliability, and Article 32 integrity-of-processing; the three-part balancing test is recorded in GDPR Policy Section 3.7 (the controller and the sole data subject are the same legal entity at v1). **Retention:** indefinite (low-volume operational audit log; revisit if volume grows — cf. the 7-day `103_github_events_retention` precedent). **Article 17 erasure cascade:** `anonymise_routine_runs(p_user_id)` SECURITY DEFINER RPC NULLs `actor_id` AND `delegating_principal` for every row referencing the erased user under the privilege-free `app.worm_bypass = 'on'` GUC carve-out (post-087 pattern), **preserving the append-only audit row** (Article 17 = scrub the subject's PII columns, keep the run-lineage row whose operational-audit value extends beyond the subject); called in `server/account-delete.ts` BEFORE `auth.admin.deleteUser` so the `ON DELETE RESTRICT` FKs are satisfied by cascade time. This **mirrors the `anonymise_workspace_activity` pattern** (Section 2.3(x)). **WORM:** `routine_runs_no_mutate()` BEFORE-UPDATE/DELETE triggers (statement-level) reject all mutation except the `app.worm_bypass` erasure path. **Visibility scope:** operator-readable SELECT only (RLS `routine_runs_authenticated_select` — single-operator tenant: the authenticated operator sees every run, including scheduled runs with `actor_id IS NULL`); no client INSERT/UPDATE/DELETE (all writes via the service-role `write_routine_run` RPC); the `routine_runs_latest` view is `security_invoker` so the operator-select RLS is enforced for the querying session. **DSAR (Articles 15 / 20):** **excluded from the automated self-serve export at present** — this is an operational system-audit log (which routine ran, when, with what outcome), not Article 15 personal-profile data; the canonical rationale is recorded in `apps/web-platform/server/dsar-export-allowlist.ts` (the `routine_runs` exclusion entry). Promote to `DSAR_TABLE_ALLOWLIST` (`actor_id`, Article 15) when a non-Soleur tenant exists or the `dsar-export.ts` chain is wired. **Article 22:** negative determination — an operational audit record; no automated decision producing legal or similarly significant effects. **Sub-processors:** **none new** — substrate is internal to the existing Supabase row; no new recipient, no new third-party egress. Article 30 register entry: Processing Activity 29 (`knowledge-base/legal/article-30-register.md`). *(Draft — requires professional legal review.)*

- **(ad)** **Owner-private beta-tester / prospect CRM (`beta_contacts` + `interview_notes` + `beta_contact_stage_transitions`, #6165, ADR-102):** A Web Platform user (the operator) records the people and teams they are in sales/product conversations with, in a **strictly owner-private** store (migration 126). Unlike (a)–(ac), here **the user is the controller and Jikigai is the processor** for the third-party personal data the user enters. **Data processed at rest** — four tables, owner-only RLS keyed on the user's `user_id`: (1) `beta_contacts` (mutable head): `name`, `company`, `role`, `source`, `stage`, `next_action`, `next_action_date`, `last_contact`, `amount` / `currency` / `amount_basis`, `expected_close_date`, timestamps; (2) `interview_notes` (append-only): verbatim dual-lens conversation `body` + `lens[]` in `{sales, product}`, `occurred_at`; (3) `beta_contact_stage_transitions` (append-only): `from_stage` / `to_stage` / `entered_at` pipeline-velocity history; (4) `beta_contact_access_log` (append-only read-accountability log, migration 127 / #6172): `user_id` + `contact_id` + `accessed_at` **metadata only** — records each operator open of a contact's detail in the read-only CRM drawer (GDPR **Article 5(2)** accountability), written **atomically** inside the `crm_get_contact_detail` SECURITY DEFINER RPC (fail-closed: no contact PII is returned unless the audit row is written; INSERT only via the RPC — no owner-write policy), **never a note body**. **Data subjects:** the beta testers / prospects — **involuntary, third-party data subjects** whose details were obtained in the course of a conversation → **Article 14 applies**; the LIA specifies the notice mechanism to the subject (contrast the (aa) Article 14(5)(b) disproportionate-effort posture — here the operator is in direct contact, so notice is feasible). **No special-category (Article 9) data is solicited** — the free-text `body` is an incidental ingress the operator is instructed to avoid populating with Article 9 data. **Agent read/write path & recipients:** the store is read and written on the operator's behalf by the platform's `cro`/`cpo` agents through `auth.uid()`-pinned SECURITY DEFINER functions; to reason over the records their content is transmitted to **Anthropic (US)** — a **Chapter V transfer** under the existing Anthropic DPA (SCCs Modules 2+3; see the Section 4.2 Anthropic row and GDPR Policy §6). Write tools are **human-gated** (the operator reviews and approves each agent write). **Legal basis:** legitimate interest (Article 6(1)(f) GDPR) — LIA at `knowledge-base/legal/legitimate-interest-assessments/2026-07-07-beta-crm-lia.md`. **Retention:** 24 months from `last_contact` (never-contacted records: from `created_at`) via an in-database `pg_cron` sweep (aligned with the 24-month PA-PII envelope). **Erasure (Article 17):** the owner's whole store is deleted by `ON DELETE CASCADE` from `public.users` on account deletion (no anonymise RPC — no statutory-retention class, ADR-102 §4); an **individual third party's** erasure request is fulfilled via the auditable service-role-only `crm_erase_contact` function (deletes a contact + CASCADEs its notes / transitions / read-accountability-log rows via the composite FK), keyed on contact identity. **TOMs (Article 32):** owner-only RLS (no cross-tenant read); RPC-only writes with an `auth.uid()` authorization pin + `SELECT … FOR UPDATE` ownership re-check (SECURITY DEFINER bypasses RLS, so the body is the gate) + no existence oracle; a RESTRICTIVE `<table>_jti_not_denied` policy (a revoked founder JWT is rejected at the PostgREST boundary); a composite FK `(contact_id, user_id)` that makes a cross-tenant child mis-stamp a DB error; PII-safe observability (the tools emit `{op, userId, code}` only and pass a **synthetic** Error so a raw Postgres error's `Failing row contains (…)` DETAIL cannot echo `name`/`company`/`body` to error-monitoring); human-gated write tools (the review gate is the mitigation for within-tenant prompt-injection over untrusted conversation content). **Automated decision-making:** none in the Article 22 sense — the records are the operator's own notes; no profiling or scoring of subjects. **Sub-processors:** Anthropic PBC (US, DPF + SCCs Modules 2+3 — agent reasoning; see the Section 4.2 row), Supabase Inc (EU — existing); **no new sub-processor**. Article 30 register entry: Processing Activity 30 (`knowledge-base/legal/article-30-register.md`). *(Draft — requires professional legal review.)*

For these activities, Jikigai acts as a Controller with respect to data it directly collects and processes (including CLA signature data and Web Platform account data). Third-party processors are engaged as described in Section 4.2.

---

## 3. Technical and Organizational Measures

### 3.1 Plugin Architecture (Local-Only)

Soleur's architecture is designed to minimize data processing concerns:

- **(a)** The Plugin executes entirely within the User's local CLI environment.
- **(b)** No data is transmitted to Soleur-operated servers.
- **(c)** No telemetry, analytics, or usage tracking is embedded in the Plugin itself.
- **(d)** The Plugin does not establish network connections to Soleur-controlled endpoints.

### 3.2 User-Side Security Recommendations

While Soleur does not process User data, we recommend the following security measures for Users:

- **(a)** Use encrypted filesystems for local data storage.
- **(b)** Restrict filesystem permissions on knowledge-base directories and generated artifacts.
- **(c)** Rotate and securely store API keys used with third-party services.
- **(d)** Review the data you send to third-party APIs (e.g., Anthropic Claude API) and ensure compliance with your own data protection obligations.
- **(e)** Maintain access controls on the development environment where the Plugin is installed.

---

## 4. Third-Party Services and Sub-processors

### 4.1 Plugin Sub-processors

The Plugin does not process Personal Data on behalf of Users (see Section 2.1). Accordingly, there are no Plugin-level Sub-processors to disclose under Article 28(2) of the GDPR.

### 4.2 Service Processors

For processing activities where Jikigai acts as Controller (see Sections 2.1b and 2.3), the following third-party processors are engaged:

**Docs Site and Newsletter Processors:**

| Processor | Processing Activity | Data Processed | Legal Basis | Sub-processor List |
|-----------|-------------------|----------------|-------------|-------------------|
| GitHub Pages ([pages.github.com](https://pages.github.com)) | Docs Site hosting | IP addresses, browser user-agent strings, page request data | Legitimate interest (Article 6(1)(f)) | [GitHub Privacy Statement](https://docs.github.com/en/site-policy/privacy-policies/github-general-privacy-statement) |
| Plausible Analytics ([plausible.io](https://plausible.io)) | Privacy-respecting website analytics (cookie-free, EU-hosted) | Aggregated anonymous data only: page URLs, referrer URLs, country, device type, browser type (no IP addresses stored; see Section 2.3(a)) | Legitimate interest (Article 6(1)(f)) | [Plausible DPA](https://plausible.io/dpa) |
| Buttondown ([buttondown.com](https://buttondown.com)) | Newsletter subscription management and email delivery | Email addresses of subscribers | Consent (Article 6(1)(a)) — double opt-in | [Buttondown Sub-processors](https://buttondown.com/legal/dpa) |

**Web Platform Processors:**

| Processor | Processing Activity | Data Processed | Legal Basis | Sub-processor List |
|-----------|-------------------|----------------|-------------|-------------------|
| Supabase Inc ([supabase.com](https://supabase.com)) | Web Platform auth + database | Email addresses, hashed passwords, auth tokens, session data, conversation metadata, message content | Contract performance (Article 6(1)(b)) | [Supabase DPA](https://supabase.com/legal/dpa) |
| Stripe Inc ([stripe.com](https://stripe.com)) | Web Platform payment processing (Stripe Checkout, PCI SAQ-A) | Customer email, subscription metadata (card data handled exclusively by Stripe) | Contract performance (Article 6(1)(b)) | [Stripe Sub-processors](https://stripe.com/legal/service-providers) |
| Hetzner Online GmbH ([hetzner.com](https://hetzner.com)) | Web Platform infrastructure hosting (**EU-only**; user-serving host + workspace data in Helsinki, Finland (`hel1`); a 2nd web host (web-2, never user-serving, retirement decided) in Falkenstein, Germany (`fsn1`) since 2026-07-13, plus a dedicated per-workspace git-data host — same Hetzner account and signed AVV, both EU, **no new sub-processor and no international transfer**) | User workspaces, encrypted API keys, Docker containers; per-workspace git data stored on a **LUKS-encrypted (at-rest)** volume, host↔host traffic **TLS-encrypted (in transit)**, with **per-workspace git-data fetch authorization** (members only; re-verified on proxied sessions) | Contract performance (Article 6(1)(b)) | [Hetzner DPA](https://www.hetzner.com/legal/terms-and-conditions/) |
| Cloudflare Inc ([cloudflare.com](https://cloudflare.com)) | Web Platform CDN/proxy (`app.soleur.ai`, extending existing `soleur.ai` zone) | IP addresses, request headers, TLS termination data | Contract performance (Article 6(1)(b)) for authenticated users; legitimate interest (Article 6(1)(f)) for unauthenticated traffic | [Cloudflare DPA](https://www.cloudflare.com/cloudflare-customer-dpa/) |
| Sentry (Functional Software GmbH) ([sentry.io](https://sentry.io)) | Web Platform error monitoring and breach detection (Sentry SDK) | Error messages, stack traces, request metadata (URL paths, HTTP headers, navigation breadcrumbs), pseudonymous user identifier (`userIdHash`) | Legitimate interest (Article 6(1)(f)) for service reliability; legal obligation (Article 6(1)(c)) for Article 33 breach-notification timeliness | [Sentry Sub-processors](https://sentry.io/legal/dpa/) |
| Resend Inc ([resend.com](https://resend.com)) | Web Platform transactional email notifications (review gate alerts, workspace invite notifications, invite acceptance confirmations, DSAR export notifications) **+ inbound email custody for ops@soleur.ai triage — Section 2.3(aa)** (receives Sieve-forwarded raw mail; webhook delivery to Jikigai is metadata-only) **+ outbound sending for `outbound.soleur.ai` — Section 2.3(ab)** (sends the founder's human-approved 1:1 cold outreach from a dedicated subdomain — a separate sender-reputation stream, distinct from the `soleur.ai` apex transactional sender `notifications@soleur.ai`; same registrable domain, distinct subdomain) | Recipient email address, email content (notification summaries, workspace names, inviter names); **inbound role:** full forwarded third-party email content (body, headers, attachments), retained 30 days on all plans with **no deletion API** (vendor-fixed window); **outbound role:** approved recipient address + message body, retained 30 days on all plans with **no deletion API** (vendor-fixed window) — Jikigai persists no plaintext recipient or body for this role (keyed `recipient_hash` + body hashes only) | Legitimate interest (Article 6(1)(f)) for transactional notifications, inbound triage custody (Section 2.3(aa) LIA), and outbound human-approved cold-send custody (Section 2.3(ab) LIA); contract performance (Article 6(1)(b)) for existing-user invites | [Resend DPA](https://resend.com/legal/dpa) |
| Anthropic PBC ([anthropic.com](https://www.anthropic.com)) — Section 2.3(aa) | Email-triage summarization for ops@soleur.ai (one API call per **non-statutory** inbound email; statutory-class and synthetic-probe mail is never sent) | Subject, sender, and truncated (64 KiB) + sanitized body of non-statutory inbound third-party mail; retained by Anthropic up to 30 days by default (Zero-Retention amendment tracked in `knowledge-base/legal/data-processing-agreements/anthropic.md`) | Legitimate interest (Article 6(1)(f)) — LIA at `knowledge-base/legal/legitimate-interest-assessments/2026-06-11-operator-inbox-triage-lia.md` | [Anthropic DPA](https://www.anthropic.com/legal/dpa) (auto-incorporated via Commercial Terms §C; EU-US DPF + SCCs Modules 2+3 + UK IDTA + Swiss Addendum) |
| Cloudflare Inc ([cloudflare.com](https://cloudflare.com)) -- R2 Storage | CLA evidence archive (`soleur-cla-evidence` bucket, region `weur`; R2 Lock Rules age-based retention, 10yr floor) | Per-signature evidence records (GitHub username, signature timestamp, sign-comment body, PR-of-record, doc-hash, capture method); monthly RFC 3161 timestamp responses | Legitimate interest (Article 6(1)(f)) -- Section 2.3(n) | [Cloudflare DPA](https://www.cloudflare.com/cloudflare-customer-dpa/) (same instrument as CDN row) |
| FreeTSA ([freetsa.org](https://freetsa.org)) -- RFC 3161 Time Stamp Authority | Monthly RFC 3161 timestamping of the CLA evidence bucket manifest (SHA-256 only); no contributor data leaves the bucket | SHA-256 hash of bucket-state manifest (no personal data) | Legitimate interest (Article 6(1)(f)) -- Section 2.3(n) | Public free-of-charge service; no DPA available. FreeTSA receives no personal data, only a 32-byte hash; processing falls outside Article 28 scope. |
| LinkedIn Ireland Unlimited Company ([linkedin.com](https://www.linkedin.com)) -- Section 2.3(p)(i)+(ii) | Jikigai LinkedIn Company Page operation (posts via Community Management API) + Page Insights aggregate analytics consumption | Post content (operator-authored, no Soleur-user PII); follower identifiers (display name, profile URL slug, voluntarily public); aggregate engagement metadata (impressions, reactions, comments, shares); operational secrets (`LINKEDIN_ORG_ACCESS_TOKEN`, `LINKEDIN_ORG_ID`) | Legitimate interest (Article 6(1)(f)) -- LIA at `knowledge-base/legal/legitimate-interest-assessments/2026-05-19-linkedin-org-page-lia.md` | [LinkedIn Subscription Agreement](https://www.linkedin.com/legal/l/subscription-agreement) + [LinkedIn Pages Terms](https://www.linkedin.com/legal/l/pages-terms) (incorporating the standard joint-controller arrangement for Page Insights per CJEU C-210/16) |
| Microsoft Ireland Operations Ltd -- Section 2.3(p)(iii) | One-time custody of K-bis business-verification documents at LinkedIn appeal-flow step (KYC-equivalent under LinkedIn Platform Terms) | K-bis extract contents (gérant full legal name, Jikigai SARL registered address, French commerce-registry number (RCS Paris 927 585 729), capital structure metadata); tax registration; registered-address proof | Legal obligation (Article 6(1)(c)) -- LinkedIn Platform Terms business-verification requirement | Microsoft's controller-to-controller transfer terms apply; document custody falls under [Microsoft's EU Data Boundary](https://aka.ms/EUDataBoundary) commitments |
| Bullet Train Limited (Flagsmith) ([flagsmith.com](https://www.flagsmith.com)) | Web Platform runtime feature-flag identity-trait evaluation (PA-1 + PA-2 — workspace-invite gate, BYOK delegation gate) | Pseudonymised `identifier` + identity traits `{ role, orgId }` ONLY (no message content, no attachments, no BYOK keys, no email); `transient: true` MANDATORY on every `getIdentityFlags(...)` call (opts out of Flagsmith server-side identity persistence) | Legitimate interest (Article 6(1)(f)) — runtime capability gating without container restarts; balancing-test mitigation via pseudonymisation + dual-control + `transient: true` | [Flagsmith Terms of Service](https://www.flagsmith.com/terms-of-service) (Data Processor Appendix — auto-incorporated DPA); [Flagsmith Sub-Processors](https://www.flagsmith.com/gdpr-sub-processor-list); snapshot at `knowledge-base/legal/data-processing-agreements/flagsmith.md` |

This disclosure is consistent with Sections 2.1b, 2.3(a), 2.3(e), 2.3(f), 2.3(g), 2.3(h), 2.3(i), 2.3(j), 2.3(k), 2.3(l), 2.3(m), 2.3(p), 2.3(aa), and 2.3(ab).

**Workspace co-member carve-out (Section 2.3(u)).** Workspace Co-Members are NOT processors under Article 28 GDPR. Co-Member access is contract-mediated under the Anthropic Commercial Terms §C "authorized users" framework — the Co-Member uses the Web Platform under the Workspace Owner's account, not as a separate processor of the Workspace Owner's data. The Workspace Owner is the controller (Article 4(7) GDPR); Jikigai remains the processor (Article 4(8) GDPR); the Co-Member is an authorized user of the Workspace Owner's account, not an entity engaged by Jikigai for the processing.

### 4.3 Third-Party Services Used by Users

Users may interact with the following third-party services through the Plugin's functionality. These interactions are initiated and controlled by the User, not by Soleur:

| Service | Purpose | User's Relationship |
|---------|---------|-------------------|
| Anthropic (Claude API) | AI model inference | Direct customer of Anthropic |
| GitHub | Code hosting, issue tracking | Direct customer of GitHub |
| npm | Package distribution | Direct customer of npm |

Users are responsible for reviewing and complying with the data processing terms of any third-party service they use in conjunction with the Plugin.

---

## 5. Data Subject Rights

### 5.1 Local Data

Because Soleur does not have access to Local Data, data subject requests (access, rectification, erasure, portability, restriction, objection) related to data processed locally must be addressed by the User directly. Soleur cannot fulfill such requests as it has no access to the data.

### 5.2 Docs Site and GitHub Data

For data processed through the Docs Site or GitHub repository:

- **(a)** Users may exercise their data subject rights by contacting us through the [Soleur GitHub repository](https://github.com/jikig-ai/soleur).
- **(b)** For data processed by GitHub as a platform, Users should refer to [GitHub's data subject request process](https://docs.github.com/en/site-policy/privacy-policies/github-general-privacy-statement).

### 5.3 Web Platform Data

For data processed through the Web Platform (app.soleur.ai) where Jikigai acts as controller (see Section 2.1b), data subjects may exercise the following rights through either the self-serve flow at `/dashboard/settings/privacy` (where applicable) or by contacting <legal@jikigai.com>:

- **(a)** **Right of Access (Article 15):** Request confirmation of whether personal data is being processed and obtain a copy of the data. Self-serve at `/dashboard/settings/privacy`. The exported bundle contains:
  - Account profile (`users` table: email, workspace status, subscription state, Stripe customer + subscription IDs).
  - Conversations and messages (with partial-turn rows, per Privacy Policy §4.7).
  - Message attachments (binary files you uploaded, under `chat-attachments/<your-id>/`). When `TEAM_WORKSPACE_INVITE_ENABLED` is ON, co-members of any shared workspace you participate in may upload attachments visible to you under their own user-id-keyed paths; co-uploader attachment metadata is included in the bundle manifest (schema 1.2.0, PR #4486 / #4445) with `redacted: true`, `redaction_reason: "art-15-co-uploader"`, `uploader_pseudonym: "member_<hex12>"`; the attachment bytes themselves are NOT included in the ZIP per Art. 15(4).
  - Knowledge-base share links (`kb_share_links`).
  - Team / agent display names (`team_names`).
  - BYOK encrypted credentials (`api_keys` -- ciphertext is base64-encoded; never decrypted server-side outside the BYOK lease).
  - BYOK usage audit log (`audit_byok_use`).
  - Beta-tester / prospect CRM records you curated (`beta_contacts` + `interview_notes` = Article 15 + 20; `beta_contact_stage_transitions` = Article 15 controller-generated velocity audit; `beta_contact_access_log` = Article 15 controller-generated read-accountability metadata, no note content), per Section 2.3(ad).
  - Workspace files (the `/workspaces/<your-id>/` tree on Jikigai infrastructure).
  Excluded from the export, with reason: DSAR-itself audit metadata (handled separately under Article 17 cascade), operational concurrency / rate-limit / revocation / mint-rate tables (security telemetry, not personal data), push notification subscriptions (transient device tokens; revoke via your browser), and statutory-deadline dispatch markers (`statutory_repin_send`, migration 135 — a foreign key to an `email_triage_items` row that is itself exported in full, plus a synthetic tick label and a timestamp; the table has no `user_id` column and holds no personal data, and is swept on a 90-day rolling schedule).
- **(b)** **Right to Rectification (Article 16):** Request correction of inaccurate personal data held by Jikigai. Email channel.
- **(c)** **Right to Erasure (Article 17):** Request deletion of personal data under applicable conditions. Self-serve via the Delete Account dialog in `/dashboard/settings` or by email. The cascade aborts any in-flight DSAR export, purges chat-attachment and DSAR-export Storage objects, anonymises the DSAR audit log, then deletes the auth record (which cascades to all owned rows, **including the whole beta-CRM store** — `beta_contacts` and its notes/transitions/read-accountability-log rows via `ON DELETE CASCADE`, Section 2.3(ad)). **Third-party erasure:** where a beta tester / prospect recorded in your CRM exercises their own Article 17 right, their individual contact and all related notes/transitions/access-log rows are deleted via the auditable service-role-only `crm_erase_contact` function (contact @ `legal@jikigai.com`). Note: subscription records subject to French tax law retention (Code de commerce Art. L123-22) may be retained for up to 10 years (see Section 2.3(g)).
- **(d)** **Right to Restriction of Processing (Article 18):** Request that Jikigai restrict processing of personal data. Email channel.
- **(e)** **Right to Data Portability (Article 20):** Request personal data in a structured, commonly used, machine-readable format. Self-serve at `/dashboard/settings/privacy` delivers a ZIP archive containing one JSON file per allowlisted SQL table (sorted keys for deterministic SHA-256, ISO 8601 timestamps with UTC offset, base64 for bytea, JSON null for SQL NULL) plus the binaries enumerated in (a). The archive includes a `manifest.json` declaring the schema version, the GDPR article tag for every file (Article 15 vs Article 15+20), per-file SHA-256, and per-file row counts; any files skipped due to data-quality issues are enumerated in `excluded_files[]` with reason.
- **(f)** **Right to Object (Article 21):** Object to processing of personal data. The legal basis for Web Platform processing is contract performance (Article 6(1)(b)), so this right applies primarily when processing extends beyond strict contractual necessity. Email channel.

Jikigai will acknowledge requests within 5 business days and respond substantively within one month of receipt, as required by GDPR Article 12(3). This period may be extended by two further months where necessary, taking into account the complexity or volume of requests, in which case we will inform you of the extension and reasons within the initial one-month period. For full details on how each right applies, see the companion [GDPR Policy](gdpr-policy.md) Section 5.

---

## 6. International Data Transfers

### 6.1 Local Data

No international data transfers are performed by Soleur with respect to Local Data.

### 6.2 Third-Party Transfers

When Users interact with third-party services (e.g., Anthropic Claude API, GitHub), data may be transferred internationally. These transfers are governed by the respective third-party's data processing agreements and transfer mechanisms. Users are responsible for ensuring adequate safeguards are in place for any such transfers.

### 6.3 Buttondown (Newsletter)

Transfers of newsletter subscriber data (email addresses, IP addresses, referrer URL, subscription timestamps, browser/device metadata) from the EEA to the United States are governed by the **EU Standard Contractual Clauses** (European Commission Implementing Decision (EU) 2021/914, Module 2: Controller-to-Processor), incorporated by reference into Buttondown's [Data Processing Agreement](https://buttondown.com/legal/data-processing-agreement). Buttondown's DPA applies to all plan tiers, including the free tier used by Jikigai.

### 6.4 Web Platform

For the Web Platform (app.soleur.ai):

- **Supabase:** EU-based deployment (AWS eu-west-1, Ireland). **No international data transfers.** Supabase Inc is a US-based company, but the Jikigai project is deployed to the EU region. DPA (Data Processing Addendum) signed 2026-03-19 via PandaDoc.
- **Stripe:** US-based (Stripe, LLC). Transfer via EU-US Data Privacy Framework (DPF, adequacy decision) and Standard Contractual Clauses (SCCs), EEA Module 2. DPA auto-incorporated in Services Agreement (verified 2026-03-19).
- **Hetzner:** EU-based (Germany). Web Platform hosted on Hetzner data centres **in the EU**, across more than one host — the host serving `app.soleur.ai` and storing workspace data is in Helsinki, Finland (`hel1`); a second web host (web-2, never user-serving) is in Falkenstein, Germany (`fsn1`) — same Hetzner account, same signed AVV, both EU. **No international data transfers**, no new sub-processor. Stored workspace git data sits on a LUKS-encrypted (at-rest) volume and host↔host traffic is TLS-encrypted (in transit). DPA (AVV) signed 2026-03-19 via Cloud Console.
- **Cloudflare:** Global CDN. Transfer via EU-US Data Privacy Framework (DPF), Standard Contractual Clauses (SCCs), and Global CBPR certification. DPA self-executing via Self-Serve Subscription Agreement (verified 2026-03-19).
- **Sentry:** DE region (Frankfurt, Germany), processed by Functional Software GmbH. Transfer mechanism: Standard Contractual Clauses (Sentry's standard EU-region terms). Intra-EU processing — no third-country transfer. DPA self-executing via Sentry's terms of service (verified 2026-05-13).
- **Cloudflare R2 (CLA evidence archive):** EU region (`weur` -- Western Europe). R2 Lock Rules age-based retention, 10yr floor. Intra-EU processing for archive contents at rest. DPA self-executing via the same Cloudflare Customer Data Processing Agreement as the CDN row.
- **FreeTSA (RFC 3161 Time Stamp Authority):** DE-based public service. The service receives only a SHA-256 hash of the monthly manifest; no contributor data is transmitted. Intra-EU processing. No DPA -- the input is not personal data within the meaning of Article 4(1) GDPR (a 32-byte unkeyed hash of bucket-state metadata).
- **LinkedIn Ireland Unlimited Company (LinkedIn Company Page operation + Page Insights):** EU-region infrastructure for Page data with US fallback. Transfer mechanism: **EU-US Data Privacy Framework (DPF)** and **Standard Contractual Clauses (SCCs), Module 2** under LinkedIn / Microsoft's DPF certifications. Joint-controller arrangement for Page Insights with Jikigai governed by the LinkedIn Pages Terms (CJEU C-210/16). DPA self-executing via the LinkedIn Subscription Agreement.
- **Microsoft Ireland Operations Ltd (K-bis business-verification, one-time):** Intra-EU controller-to-controller transfer at the appeal-flow step. Onward routing under Microsoft's [EU Data Boundary](https://aka.ms/EUDataBoundary) commitments. Microsoft's controller-to-controller transfer terms apply; no Article 28 DPA required at the custody point because Microsoft acts as a separate controller for the document-custody role.

### 6.5 Docs Site

The Docs Site is hosted on GitHub Pages, which may involve data processing in the United States and other jurisdictions where GitHub operates. GitHub maintains appropriate transfer mechanisms as described in its data processing agreements.

Plausible Analytics, used for privacy-respecting website analytics on the Docs Site (see Section 4.2), processes all data exclusively within the European Union (Hetzner, Germany). No international data transfers occur for analytics data.

---

## 7. Data Breach Notification

### 7.1 Local Breaches

Soleur has no visibility into the User's local environment and therefore cannot detect or report data breaches affecting Local Data. Users are solely responsible for breach detection and notification obligations under Article 33 and Article 34 of the GDPR with respect to locally processed data.

### 7.2 Platform Breaches

In the unlikely event that a breach affects the Soleur GitHub repository, Docs Site, Web Platform (app.soleur.ai), or distribution channels:

- **(a)** We will notify affected Users without undue delay, and in any event within 72 hours of becoming aware of the breach, where feasible (Article 33 GDPR).
- **(b)** Notification will be provided via the [Soleur GitHub repository](https://github.com/jikig-ai/soleur) and, where possible, through direct communication (including email notification for Web Platform users with an account on file).
- **(c)** The notification will include the nature of the breach, likely consequences, and measures taken or proposed to address it.

---

## 8. Cloud Features Transition

### 8.1 Transition Status

The Soleur Web Platform (app.soleur.ai) represents the introduction of cloud-hosted features described prospectively in the original version of this section. The commitments made below have been addressed as follows:

- **(a)** This DPD has been updated with Article 28 GDPR-compliant data processing terms (see Sections 2.1b, 2.3(f)-(h), and 4.2). **FULFILLED.**
- **(b)** Users are notified of cloud processing via this updated DPD and the updated Privacy Policy. **FULFILLED.**
- **(c)** Technical and organizational measures implemented: encryption at rest (AES-256-GCM for API keys), TLS for data in transit, EU-only hosting (Hetzner EU data centres; workspace data in Helsinki, Finland) for infrastructure. **FULFILLED.**
- **(d)** Processor list maintained in Section 4.2. **FULFILLED.**
- **(e)** Transfer mechanisms documented: EU-only for Supabase (eu-west-1, Ireland) and Hetzner (EU data centres — Helsinki, Finland and Falkenstein, Germany; same account and signed AVV, both EU), DPF + SCCs for Stripe (see Section 6.4). **FULFILLED.**
- **(f)** DPIA evaluation: The Web Platform processes user PII (email, auth tokens, encrypted API keys, subscription metadata) but does not involve special categories (Article 9), systematic monitoring, or automated decision-making. Processing remains below the high-risk thresholds of Article 35(3). **Evaluated -- DPIA not required.** See the companion GDPR Policy Section 9 for the full analysis.
- **(g)** Users accept the updated Terms and Conditions via a clickwrap checkbox on the Web Platform signup page (app.soleur.ai/signup). The checkbox is unchecked by default and must be actively checked before account creation. Acceptance is timestamped and recorded in the user database. **FULFILLED.**

### 8.2 Future Changes

Any further expansion of cloud processing beyond the Web Platform (e.g., additional cloud services, new data categories) will follow the same disclosure process and will be communicated:

- **(a)** At least 30 days before the change takes effect;
- **(b)** Via the Soleur GitHub repository, Docs Site, release notes, and Web Platform (app.soleur.ai) (including email notification for Web Platform users with an account on file);
- **(c)** With a clear description of what data will be processed, for what purpose, and what safeguards are in place.

---

## 9. Audit Rights

### 9.1 Current Architecture

Given the local-only nature of the Plugin, traditional audit rights under Article 28(3)(h) of the GDPR are not applicable. However:

- **(a)** The Soleur Plugin source code is available for inspection on [GitHub](https://github.com/jikig-ai/soleur).
- **(b)** Users may verify that the Plugin does not transmit data by inspecting network activity during use.
- **(c)** Soleur welcomes security audits and responsible disclosure through the GitHub repository.

### 9.2 Web Platform Audit Rights

For the Web Platform, audit rights consistent with Article 28(3)(h) are provided through the individual processor DPAs: [Supabase DPA](https://supabase.com/legal/dpa), [Stripe DPA](https://stripe.com/legal/dpa), and [Hetzner DPA](https://www.hetzner.com/legal/terms-and-conditions/). Users may request audit information from Jikigai regarding Web Platform data processing by contacting <legal@jikigai.com>.

---

## 10. Termination and Data Deletion

### 10.1 Plugin Removal

Users may uninstall the Plugin at any time. Upon removal:

- **(a)** All Local Data remains on the User's filesystem under their sole control.
- **(b)** Soleur does not retain any copy of Local Data, as no such data was ever transmitted to Soleur.
- **(c)** Users are responsible for deleting or retaining Local Data according to their own data retention policies.

### 10.2 Docs Site and Repository Data

Users who wish to have their data removed from the Soleur GitHub repository (e.g., issue comments, pull request contributions) should follow GitHub's standard data deletion procedures or contact us through the repository.

### 10.3 Web Platform Account Deletion

Users may delete their Web Platform account at any time via account settings. Upon account deletion:

- **(a)** Account data (email, authentication tokens, session data) is deleted from Supabase.
- **(b)** Encrypted API keys and workspace data are deleted from Hetzner infrastructure.
- **(c)** Stripe retains payment records (subscription metadata, invoices) for 10 years per French tax law (Code de commerce Art. L123-22).
- **(d)** Conversation data (messages and conversation metadata) is deleted from Supabase (cascade delete via foreign key).
- **(e)** Cloudflare cache entries expire per standard TTL; no persistent user data is stored by Cloudflare.
- **(f)** Any in-flight DSAR export job is aborted (status flipped to `failed` with reason `account_deleted_during_export`) BEFORE the auth record is removed. This avoids a worker reading rows owned by a soon-to-be-tombstoned user_id.
- **(g)** Storage objects under `chat-attachments/<your-id>/` (limited to conversations you own) and `dsar-exports/<your-id>/` are removed during the cascade so completed-but-undelivered DSAR bundles do not outlive the account. Attachments you uploaded into shared-workspace conversations that you do NOT own are retained under Article 6(1)(f) shared-asset legitimate interest (#4318, mig 068); uploader identity is severed at the database layer (`messages.user_id` set to NULL via `public.anonymise_departed_user_across_workspaces`) so the byte content survives without re-attaching to your account.
- **(h)** The DSAR audit log (`dsar_export_audit_pii`) PII columns (requester_ip, user_agent) are anonymised before the auth record is deleted, via a security-definer RPC gated by a per-transaction GUC that bypasses the WORM trigger only for this specific anonymisation flow. The anonymisation step is idempotent so a partial-failure cascade is recoverable on retry.
- **(i)** **LinkedIn-published content carve-out (Article 17 limitation).** Erasure requests directed at **LinkedIn-published content** — that is, posts published by Jikigai to the Soleur LinkedIn Company Page at `https://www.linkedin.com/company/soleur/` that reference a data subject, or that data subject's engagement (reactions, comments) with such posts — are subject to a necessary cascade limitation. Upon receiving a verified Article 17 request, Jikigai will (i) delete the source copy from operator-side systems (drafts, internal trackers); (ii) issue a corresponding deletion request to LinkedIn via the Page admin UI within five (5) business days; and (iii) confirm in writing once the LinkedIn-side deletion has been issued. Jikigai **cannot guarantee removal from LinkedIn's cached or replicated systems, downstream search-engine indices, third-party archival services, or member-side notification surfaces**; this limitation flows from EDPB Guidelines 5/2019 on the criteria for the right to be forgotten, which recognises best-effort cascade rather than absolute removal across third-party-replicated content. Where the content to be erased was authored by a LinkedIn member other than the requesting data subject (e.g., a comment by a third party), the third-party LinkedIn member retains primary control via their own LinkedIn account and the data subject should also contact LinkedIn directly. Initiation channel: <legal@jikigai.com> with subject "LinkedIn erasure" and a link to the specific post or comment. See Privacy Policy Section 8.1 for the data-subject-facing wording.

See the [Terms and Conditions](terms-and-conditions.md) Section 14.1b for the full account termination procedure.

---

## 11. Governing Law and Jurisdiction

**11.1** This DPD shall be governed by and construed in accordance with the laws of the European Union and the Member State in which the User is established, to the extent required by the GDPR.

**11.2** Any disputes arising out of or in connection with this DPD shall be subject to the exclusive jurisdiction of the courts of the Member State in which the User is established, unless otherwise required by mandatory law.

**11.3** Nothing in this DPD shall limit the rights of data subjects under the GDPR or the powers of supervisory authorities.

---

## 12. Contact Information

For questions, concerns, or requests related to this DPD:

- **Email:** <legal@jikigai.com>
- **GitHub Repository:** [https://github.com/jikig-ai/soleur](https://github.com/jikig-ai/soleur)
- **Website:** [https://soleur.ai](https://soleur.ai)

---

## 13. Amendments

**13.1** Soleur reserves the right to update this DPD to reflect changes in the Plugin's architecture, applicable law, or regulatory guidance.

**13.2** Material changes will be communicated at least 30 days in advance through the Soleur GitHub repository, Docs Site, and Web Platform (app.soleur.ai) (including email notification for Web Platform users with an account on file).

**13.3** Continued use of the Plugin after the effective date of changes constitutes acceptance of the updated DPD.

---

> **Related documents:** This Data Protection Disclosure references data practices and privacy obligations. Please review the companion [Privacy Policy](privacy-policy.md), [GDPR Policy](gdpr-policy.md), [Terms and Conditions](terms-and-conditions.md), and [Individual Contributor License Agreement](individual-cla.md) documents to ensure consistency.

---

<!-- PR-H (#3244) #4066 — 2026-05-19: GitHub Inc. (Microsoft Corporation) reaffirmed as US sub-processor for the new GitHub App webhook ingress (Web Platform multi-source priority signals). SCCs Module 2 + DPF cover the data egress from GitHub to Soleur. The flow is bilaterally per-founder (the App is installable on the founder's own account/orgs only); Soleur displays third-party repo content as-is and does not re-process it for LLM training, profile-building, or any downstream automated decision-making. Authoritative shape: knowledge-base/legal/article-30-register.md Processing Activity 17. -->
