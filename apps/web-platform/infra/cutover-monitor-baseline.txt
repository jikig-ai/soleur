# CUT8 monitor-health baseline — captured BEFORE the apex cutover (#7640 PR4b).
#
# CUT8 compares against this rather than demanding absolute green, because a
# monitor that was ALREADY red cannot be evidence that the cutover broke
# anything — and the T+20 rule turns any CUT8 failure into a rollback.
#
# Regenerate: bash cutover-verify.sh --capture-monitor-baseline cutover-monitor-baseline.txt
#             (needs credentials: run it under `doppler run -p soleur -c prd_terraform --`,
#             or every row captures `unknown` and the script refuses to write a
#             partial baseline. That refusal is the design working, not a fault.)
#
# RE-CAPTURED 2026-09-07T18:51:57Z (#7798, tasks.md 6.5) — this is now a real
# measurement of every row, superseding the placeholder described below.
#
# The superseded state, kept because it explains why one row read `unknown`:
# PR #7878 renamed `soleur-ai-www` to `soleur-ai-www-reachability` AND retargeted
# it from an unsatisfiable `equals 301` to a 2xx assertion. The prior capture
# predated both, so it had never measured THAT monitor, and the row was set to
# `unknown` — the read path's own fail-safe arm (not a regression, not a pass,
# reported UNREACHABLE). The alternatives both failed: leaving `soleur-ai-www`
# would stop matching SENTRY_MONITORS, score EMPTY -> REGRESSION, and roll back a
# healthy cutover; renaming while keeping `unhealthy` would score every later
# failure as pre-existing and MASK a genuinely new one.
#
# That placeholder is now discharged: post-merge verification confirmed the
# renamed monitor at uptimeStatus 1 with its most recent check `success`, and
# WEB-PLATFORM-11 resolved, so this capture measures it for real.
soleur-ai-apex	healthy
soleur-ai-www-reachability	healthy
soleur-ai-changelog-deep	healthy
soleur-ai-acme-carveout-probe	healthy
soleur dot ai apex	healthy
