#cloud-config
# #6122 (ADR-096) — zot registry host cloud-init. Rendered by templatefile() in
# zot-registry.tf. Mirrors cloud-init-git-data.yml (dedicated volume-backed host,
# cloud-init-only — NO SSH provisioner, so the CI runner cannot hang a merge-triggered
# apply; secrets injected via a scoped Doppler token, NEVER baked into user_data) and
# adds docker + a digest-pinned zot container.
#
# zot serves HTTP on the private net (${zot_pull_user}/${zot_push_user} via htpasswd);
# public ingress is deny-all (zot-registry.tf firewall). cosign digest-pinning + offline
# verify is the integrity guarantee (Phase-0 spike). Web hosts add this registry to docker
# insecure-registries in Phase 3.
#
# A bare dollar-var ($ZOT_PULL_TOKEN, $i) is left literal by terraform templatefile() —
# only a dollar-brace interpolates. `$${VAR}` renders a literal shell var (doppler DL).
package_update: true
packages:
  - docker.io # runs the zot container (RUN-only host, no build → docker.io suffices, not docker-ce)
  - apache2-utils # htpasswd -Bbn (bcrypt) — build the zot htpasswd on-host from Doppler tokens
  - curl
  - jq # parse `doppler secrets --only-names --json` for the boot-time isolation self-check (runcmd)
  - e2fsprogs # resize2fs — grow the ext4 fs to fill the 60 GB volume (#6240/#6247). The packages: stage
    # is NON-FATAL (failures logged, boot continues), so a runcmd dpkg-guard re-ensures it below.
  - cryptsetup # #6895 — guest-side LUKS-at-rest for the raw registry store volume (luksFormat/luksOpen).

groups:
  - docker

write_files:
  # SSH hardening parity with git-data / the web host's minimal set.
  - path: /etc/ssh/sshd_config.d/01-hardening.conf
    content: |
      PasswordAuthentication no
      KbdInteractiveAuthentication no
      MaxAuthTries 3
      LoginGraceTime 30
      PermitRootLogin prohibit-password
      ClientAliveInterval 300
      ClientAliveCountMax 2
    owner: root:root
    permissions: '0644'

  # zot config — the Phase-0-validated shape: local-fs storage on the mounted volume,
  # dedupe + gc (safe on a singleton; gc retains the tagged sha256-<digest>.sig referrers),
  # htpasswd auth, deny-by-default accessControl (pull user = read; push user =
  # read/create/update). Usernames are terraform-interpolated (non-secret constants).
  #
  # storage.retention (#6122 follow-up): gc alone only reclaims DANGLING blobs, not old
  # TAGS — every dual-pushed release tag (v* + its 40-hex commit-sha) is retained forever,
  # so the store grows unbounded with each deploy and the volume fills (observed: the
  # web-platform image is ~1.5-2 GB/version). This policy bounds it: keep `latest`, the 5
  # most-recently-pushed v* tags and 5 most-recent commit-sha tags per repo, and the 50
  # most-recently-pushed `sha256-*` tags (the cosign .sig/.att triangulation tags — deploy-time
  # `cosign verify` fetches the signature by tag from the SAME registry it pulls the image, so a
  # KEPT image's sig must never be pruned; see ADR-087). Non-matching tags are pruned;
  # deleteUntagged + gc then reclaim their layers after `delay`. deleteReferrers=false: cosign
  # here uses TAG-based sigs (kept above), not Subject-field OCI referrers, so leave Subject
  # referrers untouched. dryRun=false: prune for real. Schema: project-zot examples/README.md.
  #
  # CAPACITY-VS-RETENTION (#6247, 2026-07-09): counts LOWERED v*/commit-sha 10->5 and the
  # previously-UNBOUNDED `sha256-*` keep BOUNDED at mostRecentlyPushedCount 50. The 2026-07-08 fix
  # (#6246) tightened gc/retention TIMING but left the keep-SET unchanged; SOLEUR_ZOT_DISK then
  # showed the 30 GB fs genuinely full (resize_ok=true, pcent=100) — the keep-set (10 v* + 10 sha +
  # unbounded sigs, per repo × 2 repos, ~1.5-2 GB/image) legitimately exceeded 30 GB, and gc cannot
  # reclaim a blob the policy says to KEEP. Fix = grow the volume 30->60 GB (variables.tf, headroom)
  # + this keep-set tighten (bound growth). The `sha256-*` bound at 50 caps forever-growth while
  # sitting FAR above the true keep requirement (~12-18 sig-tags/repo at ~5-6 kept releases × 2-3
  # referrer types), so it never prunes a kept image's sig at current scale — the load-bearing
  # cosign-verify coupling (ADR-087): mostRecentlyPushedCount is push-ORDER heuristic and can evict
  # out of order under the ADR-096 crane-copy backfill/re-sign path, so 50 is deliberately generous.
  #
  # gc/retention TIMING (#6240 defense-in-depth, UNCHANGED here): gcInterval 24h->1h and
  # retention.delay 24h->2h so a filling store is reclaimed within ~1h, not once per 24h. gcDelay
  # stays 1h (the dangling-blob SAFETY window that protects a blob just uploaded but not yet
  # manifest-referenced during an in-flight push — shortening it risks reclaiming an in-progress
  # layer). NO on-boot gc trigger is issued: zot v2.1.20 exposes no sanctioned on-demand gc HTTP
  # endpoint — MEASURED 2026-08-05 (#7282) by probing the pinned image locally: /v2/_zot/gc,
  # /v2/_catalog/gc, /_zot/gc and /v2/_zot/ext/gc all answer 404, and the release's BinaryType
  # excludes mgmt/scrub/search entirely (hr-verify-repo-capability-claim — no fabricated admin
  # call). Re-measure on every bump, never re-word. So the tightened
  # gcInterval is the reclamation lever; the first scheduled pass runs within ~1h of boot.
  #
  # http.compat ["docker2s2"] (#6122): zot is OCI-native and REJECTS Docker manifest
  # schema2 pushes (application/vnd.docker.distribution.manifest.v2+json) with
  # MANIFEST_INVALID unless this compat flag is set. The inngest-bootstrap image is built by
  # plain `docker build` → docker-schema2, so it needs this; the web image is OCI (buildx) and
  # does not. (project-zot pkg/compat/compat.go DockerManifestV2SchemaV2 = "docker2s2".)
  #
  # http.readTimeout / http.writeTimeout "1800s" (#7555, ADR-190): zot v2.1.20 supplies BOTH
  # deadlines as 60000000000 ns (60.000 s) when the "http" block omits them — read off the
  # PINNED digest's own boot config, not from docs. At 60 s a 703_724_542 B layer must sustain
  # 11.73 MB/s, and three concurrent layers ~280 Mbit/s through one tunnel; when it does not,
  # zot closes mid-PATCH and the runner reports `connection reset by peer`. MEASURED 2026-08-13
  # off-box against the pinned digest over a docker bridge with NO tunnel and NO Cloudflare
  # edge, by dribbling a 4 MB blob over 100 s: same message, same routes.go:2078, same
  # statusCode:500, same latency:1m0s. So the tunnel is exonerated and this is a TIME wall, not
  # a size wall — the 272_236_549 B layer died at the same deadline (< 4.54 MB/s effective).
  #
  #   Arm A  both keys set      -> upload completes; zot answers the client
  #   Arm B  neither key set    -> the 60 s default; the failure above
  #   Arm C  readTimeout ALONE  -> zot-side 202 with NO response to the client
  #
  # Arm C is why these two MOVE TOGETHER and never one without the other: its failure mode is
  # SILENT SUCCESS, which is strictly worse than Arm B's honest 500. The upper bound is
  # gcDelay (1h above): a deadline at or above it lets gc reclaim staging for an upload still
  # in flight, so 1800s sits strictly below. Both bounds are enforced by
  # apps/web-platform/infra/zot-config-deadlines.test.sh against the RENDERED config, because
  # this host is cloud-init-only (ADR-096) — a config zot rejects fails AFTER the destroy and
  # create, on a host with no SSH. Re-measure on every zot bump, never re-word.
  - path: /etc/zot/config.json
    content: |
      {
        "distSpecVersion": "1.1.0",
        "storage": {
          "rootDirectory": "/var/lib/zot",
          "dedupe": true,
          "gc": true,
          "gcDelay": "1h",
          "gcInterval": "1h",
          "retention": {
            "dryRun": false,
            "delay": "2h",
            "policies": [
              {
                "repositories": ["**"],
                "deleteReferrers": false,
                "deleteUntagged": true,
                "keepTags": [
                  { "patterns": ["latest"] },
                  { "patterns": ["sha256-.*"], "mostRecentlyPushedCount": 50 },
                  { "patterns": ["v.*"], "mostRecentlyPushedCount": 5 },
                  { "patterns": ["[0-9a-f]{7,64}"], "mostRecentlyPushedCount": 5 }
                ]
              }
            ]
          }
        },
        "http": {
          "address": "0.0.0.0",
          "port": "5000",
          "compat": ["docker2s2"],
          "readTimeout": "1800s",
          "writeTimeout": "1800s",
          "auth": { "htpasswd": { "path": "/etc/zot/htpasswd" } },
          "accessControl": {
            "repositories": {
              "**": {
                "policies": [
                  { "users": ["${zot_pull_user}"], "actions": ["read"] },
                  { "users": ["${zot_push_user}"], "actions": ["read", "create", "update"] }
                ],
                "defaultPolicy": []
              }
            }
          }
        },
        "log": { "level": "info" }
      }
    owner: root:root
    permissions: '0644'

  # Disk-capacity guard + in-surface disk observability (#6122 / #6244). TWO responsibilities:
  #  (1) ABSENCE-BASED LIVENESS PING (unchanged semantics): ping the disk-health heartbeat ONLY
  #      while /var/lib/zot is <85% used. A filling store (or down host / broken cron) stops the
  #      ping → Better Stack alerts BEFORE full (hr-no-dashboard-eyeball-pull-data-yourself).
  #  (2) STRUCTURED SELF-REPORT (#6244): the registry host is a deny-all-ingress, no-SSH BLIND
  #      surface — so it self-reports its disk state as ONE `SOLEUR_ZOT_DISK` event to Better
  #      Stack Logs (queryable via scripts/betterstack-query.sh --grep SOLEUR_ZOT_DISK, NO ssh).
  #      Its fields (pcent / fs_size_gb / block_size_gb / resize_ok / zot_restarts / ping_rc)
  #      discriminate ALL competing root causes in ONE event: fs-not-grown (resize_ok=false OR
  #      fs_size_gb << block_size_gb), gc-too-slow (resize_ok=true, fs≈60, pcent>=85), and
  #      zot-mid-write-crash (pcent<85, fs≈60, zot_restarts>0).
  # disk_heartbeat_url + betterstack_ingest_url are terraform-interpolated (non-secret host
  # routing); BETTERSTACK_LOGS_TOKEN is injected AT CRON TIME by `doppler run` from the isolated
  # soleur-registry/prd config (NEVER baked into user_data). Plain dollar-VARs stay literal
  # (templatefile only expands dollar-brace); shell brace-expansions are written $${VAR:-}.
  - path: /usr/local/bin/zot-disk-heartbeat.sh
    content: |
      #!/usr/bin/env bash
      set -u
      # (1) Absence-based liveness ping — ping ONLY while /var/lib/zot <85% (unchanged).
      USE=$(df --output=pcent /var/lib/zot 2>/dev/null | tail -1 | tr -dc '0-9')
      PING_RC=0
      if [ -n "$USE" ] && [ "$USE" -lt 85 ]; then
        curl -fsS -m 10 "${disk_heartbeat_url}" >/dev/null 2>&1 || PING_RC=$?
      fi
      # (2) Structured self-report → Better Stack Logs (SOLEUR_ZOT_DISK).
      PCENT="$${USE:-}"; [ -n "$PCENT" ] || PCENT=-1
      FS_GB=$(df -B1G --output=size /var/lib/zot 2>/dev/null | tail -1 | tr -dc '0-9'); [ -n "$FS_GB" ] || FS_GB=0
      # Persisted on-boot resize breadcrumb (cloud-init resize block): resize_ok + block_size_gb.
      # PARSE-ONLY (grep|cut) — never `.`-source it: a crafted file at the zot volume root would
      # otherwise execute as root. Values are simple tokens (bool / integer), no quoting needed.
      resize_ok=unknown; block_size_gb=0
      if [ -r /var/lib/zot/.resize-result ]; then
        v=$(grep -E '^resize_ok=' /var/lib/zot/.resize-result | head -1 | cut -d= -f2); [ -n "$v" ] && resize_ok=$v
        v=$(grep -E '^block_size_gb=' /var/lib/zot/.resize-result | head -1 | cut -d= -f2); [ -n "$v" ] && block_size_gb=$v
      fi
      # (2a) Host TOTAL memory from /proc/meminfo (procps `free` is NOT in packages:) —
      # mem_total_mb self-verifies the cx33 bump landed on a no-SSH host (~8000 vs ~4000) and
      # pins the host tier for interpreting the OOM decode rows.
      # (#6292) host used-memory dropped — page-cache-confounded (a large store scan pins page cache
      # so host used fills regardless); OOM confirmation keys on zot_anon_mb/zot_oom_kills below.
      MEM_TOTAL_KB=$(awk '/^MemTotal:/{print $2}' /proc/meminfo 2>/dev/null)
      if [ -n "$MEM_TOTAL_KB" ]; then MEM_TOTAL=$(( MEM_TOTAL_KB / 1024 )); else MEM_TOTAL=-1; fi
      # (2b) zot container state — ONE inspect for ALL container fields (folds in RestartCount, so
      # zot_restarts comes from the same call — no double inspect). Mid-restart returns empty →
      # default the WHOLE capture to sentinels. Adapted from container-restart-monitor.sh:118-119
      # (adds .State.Status). Loop DETECTION keys on zot_restarts delta (reliable); the exit-reason
      # fields are for CAUSE attribution (often stale when a 4/min loop is sampled mid-run).
      # StartedAt joins THIS call rather than getting its own (#7247). A second `docker inspect`
      # would break the one-inspect invariant above in a way that defeats the field's purpose: the
      # two calls can straddle a restart and pair an exit_code from one run with an uptime from
      # the next, and if the first succeeds while the second misses you get
      # `exit_code=0 state_status=running zot_uptime_s=-1` — ambiguous again, which is the exact
      # condition zot_uptime_s exists to remove.
      # (2b1) #7282 — `.Config.Image` joins THIS call for the same one-inspect reason as
      # StartedAt above. It is the ONLY off-host way to answer "which zot binary is production
      # actually running", and this host has no SSH ingress at all (the tunnel's ssh. route
      # reaches web-1, not the registry — #7278), so without it "did the bump land?" is
      # unanswerable by any means available to this project. That matters most in the EXPECTED
      # steady state: the pin merges but stays staged (zot-registry.tf is an
      # OPERATOR_APPLIED_EXCLUSION), so the repo pins a newer zot v2.1.20 while the live host still
      # runs its older one, and that divergence is the norm until
      # the host-replace path unblocks, and today that divergence is invisible.
      #
      # `.Config.Image`, NOT `.RepoDigests`: RepoDigests is a field of the IMAGE object, so
      # reading it would require a SECOND inspect against a different object — precisely what
      # the one-inspect invariant above forbids. `.Config.Image` is on the CONTAINER and carries
      # the reference the container was CREATED with, which since we run by digest IS the pin.
      # It also still resolves for an exited/crash-looping container, which is when it is needed.
      INSPECT=$(docker inspect -f '{{.Id}} {{.RestartCount}} {{.State.Status}} {{.State.OOMKilled}} {{.State.ExitCode}} {{.State.StartedAt}} {{.Config.Image}}' zot 2>/dev/null)
      if [ -n "$INSPECT" ]; then
        read -r ID ZOT_RESTARTS STATE_STATUS OOM_KILLED EXIT_CODE STARTED_AT ZOT_IMAGE_REF <<<"$INSPECT"
        # Emit the leading 12 hex of the digest, not the ~90-char reference: unambiguous
        # against a known pin set and keeps the line bounded. `unknown` is the DEFAULT here,
        # never a fallback — same discipline as the htpasswd block below: "cannot tell" must
        # never be conflated with a value. A container created by TAG only (no @sha256) has no
        # digest to report and correctly reads `unknown` rather than a fabricated one.
        # [0-9a-f]{12}, NOT twelve dots. `.` matches `"` and `\`, and this value is
        # interpolated into --data-raw "{\"message\":\"$LINE\"}" -- an unescaped byte makes
        # Better Stack reject the WHOLE payload, taking the entire heartbeat dark. The
        # ZOT_LAST_ERR block below sanitizes for exactly that reason. A character class also
        # makes the `unknown`-is-a-default property true BY CONSTRUCTION rather than by
        # upstream convention: junk now falls to `unknown` instead of becoming a
        # plausible-looking 12-char "digest".
        ZOT_IMAGE_DIGEST=$(printf '%s' "$${ZOT_IMAGE_REF:-}" | sed -n 's/.*@sha256:\([0-9a-f]\{12\}\).*/\1/p')
        ZOT_IMAGE_DIGEST=$${ZOT_IMAGE_DIGEST:-unknown}
      else
        ID=; ZOT_RESTARTS=-1; STATE_STATUS=unknown; OOM_KILLED=unknown; EXIT_CODE=-1; STARTED_AT=
        # Rides the SAME sentinel arm as every sibling field, so a dead container reports
        # `unknown` by the same path rather than through an invented second fallback. This arm
        # also covers "docker is down" and "the container was never created (image pull
        # failed)" — all three are honestly "cannot tell", not "no digest".
        ZOT_IMAGE_DIGEST=unknown
      fi
      # (2b2) #7247 — the exit_code STALENESS discriminator. `.State.ExitCode` is 0 for ANY RUNNING
      # container; it only carries a real value while the container is stopped. A 5-minute probe
      # against a ~14-second restart cycle therefore reads `exit_code=0 state_status=running`
      # forever, which reads as "clean exit" and is in fact "we sampled mid-run". That is not a
      # hypothetical misreading: it is the one this issue's own triage had to back out of.
      # The comment at (2b) already warns these fields are "often stale when a 4/min loop is
      # sampled mid-run" — but nothing EMITTED let a reader tell stale from real.
      #
      # zot_uptime_s closes that: it is the age of the CURRENT run. A value that is always a few
      # seconds is positive proof the sample is mid-loop and that exit_code carries no information;
      # a large value means the container is genuinely up and exit_code is trustworthy. One field
      # turns an ambiguous 0 into a self-describing one, with no host access.
      # STARTED_AT comes from the single inspect above. CLAMP is load-bearing: a never-started
      # container reports StartedAt=0001-01-01T00:00:00Z, which GNU date parses to -62135596800 —
      # yielding zot_uptime_s≈6.4e10, i.e. "up for 2000 years". That is not the -1 sentinel, it
      # survives the consumers' non-sentinel filters, and it reads as "exit_code is trustworthy":
      # the precise inversion of this field's purpose. Negative or absurd => -1 (unknown).
      ZOT_UPTIME_S=-1
      if [ -n "$STARTED_AT" ]; then
        _started_epoch=$(date -u -d "$STARTED_AT" +%s 2>/dev/null || echo "")
        if [ -n "$_started_epoch" ]; then
          ZOT_UPTIME_S=$(( $(date -u +%s) - _started_epoch ))
          # Clamp: negative (clock skew) or > 10 years (the 0001-01-01 sentinel) is not a runtime.
          if [ "$ZOT_UPTIME_S" -lt 0 ] || [ "$ZOT_UPTIME_S" -gt 315360000 ]; then ZOT_UPTIME_S=-1; fi
        fi
      fi
      # (2c) Container anonymous RSS — the OOM-CONFIRMATION signal (excludes reclaimable page cache,
      # unlike host used-memory). cgroup v2 memory.stat `anon` in bytes → /1048576. Path mirrors
      # container-restart-monitor.sh:129-132. Guard -1 if the container/cgroup is absent (mid-restart).
      CGROUP_ROOT="$${CGROUP_ROOT:-/sys/fs/cgroup}"
      ZOT_ANON_MB=-1; ZOT_OOM_KILLS=-1; ZOT_MEMORY_CAP_MB=-1; ZOT_MEMORY_CAPPED=unknown
      if [ -n "$ID" ]; then
        cg="$CGROUP_ROOT/system.slice/docker-$ID.scope"
        anon_bytes=$(grep -E '^anon ' "$cg/memory.stat" 2>/dev/null | awk '{print $2}')
        [ -n "$anon_bytes" ] && ZOT_ANON_MB=$(( anon_bytes / 1048576 ))
        # The cap zot is ACTUALLY running under, read from the live cgroup rather than copied
        # from the value we templated in. The cap is now derived from the server type
        # (zot-registry.tf local.registry_memory_cap_mb), so it is no longer the constant 7168
        # every consumer used to assume: on a 4 GB host it is 3072. A gate holding a stale copy
        # would compare zot_anon_mb against a ceiling the container cannot reach and PASS a
        # starved host — the failure the "guard/gate and probe must pin the thing they name"
        # learning describes.
        #
        # THREE distinct states, and they must NOT collapse into one:
        #   number -> capped        (zot_memory_capped=true,    cap_mb=N)
        #   "max"  -> UNCAPPED      (zot_memory_capped=false,   cap_mb=-1)  <- the #6288 condition
        #   absent -> unknown       (zot_memory_capped=unknown, cap_mb=-1)  <- mid-restart, benign
        # zot_memory_capped exists because cap_mb alone CANNOT express "uncapped": -1 is the
        # parse lib's sentinel (it drops every negative), and 0 would read as "capped at nothing"
        # and fail every gate. So an uncapped container would look identical to a mid-restart one,
        # the gate would fall back to ASSUMING 7168, and nothing could ever alarm on zot running
        # with no cap at all — the exact condition this whole derivation exists to prevent.
        # Reported as a true/false/unknown string (parsed like oom_killed), never as a number.
        cap_bytes=$(cat "$cg/memory.max" 2>/dev/null)
        case "$cap_bytes" in
          '')             : ;;                                   # cgroup gone: stays unknown
          max)            ZOT_MEMORY_CAPPED=false ;;             # explicitly UNCAPPED
          *[!0-9]*)       : ;;                                   # unparseable: stays unknown
          *)              ZOT_MEMORY_CAPPED=true
                          ZOT_MEMORY_CAP_MB=$(( cap_bytes / 1048576 )) ;;
        esac
        # memory.events `oom_kill` is a MONOTONIC container-cgroup OOM-kill counter (mirrors
        # container-restart-monitor.sh's authoritative signal). Unlike the zot_anon_mb GAUGE — a
        # point sample that a 4/min loop sampled every 5 min systematically reads mid-scan, missing
        # the pre-OOM peak — this counter SURVIVES the sampling race: any nonzero value on the
        # newest boot_id means the cgroup OOM-killed zot. This is the real cgroup-OOM confirmation;
        # zot_anon_mb stays as pressure context. `^oom_kill ` (trailing space) excludes oom_group_kill.
        oom_k=$(grep -E '^oom_kill ' "$cg/memory.events" 2>/dev/null | awk '{print $2}')
        [ -n "$oom_k" ] && ZOT_OOM_KILLS=$oom_k
      fi
      # (2d) journald kernel-OOM window backstop — survives the point-sampling race (kernel OOM-kill
      # entries persist the window even when inspect catches Status=running with stale exit fields).
      OOM_KILLS_5M=$(journalctl -k --since "5 min ago" 2>/dev/null | grep -ciE 'oom-kill|out of memory'); [ -n "$OOM_KILLS_5M" ] || OOM_KILLS_5M=0
      # (2e) Non-OOM escape hatch (closes the no-SSH dead-end): bounded ≤300-byte single-line tail of
      # zot's own logs (zot logs at info, :128, so secret-exposure risk is low). Strip quotes/
      # backslashes too so a log line never breaks the JSON payload below. Read via the decode table.
      # Whitespace-controls → space; then keep ONLY printable ASCII (drops C0/DEL control bytes AND
      # mid-sequence-sliced UTF-8 — both invalid unescaped in a JSON string, RFC 8259 §7, so an
      # unstripped tab-indented Go crash trace would make Better Stack reject the WHOLE payload in
      # the exact crash case this field targets); finally drop "/\ so it can't corrupt the JSON.
      # (#7247) `--tail 3` was STRUCTURALLY incapable of carrying the crash line this field exists
      # to carry. zot logs EVERY request at info (:128) and this host's own liveness probe GETs
      # /v2/ every 60s (zot-liveness-heartbeat.timer: OnUnitActiveSec=60s — the TIMER is the
      # authority; an earlier version of this comment said "every 5 min", and #7440's row-volume
      # floor depends on the correct figure), so the last 3 lines are near-always routine HTTP
      # traffic. Across a 21-hour
      # 4.4-restarts/min loop (2026-08-03 17:08Z onward) the field carried a cause exactly zero
      # times — every sample was an HTTP 200/401 session line or a gc/dedupe info line. The comment
      # above already called this "the exact crash case this field targets"; the window was the bug.
      #
      # Scan WIDE and FILTER for cause-bearing lines instead. Falls back to the original tail-3 when
      # nothing matches, so this is never emptier than what it replaces. `--restart unless-stopped`
      # restarts the SAME container (RestartCount 5032 on one container id), so `docker logs`
      # accumulates ACROSS restarts and a prior run's fatal line is still in the buffer.
      #
      # grep -a: the buffer may contain binary from a truncated write; without it GNU grep treats
      # the stream as binary and prints nothing, which would silently re-create the empty-field bug.
      # RANKED, not merely widened. A flat grep + `tail -n 3` keeps the NEWEST matches, and the
      # broadest terms (`cannot `/`failed to `) are exactly what routine zot warn lines hit — so
      # the current run's chatter evicts the previous run's `panic:`. Try tiers in severity order
      # and stop at the first that hits.
      #
      # `--since 12m` (not `--tail N`): the container runs `--log-driver journald`, so retention is
      # journald's and a LINE bound is the wrong bound against a variable-chattiness startup scan.
      # A time bound spans the 5-min cadence regardless of how many restarts occurred.
      _zlogs() { docker logs --since 12m zot 2>&1; }
      ZOT_ERR_SRC=none
      ZOT_ERR_RAW=$(_zlogs | grep -aE 'panic:|fatal error|\[signal SIG|runtime error' | head -n 4)
      [ -n "$ZOT_ERR_RAW" ] && ZOT_ERR_SRC=panic
      if [ -z "$ZOT_ERR_RAW" ]; then
        ZOT_ERR_RAW=$(_zlogs | grep -aE '"level":"(error|fatal)"|level:(error|fatal)|level=(error|fatal)' | head -n 3)
        [ -n "$ZOT_ERR_RAW" ] && ZOT_ERR_SRC=error
      fi
      if [ -z "$ZOT_ERR_RAW" ]; then
        ZOT_ERR_RAW=$(_zlogs | grep -aiE 'cannot |failed to |unable to |could not |no such file|permission denied' | head -n 3)
        [ -n "$ZOT_ERR_RAW" ] && ZOT_ERR_SRC=warn
      fi
      # Fallback must be DISTINGUISHABLE from a match. Emitting routine tail-3 under the same field
      # name is how a downstream alarm ends up printing "gc successfully completed" as the cause of
      # a crash loop — naming a cause nobody measured (ADR-166), which is the defect this whole
      # change exists to remove. zot_last_err_src says which tier produced the text.
      if [ -z "$ZOT_ERR_RAW" ]; then ZOT_ERR_RAW=$(docker logs --tail 3 zot 2>&1); ZOT_ERR_SRC=fallback; fi
      # head -c, NOT tail -c. A Go panic puts `panic: <msg>` on the FIRST line and the frame
      # offsets after it; tail -c keeps the offsets and discards the only part that names the
      # cause. Measured: the 2026-08-04 15:00:02Z sample reached Better Stack carrying only
      # `...scheduler.go:143 +0x265 created by ...poolWorker in goroutine 32` — the panic header
      # had already been truncated away by exactly this bug.
      ZOT_LAST_ERR=$(printf '%s' "$ZOT_ERR_RAW" | tr '\n\r\t' ' ' | LC_ALL=C tr -cd '\40-\176' | tr -d '"\\' | head -c 300); [ -n "$ZOT_LAST_ERR" ] || { ZOT_LAST_ERR=none; ZOT_ERR_SRC=none; }
      # (2f) Boot discriminator — the immutable redeploy reuses the terraform hostname, so boot_id is
      # what cleanly separates old-host from new-host events for the automated decision rule.
      BOOT_ID=$(cat /proc/sys/kernel/random/boot_id 2>/dev/null); [ -n "$BOOT_ID" ] || BOOT_ID=unknown
      # (2g) #6497 — htpasswd-vs-Doppler divergence detector. THE decisive, no-SSH signal for the
      # WEB-PLATFORM-5B class: /etc/zot/htpasswd is baked ONCE at boot from these tokens, so a
      # rotation (or a boot that raced the secret write) leaves the host authenticating against a
      # value no client still presents — and nothing else on this deny-all host can report it.
      # Comparing Doppler to Doppler proves nothing (both copies derive from the same
      # random_password and agree BY CONSTRUCTION even when both have drifted from the host); the
      # only comparison with evidentiary value is host-htpasswd vs the CURRENT token, which is
      # exactly this. `htpasswd -vb` verifies a password against the file and emits only a fixed
      # status line ("Password for user X correct." / "password verification failed") — the token
      # is NEVER printed; verified on ubuntu:24.04 (the shipped host image): exit 0 = match,
      # exit 3 = mismatch. Output is discarded regardless; a BOOLEAN is all that crosses the
      # boundary. apache2-utils is already in `packages:` (:18), so htpasswd is on the host.
      # Both tokens are in the env: the cron wraps this under `doppler run --project
      # soleur-registry --config prd`, whose isolated config holds exactly these three secrets.
      # Expected under H3: htpasswd_pull_matches=false htpasswd_push_matches=true — the E4
      # asymmetry (crane/cosign push authenticating while the pull login is rejected), proven
      # from inside the host rather than inferred from the outside.
      # `unknown` is the DEFAULT, not a fallback: "cannot tell" is never conflated with
      # "does not match" (a missing htpasswd is a different, worse defect than a diverged one).
      # Every token expansion below uses `:-` — this script runs `set -u` (:151), where a BARE
      # "$ZOT_PULL_TOKEN" on an unset token raises `unbound variable` and EXITS the script
      # BEFORE $LINE is built, taking the whole SOLEUR_ZOT_DISK heartbeat dark (pcent, boot_id,
      # OOM decode — everything) and bypassing the `exit 0` at the end that exists so the cron
      # can never wedge. `|| HTP_PULL=false` does NOT rescue it: an expansion error is not a
      # command failure. That would be a probe that kills the telemetry line it rides on — and
      # since this heartbeat's ABSENCE is itself an alarm, it would page "host down" when only
      # the probe broke. The guards are therefore preconditions, never post-hoc corrections.
      # Gate on the EXIT CODE, not on zero-vs-nonzero. Measured on ubuntu:24.04 (the shipped
      # host image): 0 = match, 3 = password mismatch, 6 = user absent from the file, 1 = file
      # unreadable, 127 = htpasswd itself missing. ONLY 3 is a real divergence. Collapsing
      # every non-zero into `false` would report a confident "the credential diverged" when a
      # cloud-init edit renamed the htpasswd user (rc=6) — sending the operator to rotate a
      # credential that was never stale, which is the inverse of this probe's job.
      _htp_verify() {
        htpasswd -vb /etc/zot/htpasswd "$1" "$2" >/dev/null 2>&1
        case "$?" in
          0) printf 'true' ;;
          3) printf 'false' ;;
          *) printf 'unknown' ;;
        esac
      }
      HTP_PULL=unknown; HTP_PUSH=unknown
      if [ -f /etc/zot/htpasswd ]; then
        [ -n "$${ZOT_PULL_TOKEN:-}" ] && HTP_PULL=$(_htp_verify "${zot_pull_user}" "$${ZOT_PULL_TOKEN:-}")
        [ -n "$${ZOT_PUSH_TOKEN:-}" ] && HTP_PUSH=$(_htp_verify "${zot_push_user}" "$${ZOT_PUSH_TOKEN:-}")
      fi
      # #7440: carry the LOG SHIPPER's own health on THIS line, which is a DIFFERENT and
      # independently-working egress path. A POST-failure counter surfaced on the channel it
      # monitors is unobservable in exactly the state it reports — when the POST path is the fault,
      # the row carrying the counter never arrives. So the shipper writes these to a state file and
      # this 5-min reporter reads them, giving the follow-through probe a `shipper_post_failing`
      # reason that survives a totally dead shipper egress.
      # -1 means "no successful POST recorded yet", which is distinguishable from a fresh 0 age.
      #
      # DROP ACCOUNTING RIDES THIS LINE TOO (#7444 F-8). The shipper's own drop rows are emitted
      # through `ship()`, i.e. over the very sink whose outage is `cursor_invalidated`'s stated
      # cause — so that signal is structurally undeliverable in its own causal story. dropped_cum
      # and drop_seq are already in the state file and nothing read them. Carrying them here puts
      # drop accounting on the independently-working path, where it is readable precisely when the
      # shipper's own egress is not.
      LOG_SHIPPER_POST_FAIL=unknown; LOG_SHIPPER_LAST_OK_AGE_S=-1
      LOG_SHIPPER_DROPPED_CUM=unknown; LOG_SHIPPER_DROP_SEQ=unknown; LOG_SHIPPER_SHIPPED_CUM=unknown
      if [ -r /var/lib/zot-log-shipper/state ]; then
        LOG_SHIPPER_POST_FAIL=0
        LOG_SHIPPER_DROPPED_CUM=0; LOG_SHIPPER_DROP_SEQ=0; LOG_SHIPPER_SHIPPED_CUM=0
        while IFS='=' read -r _lsk _lsv; do
          case "$_lsk" in
            post_fail) LOG_SHIPPER_POST_FAIL="$_lsv" ;;
            dropped_cum) case "$_lsv" in ''|*[!0-9]*) : ;; *) LOG_SHIPPER_DROPPED_CUM="$_lsv" ;; esac ;;
            drop_seq)    case "$_lsv" in ''|*[!0-9]*) : ;; *) LOG_SHIPPER_DROP_SEQ="$_lsv" ;; esac ;;
            # shipped_cum was written and incremented with NO reader anywhere (#7444 R28). It is
            # the cheapest POSITIVE liveness signal available on the independently-working path:
            # every other field says how things are failing, this one says the channel has ever
            # worked. Carried rather than deleted for that reason.
            shipped_cum) case "$_lsv" in ''|*[!0-9]*) : ;; *) LOG_SHIPPER_SHIPPED_CUM="$_lsv" ;; esac ;;
            last_ok_epoch)
              case "$_lsv" in
                ''|*[!0-9]*) : ;;
                0) : ;;
                *) LOG_SHIPPER_LAST_OK_AGE_S=$(( $(date +%s) - _lsv )) ;;
              esac
              ;;
          esac
        done < /var/lib/zot-log-shipper/state
      fi
      # zot_last_err is free-text (may contain spaces) → keep it the LAST field so it never corrupts
      # key=value parsing of the fields before it.
      LINE="SOLEUR_ZOT_DISK pcent=$PCENT fs_size_gb=$FS_GB block_size_gb=$block_size_gb resize_ok=$resize_ok zot_restarts=$ZOT_RESTARTS ping_rc=$PING_RC mem_total_mb=$MEM_TOTAL zot_anon_mb=$ZOT_ANON_MB zot_memory_cap_mb=$ZOT_MEMORY_CAP_MB zot_memory_capped=$ZOT_MEMORY_CAPPED zot_oom_kills=$ZOT_OOM_KILLS state_status=$STATE_STATUS oom_killed=$OOM_KILLED exit_code=$EXIT_CODE zot_uptime_s=$ZOT_UPTIME_S zot_last_err_src=$ZOT_ERR_SRC oom_kills_5m=$OOM_KILLS_5M boot_id=$BOOT_ID zot_image_digest=$ZOT_IMAGE_DIGEST htpasswd_pull_matches=$HTP_PULL htpasswd_push_matches=$HTP_PUSH log_shipper_post_fail=$LOG_SHIPPER_POST_FAIL log_shipper_last_ok_age_s=$LOG_SHIPPER_LAST_OK_AGE_S log_shipper_dropped_cum=$LOG_SHIPPER_DROPPED_CUM log_shipper_drop_seq=$LOG_SHIPPER_DROP_SEQ log_shipper_shipped_cum=$LOG_SHIPPER_SHIPPED_CUM host=$(hostname) zot_last_err=$ZOT_LAST_ERR"
      TOKEN="$${BETTERSTACK_LOGS_TOKEN:-}"
      if [ -n "$TOKEN" ]; then
        # Fail-loud-but-not-wedge: retry ONCE, then breadcrumb to journald. ping_rc is carried in
        # the payload so an egress failure is itself observable in the NEXT successful post.
        post() { curl -fsS -m 10 -H "Authorization: Bearer $TOKEN" -H 'Content-Type: application/json' "${betterstack_ingest_url}" --data-raw "{\"message\":\"$LINE\"}" >/dev/null 2>&1; }
        post || post || echo "[zot] SOLEUR_ZOT_DISK egress to Better Stack Logs FAILED: $LINE" >&2
      else
        echo "[zot] WARN: BETTERSTACK_LOGS_TOKEN unset (run under 'doppler run --project soleur-registry --config prd') — SOLEUR_ZOT_DISK not shipped: $LINE" >&2
      fi
      # Always exit 0 so the 5-min cron never wedges (an egress failure stays observable).
      exit 0
    owner: root:root
    permissions: '0755'

  - path: /etc/cron.d/zot-disk-heartbeat
    content: |
      # Emit the zot disk heartbeat + SOLEUR_ZOT_DISK self-report every 5 min (#6122 / #6244).
      # Wrapped in `doppler run` so BETTERSTACK_LOGS_TOKEN (isolated soleur-registry/prd config)
      # is injected for the Better Stack Logs POST — never baked into user_data.
      */5 * * * * root set -a; . /etc/default/registry-doppler; set +a; doppler run --project soleur-registry --config prd -- /usr/local/bin/zot-disk-heartbeat.sh
    owner: root:root
    permissions: '0644'

  # --- #6537: zot LIVENESS heartbeat (the feeder that arms betteruptime_heartbeat.registry_prd)
  # The disk heartbeat above pings on `df` alone, so it stays GREEN with zot dead — it alarms
  # HOST death by absence (900s), never PROCESS death. This feeder closes exactly that gap:
  # zot dead, host alive, disk fine. It is the ONLY thing that feeds registry_prd; before it,
  # that monitor had zero consumers and sat paused for 9 days (#6537).
  #
  # It probes the host's OWN PRIVATE IP, never loopback. zot publishes `-p 0.0.0.0:5000:5000`,
  # so a loopback probe answers even on a host holding no 10.0.1.30 — which is how #6400 stayed
  # green for ~14 days (see the private-NIC converger's preamble below, which documents that
  # blindness in full). Probing the private IP means this heartbeat is ALSO a second, independent
  # signal for NIC absence. Using loopback here would rebuild the blind spot inside the very
  # monitor meant to see through it.
  #
  # ABSENCE-BASED, like the disk beat: ping ONLY inside the probe guard. An unconditional ping
  # would make the monitor decorative — green over a dead registry.
  # liveness_heartbeat_url is terraform-interpolated (non-secret host routing, same class as
  # disk_heartbeat_url). NO `doppler run` wrapper: the URL is baked, so there is no
  # empty-variable failure mode (#4116) and no token to inject.
  - path: /usr/local/bin/zot-liveness-heartbeat.sh
    content: |
      #!/usr/bin/env bash
      set -u
      # (1) Probe zot on the private IP — the address consumers actually pull from.
      #     Discriminate on the STATUS CODE, never on curl's -f. zot auth-gates /v2/
      #     (htpasswd + "defaultPolicy": [] above), so an anonymous probe gets 401 — and 401 IS
      #     liveness: it proves zot is up, listening, and enforcing auth. `-f` exits 22 on any
      #     >=400, so an -f probe would treat every healthy response as dead and this feeder
      #     would never ping at all. The boot readiness loop below already documents this
      #     ("401 unauth IS healthy — reachable, auth-gated") and omits -f for the same reason.
      #       200/401 -> zot answered           => alive
      #       5xx     -> wedged/erroring zot    => NOT liveness (this is what -f was reaching for)
      #       000     -> no HTTP response at all (connection refused, timeout, absent NIC)
      #     -m 10 bounds the probe: two curls x 10s worst case = 20s, well inside the timer's
      #     60s interval, so a hung zot can never stack units.
      HTTP_CODE=$(curl -sS -m 10 -o /dev/null -w '%%{http_code}' "http://${private_ip}:5000/v2/" 2>/dev/null || true)
      # (2) Ping ONLY when zot answered. Absence of this ping IS the alarm — Better Stack pages
      #     when no beat arrives within period(60s) + grace(30s).
      case "$${HTTP_CODE:-000}" in
        200|401)
          curl -fsS -m 10 -o /dev/null "${liveness_heartbeat_url}" || true
          ;;
      esac
      # Always exit 0: a failed probe must alarm via ABSENCE (the monitor's own semantics), not
      # via a failed unit that only `systemctl status` on a no-SSH host could ever reveal.
      exit 0
    owner: root:root
    permissions: '0755'

  - path: /etc/systemd/system/zot-liveness-heartbeat.service
    content: |
      [Unit]
      Description=Ping the zot liveness heartbeat when zot answers on the private IP (#6537)
      After=network-online.target
      Wants=network-online.target

      [Service]
      Type=oneshot
      ExecStart=/usr/local/bin/zot-liveness-heartbeat.sh
    owner: root:root
    permissions: '0644'

  # A systemd timer, NOT /etc/cron.d, for TWO independent reasons:
  #   1. cron's resolution floor is 60s, which against a 60s period + 30s grace leaves zero
  #      margin — every scheduling jitter would be a false page.
  #   2. cron's default PATH is /usr/bin:/bin and omits /usr/sbin. That is the #6415 trap on
  #      THIS host: a probe binary silently unresolvable under cron while curl still resolves.
  #      systemd's service PATH includes /usr/sbin, so the timer eliminates that class outright.
  #
  # AccuracySec=1s is load-bearing, NOT boilerplate. systemd's default is 1 MINUTE: the timer
  # fires anywhere in [elapse, elapse+60s], so the interval is 60s + delta with a structural
  # bound of 120s — against a 90s deadline. Measured on an A/B of this exact unit: unset drifted
  # 60.0s / 62.0s / 72.6s / 66.5s, while AccuracySec=1s held 61.0s +/- 16ms. 72.6s is already
  # inside the deadline's margin and the bound is over it.
  #
  # The inngest_prd feeder (inngest-bootstrap.sh) runs this cadence against an identical 60/30
  # monitor and is live and up — but that is margin, not design: systemd's coalescing offset is
  # derived from the BOOT ID, so its greenness is evidence about that host's current boot and
  # nothing else. This feeder ships by immutable redeploy, which re-rolls the offset on every
  # replace — so a measured-green first beat would not generalize to the next boot. On a no-SSH
  # host whose failure mode is "page the founder forever", do not ship the boundary case.
  - path: /etc/systemd/system/zot-liveness-heartbeat.timer
    content: |
      [Unit]
      Description=Run the zot liveness heartbeat every 60s (#6537)

      [Timer]
      OnBootSec=30s
      OnUnitActiveSec=60s
      AccuracySec=1s

      [Install]
      WantedBy=timers.target
    owner: root:root
    permissions: '0644'

  # --- #7440: zot CONTAINER-LOG shipper (journald -> Better Stack Logs) -------------------
  # THE GAP. Before this, the only telemetry leaving this host was the 5-min SOLEUR_ZOT_DISK
  # marker above. The zot container's own log stream went nowhere off-box, so every count derived
  # from that channel was a LOWER BOUND rather than a measurement: the reporter samples
  # `docker logs` once per interval and folds one truncated, quote-stripped excerpt into
  # zot_last_err. Measured 2026-08-11 over a 6h window — 72 rows mentioning this host, all 72
  # heartbeats; `routes.go` and `blobs/uploads` returned 0 rows; `zotregistry.dev` returned 53,
  # all 53 inside a heartbeat echo. Zero genuine zot log lines had ever reached the warehouse.
  #
  # THIS IS A SHIPPING PROBLEM, NOT AN INSTRUMENTATION ONE — the single most important scoping
  # fact. The container already runs `--log-driver journald` under the name `zot` (see the launch
  # block below), so the lines are ALREADY on the host as CONTAINER_NAME=zot. Nothing about zot's
  # own configuration changes here, which keeps this clear of ADR-172 §3's write-surface deadlock.
  #
  # WHY NOT VECTOR (the issue's literal framing). Decided in ADR-184. The binding reason is
  # PAYLOAD DESTRUCTION, not credentials: the shared vector.toml drops any top-level `message` key
  # as an Art-9 user-content key, and zerolog's log text IS a top-level `message` — so a
  # Vector-shipped zot line would arrive with its message deleted. Nominally shipping, actually
  # empty. Supporting reasons (each sufficient alone): host_metrics quota cost, a boot-time binary
  # download on the sole image-pull path, a second config to hold in lockstep, and the user_data
  # budget. The pepper/isolation-guard argument is RETRACTED as false — do not re-derive it.
  #
  # THE LOG SHAPE IS MEASURED, NOT INFERRED. zot emits QUOTED zerolog JSON. Measured against the
  # pinned image locally on 2026-08-11:
  #   {"time":"...","level":"info","message":"HTTP API","path":"/v2/","statusCode":401,
  #    "headers":{"Accept":["*/*"],"Authorization":["******"],"User-Agent":["curl/8.5.0"]},
  #    "caller":"zotregistry.dev/zot/v2/pkg/api/session.go:92",...}
  # The colon-joined `{time:...,caller:zotregistry.dev/...}` form visible in zot_last_err is the
  # SAMPLER's rendering (that field passes through `tr -d '"\\'`), NOT zot's output. Anchoring
  # either the redaction or the probe's discriminator on the stripped form ships an inert
  # mechanism: a rule shaped like vector.toml's `(?i)(authorization:\s*)bearer` cannot match
  # `"Authorization":["..."]`, and a probe grepping `caller:zotregistry.dev` matches ONLY the echo.
  #
  # ALSO MEASURED: zot masks Authorization ITSELF (`["******"]`) — the literal credential appeared
  # 0 times across basic-auth/Bearer/Basic probes. The redact() rule below is therefore DEFENCE IN
  # DEPTH against a future zot that stops masking, not the primary control. But a NON-Authorization
  # header IS logged verbatim (`"X-Custom":["plainvalue"]`), which is why the rule is anchored on
  # the header-object shape rather than trusting one header name's known masking.
  #
  # A DIRECT HOST POST BYPASSES VRL REDACTION, so this shipper owns its own sanitizer. Note the
  # two are DIFFERENT jobs and are deliberately named apart: the `tr` pipeline is PAYLOAD
  # INTEGRITY (RFC 8259 — an unescaped quote or a raw tab would make Better Stack reject the whole
  # body), and redact() is the credential backstop. Four rules an earlier draft carried CANNOT
  # fire on this host (no email in zot's identity model; no sync/extensions/credentialsFile in the
  # config, so no credentialed upstream URL; htpasswd values are never echoed) and were dropped
  # rather than kept as decorative safety.
  #
  # WAS INERT UNTIL PROVISIONED — DELIVERED 2026-08-12T20:54:12Z (#7455). hcloud_server.registry is
  # cloud-init-only (ADR-096, amended by ADR-172 §8): no config webhook, no provisioner, no
  # cloud-init re-run, and every registry resource is an OPERATOR_APPLIED_EXCLUSION. Merging this
  # applies NOTHING — that rule is UNCHANGED and governs the NEXT change to this file.
  #
  # What is retracted is only the vehicle: an earlier form of this block said delivery "rides the
  # pending step-6 `registry-host-replace` of the open zot-pin ordered path". It did not. That
  # replace had already fired atomically inside registry_luks_recut on 2026-08-10T22:08Z, ~45h
  # BEFORE this shipper merged (2026-08-12T19:38Z), so the shipper sat inert on a host born before
  # it existed and the rider had no vehicle left. A dedicated registry_host_replace job
  # (run 31639782781) delivered it; first warehouse readback 21:03:51Z flipped ADR-184 to accepted.
  # A rider is only valid while its vehicle is still pending.
  #
  # Liveness is owned by scripts/followthroughs/zot-log-channel-7440.sh on a DEDICATED tracker,
  # which now runs as a REGRESSION detector rather than a delivery wait.
  #
  # betterstack_ingest_url is terraform-interpolated (non-secret host routing, same class as the
  # reporter above); BETTERSTACK_LOGS_TOKEN is injected AT UNIT START by `doppler run` from the
  # isolated soleur-registry/prd config and is NEVER baked into user_data. Plain dollar-VARs stay
  # literal; shell brace-expansions are written $${VAR:-}.
  - path: /usr/local/bin/zot-log-shipper.sh
    content: |
      #!/usr/bin/env bash
      set -uo pipefail
      # Env overrides exist ONLY so apps/web-platform/infra/zot-log-shipper.test.sh can drive this
      # exact program against synthesized fixtures. Production takes every default.
      STATE_DIR="$${ZOT_LOG_SHIPPER_STATE_DIR:-/var/lib/zot-log-shipper}"
      # 5,000 rows/day budget against a shared source. Floor is ~1,440/day from the 60s liveness
      # GET alone (zot logs every request at info) plus ~288/day of heartbeat pings, so the cap
      # leaves real headroom without approaching the 25k/day threshold host_metrics occupies.
      # 5000 * 300 / 86400 = 17 per 5-minute window; both literals are explicit so the suite can
      # assert them (a derived-at-runtime value would not be assertable).
      #
      # THE TICK IS THE WINDOW. This runs as a 5-minute cron one-shot (ADR-184 §4), so INTERVAL_S
      # describes the cron cadence rather than an in-process rolling window. That is what dissolves
      # the "a line crossing the cap mid-interval is attributable to either interval" ambiguity a
      # long-lived process has to paper over: a tick's count starts at zero and ends at exit.
      INTERVAL_S="$${ZOT_LOG_SHIPPER_INTERVAL_S:-300}"
      CAP_PER_INTERVAL="$${ZOT_LOG_SHIPPER_CAP_PER_INTERVAL:-17}"
      # Cap-exempt classes get their OWN generous ceiling rather than no ceiling (#7444 F-5).
      # Exempt volume is proportional to zot traffic, not to shipper scheduling: PatchBlobUpload
      # fires per blob-upload chunk, and the measured crash-loop case (6,912 restarts/day x ~10
      # repos x ~2 gc lines) is ~138,000 rows/day = 28x the whole budget — in the exact scenario
      # the exemption was sized against. Source 2457081 is shared, so the observer can starve the
      # observed. Generous enough that ordinary evidence never trips it; bounded so a flood cannot
      # spend the estate's quota.
      CAP_EXEMPT_PER_INTERVAL="$${ZOT_LOG_SHIPPER_CAP_EXEMPT_PER_INTERVAL:-17}"
      MAXLEN=800
      mkdir -p "$STATE_DIR" || exit 1
      # FAIL LOUDLY ON A MISSING jq (#7444 F-11). jq is a hard per-line dependency: every row's
      # MESSAGE and __CURSOR come out of it. It is declared in `packages:`, whose own comment
      # records that the stage is NON-FATAL, and only e2fsprogs carries a runcmd `dpkg -s`
      # re-ensure. Measured with jq off PATH: 0 of 2 rows delivered, rc=0, no stderr, state all
      # zeros, no drop rows — 100% silent loss that the reporter then renders as
      # `post_fail=unknown`, which the probe's [0-9]+ cannot match, so it reports the INVERTED root
      # cause. An absent hard dependency must be an exit code, never a quiet zero.
      if ! command -v jq >/dev/null 2>&1; then
        echo "[zot-log-shipper] FATAL: jq not on PATH; every row would drop silently" >&2
        exit 1
      fi
      CURSOR_FILE="$STATE_DIR/cursor"
      CURSOR_TMP="$STATE_DIR/cursor.new"
      STATE_FILE="$STATE_DIR/state"
      STATE_TMP="$STATE_DIR/state.new"
      TOKEN="$${BETTERSTACK_LOGS_TOKEN:-}"
      HOST_NAME_V="$(hostname)"
      BOOT_ID="$(cat /proc/sys/kernel/random/boot_id 2>/dev/null || printf 'unknown')"
      POST_FAIL=0; LAST_OK_EPOCH=0; SHIPPED_CUM=0; DROPPED_CUM=0; DROP_SEQ=0
      if [ -r "$STATE_FILE" ]; then
        while IFS='=' read -r k v; do
          case "$k" in
            # DIGIT-GUARDED (#7444 R22). These feed $(( )) arithmetic; last_ok_epoch was guarded
            # and its neighbours were not. Measured: `dropped_cum=N/A` made the script die inside
            # emit_drops with `unbound variable` on EVERY tick forever, while rows kept shipping
            # and the cursor kept advancing — the channel looked healthy and drop accounting was
            # permanently dead. A value containing a space also corrupts the reporter's
            # key=value LINE for every field after it.
            post_fail)     case "$v" in ''|*[!0-9]*) : ;; *) POST_FAIL="$v" ;; esac ;;
            last_ok_epoch) LAST_OK_EPOCH="$v" ;;
            shipped_cum)   case "$v" in ''|*[!0-9]*) : ;; *) SHIPPED_CUM="$v" ;; esac ;;
            dropped_cum)   case "$v" in ''|*[!0-9]*) : ;; *) DROPPED_CUM="$v" ;; esac ;;
            drop_seq)      case "$v" in ''|*[!0-9]*) : ;; *) DROP_SEQ="$v" ;; esac ;;
          esac
        done < "$STATE_FILE"
      fi
      write_state() {
        # boot_id is NOT persisted: the shipper has no read-back case for it and the reporter has
        # no case either, so it was write-only. The boot_id that appears in drop rows is read live
        # from /proc at each tick, which is the one that carries meaning (#7444 R29).
        printf 'post_fail=%s\nlast_ok_epoch=%s\nshipped_cum=%s\ndropped_cum=%s\ndrop_seq=%s\n' \
          "$POST_FAIL" "$LAST_OK_EPOCH" "$SHIPPED_CUM" "$DROPPED_CUM" "$DROP_SEQ" \
          > "$STATE_TMP" 2>/dev/null && mv -f "$STATE_TMP" "$STATE_FILE" 2>/dev/null || true
      }
      # The cursor means DELIVERED, not READ — it is persisted only after a successful POST, with
      # an atomic rename so a torn write can never make the shipper replay or gap silently.
      #
      # journalctl's own --cursor-file is NOT used, and the reason is semantic rather than
      # incidental. Measured on systemd 259: the flag writes the cursor of the last entry READ
      # (man journalctl: "At the end, write the cursor of the last entry to FILE") — journalctl
      # has no channel by which to learn whether the consumer delivered anything, so reading 5
      # entries lands the file on the 5th regardless of what the consumer did with them, and a
      # consumer exiting non-zero does not hold it back. Adopting it would make a transient sink
      # outage a permanent, silent, unaccounted loss. Also measured: when the consumer breaks the
      # pipe early, journalctl takes SIGPIPE and the file is never written at all, which would
      # make the break-on-first-failure design below replay the entire tick.
      #
      # This is a per-entry field the shipper already parses, so self-persisting it costs ~6 lines
      # and is exact rather than tick-granular. It is the one part of the original streaming design
      # that is load-bearing independent of the scheduling shape.
      persist_cursor() {
        printf '%s' "$1" > "$CURSOR_TMP" 2>/dev/null && mv -f "$CURSOR_TMP" "$CURSOR_FILE" 2>/dev/null || true
      }
      post_body() {
        curl -fsS -m 10 -H "Authorization: Bearer $TOKEN" -H 'Content-Type: application/json' \
          "${betterstack_ingest_url}" --data-raw "$1" >/dev/null 2>&1
      }
      # POST ONCE. The disk reporter retries (`post || post || echo`) because ITS value is not
      # replayable — a missed heartbeat sample is gone. Every shipper row is replayable from the
      # cursor, so an immediate second attempt against a briefly-500ing ingest buys nothing the
      # five-minute gap to the next tick does not buy better, and back-to-back attempts are the
      # one retry shape guaranteed to fail both times against a transient fault.
      # Returns non-zero on failure so the caller can stop at the hole rather than ship past it.
      ship() {
        local line="$1" body
        body="{\"message\":\"$line\"}"
        if [ -z "$TOKEN" ]; then
          echo "[zot-log-shipper] WARN: BETTERSTACK_LOGS_TOKEN unset; not shipped: $line" >&2
          return 1
        fi
        post_body "$body" && return 0
        echo "[zot-log-shipper] egress to Better Stack Logs FAILED: $line" >&2
        return 1
      }
      # CREDENTIAL BACKSTOP — anchored on the RAW QUOTED shape measured above, and applied BEFORE
      # the sanitizer (which strips the very quotes this pattern needs). Both header renderings are
      # covered: the array form zot actually emits, and a bare-string form a future version might.
      # ANCHORED ON THE HEADER OBJECT WITH A NAME ALLOWLIST (#7444 F-10), which is what ADR-184 §3
      # and this template have always CLAIMED. The previous form keyed on the literal
      # `[Aa]uthorization`, so it was header-NAME-anchored: measured survivors shipped verbatim
      # and silently — `Cookie`, `X-Api-Key`, `X-Amz-Security-Token` — and `Proxy-Authorization`
      # shipped and THEN tripped the probe's own leak detector.
      #
      # Inverted to default-deny: every key inside "headers":{…} is redacted EXCEPT a short
      # allowlist of names that are routing metadata rather than credentials. A header zot adds in
      # a future version is therefore redacted by construction rather than by our having predicted
      # its name — the failure mode of a name-anchored rule is silent leakage, the failure mode of
      # a name-allowlist is an over-redacted diagnostic, and only one of those is recoverable.
      #
      # The value class `[^]]*\]` also had a second defect: a `]` INSIDE the value stopped the
      # match there, so the row shipped `Authorization:[REDACTED` with the credential tail three
      # characters right, and the probe's subtraction then exonerated the line. Matching the
      # bracketed array non-greedily to the LAST `]` before the next key closes that.
      # Structural, via jq — already a hard dependency (see the fail-loud guard above).
      #
      # WALK, don't peek at the top level (#7444 R8). The first version of this fix keyed on a
      # top-level, lowercase, OBJECT-valued `headers` and returned every other shape unmodified
      # with rc=0. Measured leaks: {"request":{"headers":{...}}} nested, {"Headers":{...}}
      # capitalised, an array-valued headers, a bare top-level {"Authorization":[...]}, and a
      # top-level array. Worse, it was a REGRESSION: the sed it replaced matched
      # "Authorization":[...] ANYWHERE in the line text, so those five shapes were redacted
      # before and not after. The rewrite widened NAME coverage and narrowed POSITIONAL
      # coverage, and only the first was measured.
      #
      # walk() reaches every object at any depth; the container key is compared case-folded, the
      # same way the inner keys already were. A headers key whose value is NOT an object is a
      # shape this redactor cannot reason about, so it becomes an error and the row is dropped.
      # Single-sourced: the sed rule and the residual check below must agree by construction.
      CRED_HDRS='authorization|cookie|x-api-key|proxy-authorization|x-amz-security-token'
      HDR_KEEP='["content-type","content-length","accept","accept-encoding","user-agent","host","connection","range","docker-distribution-api-version"]'
      # FAILS CLOSED, and the two branches are selected by whether the line IS JSON, decided up
      # front — NOT by whether the pipeline happened to exit zero. Conflating those is a silent
      # leak: an earlier draft indexed the allowlist wrongly, jq exited 5, and the fallback
      # shipped a Cookie verbatim while every Authorization assertion still passed.
      #
      # THE NON-JSON BRANCH IS REACHABLE AND IS NOT A LESSER PATH. An earlier comment here claimed
      # the split was "exhaustive" because only a parse failure takes the backstop — which inverts
      # its own premise, since a parse failure is exactly the case where a line HAS headers and
      # cannot be structurally redacted. zot runs --log-driver journald, so a line exceeding the
      # driver's buffer arrives as multiple invalid-JSON fragments, and header size is
      # client-controlled: a private-net client with a large Cookie chooses whether the line
      # splits. So the backstop redacts the bare/quoted/bracketed forms AND refuses any residual.
      redact() {
        local line="$1" out
        if printf '%s' "$line" | jq -e 'type == "object" or type == "array"' >/dev/null 2>&1; then
          out="$(printf '%s' "$line" | jq -c --argjson keep "$HDR_KEEP" '
      def r: if type=="array" then ["REDACTED"] else "REDACTED" end;
      def scrub: with_entries((.key|ascii_downcase) as $k | if ($keep|index($k)) then . else .value|=r end);
      walk(if type=="object" then with_entries((.key|ascii_downcase) as $k
      | if $k=="headers" then (if (.value|type)=="object" then .value|=scrub else error("nonobj headers") end)
      elif $k=="authorization" then .value|=r else . end) else . end)' 2>/dev/null)" || {
            echo "[zot-log-shipper] FATAL: redaction failed on a JSON row; dropped rather than shipped" >&2
            return 1
          }
          [ -n "$out" ] || {
            echo "[zot-log-shipper] FATAL: redaction produced empty output; row dropped" >&2
            return 1
          }
          printf '%s' "$out"
          return 0
        fi
        # Non-JSON. ONE rule over every credential-bearing header name, covering all the
        # renderings such a line can take — bracketed array, quoted string, and BARE
        # (`Authorization: Basic <b64>`, how a panic trace or a pre-logger startup error
        # actually renders). The optional quote between the name and the colon is load-bearing:
        # a TRUNCATED JSON fragment renders as `"Cookie":["…` , and a rule written for the bare
        # form alone does not match it — measured, that shipped a Cookie verbatim.
        #
        # The value class runs to the next `,` or `}` rather than to a closing quote or bracket,
        # because a truncated fragment has neither. That over-redacts the tail of a bare
        # plaintext line; on a credential-bearing line that is the safe direction.
        out="$(printf '%s' "$line" | sed -E \
          -e "s/(\"?)($CRED_HDRS)(\"?[[:space:]]*:[[:space:]]*)[^,}]*/\1\2\3REDACTED/gI")"
        # REFUSE ANY RESIDUAL. If a credential-bearing header name still carries anything other
        # than REDACTED, this redactor did not understand the line and must not ship it.
        # `[^R[:space:]]` excludes the space, not just R: with `[^R]` alone the regex backtracks
        # — `[[:space:]]*` matches zero characters and `[^R]` then matches the space itself, so a
        # correctly-redacted `Authorization: REDACTED` was read as a residual and the row dropped.
        # Measured: it discarded a panic trace whose credential had in fact been removed.
        if printf '%s' "$out" | grep -qiE "($CRED_HDRS)\"?[[:space:]]*:[[:space:]]*[^R[:space:]]" ; then
          echo "[zot-log-shipper] FATAL: unredactable credential header (non-JSON); row dropped" >&2
          return 1
        fi
        printf '%s' "$out"
      }
      # PAYLOAD INTEGRITY, not redaction. Collapse the whitespace that would break a JSON string,
      # drop anything outside printable ASCII (a mid-sequence-sliced UTF-8 byte is invalid
      # unescaped per RFC 8259 §7, and a tab-indented Go crash trace would make Better Stack
      # reject the WHOLE payload in exactly the crash case worth carrying), then drop " and \ so
      # the value cannot corrupt the single-key body. Bounded with a substring expansion rather
      # than `head -c`: under pipefail, head closing the pipe early makes the producer take
      # SIGPIPE and the whole pipeline report 141.
      sanitize() {
        local s
        s="$(printf '%s' "$1" | tr '\n\r\t' ' ' | LC_ALL=C tr -cd '\40-\176' | tr -d '"\\')"
        printf '%s' "$${s:0:MAXLEN}"
      }
      # The four measured evidence classes are CAP-EXEMPT. Without this the cap drops the
      # diagnostic evidence preferentially during exactly the flood (crash-loop / pull storm) that
      # accompanies disk growth. `executing gc` is the DENOMINATOR the downstream gc-completion
      # question needs: a stalled gc emits a start with no completion, so admitting only
      # completion lines would make "gc never ran", "gc ran fine" and "the shipper filtered it"
      # indistinguishable — the lower-bound defect reproduced one layer up.
      #
      # MATCHED ON THE PARSED `message` FIELD, never the whole line (#7444 F-5). The parse now
      # happens once per tick inside JQ_TICK (the $z field), not in a per-line helper — the
      # standalone zerolog_message() the single-jq rewrite orphaned has been deleted. Measured, zot
      # emits {"level":"info","message":"executing gc","component":"gc","caller":"…"} and
      # {"level":"error","message":"PatchBlobUpload i/o timeout","caller":"…"}, so all four classes
      # are message-field prefixes. A whole-line substring test instead lets any private-net client
      # send `User-Agent: executing gc` and make EVERY one of its request lines cap-exempt — those
      # lines carry message "HTTP API", so field-anchoring closes the bypass by construction.
      is_cap_exempt() {
        case "$1" in
          "executing gc"*)              return 0 ;;
          "gc successfully completed"*) return 0 ;;
          "garbage collected blobs"*)   return 0 ;;
          PatchBlobUpload*)             return 0 ;;
          # CRASH evidence, not just GROWTH evidence (#7444 R12). The four classes above are all
          # gc/blob-upload — so under a crash loop, which is BOTH the flood the exemption was
          # sized against AND the incident class that motivated this whole change, the rows the
          # cap dropped were the panic traces and the rows it kept were the gc lines. A Go panic
          # is 30-60 journald entries against a 17/tick ordinary cap. The sibling disk reporter
          # already encodes this severity ordering ~500 lines above.
          panic:*)                      return 0 ;;
          "fatal error"*)               return 0 ;;
          "runtime error"*)             return 0 ;;
          \[signal*)                     return 0 ;;
        esac
        return 1
      }
      # The HTTP-API HALF of an upload-failure PAIRING is cap-exempt too (#7555). zot reports a
      # failed blob upload as TWO rows: the `PatchBlobUpload` error line (already exempt above)
      # and an ordinary HTTP-API row carrying `statusCode`, `latency` and `Content-Length`. Only
      # the pairing carries the numbers that distinguish a DEADLINE from an origin fault, and
      # only the first half was surviving the cap. Measured in the 2026-08-13 failure tick:
      # `SOLEUR_ZOT_LOG_DROPPED n=124 cum=5743 reason=rate_cap` against a baseline of n=8, and
      # cumulatively `dropped_cum=5841` vs `shipped_cum=5151` — so the deciding evidence survived
      # by luck of its class, not by design.
      #
      # MATCHED ON THE `/blobs/uploads/` PAIRING, never on 5xx alone. A broad 5xx arm would admit
      # ordinary health-probe and pull-storm failures into the SHARED 17-slot exempt lane and
      # starve the panic traces out of it — reinstating the #7444 R12 priority inversion one
      # layer down, in the fix for it.
      is_upload_failure_evidence() {
        # $1 = statusCode, $2 = path. Any 5xx: 502/503/504 are as much a cut upload as 500, and
        # pinning 500 alone would make the guard blind to exactly the proxy-side shapes.
        case "$1" in
          5??) ;;
          *) return 1 ;;
        esac
        # ANCHORED on the registry API shape, not an unanchored substring (#7555 review): $2 is
        # verbatim client input, so `*/blobs/uploads/*` alone let any 5xx on a crafted path mint
        # an exempt row.
        case "$2" in
          /v2/*/blobs/uploads/*) return 0 ;;
        esac
        return 1
      }
      # THE TICK IS THE WINDOW (ADR-184 §4). A cron one-shot starts each counter at zero and ends
      # at exit, so there is no rolling-window boundary to arbitrate and no `maybe_roll_window`.
      # UPLOAD EVIDENCE GETS A SUB-QUOTA INSIDE THE SHARED EXEMPT LANE (#7555 review).
      # Admission to the 17-slot exempt lane was first-come-first-served, so one failing
      # multi-layer push (both halves of the pairing, x3 retries x3 tags) filled it and a
      # concurrent panic trace was dropped -- REPRODUCED, and it is the #7444 R12 priority
      # inversion one layer down, inside the change that claims to bound against it. Capping the
      # upload class below the lane's size reserves the remainder for is_cap_exempt (gc + crash).
      CAP_UPLOAD_EVIDENCE_PER_INTERVAL="$${ZOT_LOG_SHIPPER_CAP_UPLOAD_EVIDENCE_PER_INTERVAL:-8}"
      WIN_UPLOAD_EVIDENCE=0
      WIN_COUNT=0
      WIN_EXEMPT=0
      WIN_DROPPED=0
      WIN_DROPPED_EXEMPT=0
      WIN_DROPPED_REDACT=0
      WIN_DROPPED_EMPTY=0
      UNDELIVERED=0
      # `n` is scoped to THE TICK THIS ROW CLOSES. boot_id + drop_seq remain because two
      # boundaries still make n a lower bound with no marker saying so: a host replace resets these
      # counters on the root fs, and a replay after an undelivered row re-counts re-shipped lines.
      # They let a reader DETECT the discontinuity, which is exactly the marker silent truncation
      # lacks. (The third boundary — a line crossing the cap mid-interval — is dissolved by the
      # tick being the window.)
      # Takes the count explicitly so each cap has its OWN reason: a row dropped because the
      # exempt ceiling was reached is a different operational fact from one dropped by the
      # ordinary rate cap, and collapsing both into `rate_cap` would hide an evidence-class flood
      # behind the ordinary-traffic reason.
      emit_drops() {
        local reason="$1" n="$2"
        # `n` may be the literal `unknown` on the cursor_invalidated path, where the lost span is
        # genuinely unbounded and reporting a number would be a lie. Only a numeric count moves
        # the cumulative total; `unknown` still emits a row and still advances drop_seq, so the
        # discontinuity is detectable even though its magnitude is not.
        case "$n" in
          unknown) : ;;
          ''|*[!0-9]*) return 0 ;;
          0) [ "$reason" = "cursor_invalidated" ] || return 0 ;;
        esac
        DROP_SEQ=$(( DROP_SEQ + 1 ))
        [ "$n" = "unknown" ] || DROPPED_CUM=$(( DROPPED_CUM + n ))
        ship "SOLEUR_ZOT_LOG_DROPPED n=$n interval_s=$INTERVAL_S boot_id=$BOOT_ID seq=$DROP_SEQ cum=$DROPPED_CUM reason=$reason" || POST_FAIL=$(( POST_FAIL + 1 ))
        write_state
      }
      CURSOR=""
      CURSOR_WAS_INVALIDATED=0
      [ -r "$CURSOR_FILE" ] && CURSOR="$(cat "$CURSOR_FILE" 2>/dev/null || printf '')"
      # PROBE the cursor before reading from it. A journal rotated past the persisted cursor during
      # a sink outage makes --after-cursor fail; without this the shipper would either die in a
      # restart loop or silently gap. Instead it says so and restarts from a BOUNDED recent window.
      if [ -n "$CURSOR" ]; then
        if ! journalctl CONTAINER_NAME=zot --output=json --after-cursor "$CURSOR" -n 0 >/dev/null 2>&1; then
          # n=unknown, not n=0 (#7444 R21). WIN_DROPPED is initialised to 0 and this runs BEFORE
          # the read loop, so the count was unconditionally zero — on the ONE reason where the
          # loss is genuinely unbounded (everything between the dead cursor and --since=-5m).
          # A reader saw "0 rows lost" across a total-loss event, and dropped_cum never moved.
          # Only drop_seq changed, and nothing reads that alone. `unknown` is its own state, the
          # same discrimination F-7 established for post_fail.
          emit_drops cursor_invalidated unknown
          CURSOR=""
          CURSOR_WAS_INVALIDATED=1
          rm -f "$CURSOR_FILE" 2>/dev/null || true
        fi
      fi
      JARGS=( CONTAINER_NAME=zot --output=json )
      # On INVALIDATION the recovery must be bounded (#7444 F-9). journalctl's default with no
      # --lines/--follow is ALL retained entries, and this same change raises SystemMaxUse to 512M
      # — so an unbounded invalidation replay is precisely the runaway-volume mode the design cites
      # as its reason for rejecting --cursor-file, re-entered by a different door. --since is the
      # explicit bound; dropping --no-tail is NOT one (measured: absent --follow/-n it is a strict
      # no-op, 702 vs 702 entries, and only undoes an explicit --lines=).
      #
      # This branches on INVALIDATION, not on cursor-emptiness: a genuine COLD start (a fresh host
      # that has never persisted a cursor) must still read from the beginning, because that host's
      # journal is small and the boot backlog is the thing the persistence exists to preserve.
      if [ "$CURSOR_WAS_INVALIDATED" = "1" ]; then
        JARGS+=( --since=-5m )
      fi
      [ -z "$CURSOR" ] || JARGS+=( --after-cursor "$CURSOR" )
      # ONE jq FOR THE WHOLE TICK (#7444 R2). Previously this forked three jq processes PER
      # JOURNAL ENTRY — .MESSAGE, .__CURSOR, and the zerolog .message for the exemption test — and
      # all three ran BEFORE the cap was consulted, so the caps bounded EGRESS but not WORK.
      # Measured: a full 512M journal is ~806,000 entries, i.e. ~2.4 MILLION execs in one tick,
      # 2.7-5.4 hours of CPU on this 2-shared-vCPU host, to deliver at most CAP rows. A single
      # streaming jq emitting all three fields as TSV is one process for the entire tick.
      #
      # --unbuffered is LOAD-BEARING, not hygiene: jq block-buffers when stdout is a pipe, so
      # without it the shipper reads nothing until journalctl closes. A tick killed mid-drain
      # (OOM, reboot) would then have persisted no cursor for rows it had already read, and the
      # whole drain replays. Measured by T10b, which went RED the moment the single-jq pipeline
      # landed without it.
      # THE PARENTHESES AROUND fromjson? ARE LOAD-BEARING. `$m | fromjson? | .message? // ""`
      # binds nothing when $m is not JSON — `fromjson?` emits NO value on a parse error, so the
      # `as $z` never binds and jq drops the WHOLE record. Every plaintext line (Go panic traces,
      # docker daemon lines, pre-logger startup errors — the highest-value evidence) vanished at
      # the jq stage with no drop row and no stderr. `(...) // ""` applies the alternative to the
      # empty stream instead. Caught by T6b's non-vacuity fixture, not by any leak fixture.
      # @tsv escapes embedded tabs and newlines, so a field cannot split a record. The fields are
      # emitted in a fixed order and `read -r` with IFS=tab takes them positionally.
      #
      # EVERY FIELD IS SENTINELLED WITH `-`, and that is load-bearing rather than tidy (#7555).
      # `IFS=$'\t'` makes tab an IFS-*WHITESPACE* character, so `read` COLLAPSES runs of tabs and
      # strips leading ones: one empty field does not read back as empty, it SHIFTS every later
      # field one position LEFT. Measured on this exact record — with an empty `message`,
      # zstatus lands in zmsg and zpath lands in zstatus, so is_cap_exempt classifies a status
      # code and the upload predicate reads a path as a status. Prefix matching still returns 0
      # and the suite stays GREEN over a corrupted record, which is the fail-open this
      # sentinelling removes by construction. `-` matches no is_cap_exempt arm and is neither a
      # legal statusCode nor a legal path, so "absent" stays representable and unambiguous.
      #
      # `(($m | fromjson?) // {})` and the per-field `// null` are the same empty-stream trap the
      # comment above describes: `fromjson?` on a plaintext line yields EMPTY, not null, and an
      # empty stream inside an array literal makes the element VANISH — a 5-field contract would
      # silently emit 4. `// null` converts empty to null so `s` can sentinel it.
      #
      # statusCode and path are appended AFTER zmsg so existing positional reads keep their
      # meaning; the `read -r` arity below is widened IN THE SAME EDIT, because a field list and
      # an arity that disagree is exactly the corruption described above.
      JQ_TICK='def s: if (. == null or . == "") then "-" else (. | tostring) end; (.MESSAGE // "") as $m | (($m | fromjson?) // {}) as $j | [((.__CURSOR // null) | s), (($m // null) | s), ((($j.message?) // null) | s), ((($j.statusCode?) // null) | s), ((($j.path?) // null) | s)] | @tsv'
      while IFS=$'\t' read -r cur msg zmsg zstatus zpath; do
        # Translate sentinels back to the empty string the downstream code expects. Done here,
        # once, so no consumer has to know about the wire representation.
        [ "$cur" = "-" ] && cur=""
        [ "$zmsg" = "-" ] && zmsg=""
        [ "$msg" = "-" ] && msg=""
        [ -n "$msg" ] || continue
        # THE CURSOR ADVANCES ON EVERY *DECIDED* ROW, not only on delivered ones (#7444 R1).
        #
        # Previously a cap-dropped row hit `continue` before persist_cursor, so it was re-read on
        # every subsequent tick. Three consequences, all measured: (a) draining N entries cost
        # ~N^2/CAP scans, so a tick stopped finishing inside its 5-minute slot after ~5 hours of a
        # crash-loop flood and `flock -n` then silently skipped every successor; (b) dropped_cum
        # counted the same physical row once per tick until it eventually shipped, so 40 offered
        # rows that were ALL delivered reported dropped_cum=140; (c) worst, an EXEMPT row arriving
        # after capped rows bypassed the cap test and advanced the cursor past them, so the same
        # 40-row input lost 0 rows or 35 rows depending only on whether one gc line trailed it —
        # with a byte-identical `n=35 reason=rate_cap` record in both cases.
        #
        # A capped row is DECIDED and ACCOUNTED (emit_drops carries it), not undelivered. Only an
        # undelivered row may hold the cursor, which is exactly what F-3 established.
        decided=0
        # PLAINTEXT CRASH EVIDENCE (#7555, pre-existing since #7444 R12). `zmsg` is the PARSED
        # zerolog `message` field, so it is EMPTY for any line that is not zot JSON — and a Go
        # panic is written to stderr as PLAINTEXT. So the four crash arms of is_cap_exempt
        # (panic:, fatal error, runtime error, [signal) could never match the input they were
        # added for: measured on origin/main, a `panic: runtime error: …` journald line yields
        # zmsg="" and classifies as ORDINARY, i.e. the crash class was droppable at the cap
        # during exactly the crash flood R12 exists to survive. The suite missed it because its
        # panic fixtures wrap the panic text INSIDE a JSON `message`.
        #
        # The fallback fires ONLY when zmsg is empty — that is, only for a line that is not zot
        # JSON — so the #7444 F-5 whole-line bypass stays closed by construction: an attacker's
        # `User-Agent: executing gc` rides on an HTTP-API row, which always parses and therefore
        # always has a non-empty zmsg and never reaches this arm. T19 pins both directions.
        _zclass="$zmsg"
        if [ -z "$_zclass" ]; then
          # ONLY THE CRASH CLASSES FALL BACK TO THE RAW LINE (#7555 review).
          #
          # The earlier form fell back to "$msg" for ALL eight arms, and its comment argued the
          # #7444 F-5 bypass stayed closed because "an HTTP-API row always parses". THAT PREMISE
          # IS FALSIFIED ~260 LINES ABOVE, in this same file: zot runs --log-driver journald, so a
          # line exceeding the driver's buffer arrives as MULTIPLE INVALID-JSON FRAGMENTS, and
          # header size is client-controlled. A fragment does not parse, so zmsg is empty, and the
          # fallback then classified the fragment's raw bytes -- which are the client's own header
          # payload. Reproduced: 17 forged fragments beginning "executing gc" consumed the entire
          # exempt lane and starved a genuine panic trace to zero.
          #
          # The four gc/upload classes ALWAYS arrive as parsed zot JSON, so they have no plaintext
          # justification and are exactly what a client can forge cheaply. A Go panic is written to
          # stderr as plaintext and has no other route, which is the whole reason this fallback
          # exists, so the crash classes keep it.
          case "$msg" in
            panic:*|"fatal error"*|"runtime error"*|\[signal*) _zclass="$msg" ;;
          esac
        fi
        _upload_only=0
        if is_cap_exempt "$_zclass"; then
          _exempt=1
        elif is_upload_failure_evidence "$zstatus" "$zpath"; then
          _exempt=1; _upload_only=1
        else
          _exempt=0
        fi
        if [ "$_exempt" = "1" ]; then
          if [ "$_upload_only" = "1" ] && [ "$WIN_UPLOAD_EVIDENCE" -ge "$CAP_UPLOAD_EVIDENCE_PER_INTERVAL" ]; then
            # Sub-quota spent: the remaining exempt slots are reserved for gc + crash rows.
            WIN_DROPPED_EXEMPT=$(( WIN_DROPPED_EXEMPT + 1 )); decided=1
          elif [ "$WIN_EXEMPT" -ge "$CAP_EXEMPT_PER_INTERVAL" ]; then
            WIN_DROPPED_EXEMPT=$(( WIN_DROPPED_EXEMPT + 1 )); decided=1
          else
            WIN_EXEMPT=$(( WIN_EXEMPT + 1 ))
            [ "$_upload_only" = "1" ] && WIN_UPLOAD_EVIDENCE=$(( WIN_UPLOAD_EVIDENCE + 1 ))
          fi
        elif [ "$WIN_COUNT" -ge "$CAP_PER_INTERVAL" ]; then
          WIN_DROPPED=$(( WIN_DROPPED + 1 )); decided=1
        else
          WIN_COUNT=$(( WIN_COUNT + 1 ))
        fi
        if [ "$decided" = "1" ]; then
          [ -n "$cur" ] && persist_cursor "$cur"
          continue
        fi
        # A redaction failure DROPS the row (redact() fails closed) under its OWN reason — folding
        # it into rate_cap sent an operator to raise a cap that was never the cause (#7444 R4).
        if ! redacted="$(redact "$msg")"; then
          WIN_DROPPED_REDACT=$(( WIN_DROPPED_REDACT + 1 ))
          [ -n "$cur" ] && persist_cursor "$cur"
          continue
        fi
        clean="$(sanitize "$redacted")"
        # A row that sanitizes to empty was silently discarded with no accounting while the cursor
        # advanced past it — the literal F-3 signature (offered N, shipped N-1, dropped 0) on a
        # path the F-3 fix never touched. Reproduced with a non-ASCII line, which tr -cd empties.
        if [ -z "$clean" ]; then
          WIN_DROPPED_EMPTY=$(( WIN_DROPPED_EMPTY + 1 ))
          [ -n "$cur" ] && persist_cursor "$cur"
          continue
        fi
        # THE ENVELOPE. Source 2457081 is shared by EVERY host and host_name is a VECTOR-populated
        # field — this channel has no Vector, so a direct POST carries no host_name and no
        # SYSLOG_IDENTIFIER at all. The in-message host= token is the ONLY isolation available, and
        # the probe asserts on it POSITIVELY. A negative discriminator ("raw does not begin with
        # the heartbeat prefix") would be fail-open: under any raw-encoding drift every echo row
        # reclassifies as genuine and the probe auto-PASSes on the exact state it exists to reject.
        if ship "SOLEUR_ZOT_LOG shipper=zot-log-shipper host=$HOST_NAME_V $clean"; then
          SHIPPED_CUM=$(( SHIPPED_CUM + 1 ))
          LAST_OK_EPOCH="$(date +%s)"
          [ -n "$cur" ] && persist_cursor "$cur"
        else
          # STOP AT THE HOLE (#7444 F-3). Previously this counted the failure and carried on, so
          # the NEXT successful row advanced the cursor past the undelivered one — a transient
          # Better Stack outage became permanent, silent, unaccounted loss.
          #
          # The recovery is NOT the exit code: it is that the cursor was never advanced past this
          # row, so the next tick resumes exactly here. Emitting a `reason=post_fail` drop row
          # instead was rejected — that row would ship over the very sink that just failed, so its
          # only signal is structurally undeliverable in its own causal story (F-8).
          POST_FAIL=$(( POST_FAIL + 1 ))
          UNDELIVERED=1
          write_state
          break
        fi
        write_state
      done < <(journalctl "$${JARGS[@]}" 2>/dev/null | jq -r --unbuffered "$JQ_TICK" 2>/dev/null)
      # One reason per cause. A single bucket sent an operator to raise a cap that was never the
      # cause: a jq regression failing redact() on every structured row presented as
      # `reason=rate_cap` with zero envelope rows, whose documented remediation is a no-op.
      for _rc in "rate_cap $WIN_DROPPED" "exempt_cap $WIN_DROPPED_EXEMPT" \
                 "redact_failed $WIN_DROPPED_REDACT" "sanitized_empty $WIN_DROPPED_EMPTY"; do
        emit_drops $_rc
      done
      WIN_DROPPED=0; WIN_DROPPED_EXEMPT=0; WIN_DROPPED_REDACT=0; WIN_DROPPED_EMPTY=0
      write_state
      # A tick that left a row undelivered exits non-zero. That exit is visible because the cron
      # line pipes this program's output through `logger -t zot-log-shipper` — NOT because cron
      # reports it. The claim that used to sit here ("visible from the journal and from cron's
      # own mail/status") was false in both halves and three review lenses said so
      # independently: vixie cron logs job START, not exit status, and `packages:` installs no
      # MTA, so cron's mail path discards job output entirely. The durable signal remains
      # post_fail in the state file, which the 5-minute reporter carries on its own egress path.
      [ "$UNDELIVERED" = "0" ] || exit 1
      exit 0
    owner: root:root
    permissions: '0755'
  # A 5-MINUTE CRON ONE-SHOT, NOT A DAEMON (ADR-184 §4). Every unit on this host is Type=oneshot
  # and every recurring job is a cron.d line wrapped in `doppler run`; this is the third such line
  # rather than the host's first Restart=always unit.
  #
  # The governing constraint is REVERSIBILITY, not efficiency. hcloud_server.registry is
  # cloud-init-only (ADR-096) with no in-place execution path, so the repair cost of any defect
  # shipped here is an operator-authorized destructive replace of the fleet's sole image-pull path.
  # A daemon's failure modes are all permanent under that constraint — a bad ExecStart, a missing
  # dependency, a fast-failing start. A one-shot's are one failed tick, five minutes from the next.
  # That is not hypothetical: an ExecStart naming /usr/bin/doppler (a path this template never
  # creates) made the daemon a permanent 5s restart loop shipping nothing, and the local
  # systemd-analyze verify gate passed it because it resolved against the runner's filesystem.
  #
  # The latency a daemon buys has no consumer here: the disk reporter runs at 5 min, the liveness
  # feeder at 60s against a 90s deadline, and the follow-through probe reads warehouse windows of
  # 12+ min.
  #
  # THE MINUTE OFFSET IS LOAD-BEARING. zot-disk-heartbeat holds */5 (0,5,10…) and
  # soleur-private-nic-guard holds 2-59/5 (2,7,12…); that guard's own comment records why the
  # offset exists — the host budgets ~1024 MB for "cron+doppler+sshd+OS" and must not have these
  # concurrently resident. A third `doppler run` at */5 would reintroduce the daemon's RAM risk
  # through the schedule on a host with an OOM restart-loop history (#6288). 4-59/5 takes the
  # furthest free slot.
  #
  # flock -n carries over from the daemon's ExecStart, where systemd's single-instance guarantee
  # already made it decorative. Here it is the tick-overrun guard, matching the NIC guard's shape.
  - path: /etc/cron.d/zot-log-shipper
    content: |
      # Ship zot container logs from journald to Better Stack Logs every 5 min (#7440).
      # Wrapped in `doppler run` so BETTERSTACK_LOGS_TOKEN (isolated soleur-registry/prd config)
      # is injected for the POST — never baked into user_data. --only-secrets narrows the injected
      # environment to the one secret this job needs, so REGISTRY_LUKS_KEY and the zot tokens are
      # never resident in it (#7444 F-14). --no-fallback declines the on-disk encrypted fallback
      # cache, which would otherwise persist the resolved secret set under $HOME/.doppler (F-13).
      # PATH IS LOAD-BEARING, and this block omitted it while the NIC guard's block below states
      # exactly why and adds that "the sibling disk-heartbeat cron is not cover for this".
      # doppler lives ONLY at /usr/local/bin/doppler (F-1 established this template writes no
      # symlink) and /etc/default/registry-doppler sets no PATH, so under the model this repo
      # itself commits, a bare `doppler` does not resolve, every tick exits 127, and the channel
      # is silently dead. The disk-heartbeat working in production implies Debian cron uses
      # _PATH_DEFPATH_ROOT for root jobs — but two committed models of cron's PATH contradict
      # each other here and nothing asserted either, so this pins it rather than inheriting it.
      PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
      # `timeout 240` bounds the tick BELOW the 5-minute cadence. Without it a tick had no bound
      # of any kind: the daemon's RuntimeMaxSec/MemoryMax/CPUQuota went with the unit, and a
      # cron.d job inherits no cgroup. A killed tick loses nothing — the cursor stops at the last
      # delivered row — so the bound is free.
      #
      # 3-59/5, not 4-59/5. The disk heartbeat holds */5 and the NIC guard 2-59/5, so the free
      # slots are 1, 3 and 4. Minute 4 has ONE minute of forward clearance to the heartbeat;
      # minute 3 has two. The earlier "furthest free slot" claim maximised distance from the
      # PREVIOUS neighbour and left the least room ahead — for the one job of the three whose
      # runtime is not fixed. `logger -t` is what makes the shipper's stderr observable at all:
      # cron has no MTA here, so without it every fail-loud breadcrumb goes to /dev/null.
      3-59/5 * * * * root set -a; . /etc/default/registry-doppler; set +a; flock -n /run/lock/zot-log-shipper.lock timeout 240 doppler run --project soleur-registry --config prd --only-secrets BETTERSTACK_LOGS_TOKEN --no-fallback -- /usr/local/bin/zot-log-shipper.sh 2>&1 | logger -t zot-log-shipper
    owner: root:root
    permissions: '0644'

  # JOURNALD SIZING — set UNCONDITIONALLY so the literals are assertable by test. A conditional
  # bound is not.
  #
  # This template previously set no Storage=, no SystemMaxUse= and no /var/log/journal, so the
  # behaviour was the base-image default. Ubuntu ships Storage=auto with no /var/log/journal
  # present, which means VOLATILE: the journal lives in /run/log/journal, a tmpfs bounded by
  # RuntimeMaxUse (~10% of /run). On a 3,814 MB host where zot is capped at 3,072 MB and the
  # documented reserve is 1,024 MB, that is RAM pressure on the host whose OOM history is the
  # reason the cap exists. Storage=persistent moves the buffer off tmpfs; both media are bounded.
  #
  # SystemMaxUse is sized against the measured 4.8-restarts/min flood rate rather than against
  # root exhaustion. A percentage default gives a very SHORT retention window under flood, which
  # is what the shipper's catch-up reach then converts into a gap. At ~20 startup lines per
  # restart and ~300 bytes per line, a sustained flood writes ~1.7 MB/hour, so 512M holds roughly
  # 12 days of it — against a 59 GB root filesystem currently 8% used, i.e. under 1% of the disk.
  - path: /etc/systemd/journald.conf.d/10-zot-log-shipper.conf
    content: |
      [Journal]
      Storage=persistent
      SystemMaxUse=512M
    owner: root:root
    permissions: '0644'

  # --- #6895 (D2): idempotent boot-time LUKS open of the registry store mapper ------------
  # runcmd is PER-INSTANCE and does NOT re-run on reboot — and THIS host self-reboots as a
  # convergence primitive (soleur-private-nic-guard.sh calls `reboot`, see the NIC-guard block
  # below). Without a boot-time reopen the /dev/mapper/registry mapper stays CLOSED after a
  # reboot, the nofail fstab line cannot mount, the NIC guard's `mount -a` self-heal fails, and
  # zot binds an empty root-disk dir and 404s every image while nic_ok=true. This oneshot reopens
  # the mapper (if closed) after network-online (Doppler egress) and BEFORE docker + cron, so the
  # fstab mount / the NIC-guard `mount -a` self-heal remount /dev/mapper/registry. Reject
  # /etc/crypttab-with-keyfile: it would write the passphrase in cleartext on the host disk,
  # defeating the Doppler-only custody property. Reads REGISTRY_LUKS_KEY via the persisted
  # /etc/default/registry-doppler (survives reboot on the root disk) under the scoped
  # soleur-registry/prd token. `${registry_volume_id}` is terraform-interpolated; $REGISTRY_LUKS_KEY
  # / $DEV (no braces) stay literal for the doppler shell.
  - path: /usr/local/bin/registry-luks-open.sh
    content: |
      #!/usr/bin/env bash
      set -uo pipefail
      DEV=/dev/disk/by-id/scsi-0HC_Volume_${registry_volume_id}
      # Already open? just ensure the mount is present (idempotent) and exit.
      if [ -e /dev/mapper/registry ]; then
        mountpoint -q /var/lib/zot || mount /dev/mapper/registry /var/lib/zot || true
        exit 0
      fi
      # Device-wait: on a reboot the volume node can reattach AFTER this oneshot fires — mirror the
      # provision block's own wait (~30 x 2s) so a slow reattach is not misread as a genuinely-absent
      # device. If the device is still absent after the wait, no-op (exit 0) like the provision block's
      # device-absent path: fresh-provision formatting is the mount block's job, not this reopen path.
      for i in $(seq 1 30); do [ -b "$DEV" ] && break; sleep 2; done
      [ -b "$DEV" ] || { echo "[zot] registry-luks-open: $DEV absent after 60s wait — cannot open mapper" >&2; exit 0; }
      # Only reopen a device that is actually LUKS. First-provision (blkid TYPE empty) is the LUKS
      # mount block's job (it luksFormats), not this reopen path — refuse anything not crypto_LUKS.
      BLKTYPE=$(blkid -o value -s TYPE "$DEV" 2>/dev/null || true)
      if [ "$BLKTYPE" != crypto_LUKS ]; then
        echo "[zot] registry-luks-open: $DEV TYPE=$BLKTYPE not crypto_LUKS — first-provision handles luksFormat; not reopening" >&2
        exit 0
      fi
      set -a
      . /etc/default/registry-doppler
      set +a
      doppler run --project soleur-registry --config prd -- bash -s <<'REGOPENEOF'
      set -uo pipefail
      DEV=/dev/disk/by-id/scsi-0HC_Volume_${registry_volume_id}
      [ -n "$REGISTRY_LUKS_KEY" ] || { echo "[zot] registry-luks-open FATAL: REGISTRY_LUKS_KEY empty — cannot open mapper" >&2; exit 1; }
      [ -e /dev/mapper/registry ] || printf '%s' "$REGISTRY_LUKS_KEY" | cryptsetup luksOpen --key-file - "$DEV" registry
      mountpoint -q /var/lib/zot || mount /dev/mapper/registry /var/lib/zot || true
      REGOPENEOF
    owner: root:root
    permissions: '0755'

  # The oneshot that runs the reopen on EVERY boot (unlike per-instance runcmd). Ordered after
  # network-online (Doppler egress) and BEFORE docker.service + cron.service, so the mapper is
  # open before zot launches and before the NIC-guard self-heal's `mount -a` fires (D2).
  - path: /etc/systemd/system/registry-luks-open.service
    content: |
      [Unit]
      Description=Reopen the registry LUKS store mapper on boot (#6895, D2) — before docker + the NIC-guard self-heal
      After=network-online.target
      Wants=network-online.target
      Before=docker.service cron.service

      [Service]
      Type=oneshot
      RemainAfterExit=yes
      ExecStart=/usr/local/bin/registry-luks-open.sh

      [Install]
      WantedBy=multi-user.target
    owner: root:root
    permissions: '0644'

  # --- #6415 / ADR-115: private-NIC boot converger ----------------------------------------
  # Root cause of the 2026-07-14 outage (#6400). hcloud_server_network is an ADDITIVE ONLINE
  # ATTACH (network.tf:9-13) — it needs a created server, and a created server is already
  # booting, so NO terraform ordering can guarantee the attach lands before cloud-init's
  # network stage. When it lands late (or the IMDS fetch blips), cloud-init renders a netplan
  # with ONLY the public eth0 and the host holds no 10.0.1.30.
  #
  # Why that went unseen for ~14 days: a NIC-less host keeps PUBLIC egress, so the disk
  # heartbeat kept pinging green; and the boot readiness poll targets `localhost:5000` (the
  # `curl … http://localhost:5000/v2/` loop in the zot-launch runcmd below), which succeeds
  # because zot binds `0.0.0.0:5000` (its `-p 0.0.0.0:5000:5000` publish). Every existing signal
  # is structurally blind to this — hence a NEW signal, not a re-thresholded old one.
  # (Anchors are named rather than line-cited on purpose: this block's own insertion shifted
  # every later line, and the original numeric citations rotted immediately.)
  #
  # This is the ON-HOST layer deliberately: the registry resources are an
  # OPERATOR_APPLIED_EXCLUSION (zot-registry.tf, the "APPLY-PATH (CTO ruling 2026-07-06,
  # apply-path-cto-ruling.md)" header block), applied by the operator's full
  # untargeted apply, so a reboot bolted into the dispatch job would cover only the maintenance
  # path and leave the PRIMARY provisioning path uncovered.
  - path: /usr/local/bin/soleur-private-nic-guard.sh
    content: |
      #!/usr/bin/env bash
      set -u
      # Assert the expected private IP is configured; converge it with ONE bounded primitive
      # (a guarded reboot — it re-runs cloud-init's OWN renderer, so MTU and routes are correct
      # BY CONSTRUCTION, and it is the recovery verified in production per #6122); and emit a
      # discriminating SOLEUR_PRIVATE_NIC event on EVERY run.
      #
      # NOT `cloud-init clean && cloud-init init`: it wipes the datasource semaphore, so a
      # transient IMDS failure on the re-run yields DataSourceNone => default network config =>
      # the host loses its PUBLIC NIC too => unrecoverable on a deny-all no-SSH box. The
      # proposed cure for a transient IMDS blip is triggered by a transient IMDS blip.
      #
      # SAFETY: the healthy path is a PURE READ. The reboot is gated on the local fact AND IMDS
      # corroboration AND uptime AND a hard budget, counter on the ROOT DISK, written BEFORE the
      # reboot. A cap of 2 makes a reboot storm definitionally impossible (so no cooldown).
      #
      # SOLEUR_NIC_TEST_ROOT is the ONLY env seam (mirrors zot-restart-loop-alarm.sh's
      # ZOT_BQ_OVERRIDE): it re-roots FS reads so private-nic-guard.test.sh can execute this
      # exact body against synthesized fixtures without root. The cron and the boot invocation
      # NEVER set it; unset (production) it resolves to the real filesystem.
      EXPECTED_IP='${private_ip}'
      R="$${SOLEUR_NIC_TEST_ROOT:-}"
      REBOOT_CAP=2
      COUNTER_FILE="$R/var/lib/soleur/private-nic-reboots"
      # (0) PROBE RESOLUTION — load-bearing, and the reason this guard has its own PATH.
      # `ip` and `reboot` live in /usr/sbin, which is NOT on cron's default PATH (/usr/bin:/bin).
      # The cron.d block below declares a full PATH, but resolve explicitly anyway and FAIL SAFE:
      # if the probe binary is missing, we have NO local fact — and "the probe never ran" must
      # never be conflated with "the IP is absent". Conflating them is fatal here: `curl` lives in
      # /usr/bin and WOULD still resolve, so IMDS would corroborate (the network genuinely IS
      # attached) and the reboot gate would be fully satisfied ON A HEALTHY HOST — then, budget
      # spent, the guard would emit the terminal signature forever and the runbook would tell an
      # operator to destroy and recreate a healthy production registry. This is the guard's own
      # doctrine (never act on zero corroborating evidence) applied to its own instrument.
      IP_BIN=$(command -v ip 2>/dev/null || true)
      PROBE_OK=true; [ -n "$IP_BIN" ] && [ -x "$IP_BIN" ] || PROBE_OK=false
      # (1) Trigger predicate — the LOCAL FACT ALONE. The IP is baked at template time, so this
      # has ZERO runtime dependencies: IMDS is telemetry and corroboration, NEVER the trigger.
      # -w + -F: exact word, fixed string — so 10.0.1.3 can never match inside 10.0.1.30, and
      # the dots are not treated as regex wildcards.
      ip_present=false
      if [ "$PROBE_OK" = true ] && "$IP_BIN" -4 -o addr show 2>/dev/null | grep -qwF -- "$EXPECTED_IP"; then ip_present=true; fi
      # (2) Bounded wait — the same shape as the volume device-wait in runcmd below (~30 x 2s).
      # Under the leading hypothesis (H2) the attach lands AFTER boot, so give it a chance to
      # arrive before we consider acting. Only runs when the IP is already absent.
      if [ "$ip_present" = false ]; then
        for i in $(seq 1 30); do
          sleep 2
          if ip -4 -o addr show 2>/dev/null | grep -qwF -- "$EXPECTED_IP"; then ip_present=true; break; fi
        done
      fi
      # (3) Store-mount self-heal. Runs on EVERY path (including the NIC-healthy one) because
      # the bug is reachable from ANY reboot cause — kernel panic, Hetzner migration, the
      # operator's own recovery — not just this guard's. runcmd is PER-INSTANCE and does NOT
      # re-run on reboot, so the device-wait + mount + resize2fs below all skip and only the
      # `nofail` fstab entry mounts the store; a slow volume node then means the mount silently
      # fails and zot (launched `--restart unless-stopped`) bind-mounts an EMPTY root-disk dir => 404
      # for every image while nic_ok=true. `mount -a` alone is NOT enough: zot may already be
      # running against the empty dir, so it needs the restart.
      ZOT_STORE_MOUNTED=false
      if mountpoint -q /var/lib/zot 2>/dev/null; then
        ZOT_STORE_MOUNTED=true
      else
        # #6895 D2: the store is /dev/mapper/registry, and a reboot (incl. THIS guard's own self-
        # reboot) leaves the mapper CLOSED — so `mount -a` on the nofail fstab line would fail with
        # nothing to remount. Re-open it FIRST (idempotent: registry-luks-open.sh no-ops if the
        # mapper is already open), so the 5-min cron self-heals a closed mapper instead of waiting
        # for the next reboot's registry-luks-open.service.
        /usr/local/bin/registry-luks-open.sh >/dev/null 2>&1 || true
        mount -a >/dev/null 2>&1 || true
        if mountpoint -q /var/lib/zot 2>/dev/null; then
          # The mount landed — but zot is STILL bound to the old (empty) dir: bind mounts are
          # rprivate, so the new mount does not propagate into a running container. Report
          # zot_store_mounted=true ONLY if the restart actually re-resolved the bind. Setting it
          # true before the restart would report a healthy store while zot keeps 404-ing every
          # image — a green signal over a dead pull path, i.e. #6400's exact shape.
          if docker restart zot >/dev/null 2>&1; then
            ZOT_STORE_MOUNTED=true
          else
            echo "[nic] SOLEUR_PRIVATE_NIC store remounted but 'docker restart zot' FAILED — zot is still bound to the empty root-disk dir" >&2
          fi
        fi
      fi
      # (4) Facts (pure reads).
      UPTIME_S=$(awk '{printf "%d", $1}' "$R/proc/uptime" 2>/dev/null); [ -n "$UPTIME_S" ] || UPTIME_S=0
      BOOT_ID=$(cat "$R/proc/sys/kernel/random/boot_id" 2>/dev/null); [ -n "$BOOT_ID" ] || BOOT_ID=unknown
      INSTANCE_ID=$(cat "$R/var/lib/cloud/data/instance-id" 2>/dev/null); [ -n "$INSTANCE_ID" ] || INSTANCE_ID=unknown
      # Counter, keyed by instance-id. A host REPLACE gives a new root disk => a fresh budget for
      # free; the id key additionally guarantees a stale file can never hand a fresh host an
      # exhausted budget. NOT /var/lib/zot (that volume SURVIVES the replace). NOT /run or /tmp
      # (tmpfs rotates per boot => that path choice alone would reintroduce the infinite-reboot
      # trap this counter exists to prevent).
      REBOOT_COUNT=0
      if [ -r "$COUNTER_FILE" ]; then
        stored_id=$(grep -E '^instance_id=' "$COUNTER_FILE" 2>/dev/null | head -1 | cut -d= -f2)
        stored_n=$(grep -E '^count=' "$COUNTER_FILE" 2>/dev/null | head -1 | cut -d= -f2)
        if [ "$stored_id" = "$INSTANCE_ID" ] && printf '%s' "$stored_n" | grep -qE '^[0-9]+$'; then
          REBOOT_COUNT="$stored_n"
        fi
      fi
      # (5) Diagnose. EXIT-CODE-NEUTRALIZED: a nonzero curl exit is a VALID data outcome (it is
      # literally hypothesis H1), so it must never abort the script or read as a script error.
      IMDS_RC=0
      IMDS_BODY=$(curl -sf -m 5 http://169.254.169.254/hetzner/v1/metadata/private-networks 2>/dev/null) || IMDS_RC=$?
      IMDS_NETS=0
      IMDS_HAS_EXPECTED=false
      if [ "$IMDS_RC" -eq 0 ] && [ -n "$IMDS_BODY" ]; then
        IMDS_NETS=$(printf '%s\n' "$IMDS_BODY" | grep -cE '^[[:space:]]*network_id:' || true)
        # Corroborate on the EXPECTED ADDRESS, not merely "some network is attached". IMDS already
        # tells us the address, so using the weaker predicate throws that away — and the weaker
        # one is what makes a drifted EXPECTED_IP dangerous: a wrong constant would be
        # *corroborated* (a network genuinely IS attached), and the guard would reboot a healthy
        # host to its cap and then go terminal. Keying on the address makes that structurally
        # unreachable rather than a convention two future edits can break. imds_nets stays as a
        # separate emit field so H1/H2 discrimination is unchanged.
        # The `-?` is load-bearing: IMDS returns a YAML LIST, so the address line is `- ip: <addr>`
        # on the first key of each entry and `  ip: <addr>` would never appear.
        printf '%s\n' "$IMDS_BODY" | grep -qE "^[[:space:]]*-?[[:space:]]*ip:[[:space:]]*$EXPECTED_IP[[:space:]]*$" && IMDS_HAS_EXPECTED=true
      fi
      printf '%s' "$IMDS_NETS" | grep -qE '^[0-9]+$' || IMDS_NETS=0
      # (6) Converge — ONE primitive. Never reboot on zero corroborating evidence (the
      # zot-restart-loop-alarm.sh doctrine: an alarm that fires on its own probe fault trains the
      # operator to ignore it; a HOST that reboots on its own probe fault is the same mistake
      # with teeth). uptime_s>600 says "don't reboot a host that just booted" DIRECTLY — it needs
      # zero persistent state, cannot be corrupted, is already an emit field, and makes the boot
      # invocation naturally a no-op for the reboot arm.
      NIC_OK=false
      CONVERGED_BY=none
      DO_REBOOT=false
      if [ "$PROBE_OK" != true ]; then
        # Zero evidence: we never read the local fact. NOT "the IP is absent" — do not reboot.
        CONVERGED_BY=probe-fault
      elif [ "$ip_present" = true ]; then
        NIC_OK=true
        CONVERGED_BY=already
      elif [ "$IMDS_HAS_EXPECTED" = true ] && [ "$UPTIME_S" -gt 600 ] && [ "$REBOOT_COUNT" -lt "$REBOOT_CAP" ]; then
        # The budget must be DURABLE before we grant reboot authority. An unchecked write is a
        # reboot loop: if the root fs is read-only (ext4 errors=remount-ro) or full, the counter
        # never persists, every boot re-reads 0, and the cap NEVER binds — the box power-cycles
        # forever. So: write atomically (rename(2), so a reader sees the old or the new file and
        # never a torn one), fsync the dirent, VERIFY it landed, and only then reboot.
        REBOOT_COUNT=$((REBOOT_COUNT + 1))
        if mkdir -p "$(dirname "$COUNTER_FILE")" 2>/dev/null \
           && printf 'instance_id=%s\ncount=%s\n' "$INSTANCE_ID" "$REBOOT_COUNT" > "$COUNTER_FILE.tmp" 2>/dev/null \
           && mv -f "$COUNTER_FILE.tmp" "$COUNTER_FILE" 2>/dev/null \
           && grep -qxF "count=$REBOOT_COUNT" "$COUNTER_FILE" 2>/dev/null; then
          sync "$(dirname "$COUNTER_FILE")" 2>/dev/null || true
          CONVERGED_BY=reboot
          DO_REBOOT=true
        else
          # The budget is not durable => rebooting would be unbounded. Refuse, and say why: an
          # unwritable root fs is itself a high-value signal.
          REBOOT_COUNT=$((REBOOT_COUNT - 1))
          CONVERGED_BY=counter-unwritable
        fi
      fi
      # (7) Emit ALWAYS — success AND failure. The field set discriminates every competing
      # hypothesis in ONE event:
      #   imds_rc!=0                           -> H1, a real IMDS blip (the issue's framing)
      #   imds_rc=0 && imds_nets=0             -> H2, the structural attach race (leading)
      #   imds_nets>0 && converged_by!=already -> a third mode: the attach landed and the guest
      #                                           never configured it.
      # zot_last_err is LAST and free-text: scripts/lib/zot-telemetry-parse.sh:27 strips the
      # LITERAL ` zot_last_err=` tail to bound the trusted region, so this field MUST carry that
      # exact name (a `last_err=` would silently never be stripped, and the spoof guard would
      # never fire) and MUST be trailing. It carries the host's actual v4 addresses — exactly
      # what you want to read when nic_ok=false. Sanitized like the :215 shape so a stray byte
      # can never break the JSON payload.
      # `host` is deliberately ABSENT: the parse lib documents that the immutable
      # registry-host-replace REUSES the terraform hostname, so boot_id — not host — is what
      # separates old-host from new-host events.
      NIC_ADDRS=$(ip -4 -o addr show 2>/dev/null | awk '{print $2":"$4}' | tr '\n' ' ' | tail -c 200 | LC_ALL=C tr -cd '\40-\176' | tr -d '"\\'); [ -n "$NIC_ADDRS" ] || NIC_ADDRS=none
      LINE="SOLEUR_PRIVATE_NIC nic_ok=$NIC_OK converged_by=$CONVERGED_BY imds_rc=$IMDS_RC imds_nets=$IMDS_NETS reboot_count=$REBOOT_COUNT zot_store_mounted=$ZOT_STORE_MOUNTED uptime_s=$UPTIME_S boot_id=$BOOT_ID zot_last_err=$NIC_ADDRS"
      TOKEN="$${BETTERSTACK_LOGS_TOKEN:-}"
      if [ -n "$TOKEN" ]; then
        post() { curl -fsS -m 10 -H "Authorization: Bearer $TOKEN" -H 'Content-Type: application/json' "${betterstack_ingest_url}" --data-raw "{\"message\":\"$LINE\"}" >/dev/null 2>&1; }
        post || post || echo "[nic] SOLEUR_PRIVATE_NIC egress to Better Stack Logs FAILED: $LINE" >&2
      else
        echo "[nic] WARN: BETTERSTACK_LOGS_TOKEN unset (run under 'doppler run --project soleur-registry --config prd') — SOLEUR_PRIVATE_NIC not shipped: $LINE" >&2
      fi
      # (8) Reboot LAST — strictly after the event has shipped, so the converge is never silent.
      if [ "$DO_REBOOT" = true ]; then
        echo "[nic] SOLEUR_PRIVATE_NIC converging: $EXPECTED_IP absent, IMDS corroborates ($IMDS_NETS net(s)); reboot $REBOOT_COUNT/$REBOOT_CAP" >&2
        reboot
      fi
      exit 0
    owner: root:root
    permissions: '0755'

  - path: /etc/cron.d/soleur-private-nic-guard
    content: |
      # Converge + self-report the private NIC every 5 min (#6415).
      # WHY A CADENCE, not a boot-only oneshot: under H2 the attach lands AFTER boot, so a
      # boot-only check would emit a false failure and heal nothing. It also catches a NIC lost
      # later. flock -n so a slow tick (the bounded wait) can never stack with the next one, and
      # so the boot invocation and the cron cannot race.
      #
      # PATH IS LOAD-BEARING. cron's default is /usr/bin:/bin (_PATH_DEFPATH) — it does NOT include
      # /usr/sbin, where BOTH `ip` and `reboot` live. Without this line the guard's probe silently
      # resolves nothing under cron while `curl` (in /usr/bin) still resolves, so IMDS corroborates
      # and the reboot gate is satisfied ON A HEALTHY HOST. The boot invocation runs under
      # cloud-init's richer PATH, so the post-merge check would pass green and the box would page
      # 10 minutes later. The sibling disk-heartbeat cron is not cover for this: it uses no
      # /usr/sbin binary, so this guard is the host's first cron consumer of one.
      PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
      # Offset from the disk heartbeat's `*/5` so the two `doppler run` processes are not
      # concurrently resident (the host budgets ~1024m for cron+doppler+sshd+OS).
      2-59/5 * * * * root set -a; . /etc/default/registry-doppler; set +a; flock -n /run/lock/soleur-private-nic-guard.lock doppler run --project soleur-registry --config prd -- /usr/local/bin/soleur-private-nic-guard.sh
    owner: root:root
    permissions: '0644'

runcmd:
  # Apply SSH hardening immediately.
  - systemctl restart sshd

  # #6895: the zot store volume mount + resize is now the guest-side LUKS block, and it needs the
  # Doppler CLI + /etc/default/registry-doppler in place to read REGISTRY_LUKS_KEY (P1-B). So the
  # Doppler CLI install + env-file write below run FIRST, and the LUKS format/open/mount/resize
  # block runs right after them (before `systemctl enable --now docker`). The plaintext by-id
  # `mount $DEV /var/lib/zot` + its by-id fstab line are DELETED — the store is mounted from
  # /dev/mapper/registry only (a stale by-id fstab line would reboot-double-mount /var/lib/zot and
  # attempt a raw-ext4 mount of a now-crypto_LUKS device).

  # --- Doppler CLI (${doppler_arch}) — the ONLY channel for the ZOT_* tokens + LUKS key --
  # The tokens are NEVER baked into user_data (retrievable via the hcloud metadata API). They
  # arrive ONLY as the Doppler-injected env at boot. Arch (arm64/amd64) + its checksum are
  # terraform-interpolated from local.registry_arch (derived from registry_server_type), so the
  # host provisions correctly on either arch. That is a property of the DERIVATION, not a live
  # menu: the `cax` ARM line has been unorderable in hel1-dc2 at every probe since 2026-07-26,
  # and the pin is cpx22/amd64 (#7309). Egress (github
  # release DL) is open — zot-registry.tf denies only PUBLIC ingress.
  - |
    DOPPLER_VERSION="3.75.3"
    DOPPLER_SHA256="${doppler_sha256}"
    curl -fsSL --retry 3 "https://github.com/DopplerHQ/cli/releases/download/$${DOPPLER_VERSION}/doppler_$${DOPPLER_VERSION}_linux_${doppler_arch}.tar.gz" -o /tmp/doppler.tar.gz
    echo "$${DOPPLER_SHA256}  /tmp/doppler.tar.gz" | sha256sum -c -
    tar xzf /tmp/doppler.tar.gz -C /usr/local/bin doppler
    chmod +x /usr/local/bin/doppler
    rm /tmp/doppler.tar.gz
  # Restricted (0600 root) env file with the scoped Doppler service token — NOT world-readable
  # /etc/environment (mirrors git-data's /etc/default/git-data-doppler). The token is scoped to
  # the isolated `soleur-registry` project's `prd` root config (a SEPARATE project — NO
  # inheritance path to soleur/prd), so a host compromise reads ONLY the two ZOT tokens.
  # HOME is REQUIRED: cloud-init runcmd runs with no $HOME, and the Doppler CLI aborts with
  # "Doppler Error: $HOME is not defined" even when DOPPLER_CONFIG_DIR is set — so `doppler run`
  # below (and its nested `doppler secrets` self-check) fail and zot never launches (#6122 boot fix).
  - printf 'HOME=/root\nDOPPLER_TOKEN=%s\nDOPPLER_CONFIG_DIR=/tmp/.doppler\nDOPPLER_ENABLE_VERSION_CHECK=false\n' '${doppler_token}' > /etc/default/registry-doppler
  - chmod 600 /etc/default/registry-doppler

  # --- #6895: guest-side LUKS-at-rest mount + resize of the zot store volume ------------
  # Replaces the old plaintext `mount $DEV /var/lib/zot`. The volume is a RAW block device
  # (zot-registry.tf hcloud_volume.registry has no `format`); cryptsetup luksFormats it luks2 in
  # the guest, unlocked by REGISTRY_LUKS_KEY delivered ONLY as the Doppler-injected env (never an
  # argv positional, never baked into user_data). Runs UNDER `doppler run` scoped to the isolated
  # soleur-registry/prd config. D1/Option B `blkid TYPE` discriminator: "" (fresh) -> luksFormat;
  # crypto_LUKS -> reuse (idempotent re-provision); ANY OTHER TYPE (e.g. a populated plaintext
  # ext4 the registry-host-replace preserve-path kept) -> FATAL refuse, NEVER a silent wipe
  # (ADR-096 footgun). FAIL LOUD on an empty key — never an unencrypted fallback. Resize targets
  # the MAPPER (cryptsetup resize + resize2fs /dev/mapper/registry), not the raw device, and
  # PERSISTS /var/lib/zot/.resize-result for the SOLEUR_ZOT_DISK reporter (fail-loud, not
  # fail-dark: resize2fs's exit code is captured, not swallowed). `${registry_volume_id}` is
  # terraform-interpolated; $REGISTRY_LUKS_KEY / $DEV (no braces) stay literal for the doppler shell.
  - mkdir -p /var/lib/zot
  - |
    set -a
    . /etc/default/registry-doppler
    set +a
    doppler run --project soleur-registry --config prd -- bash -s <<'REGLUKSEOF'
    set -uo pipefail
    DEV=/dev/disk/by-id/scsi-0HC_Volume_${registry_volume_id}
    [ -n "$REGISTRY_LUKS_KEY" ] || { echo "[zot] FATAL: REGISTRY_LUKS_KEY empty — refusing unencrypted registry mount" >&2; exit 1; }
    # Device-wait: the volume attach can land AFTER cloud-init's disk stage (~30 x 2s).
    for i in $(seq 1 30); do [ -b "$DEV" ] && break; sleep 2; done
    RESIZE_OK=false; BLK_GB=0; DF_BEFORE=0; DF_AFTER=0
    if [ ! -b "$DEV" ]; then
      echo "[zot] SOLEUR_ZOT_DISK resize_ok=false reason=device-absent dev=$DEV — volume node never appeared after 60s; refusing to luksFormat/mount a missing device" >&2
    else
      # D1/Option B — blkid TYPE discriminator on the RAW device (replaces the old lsblk ^part
      # ext4-on-raw invariant). Only "" (fresh) or crypto_LUKS (already-LUKS) are admitted; any
      # other TYPE FATALs (a plaintext volume must be RECUT, not wiped — ADR-096 footgun).
      TYPE=$(blkid -o value -s TYPE "$DEV" 2>/dev/null || true)
      case "$TYPE" in
        "")          printf '%s' "$REGISTRY_LUKS_KEY" | cryptsetup luksFormat --batch-mode --type luks2 --key-file - "$DEV" ;;
        crypto_LUKS) : ;; # already LUKS — skip format (idempotent re-provision), luksOpen below
        *)           echo "[zot] SOLEUR_ZOT_DISK resize_ok=false reason=refusing-non-luks-device dev=$DEV TYPE=$TYPE — expected empty (fresh raw) or crypto_LUKS; a plaintext volume must be recut, not wiped (ADR-096)" >&2; exit 1 ;;
      esac
      # Open the mapper if closed, then lay/reuse the ext4 fs INSIDE it.
      [ -e /dev/mapper/registry ] || printf '%s' "$REGISTRY_LUKS_KEY" | cryptsetup luksOpen --key-file - "$DEV" registry
      blkid /dev/mapper/registry >/dev/null 2>&1 || mkfs.ext4 -q /dev/mapper/registry
      dpkg -s e2fsprogs >/dev/null 2>&1 || apt-get install -y e2fsprogs || echo "[zot] WARN: e2fsprogs install failed — resize2fs may be missing" >&2
      mountpoint -q /var/lib/zot || mount /dev/mapper/registry /var/lib/zot || echo "[zot] WARN: mount /dev/mapper/registry /var/lib/zot failed (may already be mounted)" >&2
      DF_BEFORE=$(df -B1G --output=size /var/lib/zot 2>/dev/null | tail -1 | tr -dc '0-9'); [ -n "$DF_BEFORE" ] || DF_BEFORE=0
      # Grow the LUKS mapper to fill a grown underlying volume FIRST, then the ext4 inside it.
      # resize2fs targets the MAPPER, not the raw $DEV (a raw resize2fs would hit the LUKS header).
      cryptsetup resize registry || echo "[zot] WARN: cryptsetup resize registry failed — mapper not grown" >&2
      # NO `|| true` here — the resize2fs exit code is CAPTURED (not swallowed) and shipped.
      if resize2fs /dev/mapper/registry; then
        RESIZE_OK=true
      else
        echo "[zot] SOLEUR_ZOT_DISK resize_ok=false reason=resize2fs-failed dev=/dev/mapper/registry — ext4 not grown; launching zot on the existing fs (telemetry stays loud)" >&2
      fi
      DF_AFTER=$(df -B1G --output=size /var/lib/zot 2>/dev/null | tail -1 | tr -dc '0-9'); [ -n "$DF_AFTER" ] || DF_AFTER=0
    fi
    BLK_GB=$(lsblk -bno SIZE /dev/mapper/registry 2>/dev/null | head -1 | awk '{printf "%d", $1/1024/1024/1024}'); [ -n "$BLK_GB" ] || BLK_GB=0
    printf 'resize_ok=%s\nfs_size_gb=%s\nblock_size_gb=%s\ndf_before_gb=%s\ndf_after_gb=%s\n' \
      "$RESIZE_OK" "$DF_AFTER" "$BLK_GB" "$DF_BEFORE" "$DF_AFTER" > /var/lib/zot/.resize-result
    # Assert the fs reached ~ the mapper size (within 2 GB). Loud WARN only — never wedge boot.
    if [ "$BLK_GB" -gt 0 ] && [ "$DF_AFTER" -gt 0 ] && [ "$(( BLK_GB - DF_AFTER ))" -gt 2 ]; then
      echo "[zot] SOLEUR_ZOT_DISK resize_ok=$RESIZE_OK WARN fs_size_gb=$DF_AFTER lags block_size_gb=$BLK_GB — ext4 did not fill the mapper (#6240 hypothesis a)" >&2
    fi
    # fstab: mount the MAPPER (NOT the raw by-id device). nofail so a volume hiccup never wedges
    # boot; the D2 boot-open oneshot (registry-luks-open.service) reopens the mapper on reboot so
    # this line can remount /dev/mapper/registry. Append-if-absent (idempotent re-provision).
    grep -q '/dev/mapper/registry' /etc/fstab || echo '/dev/mapper/registry /var/lib/zot ext4 defaults,nofail 0 2' >> /etc/fstab
    REGLUKSEOF

  # Ensure docker is up before the container launch.
  - systemctl enable --now docker

  # --- Build htpasswd from the Doppler tokens + launch zot -----------------------------
  # Runs UNDER `doppler run` (scoped to the isolated `soleur-registry` project's `prd` root)
  # so ZOT_PULL_TOKEN/ZOT_PUSH_TOKEN are in the env. Usernames are terraform-interpolated
  # (non-secret). FAIL LOUD on an empty token — never launch zot with a half-built htpasswd.
  # Idempotent: `docker rm -f zot` first so a re-provision (config/token change → clean host
  # replace) relaunches with fresh state.
  - |
    set -a
    . /etc/default/registry-doppler
    set +a
    doppler run --project soleur-registry --config prd -- bash -s <<'ZOTEOF'
    set -euo pipefail
    # Isolation self-check — the load-bearing, fail-CLOSED defense, run under the host's OWN
    # shipped boot token: the credential MUST resolve EXACTLY the FOUR admitted secrets
    # {ZOT_PULL_TOKEN, ZOT_PUSH_TOKEN, BETTERSTACK_LOGS_TOKEN, REGISTRY_LUKS_KEY}. Every other boot
    # signal (the empty-token guard below, the heartbeat, `terraform apply`) fails OPEN on an
    # over-scoped credential — a 116-secret token still populates the ZOT tokens non-empty. This
    # asserts cardinality (==4) AND identity (the exact admitted set), on the shipped token, before
    # any docker run. `grep -v '^DOPPLER_'` strips the DOPPLER_PROJECT/CONFIG/ENVIRONMENT built-ins.
    # BETTERSTACK_LOGS_TOKEN is admitted BY NAME (#6244): the disk-report reporter ships
    # SOLEUR_ZOT_DISK under `doppler run` from this same isolated config, mirroring the
    # cloud-init-inngest.yml precedent (ADR-100). REGISTRY_LUKS_KEY is admitted BY NAME (#6895): it
    # is the guest-side LUKS passphrase this same isolated config now holds (doppler_secret.
    # registry_luks_key) and the LUKS mount block above reads it under `doppler run`; it is a
    # registry-scoped secret, so the #6122 isolation property is preserved. DELETING either admitted
    # secret post-cutover FATALs the bootstrap (loud fail > silent observability/at-rest blind spot);
    # the check keys on the NAME, so a value rotation is safe. All four MUST be applied in the SAME
    # apply as the host — a 3-secret config now FATALs.
    names="$(doppler secrets --only-names --json | jq -r 'keys[]' | grep -v '^DOPPLER_' || true)"
    n_total="$(printf '%s\n' "$names" | grep -c . || true)"
    n_admitted="$(printf '%s\n' "$names" | grep -Ec '^(ZOT_(PULL|PUSH)_TOKEN|BETTERSTACK_LOGS_TOKEN|REGISTRY_LUKS_KEY)$' || true)"
    if [ "$n_total" -ne 4 ] || [ "$n_admitted" -ne 4 ]; then
      echo "[zot] FATAL: boot credential is not isolated (sees $n_total non-DOPPLER secrets, $n_admitted admitted {ZOT_PULL_TOKEN,ZOT_PUSH_TOKEN,BETTERSTACK_LOGS_TOKEN,REGISTRY_LUKS_KEY}) — refusing to launch"; exit 1
    fi
    [ -n "$ZOT_PULL_TOKEN" ] || { echo "[zot] FATAL: ZOT_PULL_TOKEN empty — refusing to build htpasswd"; exit 1; }
    [ -n "$ZOT_PUSH_TOKEN" ] || { echo "[zot] FATAL: ZOT_PUSH_TOKEN empty — refusing to build htpasswd"; exit 1; }
    mkdir -p /etc/zot
    htpasswd -Bbn '${zot_pull_user}' "$ZOT_PULL_TOKEN"  > /etc/zot/htpasswd
    htpasswd -Bbn '${zot_push_user}' "$ZOT_PUSH_TOKEN" >> /etc/zot/htpasswd
    chmod 600 /etc/zot/htpasswd
    for i in $(seq 1 30); do docker info >/dev/null 2>&1 && break; sleep 2; done
    docker rm -f zot >/dev/null 2>&1 || true
    # Container memory cap (ADR-062 pattern) — bounds zot's startup scan so a host-level OOM can't
    # restart-loop the box (#6288). cap = host RAM − ~1024m host overhead (cron+doppler+sshd+OS,
    # lighter than ADR-062's 1.3 GB web host), DERIVED from var.registry_server_type via the live
    # Hetzner catalog (zot-registry.tf local.registry_memory_cap_mb) and baked in at template time.
    # It was a hardcoded `7168m` with no edge to the server type: a 4 GB host would have kept the
    # 7168m cap, which can never bind on 4096m of RAM — i.e. silently back to the UNCAPPED-on-cx23
    # condition that caused #6288's restart-loop in the first place. Do not re-hardcode it.
    # --memory-swap == --memory disables swap growth → a deterministic cgroup-OOM, not swap-thrash.
    # NO --init: zot is a single Go process (PID 1 handles SIGTERM, spawns no children) — ADR-062's
    # Node zombie-reaping idiom buys nothing here. Env-overridable const (mirrors PROD_MEMORY_CAP);
    # the template-derived value is the default, so an operator override still wins for a one-off.
    ZOT_MEMORY_CAP="$${ZOT_MEMORY_CAP:-${zot_memory_cap_mb}m}"
    # #6895 P1: GATE the launch on the LUKS mount. The LUKS mount block and this zot launch are
    # SEPARATE runcmd entries and cloud-init continues past a failed entry — so on ANY LUKS failure
    # (empty key, wrong-TYPE FATAL refuse, closed mapper) /var/lib/zot would stay an empty root-disk
    # dir and zot would serve an empty/unencrypted store that answers 401, keeping the liveness
    # heartbeat GREEN with no alert. Refuse unless /var/lib/zot is backed by /dev/mapper/registry:
    # fail-loud here => zot never starts => the liveness-absence alarm fires (git-data's precedent).
    findmnt -no SOURCE /var/lib/zot | grep -qx /dev/mapper/registry || { echo "[zot] FATAL: /var/lib/zot not backed by /dev/mapper/registry — refusing to serve an empty/unencrypted store" >&2; exit 1; }
    docker run -d --name zot --restart unless-stopped \
      --log-driver journald \
      --memory "$ZOT_MEMORY_CAP" --memory-swap "$ZOT_MEMORY_CAP" \
      -v /var/lib/zot:/var/lib/zot \
      -v /etc/zot/config.json:/etc/zot/config.json:ro \
      -v /etc/zot/htpasswd:/etc/zot/htpasswd:ro \
      -p 0.0.0.0:5000:5000 \
      '${zot_image}' serve /etc/zot/config.json
    # Readiness: /v2/ answers (401 unauth IS healthy — reachable, auth-gated). `curl -s`
    # without -f returns 0 on any HTTP response, non-zero only on connection-refused.
    for i in $(seq 1 30); do
      curl -s -o /dev/null "http://localhost:5000/v2/" && { echo "[zot] listening on :5000"; break; }
      sleep 2
    done
    ZOTEOF

  # Disk-capacity guard: ensure cron is active and emit the first disk heartbeat + SOLEUR_ZOT_DISK
  # self-report now that zot is up, so Better Stack goes green AND the first telemetry event lands
  # within the redeploy (not after the first 5-min cron tick). Wrapped in `doppler run` so the
  # BETTERSTACK_LOGS_TOKEN is injected for the Better Stack Logs POST (mirrors the cron wrapper).
  - systemctl enable --now cron || true
  - set -a; . /etc/default/registry-doppler; set +a; doppler run --project soleur-registry --config prd -- bash /usr/local/bin/zot-disk-heartbeat.sh || true

  # Private-NIC self-report at boot (#6415), so the very first SOLEUR_PRIVATE_NIC event lands
  # within the redeploy rather than up to 5 min later — that first event is the empirical
  # H1-vs-H2 verdict for this boot (converged_by=already => no race; =reboot => the race is real
  # and the guard healed it).
  #
  # PLACEMENT IS LOAD-BEARING: this MUST come after /etc/default/registry-doppler is written
  # (the printf above), NOT merely after the Doppler CLI install. Anywhere earlier has the binary
  # but no token, so `doppler run` resolves nothing, the POST dies, and the `|| true` swallows it
  # SILENTLY — a silent failure inside the control built to end silent failures.
  #
  # `|| true` is fail-OPEN toward serving: observing the boot must never break it. It takes the
  # SAME flock as the cron so the boot run and a 5-min tick cannot race.
  - set -a; . /etc/default/registry-doppler; set +a; flock -n /run/lock/soleur-private-nic-guard.lock doppler run --project soleur-registry --config prd -- bash /usr/local/bin/soleur-private-nic-guard.sh || true

  # Arm the zot liveness feeder (#6537). `--now` starts the timer within this boot, so the first
  # beat lands ~30s from here rather than after the next interval — the monitor goes green inside
  # the redeploy window, which is what Phase 4.3 measures BEFORE the heartbeat is unpaused.
  #
  # `|| true` is fail-OPEN toward serving, matching every sibling above: a broken observer must
  # never take down the registry it observes. That is safe precisely BECAUSE the monitor is
  # absence-based — a timer that fails to enable produces no beats, which pages, rather than
  # passing silently. This is the one place where fail-open and fail-loud agree.
  - systemctl daemon-reload || true
  - systemctl enable --now zot-liveness-heartbeat.timer || true
  # #7440 (F-11): RE-ENSURE jq, mirroring the e2fsprogs guard above. jq is declared in `packages:`,
  # whose own comment records that the stage is NON-FATAL, and jq is a HARD per-line dependency of
  # the shipper — every row's MESSAGE and __CURSOR come out of it. Measured with jq off PATH before
  # the shipper's own startup guard was added: 0 of 2 rows delivered at rc=0 with no stderr, state
  # all zeros and no drop rows, while the reporter rendered `post_fail=unknown` — an inverted root
  # cause on top of 100% silent loss. Belt and braces: this re-ensures it, the shipper's startup
  # guard turns any remaining absence into a loud non-zero exit.
  - |
    dpkg -s jq >/dev/null 2>&1 || apt-get install -y jq || echo "[zot] WARN: jq install failed — zot-log-shipper will exit non-zero rather than drop rows silently" >&2
  # #7440: apply the journald sizing drop-in BEFORE the shipper's first tick, so the buffer it
  # reads from is already persistent and bounded rather than a tmpfs the first flood can blow
  # through.
  - systemctl restart systemd-journald || true
  # #7440: the container-log shipper needs no arming step — it is an /etc/cron.d drop-in, and cron
  # is already enabled above. That absence of a unit to enable is the point (ADR-184 §4): there is
  # no start that can fail, no `failed` state to latch, and nothing to restart.
  # #7440 IN-SURFACE DELIVERY SIGNAL. This host is cloud-init-only, so "was this change ever
  # actually delivered?" is otherwise unanswerable from the observability layer — and it is the
  # question that separates "not delivered yet" (wait) from "delivered and dead" (act). Fired ONCE
  # from runcmd, mirroring the precedent where both existing reporters also fire once at boot.
  # Absent boot marker + unchanged SOLEUR_ZOT_DISK boot_id = not delivered. Deliberately NOT routed
  # through the shipper: a marker that needs the shipper alive cannot report the shipper's absence.
  - |
    set -a; . /etc/default/registry-doppler 2>/dev/null || true; set +a
    doppler run --project soleur-registry --config prd -- bash -s <<'ZLSBOOTEOF' || true
    BOOT_ID="$(cat /proc/sys/kernel/random/boot_id 2>/dev/null || printf 'unknown')"
    # Both fields are READBACKS of the effect, not restatements of the intent.
    #
    # shipper_cron replaces a `systemctl is-active zot-log-shipper.service` that was structurally
    # incapable of failing closed (#7444 F-20): Type=simple marks a unit active on fork, sampled
    # from the very next runcmd entry, so it read `active` whether or not the shipper could run.
    # A cron drop-in's presence is a real file test with a real absent case.
    #
    # journald_storage replaces a hardcoded `persistent` literal (F-19) behind a `|| true`
    # restart — it asserted the value this template WROTE, not the one journald ADOPTED, so it
    # would have reported persistent even when the restart failed and storage stayed volatile.
    # /var/log/journal exists only once journald has actually adopted persistent storage.
    SHIPPER_CRON=absent; [ -f /etc/cron.d/zot-log-shipper ] && SHIPPER_CRON=present
    JOURNALD_STORAGE=volatile; [ -d /var/log/journal ] && JOURNALD_STORAGE=persistent
    LINE="SOLEUR_ZOT_LOG_BOOT boot_id=$BOOT_ID host=$(hostname) shipper_cron=$SHIPPER_CRON journald_storage=$JOURNALD_STORAGE"
    TOKEN="$${BETTERSTACK_LOGS_TOKEN:-}"
    if [ -n "$TOKEN" ]; then
      post() { curl -fsS -m 10 -H "Authorization: Bearer $TOKEN" -H 'Content-Type: application/json' "${betterstack_ingest_url}" --data-raw "{\"message\":\"$LINE\"}" >/dev/null 2>&1; }
      post || post || echo "[zot-log-shipper] SOLEUR_ZOT_LOG_BOOT egress FAILED: $LINE" >&2
    else
      echo "[zot-log-shipper] WARN: BETTERSTACK_LOGS_TOKEN unset — SOLEUR_ZOT_LOG_BOOT not shipped: $LINE" >&2
    fi
    ZLSBOOTEOF
  # #6895 (D2): ARM the boot-time LUKS reopen so a reboot (the NIC guard self-`reboot`s the host)
  # reopens /dev/mapper/registry before docker + the self-heal. `enable` persists it for the next
  # boot; the mapper is already open+mounted on THIS provision boot (the LUKS mount block above),
  # so `--now` is an idempotent no-op that also validates the unit loudly at provision time.
  - systemctl enable --now registry-luks-open.service || true
