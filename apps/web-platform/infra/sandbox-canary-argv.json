{
  "_comment": "Real-captured SDK bwrap SETUP argv (canonical projection) for the faithful sandbox canary (#5875 / #5913 / ADR-079). Populated by --capture driving the real @anthropic-ai/claude-agent-sdk query() with buildAgentSandboxConfig(), then normalizeCapturedArgv() (drops env-forwarding + random/host paths, keeps the seccomp-relevant --unshare-*/mount structure; ${CANARY_WS}/${CANARY_EMPTY} placeholders substituted at replay). MUST NOT be hand-authored (#4932 trap).",
  "status": "captured",
  "schema": "canonical-bwrap-v1",
  "sdkPackage": "@anthropic-ai/claude-agent-sdk",
  "sdkVersion": "0.3.197",
  "bwrapSetupArgv": [
    "--new-session",
    "--die-with-parent",
    "--unshare-net",
    "--ro-bind",
    "/",
    "/",
    "--bind",
    "${CANARY_WS}",
    "${CANARY_WS}",
    "--bind",
    "${CANARY_WS}",
    "${CANARY_WS}",
    "--tmpfs",
    "/proc",
    "--tmpfs",
    "/etc/ssh/ssh_config.d",
    "--ro-bind",
    "${CANARY_EMPTY}",
    "${CANARY_WS}/.claude",
    "--ro-bind",
    "${CANARY_EMPTY}",
    "${CANARY_WS}/.claude",
    "--ro-bind",
    "${CANARY_EMPTY}",
    "${CANARY_WS}/.claude",
    "--ro-bind",
    "${CANARY_EMPTY}",
    "${CANARY_WS}/.claude",
    "--ro-bind",
    "${CANARY_EMPTY}",
    "${CANARY_WS}/.claude",
    "--ro-bind",
    "${CANARY_EMPTY}",
    "${CANARY_WS}/.claude",
    "--ro-bind",
    "${CANARY_EMPTY}",
    "${CANARY_WS}/.claude",
    "--ro-bind",
    "${CANARY_EMPTY}",
    "${CANARY_WS}/.claude",
    "--ro-bind",
    "/dev/null",
    "${CANARY_WS}/.mcp.json",
    "--ro-bind",
    "/dev/null",
    "${CANARY_WS}/.gitconfig",
    "--ro-bind",
    "/dev/null",
    "${CANARY_WS}/.gitmodules",
    "--ro-bind",
    "/dev/null",
    "${CANARY_WS}/.bashrc",
    "--ro-bind",
    "/dev/null",
    "${CANARY_WS}/.bash_profile",
    "--ro-bind",
    "/dev/null",
    "${CANARY_WS}/.zshrc",
    "--ro-bind",
    "/dev/null",
    "${CANARY_WS}/.zprofile",
    "--ro-bind",
    "/dev/null",
    "${CANARY_WS}/.profile",
    "--ro-bind",
    "/dev/null",
    "${CANARY_WS}/.ripgreprc",
    "--ro-bind",
    "/dev/null",
    "${CANARY_WS}/.vscode",
    "--ro-bind",
    "/dev/null",
    "${CANARY_WS}/.idea",
    "--ro-bind",
    "${CANARY_EMPTY}",
    "${CANARY_WS}/.claude",
    "--ro-bind",
    "${CANARY_EMPTY}",
    "${CANARY_WS}/.claude",
    "--dev",
    "/dev",
    "--unshare-pid",
    "--unshare-user",
    "--bind",
    "/proc",
    "/proc"
  ],
  "prepDirs": [
    "${CANARY_WS}",
    "${CANARY_EMPTY}"
  ],
  "droppedForDeterminism": {
    "setenv": 26,
    "hostBind": 3,
    "randomSocket": 1,
    "randomEmptyDirBind": 10
  }
}
