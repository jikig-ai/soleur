# rung-2 boot rehearsal evidence — generated by scripts/followthroughs/git-data-rung2-evidence-capture.sh (#7025)
#
# READ THIS BEFORE MERGING IT. Merging this file RELEASES git_data_rung2_rehearsal_gate,
# which is the binding mechanical hold on the birth of the host that stores every
# connected user's source code. It is deliberately NOT auto-committed by any workflow:
# a route that writes its own gate-releasing evidence is self-approving.
#
# Rehearsal host : soleur-git-data-rehearsal-35465756680
# Captured (UTC) : 2026-09-19T20:05:40Z
# TABLE: BS_TABLE=t520508_soleur_git_data_prd_logs BS_TABLE_S3=t520508_soleur_git_data_prd_s3
#
# WHAT THE HASH BINDS: the cloud-init template plus every file()-bound payload in
# modules/git-data-userdata/main.tf. It does NOT bind the templatefile ARGUMENTS, so
# the divergence set below is the declared, machine-checked scope of what differed
# between this rehearsal and production. git_data_rung2_rehearsal_gate refuses any
# entry outside its identity-only allowlist.
#
# ARTIFACT 1 — source-liveness anchor. Excludes this host BY DESIGN: an anchor a
# silent host could satisfy would collapse "dark boot" and "broken instrument".
# QUERY: SELECT /* __ANCHOR__ source-liveness: is this log source answering AT ALL? */ dt, JSONExtractString(raw,'host_name') AS host FROM (SELECT dt, raw FROM remote($BS_TABLE) UNION ALL SELECT dt, raw FROM s3Cluster(primary, $BS_TABLE_S3) WHERE _row_type = 1) WHERE dt > now() - INTERVAL 30 DAY AND JSONExtractString(raw,'host_name') != '' AND JSONExtractString(raw,'host_name') != 'soleur-git-data-rehearsal-35465756680' ORDER BY dt DESC LIMIT 5 FORMAT JSONEachRow
#
# ARTIFACT 2 — every stage this host reported, and ARTIFACT 3 — the fatal/false-assertion
# arms, both read from this one result set.
# QUERY: SELECT /* __HOSTROWS__ every stage this rehearsal host reported */ dt, JSONExtractString(raw,'stage') AS stage, JSONExtractString(raw,'level') AS level, JSONExtractString(raw,'host_name') AS host, JSONExtractString(raw,'detail') AS detail, JSONExtractString(raw,'rc') AS rc, JSONExtractString(raw,'luks_mounted') AS luks_mounted, JSONExtractString(raw,'repo_root') AS repo_root, JSONExtractString(raw,'hooks_path') AS hooks_path, JSONExtractString(raw,'provision') AS provision, JSONExtractString(raw,'luks_reopen_unit') AS luks_reopen_unit, JSONExtractString(raw,'action') AS action, JSONExtractString(raw,'restarts') AS restarts FROM (SELECT dt, raw FROM remote($BS_TABLE) UNION ALL SELECT dt, raw FROM s3Cluster(primary, $BS_TABLE_S3) WHERE _row_type = 1) WHERE dt > parseDateTimeBestEffort('2026-09-19T20:03:25') AND JSONExtractString(raw,'host_name') = 'soleur-git-data-rehearsal-35465756680' ORDER BY dt DESC LIMIT 50 FORMAT JSONEachRow
# QUERY_FATAL: SELECT /* __FATALROWS__ every FATAL this rehearsal host reported, unbounded by row chatter */ dt, JSONExtractString(raw,'stage') AS stage, JSONExtractString(raw,'level') AS level, JSONExtractString(raw,'host_name') AS host, JSONExtractString(raw,'detail') AS detail, JSONExtractString(raw,'rc') AS rc FROM (SELECT dt, raw FROM remote($BS_TABLE) UNION ALL SELECT dt, raw FROM s3Cluster(primary, $BS_TABLE_S3) WHERE _row_type = 1) WHERE dt > parseDateTimeBestEffort('2026-09-19T20:03:25') AND JSONExtractString(raw,'host_name') = 'soleur-git-data-rehearsal-35465756680' AND JSONExtractString(raw,'level') = 'fatal' ORDER BY dt ASC LIMIT 1000 FORMAT JSONEachRow
#
# ARTIFACT 4 — the Sentry cross-check, run before this file was written.
# CLEAN = a fatal read returned zero rows AND the liveness anchor confirmed the
# source is answering. UNAVAILABLE = the read did not happen or could not be
# trusted; the PASS below rests on Better Stack alone.
# QUERY: sentry-issue.sh --host-events soleur-git-data-rehearsal-35465756680 --start 2026-09-19T20:03:25 --end 2026-09-19T20:05:40 
RUNG2_SENTRY_CROSSCHECK=UNAVAILABLE
RUNG2_BOOT_REHEARSAL=PASS
RUNG2_EVIDENCE_URL=https://github.com/jikig-ai/soleur/actions/runs/35465756680
RUNG2_TEMPLATE_SHA256=a0b5f37b2fefcb095c1d5e4a559dfac047840db87bb087ffdab5c78d514a0517
RUNG2_VAR_DIVERGENCE=host_name,git_data_volume_id,git_data_luks_volume_id,doppler_token,doppler_config_name,git_transport_pubkey,git_provision_pubkey,git_remove_pubkey
# QUERY: SELECT /* __HOSTROWS__ every stage this rehearsal host reported */ dt, JSONExtractString(raw,'stage') AS stage, JSONExtractString(raw,'level') AS level, JSONExtractString(raw,'host_name') AS host, JSONExtractString(raw,'detail') AS detail, JSONExtractString(raw,'rc') AS rc, JSONExtractString(raw,'luks_mounted') AS luks_mounted, JSONExtractString(raw,'repo_root') AS repo_root, JSONExtractString(raw,'hooks_path') AS hooks_path, JSONExtractString(raw,'provision') AS provision, JSONExtractString(raw,'luks_reopen_unit') AS luks_reopen_unit, JSONExtractString(raw,'action') AS action, JSONExtractString(raw,'restarts') AS restarts FROM (SELECT dt, raw FROM remote($BS_TABLE) UNION ALL SELECT dt, raw FROM s3Cluster(primary, $BS_TABLE_S3) WHERE _row_type = 1) WHERE dt > parseDateTimeBestEffort('2026-09-19T20:07:40') AND JSONExtractString(raw,'host_name') = 'soleur-git-data-rehearsal-35465756680' ORDER BY dt DESC LIMIT 50 FORMAT JSONEachRow
# QUERY: sentry-issue.sh --host-events soleur-git-data-rehearsal-35465756680 --stage luks_reopen_ok --start 2026-09-19T20:07:40 --end <now>
RUNG2_REBOOT_REOPEN=PASS
RUNG2_REBOOT_REOPEN_CHANNEL=both
RUNG2_REBOOT_REOPEN_RESTARTS=0
RUNG2_SENTRY_CROSSCHECK_ACK=35465756680:the Sentry liveness anchor did not answer, so the zero-row fatal read cannot be told apart from a dark channel. The PASS rests on Better Stack, which answered for this host and carried the MEASURED luks_reopen_unit arm. RO-token org membership is the named suspect in the capture log.
