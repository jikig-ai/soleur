#cloud-config
package_update: true
packages:
  - curl
  - fail2ban
  - jq

groups:
  - docker

users:
  - default
  - name: deploy
    groups: docker
    shell: /bin/bash
    lock_passwd: true
    # No SSH key -- deploys now arrive via webhook through Cloudflare Tunnel (#968).
    # The deploy user is kept for Docker group membership and sudoers.

write_files:
  - path: /etc/ssh/sshd_config.d/01-hardening.conf
    content: |
      PasswordAuthentication no
      KbdInteractiveAuthentication no
      MaxAuthTries 3
      LoginGraceTime 30
      PermitRootLogin prohibit-password
      AllowUsers root
      ClientAliveInterval 300
      ClientAliveCountMax 2
    owner: root:root
    permissions: '0644'
  - path: /etc/sudoers.d/deploy-chown
    content: |
      deploy ALL=(root) NOPASSWD: /usr/bin/chown 1001\:1001 /mnt/data/workspaces
    owner: root:root
    permissions: '0440'

  # CI deploy script invoked by the webhook listener.
  # Content injected from ci-deploy.sh via Terraform base64encode(file()).
  - path: /usr/local/bin/ci-deploy.sh
    encoding: b64
    content: ${ci_deploy_script_b64}
    owner: root:root
    permissions: '0755'

  # Webhook configuration for adnanh/webhook.
  # Validates HMAC-SHA256 signature, sets SSH_ORIGINAL_COMMAND from payload,
  # and invokes ci-deploy.sh asynchronously (#968).
  # Fire-and-forget: returns 202 immediately, deploy runs in background.
  # CI verifies success by polling the app health endpoint.
  - path: /etc/webhook/hooks.json
    content: |
      [
        {
          "id": "deploy",
          "execute-command": "/usr/local/bin/ci-deploy.sh",
          "command-working-directory": "/",
          "include-command-output-in-response": false,
          "success-http-response-code": 202,
          "response-message": "Deploy initiated",
          "http-methods": ["POST"],
          "trigger-rule-mismatch-http-response-code": 403,
          "pass-environment-to-command": [
            {
              "source": "payload",
              "name": "command",
              "envname": "SSH_ORIGINAL_COMMAND"
            }
          ],
          "trigger-rule": {
            "match": {
              "type": "payload-hmac-sha256",
              "secret": "${webhook_deploy_secret}",
              "parameter": {
                "source": "header",
                "name": "X-Signature-256"
              }
            }
          }
        }
      ]
    owner: root:deploy
    permissions: '0640'

  # Hardened systemd unit for the webhook listener.
  # Runs as deploy user (docker group) on localhost:9000.
  - path: /etc/systemd/system/webhook.service
    content: |
      [Unit]
      Description=Webhook deploy listener
      After=network.target

      [Service]
      Type=simple
      ExecStart=/usr/local/bin/webhook -verbose -hooks /etc/webhook/hooks.json -port 9000 -ip 127.0.0.1
      Restart=on-failure
      RestartSec=5
      User=deploy
      Group=deploy
      # NoNewPrivileges omitted: ci-deploy.sh requires sudo for chown (#967)
      ProtectSystem=strict
      ProtectHome=read-only
      PrivateTmp=true
      ReadWritePaths=/mnt/data
      ReadOnlyPaths=/etc/webhook /usr/local/bin
      TimeoutStopSec=180

      [Install]
      WantedBy=multi-user.target
    owner: root:root
    permissions: '0644'

  # Cloudflare apt repository source list for cloudflared.
  - path: /etc/apt/sources.list.d/cloudflare-main.list
    content: |
      deb [signed-by=/usr/share/keyrings/cloudflare-main.gpg] https://pkg.cloudflare.com/cloudflared noble main
    owner: root:root
    permissions: '0644'

runcmd:
  # Apply SSH hardening immediately (drop-in written by write_files above)
  - systemctl restart sshd

  # Install Doppler CLI (for secrets injection)
  - curl -Ls --tlsv1.2 --proto "=https" --retry 3 https://cli.doppler.com/install.sh | sh
  # Persist Doppler token for deploy user (read by SSH forced commands via /etc/environment)
  - echo "DOPPLER_TOKEN=${doppler_token}" >> /etc/environment

  # Install cloudflared from Cloudflare apt repository.
  # GPG key downloaded at provision time (rotated Oct 2025).
  - curl -fsSL https://pkg.cloudflare.com/cloudflare-main.gpg -o /usr/share/keyrings/cloudflare-main.gpg
  - apt-get update
  - apt-get install -y cloudflared

  # Register cloudflared as a systemd service with the tunnel token.
  # The token encodes tunnel configuration managed remotely via Terraform.
  - cloudflared service install ${tunnel_token}

  # Install webhook binary (adnanh/webhook v2.8.2) with checksum verification
  - |
    WEBHOOK_VERSION="2.8.2"
    WEBHOOK_SHA256="7a190ec7b4c2ffbb4eb1e11755a2e7acd82f1ffe74f60f235a360441daf22fd2"
    curl -fsSL "https://github.com/adnanh/webhook/releases/download/$${WEBHOOK_VERSION}/webhook-linux-amd64.tar.gz" -o /tmp/webhook.tar.gz
    echo "$${WEBHOOK_SHA256}  /tmp/webhook.tar.gz" | sha256sum -c -
    tar xzf /tmp/webhook.tar.gz -C /usr/local/bin --strip-components=1 webhook-linux-amd64/webhook
    chmod +x /usr/local/bin/webhook
    rm /tmp/webhook.tar.gz
  - systemctl daemon-reload
  - systemctl enable --now webhook

  # Install Docker
  - curl -fsSL https://get.docker.com | sh

  # Configure Docker log rotation
  - |
    cat > /etc/docker/daemon.json << 'DOCKEREOF'
    {
      "log-driver": "json-file",
      "log-opts": {
        "max-size": "10m",
        "max-file": "3"
      }
    }
    DOCKEREOF
  - systemctl restart docker

  # Mount volume (Hetzner pre-formats with ext4 via Terraform)
  - mkdir -p /mnt/data
  - mount /dev/disk/by-id/scsi-0HC_Volume_* /mnt/data || true
  - echo '/dev/disk/by-id/scsi-0HC_Volume_* /mnt/data ext4 defaults 0 2' >> /etc/fstab

  # Create .env placeholder and grant deploy user ownership of /mnt/data
  # (deploy user manages containers via webhook/ci-deploy.sh)
  #
  # After provisioning, populate /mnt/data/.env with secrets.
  # See apps/telegram-bridge/.env.example (bridge keys) and
  #     apps/web-platform/.env.example (platform keys).
  # Both containers read from this single shared file.
  # Create plugin directory used by ci-deploy.sh volume mount
  - mkdir -p /mnt/data/plugins/soleur
  - touch /mnt/data/.env
  - chmod 600 /mnt/data/.env
  - chown -R deploy:deploy /mnt/data

  # Weekly Docker image cleanup -- keeps only images used by running containers.
  # Prevents disk exhaustion from accumulated deploy images (#967).
  - |
    cat > /etc/cron.weekly/docker-cleanup << 'CRONEOF'
    #!/bin/sh
    docker image prune -af --filter "until=168h" > /dev/null 2>&1
    CRONEOF
    chmod +x /etc/cron.weekly/docker-cleanup

  # Pull and run container (prefer Doppler secrets, fall back to /mnt/data/.env)
  - docker pull ${image_name}
  - |
    ENV_FILE="/mnt/data/.env"
    DOPPLER_TOKEN="${doppler_token}"
    export DOPPLER_TOKEN
    if command -v doppler >/dev/null 2>&1 && [ -n "$DOPPLER_TOKEN" ]; then
      TMPENV=$(mktemp /tmp/doppler-env.XXXXXX)
      chmod 600 "$TMPENV"
      if doppler secrets download --no-file --format docker --project soleur --config prd > "$TMPENV" 2>/dev/null; then
        ENV_FILE="$TMPENV"
      else
        rm -f "$TMPENV"
      fi
    fi
    docker run -d \
      --name soleur-bridge \
      --restart unless-stopped \
      --env-file "$ENV_FILE" \
      -v /mnt/data:/home/soleur/data \
      -p 127.0.0.1:8080:8080 \
      ${image_name}
    if [ "$ENV_FILE" != "/mnt/data/.env" ]; then rm -f "$ENV_FILE"; fi
