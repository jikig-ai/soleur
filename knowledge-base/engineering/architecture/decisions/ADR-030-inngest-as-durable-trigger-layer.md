---
title: Inngest as durable trigger layer for server-side agents
status: accepted
date: 2026-05-17
related: [3244, 3940, 3947, 3948, 5450]
related_adrs: [ADR-005, ADR-023, ADR-027, ADR-046]
related_plans:
  - knowledge-base/project/plans/2026-05-05-feat-soleur-server-side-agentic-runtime-plan.md
  - knowledge-base/project/plans/2026-05-17-feat-pr-f-inngest-trigger-layer-plan.md
brand_survival_threshold: single-user incident
---

# ADR-030: Inngest as durable trigger layer for server-side agents

## Status

**Accepted** (2026-05-17, PR #3940).

Flipped from `proposed` at Phase 6 of `2026-05-17-feat-pr-f-inngest-trigger-layer-plan.md` after the substrate landed green: Inngest client + serve route (Phase 2), CFO function with single-pass verify + per-step lease (Phase 3), Stripe `invoice.payment_failed` → `inngest.send` bridge gated by `SOLEUR_FR5_ENABLED` (Phase 4), `/api/dashboard/today` + page-level disclosure banner (Phase 5).

## Context

PR-A through PR-E (#3240, #3395, #3854, #3883, #3922) shipped the security/isolation hardening required to safely run autonomous agents on behalf of multiple founders: user-scoped Supabase JWT minting, `audit_byok_use` writer at every BYOK SDK call path, `is_jti_denied` JWT-mint consumer, RLS-bound attachments. The runtime is now safe to run *synchronous* workloads on behalf of a logged-in founder.

The parent epic (#3244) calls for *autonomous* leaders that react to inbound events while the founder sleeps:

- Inbound-event-to-leader pipelines (Stripe failed-payment, GitHub issue created, KB drift detected).
- Cross-domain priority arbitration ("Today" surface).
- Cron-driven background analyses.

Without a durable trigger substrate, these are unbuildable. PR-F (this slice) chooses the substrate.

Brand-survival threshold: `single-user incident`. Operator (2026-05-17) confirmed all-of-the-above failure framing — cross-tenant data leak, BYOK credential leak, wrong-action while founder sleeps, billing surprise. The substrate decision is load-bearing for all four vectors.

## Decision

**Adopt Inngest as the durable trigger layer for server-side agents. Deploy the OSS `inngest` server binary (`inngest start`) self-hosted alongside the existing Node process on the Hetzner host. ~~SQLite for state persistence (alpha-default).~~ Bound to `127.0.0.1` only.**

> **Amended 2026-06-17 (#5450, `adopting`).** State/queue persistence flipped from bundled SQLite + in-memory Redis (ephemeral root disk) to **Supabase Postgres** (`--postgres-uri`, dedicated EU project `soleur-inngest-prd`, Supavisor **session pooler :5432**) for config/history **+ self-hosted Redis** (`--redis-uri`, AOF on the persistent `/mnt/data` volume) for the durable queue/run-state. This executes the Postgres migration this ADR deferred (see Trade-offs). Empirically required: the Phase-0 spike proved Postgres-alone loses armed future-`ts` reminders on a host re-provision; durable Redis is what survives (runbook § Durable backend). `status: adopting` until the Phase-2 cutover wiped-volume invariant verifies in prod.

The Node application uses `inngest@^3` SDK and exposes `/api/inngest` via `serve()`. Events are sent via `inngest.send(...)` to the local server endpoint `http://127.0.0.1:8288`.

## Rejected alternatives

### Inngest Cloud (Hobby / Pro)

**Rejected.** Routing founder-tagged event payloads (Stripe `customer_email`, draft response text, `founderId`) through Inngest Cloud creates a 5th sub-processor and a corresponding DPA / Article 30 / Privacy Policy / DPD / GDPR Policy / sub-processor-page / breach-runbook update cycle. PR-B→E spent five increments keeping founder data off external substrates inside the EU-only Hetzner posture; inverting that ladder for alpha velocity contradicts the brand-survival threshold.

**Re-evaluation criteria** (operator-confirmed):

1. **Concurrency cap pressure.** Hobby tier limits to 5 concurrent steps; Pro is $75/mo (corrected from umbrella spec's $25 — verified at plan time). When self-hosted concurrency on the Hetzner box becomes the bottleneck OR a tenant-cost-fairness primitive requires Cloud-hosted multi-tenant queuing, re-open.
2. **Third hosted founder onboarded.** At 3 active founders, the operational cost of running the OSS Inngest server (process supervision, upgrades, SQLite-to-Postgres migration if needed, debugging UI absence) MAY exceed the legal-surface cost of a 5th sub-processor. Re-evaluate.

### LangGraph + custom orchestration

**Rejected.** Operationally heavy (separate worker pool, separate state store, separate retry semantics). Forces a duplicate "what's next" implementation alongside the existing `step.run` model. Inngest's batteries-included primitives (concurrency CEL, schema versioning, signed webhooks, replay-window, dashboard) ship correctness PR-F would otherwise hand-roll.

### Bedrock AgentCore (AWS)

**Rejected.** AWS vendor lock; no EU-residency parity with the current Hetzner posture; would add AWS as a sub-processor (same disclosure cycle as Inngest Cloud above).

### Cloudflare Durable Objects + LISTEN/NOTIFY

**Rejected.** Cannot host the Claude Agent SDK long-running process (DO execution model is incompatible with multi-minute SDK turns). LISTEN/NOTIFY in Supabase Postgres lacks durable replay and would require custom dead-letter + retry implementation on top.

## Load-bearing invariants

The following invariants are LOAD-BEARING. Violation = brand-survival regression at `single-user incident` threshold. Each is enforced by code, test, or DB constraint per PR-F's implementation plan.

### I1 — BYOK lease opened INSIDE each `step.run` that calls the Anthropic SDK

`AsyncLocalStorage` at `apps/web-platform/server/byok-lease.ts:115` does NOT survive across Inngest step-replay boundaries. Every `step.run("name", async () => { ... })` that calls the SDK MUST open its own `runWithByokLease(founderId, async (lease) => { ... })` scope. Outer-scope lease reuse triggers the sync-escape check at `byok-lease.ts:133–139` → `ByokLeaseError("escape")` → fail-closed.

**Enforced by:** the existing `byok-audit-writer-sweep.test.ts` CI sentinel + the new alias-rename extension (RV3 / Phase 2 of PR-F plan).

### I2 — User-scoped Supabase JWT minted INSIDE each `step.run` that touches tenant data

`getFreshTenantClient(event.data.founderId)` runs per-step. The `is_jti_denied` consumer at `apps/web-platform/lib/supabase/tenant.ts:341` fires automatically on every fresh-client call. **Never cache JWTs across step boundaries via `event.data` or step return values.**

**Enforced by:** code review at PR-F Phase 3 + Phase 4 tests.

### I3 — Singleton concurrency per founderId (per function-name)

Each Inngest function declares `concurrency: [{ scope: "fn", key: "event.data.founderId", limit: 1 }]`. Function-name namespace is implicit (each function triggers on one event-name; Inngest v3 has no wildcard triggers).

**Enforced by:** the CEL key in the function declaration + a test asserting 5 events same `founderId` → exactly 1 runs, 4 blocked.

### I4 — Signature verification REQUIRED at startup

`serve()` is configured with `signingKey: process.env.INNGEST_SIGNING_KEY`. Missing key = throw at process boot, NOT log-and-continue. The Inngest server binary also requires the key, read from the `INNGEST_SIGNING_KEY` **environment variable** (the `signkey-prod-` prefix stripped for the self-hosted server) — see I7; it is NOT passed on argv.

**Enforced by:** `apps/web-platform/server/inngest/client.ts` module-load throw + `apps/web-platform/app/api/inngest/route.ts` module-load throw.

### I7 — Server secrets delivered via the environment, never argv (#5560)

inngest-server reads `INNGEST_POSTGRES_URI`, `INNGEST_REDIS_URI`, `INNGEST_SIGNING_KEY`, and `INNGEST_EVENT_KEY` from the **inherited environment** (`doctor`/self-hosting docs), never the `inngest start` argv. argv is world-readable via `/proc/<pid>/cmdline` (mode 0444); the env is owner-only (`/proc/<pid>/environ`, mode 0400). The `inngest-bootstrap.sh` ExecStart therefore passes **no** secret flag — `--signing-key`/`--event-key`/`--postgres-uri`/`--redis-uri` are all absent; the doppler-run wrapper injects the values, with `INNGEST_REDIS_URI` constructed from `INNGEST_REDIS_PASSWORD` and `INNGEST_SIGNING_KEY` re-exported stripped. The durable backend is detected on argv by the **non-secret** `--postgres-max-open-conns` sentinel (ci-deploy.sh, inngest-inventory.sh, inngest-wiped-volume-verify.sh). The SQLite-only fail-safe `unset INNGEST_POSTGRES_URI` so inngest does not connect to Postgres when Redis is unready.

**Enforced by:** `apps/web-platform/infra/inngest.test.sh` (#5560 security invariant: ExecStart carries no secret flag + uses `exec`) + the durability drift-guard in `inngest-inventory.test.sh`.

### I5 — "Drafts everywhere, sends nowhere" — DB-level CHECK constraint AND code

The `messages_external_tier_status_check` constraint on `public.messages` (migration 046) enforces `status IN ('draft', 'archived')` for `tier IN ('external_brand_critical', 'external_low_stakes')`. Any future code attempting to INSERT `status='sent'` on an external-tier row is rejected at DB level with SQLSTATE 23514.

**Future auto-send capability** (e.g., a class that would transition from `draft` → `sent` for an external tier) requires (a) explicit migration to DROP and replace this constraint, (b) Article 22(3) right-to-human-review notice and DPD update, (c) re-amendment of this ADR.

**Enforced by:** Postgres CHECK constraint + Phase 1 test.

### I6 — Verify-external-state is single-pass-only

`step.run` memoizes results. On a 6h-deadlettered retry, a checkpointed verify result becomes stale (Stripe state may have moved `failed → succeeded → refunded`). PR-F's CFO function therefore does NOT split verify into a `step.run` artifact that downstream steps consume by reference; verify lives in the function body and is recomputed on each pass.

Any future code adding a step.run-checkpointed verify whose result feeds a downstream draft MUST first amend this ADR to either (a) split verify into a watchdog + last-call-before-fire pattern, or (b) bound the deadletter window such that staleness is acceptable.

**Enforced by:** code review at PR-F Phase 3 + a test asserting any retry path re-enters from verify.

### I8 — RLS lockdown on **every** project hosting Inngest's public tables

**Scope (widened 2026-07-15).** This invariant originally scoped to *"The Inngest backing project's (`soleur-inngest-prd`)…"* — singular, assuming Inngest's tables live on exactly one project. That assumption was false: the pre-cutover **dark** Inngest host (#6178 / ADR-100) ran goose against **soleur-dev** (`mlwiodleouzwniehynfz`), creating 14 Inngest tables in a **co-tenanted** `public` schema alongside the web-platform app's 52 dev tables. I8 now scopes to **every project hosting Inngest's public tables**, for as long as it hosts them.

**The invariant.** On any such project, Inngest's public tables have RLS **enabled (not forced)**, **zero policies**, and **`anon`/`authenticated` table+sequence grants revoked**. Inngest reaches its tables solely as the `postgres` owner over the session pooler (owner bypasses non-forced RLS), so the lockdown is non-breaking. `FORCE ROW LEVEL SECURITY` is forbidden (it removes the owner bypass and locks Inngest out); `anon`/`authenticated` must never be re-granted; `postgres`/`service_role` must never be revoked. Enforcement **must assert grants, not merely `relrowsecurity`**: RLS does not gate `TRUNCATE` (privilege-gated, not policy-gated) and PostgREST exposes no TRUNCATE verb, so a catalog gate is the only place that wipe vector can be proven closed.

**Enforcement is project-shape-dependent — this is the load-bearing 2026-07-15 addition:**

- **Dedicated project** (`soleur-inngest-prd`): a schema-wide revoke is correct, and `ALTER DEFAULT PRIVILEGES FOR ROLE postgres … REVOKE` (TABLES/SEQUENCES/FUNCTIONS) is the durable recurrence fix — a future Inngest table is born ungranted.
- **Co-tenanted project** (`soleur-dev`, transiently): enforcement **MUST be table-scoped to an explicit allowlist**. A schema-wide revoke is **FORBIDDEN** — it would strip `anon`/`authenticated` across the app's tables. `ALTER DEFAULT PRIVILEGES … REVOKE` is likewise **FORBIDDEN**: it would break every *future* app migration on that project. The cost of the allowlist is honest and accepted: a new Inngest table is **not** auto-covered; healing requires a human PR adding the name (window = time-to-PR, **not** self-healing). This is a reason to end co-tenancy, not to widen the revoke.

**The sentinel falsification (record it so it is not re-derived the hard way).** `0001`'s fail-closed preflight aborts unless `goose_db_version` AND `function_runs` exist — encoding *"these exist ⟹ Inngest-only project."* The dark backend falsified that: **soleur-dev satisfies it too**, so `0001` would have **PROCEEDED** there and killed the dev app. **An Inngest-sentinel-only preflight cannot identify an Inngest-only project once any dark or co-tenanted backend exists.** Project identity must be bound by the project's **own identity record** (Management API `GET /v1/projects/{ref}` → `.name`), not inferred from schema contents. `0001` additionally carries a **negative** guard (abort when app-distinctive tables exist), which must use app-distinctive names — never generic nouns like `users`, which Inngest could plausibly ship, permanently killing this self-heal.

**Enforced by:** `apps/web-platform/infra/inngest-rls/0001_enable_rls_lockdown.sql` (dedicated project; Inngest-sentinel preflight + app-distinctive negative guard preceding the revoke loop) + `apps/web-platform/infra/inngest-rls/0002_dev_inngest_tables_lockdown.sql` (co-tenanted soleur-dev; allowlist-driven, **no** `ALTER DEFAULT PRIVILEGES`, **no** schema-wide catalog loop) + `inngest-rls.test.sh` (**per-artifact** static shape profiles — the two artifacts have deliberately INVERTED required sets, so a single shared assertion set is a defect) + `apply-inngest-rls-dev-workflow.test.sh` (workflow pair: path routing, project pinning, issue/concurrency identity) + `.github/workflows/apply-inngest-rls.yml` (merge-apply + **hourly** self-healing re-apply; schema-wide catalog/grant gate `violations=0`) + `.github/workflows/apply-inngest-rls-dev.yml` (merge-apply + image-pin-bump re-apply; **allowlist-scoped** gate `violations=0` with a coverage guard) + `anon-probe.sh` (PostgREST client-path probes). Both workflows assert project identity via the Management API before applying, and each workflow's `paths:` names **only its own** SQL artifact — a shared `**` glob previously auto-applied `0001` to Inngest prd on any edit in the directory.

The hourly cadence on the prd workflow (CPO decision 2026-07-01, changelog below) bounds the cosmetic `rls_disabled_in_public` advisor-recurrence window on a newly-created Inngest table to ≤1h; the anon-read hole itself never re-opens there (the `ALTER DEFAULT PRIVILEGES` revoke means a new table is born ungranted). The dev workflow deliberately carries **no** schedule — goose can only re-run on a bootstrap image-pin bump, which is a reviewable PR and therefore a deterministic trigger.

## Implementation references

- Plan: `knowledge-base/project/plans/2026-05-17-feat-pr-f-inngest-trigger-layer-plan.md` (v2, post-review)
- Spec: `knowledge-base/project/specs/feat-pr-f-inngest-trigger-layer/spec.md`
- Brainstorm: `knowledge-base/project/brainstorms/2026-05-17-pr-f-inngest-trigger-layer-brainstorm.md`
- Parent plan (Increment 3 is PR-F): `knowledge-base/project/plans/2026-05-05-feat-soleur-server-side-agentic-runtime-plan.md` §3.1–3.5
- Parent epic: [#3244](https://github.com/jikig-ai/soleur/issues/3244)
- Predecessor PRs (all MERGED, verified /work Phase 0.1 2026-05-17): #3240, #3395, #3854, #3883, #3922
- Follow-up issues: #3947 (PR-G cohort onboarding), #3948 (cron migration TR9)

<!-- lint-infra-ignore start: ADR architectural trade-off + amendment-log prose describes terraform-replace / SSH / cutover SCENARIOS for decision context — it does NOT prescribe operator runbook steps (all execution is CI/webhook-driven, no-SSH). Wrapped #6407 so touching this ADR doesn't trip lint-infra-no-human-steps on pre-existing prose. -->

## Trade-offs accepted

- **Operational cost.** Self-hosted Inngest adds a sidecar process (systemd unit, upgrade cadence, no debugging UI for non-Pro). Mitigated by: SQLite default (zero new persistence config), `Restart=always`, `127.0.0.1` binding.
- **No Cloud dashboard.** The OSS server has a basic UI; not the Pro debugging surface. Acceptable for alpha (operator + 1 dogfood founder). Re-evaluate per criteria above.
- **SQLite single-host limitation.** ~~State persistence is local-disk; no high-availability. Mitigated by: Hetzner backups, Inngest's redelivery semantics (Stripe redelivers webhooks for ~3 days; missed events recoverable). Migration to Postgres-backed Inngest deferred to future PR if/when warranted.~~ **Corrected + resolved 2026-06-17 (#5450):** the "Hetzner backups" mitigation was falsified — `hcloud_server.web` carries `lifecycle { ignore_changes = [user_data, ssh_keys, image] }` (server.tf), so SQLite state is NOT lost on every `terraform apply`; it is lost only on an explicit `terraform apply -replace` (or a replace-forcing change), which boots a fresh root disk with **no backup restore path** (Hetzner volume snapshots cover `/mnt/data`, not the root disk where SQLite lived). Rarer than implied, but real and silent — and the redelivery semantics do NOT cover HTTP-armed `event-scheduled-reminder` events (they have no upstream to redeliver). **Resolved** by the durable-backend amendment above. The deferral's re-eval trigger fired not on the original "3rd founder / 5th-sub-processor" criterion but on the **#5450 durability gap**; the migration adds **no new sub-processor** (Supabase is already the primary DB + an existing sub-processor; Redis is self-hosted on our own host), which dissolves the deferral's sole legal-surface objection.
- **Availability coupling (new, permanent — #5450).** Post-cutover Inngest **cannot start** without Supabase + Redis reachable (the in-memory fallback is gone — proven fail-closed in the Phase-0 spike). Pre-migration it survived a Supabase outage on local SQLite; it no longer will. Knowingly traded for durability + Supabase PITR + closing this deferral. The dedicated Inngest Supabase project + co-located Redis keep the blast radius off the main app's project.

## Updates / amendment log

- **2026-07-13 (#6374) — the external health watchdog's authoritative liveness signal now rides the `/health`/loopback surface this ADR defines (observability refinement; no backend change).** The external inngest health watchdog (`.github/workflows/scheduled-inngest-health.yml`, built for the #5542 silent-crash-loop) previously took its SOLE liveness signal from the `inngest-inventory` cutover-baseline hook, whose heavy paginated 365-day `eventsV2` scan false-positived as `inngest_down` when the read path faulted (deadline/page-ceiling/pool/gateway) while the cron executor stayed healthy — the #6374 P1 (a `[ci/inngest-down]` alarm ran ~14h; the `scheduled-inngest-cron-watchdog` beacon checked in `ok` throughout, proving the executor was alive). Corrected by decoupling the liveness verdict from the eventsV2 read: `inngest-inventory.sh` gains an `INVENTORY_LIVENESS_ONLY` mode (the cheap `/v0/gql functions` query + `durability_state` ONLY, no eventsV2), exposed via a new `/hooks/inngest-liveness` GET hook the watchdog probes every `*/15`. This moves the watchdog onto the same lightweight loopback-liveness signal class this ADR's durable trigger layer already relies on (the on-host `curl 127.0.0.1:8288/health` HARD gate + the `/v0/gql functions` registry read), rather than the cutover inventory. The `durability_state` enum emitted by the probe (`durable|degraded|sqlite_only|unknown` — the watchdog defaults a missing field to `absent` at the consumer via `jq '.durability_state // "absent"'`) and the `#5553` between-deploy detector wiring are preserved verbatim. A companion classifier distinguishes a genuine `inngest_down` from a broken/undeployed probe path (`probe_unavailable`, no restart), and a restart give-up age gate caps the auto-restart churn. No new sub-processor, no new secret (reuses `WEBHOOK_DEPLOY_SECRET` + CF-Access), no C4 change (the `deploy.soleur.ai`→host webhook edge + the loopback health surface are already modeled; the new hook is an instance of an already-modeled element type). **Refined 2026-07-14 (#6407) — corroborate an apparent `inngest_down` against loopback `/health` before declaring down; new soft mode `functions_query_degraded` + a persistence-escalation ceiling (completes #6374; no backend change).** #6374 moved the liveness verdict off the heavy eventsV2 read, but the residual false-positive was the **cheap `/v0/gql functions` query ITSELF transiently failing** (a curl transport blip → the `__FETCH_FAILED__` envelope → the `inngest-inventory: FATAL` sentinel → classifier `inngest_down` → restart), while inngest-server was UP and processing events (6th `[ci/inngest-down]` recurrence in ~2 days; each a false P1 whose dispatched restart then RED-failed on `flock` `lock_contention`). Corrected by CORROBORATION: in `INVENTORY_LIVENESS_ONLY` mode, before emitting the FATAL sentinel on a functions-query failure, `inngest-inventory.sh` probes the SAME loopback server's `/health` (the exact endpoint `ci-deploy.sh verify_inngest_health` gates on) — `/health=200` (HTTP server serving; GQL read blipped) downgrades to a new **soft** sentinel `inngest-inventory: DEGRADED` → classifier mode **`functions_query_degraded`** (NO restart, own soft `[ci/inngest-functions-degraded]` issue class, heartbeat stays `ok`); `/health != 200` (wedged/stopped) keeps FATAL → `inngest_down` → restart (which recovers a wedge). `systemctl is-active`/ExecStart were rejected as corroborators (both read for a wedged-or-stopped unit — not specificity-correct); `/health` is the only same-signal-class liveness signal. A **persistence-escalation ceiling** (reusing the #6374 age-gate logic against the new issue title, ~45 min ≈ 3 `*/15` cycles) reclassifies a SUSTAINED degraded state (the `/health`-ok-but-functions-permanently-wedged residual) back to `inngest_down` so it is never soft-masked forever. Companion Defect B: the dispatched restart's verify poll (`restart-inngest-server.yml`) now treats `reason=lock_contention` as NON-TERMINAL with a final-state re-read — **cross-ref ADR-079 amendment #5960**, the precedent applied to the sibling web-platform-release deploy poll. Journald `SOLEUR_INNGEST_LIVENESS_VERDICT` / `SOLEUR_INNGEST_RESTART_LOCK_CONTENTION` markers (Vector Source 4 → Better Stack) self-report both decisions. No new sub-processor/secret/C4 change (a new soft *mode* + issue *class* is a new observable state, not a C4 element).
- **2026-07-15 — I8 scope widened to every Inngest-hosting project; table-scoped lockdown on the co-tenanted soleur-dev (security-advisor remediation; no backend change).** A Supabase `rls_disabled_in_public` advisor (dated 2026-07-12) fired against **soleur-dev** (`mlwiodleouzwniehynfz`) — a project I8 did not previously cover, because I8 assumed Inngest's tables lived on exactly one project. Live-catalog enumeration found **exactly 14** tables, all created by the pre-cutover **dark** Inngest host (#6178 / ADR-100), whose `INNGEST_POSTGRES_URI` (Doppler `soleur-inngest/prd`) resolves to username `postgres.mlwiodleouzwniehynfz`. All 14 carried full `anon`+`authenticated` grants with RLS off; proven live with the browser-shipped dev anon key: `GET /rest/v1/apps` → **HTTP 200, 310 bytes** to an anonymous caller. **Framed on write-integrity, not data exposure**: 12 of the 14 payload tables held zero rows (`apps`=1 Inngest app-registration with an RFC1918 URL, `goose_db_version`=6 version ids), so no personal data was reachable (Art. 4(12) not met ⇒ Art. 33 not engaged — distinct from 2026-06-29). The live vector was **TRUNCATE/DELETE by an anonymous caller** against the scheduler state an in-flight cutover depends on.
  **`0001` could not be reused** — and this is the durable lesson. It loops `pg_tables` schema-wide and revokes default privileges; soleur-dev is **co-tenanted** (52 app tables), so applying it there would have killed the dev app. Worse, its fail-closed Inngest-sentinel preflight **passed on soleur-dev** (the dark backend created `goose_db_version` + `function_runs` there), so the guard that existed to prevent exactly this would have waved it through. Sentinel-by-schema-contents is structurally incapable of identifying an Inngest-only project; project identity is now bound via the Management API's own identity record on **both** workflows, and `0001` gained a negative guard (app-distinctive tables ⇒ abort) placed **before** its revoke loop.
  Remediated by a **table-scoped, allowlist-driven** `0002` (non-forced RLS + `REVOKE ALL FROM anon, authenticated` over the explicit 14, sequences derived via `pg_depend`; **no** `ALTER DEFAULT PRIVILEGES`, **no** schema-wide catalog loop). Applied live 2026-07-15: allowlist-scoped gate **violations 14 → 0** (RLS on, `postgres`-owned, zero anon/authenticated SELECT/INSERT/UPDATE/DELETE/**TRUNCATE**); anon `GET /rest/v1/apps` **310 bytes → HTTP 401 + 42501**; anon DELETE/INSERT proven inert by owner read-back (the 204 trap: status codes prove nothing). **Non-breaking, verified**: the app's 52 dev tables' grants are byte-identical to the pre-apply baseline and `pg_default_acl` is untouched (so future dev app migrations still work), owner INSERT succeeds under non-forced RLS, and the external `scheduled-inngest-health.yml` watchdog was green on a **post-apply** run.
  Also closed a latent trigger-bleed: `apply-inngest-rls.yml` triggered on `inngest-rls/**`, so an edit to **any** file in that directory auto-applied `0001` to Inngest **prd**. Both workflows' `paths:` now name only their own artifact, asserted by a checked-in shape guard (`actionlint` runs in zero workflows, so a PR-time grep is not an enforceable gate). **Transient by design**: soleur-dev hosts these tables only until the cutover writes the prod DSN; `0002` + `apply-inngest-rls-dev.yml` retire **atomically** with the Phase-5 `DROP TABLE`. The `ddl_command_end` event trigger remains **rejected** (2026-07-01, below) and applies with *more* force here, since goose runs against this very project. No new secret (reuses `SUPABASE_ACCESS_TOKEN`), no new sub-processor, no C4 change (soleur-dev is not modeled — the C4 model is strictly prod topology). Cause-level gap (a non-`soleur` Doppler project's DSN resolving to the dev ref, which `preflight` Check 4 structurally cannot see) is tracked separately; the symptom-level advisor scan is #3366.
- **2026-07-01 — RLS self-heal cadence daily→hourly (I8 hardening; CPO decision; no backend change).** The 2026-06-29 lockdown closed the *data-exposure* hole permanently (the `ALTER DEFAULT PRIVILEGES … REVOKE` means a new Inngest table is created with no `anon`/`authenticated` grant), but a new table sits at `relrowsecurity=false` until the next `apply-inngest-rls.yml` re-apply, so the **cosmetic** `rls_disabled_in_public` advisor lint (CRITICAL email) recurred for up to the cron interval — observed again 2026-06-30 on a Supabase digest dated 2026-06-28. Two options were weighed: **(a)** a `ddl_command_end` event trigger that auto-enables RLS at CREATE-TABLE time (advisor window → ~0) and **(b)** tightening the existing self-heal cron to hourly (window ≤1h). The event trigger was **declined**: it fires inside Inngest's own `CREATE TABLE` transaction, introducing a new migration-abort failure mode on the brand-survival-critical agentic-run path purely to remove a cosmetic email — judged an asymmetric trade by the architecture + simplicity reviewers, and routed to the plan's `requires_cpo_signoff` gate. **CPO chose (b)**: `apply-inngest-rls.yml` schedule `17 4 * * *` → `17 * * * *`. One-line change, zero new primitive, zero new failure mode; the daily→hourly cron remains the sole self-heal mechanism. The deepen-plan for the event-trigger approach is retained as a rejected-alternative record at `knowledge-base/project/plans/2026-06-30-security-durable-rls-inngest-event-trigger-plan.md`. No new secret/sub-processor/C4 change. Verify: after merge, the apply workflow runs hourly (next `:17`); a security advisor check confirms `rls_disabled_in_public = 0`.
- **2026-06-29 — RLS lockdown on the Inngest backing project (security-advisor remediation; new invariant I8; no backend change).** A Supabase security advisor (`rls_disabled_in_public`, dated 2026-06-22) flagged all 14 public tables on `soleur-inngest-prd` (`pigsfuxruiopinouvjwy`): RLS disabled **and** `anon`/`authenticated` holding full DML, with PostgREST serving the `public` schema — every Inngest event payload + tenant id (`account_id`/`workspace_id`/`event_user`) was reachable via the project's anon REST endpoint by anyone holding the anon key. Remediated by enabling RLS (not forced) with zero policies + revoking `anon`/`authenticated` table+sequence grants + revoking the recurrence-causing Supabase default privileges for grantor `postgres` (TABLES/SEQUENCES/FUNCTIONS). Non-breaking: Inngest connects as the `postgres` owner over the session pooler (owner bypasses non-forced RLS) — verified live (owner read intact: 3603 events; anon read → `permission denied` 42501; advisor 14→0; `pg_default_acl` 3→0). GDPR: confirmed real reachability of personal-data-bearing tables, but the anon/service_role keys were **never published or committed** (the exploitation precondition was never publicly available) and retained edge logs showed zero anon traffic; CLO determination = reachability-only, **no notifiable breach** (Art. 33/34 not triggered; edge-log retention < exposure window → access-log dimension recorded INCONCLUSIVE). No new sub-processor, no new secret (reuses `SUPABASE_ACCESS_TOKEN`), **no C4 structural change** (the `inngestPostgres` store + owner edge are already modeled and internal; only the element description is refreshed). Artifacts: `apps/web-platform/infra/inngest-rls/0001_enable_rls_lockdown.sql`, `apply-inngest-rls.yml`, `inngest-rls.test.sh`.
- **2026-06-18 (#5560) — secrets via environment, not argv (security hardening; no backend change).** The durable-backend ExecStart (#5450) expanded `INNGEST_POSTGRES_URI` / `INNGEST_REDIS_PASSWORD` / `INNGEST_SIGNING_KEY` / `INNGEST_EVENT_KEY` into the `inngest start` argv, exposing them via world-readable `/proc/<pid>/cmdline`. Hardened to env-delivery (new invariant **I7**): inngest reads all four from the inherited (owner-only) environment; the ExecStart passes no secret flag. Durable detection moved from the now-env-only `--postgres-uri`/`--redis-uri` argv substrings to the non-secret `--postgres-max-open-conns` sentinel across all three runtime parsers (kept lockstep by the existing drift-guard). The SQLite-only fail-safe `unset INNGEST_POSTGRES_URI`. No backend, sub-processor, or C4 change (secret-delivery is an attribute of the already-modeled `doppler -> inngest "Injects secrets"` edge). Exposed creds rotated post-deploy (`terraform taint` for Redis; Supabase Management API for the inngest-project Postgres password).
- **2026-06-17 (#5450, PR #5459) — durable backend; `status: adopting`.** Flipped state/queue persistence from SQLite + in-memory Redis to **Supabase Postgres (dedicated EU project, session pooler :5432) + self-hosted Redis (AOF on /mnt/data)**; see the amended `## Decision` + `## Trade-offs accepted`. Corrected the falsified "Hetzner backups" mitigation (root-disk SQLite is not backed up; loss trigger is `terraform apply -replace`, not every apply). Recorded the "Supabase already a sub-processor → no 5th sub-processor" reframe (the deferral's sole objection) and the new permanent **availability-coupling** trade-off (in-memory fallback removed → fail-closed on backend down). Phase-0 spike verdicts in `knowledge-base/engineering/operations/runbooks/inngest-server.md` § Durable backend. **Cross-ref ADR-046:** its "bounded by single-host SQLite durability (ADR-030)" line now points at the durable backend; the boot-arm/re-arm-every-deploy (ADR-046 I4) remains correct as the dedup-window recovery path — NOT made redundant. Reverts to `accepted` once the Phase-2 cutover wiped-volume invariant verifies in prod.
- **2026-06-17 (#5450) — no-SSH cutover orchestration (execution mechanism, no new decision).** The Phase-2 cutover's host-side steps (enumerate still-armed reminders; the opt-in wiped-volume durability verify) run through **HMAC-gated webhook hooks** on the existing `deploy.soleur.ai` ingress + a `workflow_dispatch` driver (`.github/workflows/cutover-inngest.yml`), mirroring the `infra-config` / `restart-inngest-server` pattern — NOT operator SSH (`hr-no-ssh-fallback-in-runbooks`) and NOT the `ci-deploy.sh` 4-field command parser. New host scripts (`inngest-enumerate-reminders.sh`, `inngest-rearm-reminders.sh`, `inngest-wiped-volume-verify.sh`, `cat-inngest-verify-state.sh`) reach `/usr/local/bin` via the no-SSH `infra-config` push (FILE_MAP↔DEST_SPEC lockstep); the stop/start sudoers grant (B3) is root-managed. No new sub-processor and **no new secret** (reuses `WEBHOOK_DEPLOY_SECRET` + CF-Access). This is the execution surface for the durable-backend decision above, not a new architectural decision. C4: no model change (the `deploy.soleur.ai`→host webhook edge already exists; no `.c4` enumerates individual hooks).

<!-- lint-infra-ignore end -->

