---
last_updated: 2026-07-20
---

<!-- 2026-07-20: PR fix-cron-statutory-repin-idempotency-guard (#6781) — GDPR legal-doc lockstep for the new `public.statutory_repin_send` table (migration 135): a **dispatch marker** for the statutory-deadline repin cron, added because the cron send-path carried no idempotency guard and a double-fire would have sent duplicate statutory-deadline email per user **per tick, indefinitely** (the sibling `notifyInboxItem` path already had the guard; this closed an asymmetry inside the same module). Schema: `item_id` (FK → `email_triage_items`, ON DELETE CASCADE), `tick_key` (a synthetic label, CHECK-pinned to the constant `headsup` or `daily:YYYY-MM-DD`), `created_at`; PK `(item_id, tick_key)`; RLS enabled with **zero policies** (service-role only). **NO `user_id` column by design** — the data-subject linkage lives solely on the parent ledger row, and adding one here would have created a second Art. 17 erasure surface for no benefit. **DSAR (Art. 15):** recorded in `DSAR_TABLE_EXCLUSIONS`, NOT the allowlist — the table holds no personal data (a FK to a row that is itself exported in full, plus an opaque tick label), and `joinVia` is not data-driven so an allowlist entry would export nothing while hard-failing `test/dsar-worker-per-row-where.test.ts`. **Art. 17 erasure:** no direct path needed — anonymisation already runs on the parent `email_triage_items` row. **Retention:** an EXPLICIT 90-day sweep (`purge_statutory_repin_send`, service-role only, called from the cron's existing `retention-purge` step), deliberately NOT retention-by-cascade: statutory parent rows are accountability evidence and are never purged, so the marker's cascade would in practice never fire. The same RPC doubles as the operator **release verb** (targeted delete when `p_item_id` is supplied), for the case where a send was marked but demonstrably never delivered. **NO new Article 30 Processing Activity** — this is a technical-measure marker within the existing **PA-27** (operator inbox triage); PA-27 limbs (c)/(f)/(g) amended in lockstep. Legal docs updated in lockstep: `privacy-policy.md` §7, `gdpr-policy.md` §8.4, `data-protection-disclosure.md` §5.3(a); `LEGAL_DOC_SHAS` repinned for the three edited `docs/legal/*.md`. **TC_VERSION bump: NOT required** — non-T&C edits only. A known residual is recorded rather than silently accepted: because the marker suppresses re-sends, a push that fails for any reason other than HTTP 410 no longer self-heals on the next tick, so `notifyOfflineUser` now emits a `statutory-notify-zero-delivery` warning when a statutory notification reaches zero devices. Brand-survival threshold: `single-user incident`. CPO sign-off returned 2026-07-20 (condition C2 upheld, C5 amended with a test tripwire). CLO attestation: pending counsel review. Output marked draft-requiring-professional-review. -->

<!-- 2026-07-09: PR feat-beta-crm-ui (#6239, issue #6172, ADR-102 UI phase) — GDPR legal-doc lockstep for migration 127, which EXTENDS the migration-126 owner-private beta-CRM (#6165) with ONE new table `beta_contact_access_log` + ONE new RPC `crm_get_contact_detail`. The table is an **append-only, owner-private read-accountability log** (GDPR **Art. 5(2)**): columns `id` / `user_id` (owner) / `contact_id` / `accessed_at` **only** — records WHICH contact the operator opened in the read-only CRM drawer and WHEN; **no note bodies, no third-party content**. Written **atomically** inside the `crm_get_contact_detail` SECURITY DEFINER RPC (fail-closed: no contact PII egress unless the audit row is written). RLS: SELECT-owner-only; NO owner-write policy (INSERT only via the RPC); table-level INSERT/UPDATE/DELETE REVOKEd from PUBLIC/anon/authenticated/service_role; RESTRICTIVE `<table>_jti_not_denied` policy + in-RPC jti-deny re-assert. **NO new Article 30 Processing Activity** — inherits PA-30 (Art. 6(1)(f) LIA `knowledge-base/legal/legitimate-interest-assessments/2026-07-07-beta-crm-lia.md`); **no new lawful basis, no new data subject, no new recipient / sub-processor / Chapter V transfer** (the log never leaves the DB — no note content, not surfaced to Anthropic). **DSAR (Art. 15):** `beta_contact_access_log` = article `15` access-only (controller-generated read metadata, NOT Art. 20-portable) — registered in `server/dsar-export-allowlist.ts` + read in `server/dsar-export.ts`. **Art. 17 erasure:** no independent retention clock — the composite FK `(contact_id, user_id) → beta_contacts(id, user_id) ON DELETE CASCADE` means the existing `crm_erase_contact` RPC and whole-store account-deletion CASCADE both sweep it. Legal docs updated in lockstep: `privacy-policy.md` §4.7, `gdpr-policy.md` §3.13 (+ Eleventy mirror §3.13), `data-protection-disclosure.md` §2.3(ad) + §5.3(a) + §5.3(c); `LEGAL_DOC_SHAS` repinned for the three edited `docs/legal/*.md`. **NO `article-30-register.md` amendment** (no new processing activity — same PA-30). **TC_VERSION bump: NOT required** — non-T&C edits. Brand-survival threshold: `single-user incident`. CLO attestation: **DISCHARGED** (CLO-agent-attested, Soleur-as-tenant-zero v1) — `knowledge-base/legal/audits/2026-07-counsel-review-6172.md`; external counsel re-review on the PA-30 frontmatter triggers. Note: the privacy-policy and DPD Eleventy mirrors carry pre-existing #6165 legacy drift (they lack the §4.7 / §2.3(ad) beta-CRM prose entirely); body-equivalence is not gated for non-T&C docs, so this is a carried-forward mirror-sync backlog item, not a new gap. Output marked draft-requiring-professional-review. -->

<!-- 2026-07-07: PR feat-beta-conversation-capture (#6165, ADR-102) — GDPR legal-doc lockstep for the new owner-private beta-tester / prospect CRM (migration 126): three owner-only tables `beta_contacts` (contact head) + `interview_notes` (append-only dual-lens conversation notes) + `beta_contact_stage_transitions` (append-only velocity history). **NEW Article 30 Processing Activity 30** — the operator is CONTROLLER for third-party (beta-tester/prospect) personal data and Jikigai is PROCESSOR; lawful basis legitimate interest (Art. 6(1)(f)) with a dedicated LIA (`knowledge-base/legal/legitimate-interest-assessments/2026-07-07-beta-crm-lia.md`) + an Art. 14 notice to the involuntary third-party subject; **no special-category (Art. 9) data solicited**. **New recipient / Chapter V transfer:** the `cro`/`cpo` agent-read path surfaces record content to **Anthropic (US)** under the existing Anthropic DPA (SCCs Modules 2+3) — no NEW sub-processor. **DSAR (Art. 15 + 20):** `beta_contacts`/`interview_notes` (owner-provided → 15+20), `beta_contact_stage_transitions` (controller-generated → 15) added to `DSAR_TABLE_ALLOWLIST` + export chain in `server/dsar-export.ts`. **Retention:** 24 months from `last_contact` (COALESCE `created_at`) via in-DB `pg_cron`. **Art. 17 erasure:** owner whole-store via `ON DELETE CASCADE` from `public.users` (no anonymise RPC — no statutory-retention class, ADR-102 §4); individual third-party via the auditable service-role-only `crm_erase_contact` RPC. Legal docs updated in lockstep: `privacy-policy.md` §4.7, `gdpr-policy.md` §3.13 + §6, `data-protection-disclosure.md` §2.3(ad) + §5.3 (+ Eleventy mirror for the gdpr §3.13 heading); `article-30-register.md` PA-30 added; `LEGAL_DOC_SHAS` repinned for the three edited `docs/legal/*.md`. **TC_VERSION bump: NOT required** — non-T&C edits. Brand-survival threshold: `single-user incident`. CLO attestation: pending counsel review (LIA channel/notice-mechanism analysis is the named residual). Output marked draft-requiring-professional-review. -->

<!-- 2026-07-05: PR feat-severity-ranked-inbox (#6007) — GDPR legal-doc lockstep for the new `public.inbox_item` table (migration 122, ADR-085): a workspace-grain operational-notification store for the founder's severity-ranked attention inbox. It holds server-generated notification metadata only — `workspace_id` (FK), an optional `user_id` (recipient), `severity`, `source`, a server-generated `title`, ids-only `source_ref`, and read/act/archive timestamps; **content-minimized — no agent output, message body, or email content**. **DSAR (Art. 15):** personally-targeted rows (`user_id` set) are INCLUDED in the self-serve export — their `created_at`/`read_at`/`acted_at`/`archived_at` notification-interaction history is personal data held in no other exported table (canonical rationale in `apps/web-platform/server/dsar-export-allowlist.ts` `inbox_item` allowlist entry + the worker read in `server/dsar-export.ts`, both added in this branch). **Art. 20 portability does NOT apply** — `title`/`source_ref` are controller-generated, not user-provided. Workspace-broadcast rows (`user_id` NULL) are non-personal and excluded by the per-user export filter. **Art. 17 erasure:** `ON DELETE CASCADE` from `public.users` and `public.workspaces` (non-WORM operational data; no anonymise RPC). **NO new Article 30 Processing Activity** — operational-notification metadata under legitimate interest (Art. 6(1)(f)); no new personal-data category beyond the founder's own account association. Legal docs updated in lockstep: `privacy-policy.md`, `gdpr-policy.md`, `data-protection-disclosure.md` (`**Last Updated:**` changelog); `LEGAL_DOC_SHAS` repinned for the three edited `docs/legal/*.md`. **NO `article-30-register.md` amendment** (no new processing activity). **TC_VERSION bump: NOT required** — non-T&C edits (per `apps/web-platform/scripts/check-tc-document-sha.sh`: the non-T&C SHA repin is the only gate). Brand-survival threshold: `single-user incident`. CLO attestation: pending counsel review. Output marked draft-requiring-professional-review. -->

<!-- 2026-06-30: PR feat-phase2-git-data-lease-fencing (epic #5274, Phase 2 PR A) — GDPR legal-doc lockstep for the new `public.worktree_write_lease` table (migration 116, ADR-068 §2): a per-(workspace_id, worktree_id) write-lease coordination row for the multi-host `/workspaces` layer. **NO new Article 30 Processing Activity** — the table holds NO personal data: columns are `workspace_id` (FK), `worktree_id`, `host_id` (a host-**stable infrastructure server identity**, explicitly NEVER an `auth.uid()` per migration 116 — it identifies a machine, not a data subject), `lease_generation`, `acquired_at`, `heartbeat_at`. No user-provided content; no personal-profile data. **DSAR (Art. 15/20):** EXCLUDED from the self-serve export — canonical rationale in `apps/web-platform/server/dsar-export-allowlist.ts` `worktree_write_lease` exclusion entry (added in this branch); no re-evaluation trigger (the table is infra coordination, not a future-promotable audit of operator actions). **Art. 17 erasure:** satisfied by `ON DELETE CASCADE` from `public.workspaces` (the row has zero audit lineage — no anonymise RPC; proven by the AC5 cascade integration test). **No new sub-processor, recipient, or third-country transfer** (substrate internal to the existing Supabase row). Legal docs updated in lockstep: `privacy-policy.md`, `gdpr-policy.md`, `data-protection-disclosure.md` (`**Last Updated:**` changelog — no new disclosure section, as there is no personal-data processing to disclose); `LEGAL_DOC_SHAS` repinned for the three edited `docs/legal/*.md`; Eleventy mirrors synced. **NO `article-30-register.md` amendment** (no new processing activity). **TC_VERSION bump: NOT required** — no new user-facing data collection, recipient, or sub-processor (per `apps/web-platform/scripts/check-tc-document-sha.sh`: the non-T&C SHA repin is the only gate). Brand-survival threshold: `single-user incident`. CLO attestation: pending counsel review. Output marked draft-requiring-professional-review. -->

<!-- 2026-06-16: PR #5342 (feat-routines-management, #5345) — GDPR legal-doc lockstep for the new `public.routine_runs` operational run-log (migration 107). Append-only WORM audit log for the `EXPECTED_CRON_FUNCTIONS` scheduled routines: ONE row per terminal run (which routine, started/ended/duration, completed/failed status, trigger_source = scheduled/manual/agent, actor_class = system/human/agent, scrubbed+truncated error_summary for failures), written centrally by the run-log Inngest middleware via the service-role-only `write_routine_run` SECURITY DEFINER RPC. **New Article 30 PA-29** (Art. 6(1)(f) legitimate interest — operational audit of operator-/agent-triggered runs; three-part balancing test inline; Art. 22 negative determination; retention indefinite as a low-volume audit log). Personal-data footprint is `actor_id` + `delegating_principal` (FKs to `public.users(id)` `ON DELETE RESTRICT`); on the single-operator tenant the ONLY data subject is Soleur itself (operator's own id on off-schedule human/agent runs; NULL for scheduled system runs). **Art. 17:** `anonymise_routine_runs(uuid)` SECURITY DEFINER RPC NULLs both actor columns under the privilege-free `app.worm_bypass = 'on'` GUC carve-out (post-087 pattern), PRESERVING the append-only audit row, called in `server/account-delete.ts` BEFORE `auth.admin.deleteUser` — MIRRORS the `anonymise_workspace_activity` (PA-24) pattern. **DSAR (Art. 15/20):** EXCLUDED from the self-serve export at present (operational system-audit log, not personal-profile data — canonical rationale already in `apps/web-platform/server/dsar-export-allowlist.ts` `routine_runs` exclusion entry, added in this branch); promote to `DSAR_TABLE_ALLOWLIST` (`actor_id`, Art. 15) when a non-Soleur tenant exists or the `dsar-export.ts` chain is wired (re-evaluation trigger). **No new sub-processor, recipient, or third-country transfer** (substrate internal to the existing Supabase row). Legal docs updated in lockstep: `privacy-policy.md` §4.12, `gdpr-policy.md` §3.7, `data-protection-disclosure.md` §2.3(ac), `article-30-register.md` PA-29; `LEGAL_DOC_SHAS` repinned for the three edited `docs/legal/*.md`. **TC_VERSION bump: NOT required** — internal-audit amendment, no new user-facing data collection, no new recipient, no new sub-processor (per `apps/web-platform/scripts/check-tc-document-sha.sh`: the non-T&C SHA repin is the only gate; the TC re-acceptance bump is reserved for material T&C-body changes). Data-model posture (FKs, WORM bypass, anonymise approach) settled in Jean's commit be3f7061 — NOT redesigned. Brand-survival threshold: `single-user incident`. CLO attestation: **SIGNED-OFF** (CLO-agent-attested, Soleur-as-tenant-zero v1) at `knowledge-base/legal/audits/2026-06-counsel-review-5345.md` — DISCHARGED (prose zero-drift vs migration 107 / account-delete.ts / dsar-export-allowlist.ts / run-log.ts; Art. 6(1)(f) LIA sufficient; Art. 22 negative; DSAR exclusion sound; indefinite retention acceptable). Output marked draft-requiring-professional-review. -->

<!-- 2026-06-15: PR #5363 (feat-reasoning-chat-boxes, #5370) — the Concierge (cc-router conversation runtime) now emits two kinds of user-facing narration via MCP tools: `narrate` (a TRANSIENT live status line — NOT persisted, live-only, mirrors the existing ephemeral debug stream, no new data category) and `summarize` (a DURABLE per-turn plain-language outcome persisted as ONE `public.messages` row with a new additive + nullable `message_kind='turn_summary'` discriminator, migration 105; `role='assistant'`, `user_id=founderId`, `workspace_id=founderId` solo-pin; redacted content; written only on successful turns, dropped on abort/stop). **NOT a new Processing Activity** — an AMENDMENT to existing PA-2 (conversation-runtime / `messages`): sub-clause (b) Purposes + (g) TOMs amended in `article-30-register.md`; no new PA minted. The summary is the USER'S OWN data — it inherits the `messages` table's existing DSAR export (table-enumerated `select("*")`) + Art-17 erasure (FK cascade on conversation delete AND account delete), and because the Art-15(4) author-redaction keys on `user_id` (NOT role), the `user_id=founderId` insert keeps the summary content UN-redacted in the user's own export. `message_kind` is classified in `MESSAGE_NON_REDACT_ALLOWLIST` (`server/dsar-export.ts`) as a structural row-shape discriminator (NOT third-party content), enforced by CI sentinel `dsar-message-redact-fields-sweep.test.ts`; the summary CONTENT lives in `content` (already a redact-field). **Art. 22 negative determination:** the turn_summary feature involves NO automated decision-making producing legal or similarly significant effects on the data subject — descriptive narration of work the user requested, no decisional output about the user. **No Art. 9 special-category data.** **Residual risk (cross-tenant prose — the durable-breach class):** another tenant's data named in agent prose could persist into an exportable record; controls are (1) the RLS tenant-client `getFreshTenantClient(founderId)` (solo-pin workspace_id), (2) a **system-prompt directive** forbidding the agent from naming any entity/org/person/file path/skill/issue outside the user's own context, and (3) PR-time `user-impact-reviewer`. The redaction probe scrubs paths/secret-shapes ONLY (drop-on-trip, fail-loud to Sentry) — it does NOT scrub cross-tenant prose; the directive is the operative (directive-only) control and the redaction-probe tripwire is the fail-loud secret-shape backstop. NOTE: `assertWriteScope` is a no-op forward-compat seam, NOT a control today. Migration 105 additive + nullable (no NOT-NULL sweep); `messages_message_kind_chk` pins `turn_summary` rows to `role='assistant'`. Brand-survival threshold: `single-user incident`. CLO attestation pending. Output marked draft-requiring-professional-review. Legal docs updated in lockstep: `article-30-register.md` PA-2 (b)+(g)+lawful-basis, `privacy-policy.md` §4.7, `gdpr-policy.md` §3.12, `data-protection-disclosure.md` §2.3(i). -->

<!-- 2026-06-11: feat-operator-inbox-delegation (#5103, Ref) — GDPR legal bundle for the 3rd ingress (Proton Sieve forward-and-keep → Resend Inbound → svix webhook → Inngest → deterministic statutory fast-path OR read-only Anthropic summarizer → email_triage_items, NO body column). New Article 30 **PA-27** (involuntary data subjects — inbound senders; Art. 6(1)(f); Art. 14(5)(b) public-notice posture; retention probe 7d / non-statutory 365d / statutory per accountability period with Art. 17(3)(b) carve-out; vendor windows disclosed: Resend 30d no-delete-API, Anthropic 30d default, self-hosted Inngest event store: no automatic deletion — persists until operator-side store maintenance; corrected 2026-06-11 during the bundle cross-consistency audit from "~24h" (event-id dedup window, not retention), and DPD §2.3(o)'s uncorroborated "30-day rolling SQLite window" for the same store was corrected in lockstep; bound tracked as Active Items (iv) below). New LIA `knowledge-base/legal/legitimate-interest-assessments/2026-06-11-operator-inbox-triage-lia.md` (incl. involuntary-subject balancing + Art. 17 path via GUC-gated RPCs). DPIA screening memo `knowledge-base/legal/audits/2026-06-11-dpia-screening-operator-inbox-triage.md` — full DPIA NOT required at single-operator scale; three NAMED accepted residuals: (a) Art. 9 content surviving into the persisted summary despite the prompt instruction, (b) prompt-injection-distorted summaries (read-only/no-tools bound), (c) Resend 30-day no-delete window. New `knowledge-base/legal/statutory-response-catalog.md` (anchors #breach/#service-of-process/#dsar/#regulator consistent with statutory-rules.ts catalogExcerpts). Anthropic DPA snapshot scope-amended (+PA-27 Jikigai-keyed summarizer) with §(g)-precedent honest residual-risk admissions (#4954 shape). Resend Vendor DPA row scope-amended (inbound raw-mail custody). Privacy Policy §4.13 + §5.9, GDPR Policy §3.10, DPD §2.3(aa) + §4.2 updated in lockstep (canonical + Eleventy mirrors); LEGAL_DOC_SHAS repinned. Output marked draft-requiring-professional-review; counsel review pending as a single bundle. Brand-survival threshold: `single-user incident`. -->

<!-- 2026-06-08: PR #5014 (#5005) — converged the remaining DSAR-surface read (`server/dsar-export.ts` `resolveDsarWorkspacePath`) off the stale `users.workspace_path` column onto the workspace-id resolver (`workspacePathForWorkspaceId(expectedUserId)`, solo/N2), alongside the kb/sync, attachment-pipeline, vision, and repo/status workspace-id-resolver convergence (the ADR-044 `users → workspaces` read-path sweep that PR #5003's PIR `shared-document-404-post-adr044-postmortem.md` tracked as action item #5005). **DSAR impact:** FIXES an Art. 15+20 completeness gap — data subjects provisioned after the ADR-044 relocation had an empty/stale `users.workspace_path`, so the self-serve export silently omitted their OWN workspace files; the id-keyed resolver restores them. No new processing activity, personal-data category, lawful basis, recipient, or sub-processor; the solo-keyed resolver (single-arg, no active-workspace claim) structurally prevents the inverse over-export of a shared-workspace owner's files into a member's export. **No Article 30 amendment** (PA-2 DSAR coverage unchanged — a read-path completeness remediation, not a new processing purpose). No migration. Privacy Policy + GDPR Policy + DPD updated in lockstep (Last-Updated prose). Brand-survival threshold: `single-user incident`; CPO sign-off operator-attested at plan time; CLO attestation via /ship Phase 5.5 Counsel-Review gate. -->

<!-- 2026-06-04: PR #4954 (#4952, ref #4949) — AUP §5.7 + §2 clause and T&C §3a.7 + §10.4 + §9 bullet disclose the Web Platform agent's autonomous (auto-run) shell-command surface shipped by #4949 (default-ON, first-run owner consent soft-gate). Residual-risk admission to CLO bar: the blocklist (`BLOCKED_BASH_PATTERNS`) is illustrative not exhaustive, a non-blocked command can auto-run and change/delete files in the connected workspace; mitigations named honestly (git-backed recovery + visible-in-chat) not as a safety guarantee; own-workspace commands distinguished from third-party sends (Art. 22 / §3a.1-3a.6). Substantively consistent with the in-product `AUTONOMOUS_DISCLOSURE_COPY` LOCKED COPY banner (no second source of truth). Tier-1 MATERIAL T&C change (new disclaimer-of-warranty text, §10.4) → `TC_VERSION 2.2.1 → 2.3.0` (forces `/accept-terms` re-acceptance — correct UX for a residual-risk admission); `TC_DOCUMENT_SHA` + `LEGAL_DOC_SHAS["acceptable-use-policy"]` repinned; seed-script TC_VERSION parity (`seed-dev-users.sh` + `seed-qa-user.sh` → 2.3.0). Eleventy mirrors updated in 3-way lockstep (heading-sequence + Last-Updated date + T&C body-equivalence green). **No Article 30 amendment.** The interactive Concierge chat agent runtime (the `cc-dispatcher` / `permission-callback` conversation surface that persists user prompts, assistant responses, and tool-call metadata) is already registered as **Processing Activity 2 — Conversation Data (Messages, Turns, Usage Telemetry)**. Autonomous command execution is a tool-call on the operator's own connected workspace within that already-registered runtime — no new processing purpose, personal-data category, recipient, or sub-processor. The per-command human-approval step that default-ON autonomy removes is a **workspace-safety control on the operator's own systems, not a registered PA-2 Art. 32 §(g) TOM protecting third-party data subjects** (PA-2's §(g) measures are RLS / per-user JWT mint / write-boundary sentinel / data-minimisation / attachment isolation / DSAR redaction — none gate shell-command approval), so its removal narrows no registered TOM and triggers no PA-2 limb amendment. (NOT PA 21/22 — those are the distinct Inngest `agent.spawn.requested` TodayCard leader-prompt runtime, a different surface.) gdpr-gate: no regulated-path diff (no schema / migration / auth / api route), zero Critical/Important findings. Brand-survival threshold: `single-user incident`; CPO sign-off (plan) + CLO sign-off (Tier-1 material) operator-attested in PR body. No new sub-processor. -->
<!-- 2026-05-26: PR #4509 (#4396) — bug fix: restored missing workspace_id in DSAR export enqueue INSERT; NOT NULL violation since mig 059 / PR #4225 caused every Art. 15 self-serve export to 500. No data handling change; application-layer INSERT was missing the value. Both callers (HTTP route + MCP agent tool) fixed. No new sub-processor engaged. -->

<!-- 2026-05-26: PR #4491 (#4358) — extended DSAR conversations fetch to include participated workspace conversations for Art. 15 completeness; subject-authored messages in co-member-owned conversations now included in the bundle. Legal docs updated in lockstep (privacy-policy.md, gdpr-policy.md, data-protection-disclosure.md, compliance-posture.md, Eleventy mirrors). -->

<!-- 2026-05-25: PR-1 of umbrella #4456 (feat-audit-env-flags-flagsmith-policy / PR #4455) — Flagsmith sub-processor disclosure landed across 8 legal artifacts in lockstep ahead of PR-2's code-side `orgId` identity-trait egress. New DPA snapshot at `knowledge-base/legal/data-processing-agreements/flagsmith.md`. Bullet Train Limited row added to Vendor DPA Status table (AUTO via Flagsmith ToS Data Processor Appendix; UK IDTA + EU SCCs M2+3; UK controller → AWS-hosted global edge `edge.api.flagsmith.com`; region not pinned on managed-SaaS tier). Bullet Train Ltd added to data-processing-agreement-template.md Schedule 2 + §11.2 SCCs classification, article-30-register.md PA-1 + PA-2 Recipients + Vendor Mapping, privacy-policy.md §5.15, data-protection-disclosure.md §4.2 (root + Eleventy mirror diff zero), gdpr-policy.md §2.2. AUP §10 sub-processor list does NOT exist; AUP grep at PR-1 time returns zero hits (no lockstep update required). tenant-dpa-register.md Re-evaluation triggers extended to document the §6.1 30-day clock state baseline (zero customer DPAs today → clock NOT triggered). `transient: true` MANDATORY on every `getIdentityFlags(...)` call (data-min lever — opts out of Flagsmith server-side identity persistence; verified against SDK signature `flagsmith-nodejs/build/cjs/sdk/index.d.ts:89`). Single-control gating (updated 2026-05-26, PR #4481): Flagsmith segment rule is the sole per-org gate; env-allowlist removed. Brand-survival threshold: `single-user incident`; CPO sign-off operator-attested in PR body. Blocks: PR-2 (combined per-org capability + WORM audit + both flag migrations) cannot land until this PR is merged. -->

<!-- 2026-05-25: PR feat-one-shot-jti-revoke-rls-3930-3932 (PR #4418) — closes the cross-process JWT-deny gap PR-E (#3922) left open. Bundles two deferred-scope-outs into one migration (068): #3930 admin `revoke_jti(uuid, uuid, text)` SECURITY DEFINER writer (with ON CONFLICT DO NOTHING for operator-retry idempotency) + founder-readable `my_revocation_status()` reader, and #3932 PostgREST-side RLS predicate via 21 RESTRICTIVE `<table>_jti_not_denied` policies on every tenant table (`conversations`, `messages`, `users`, `api_keys`, `audit_byok_use`, `scope_grants`, `audit_github_token_use`, `kb_share_links`, `push_subscriptions`, `user_concurrency_slots`, `dsar_export_jobs`, `action_sends`, `template_authorizations`, `byok_delegations`, `workspaces`, `workspace_members`, `workspace_member_attestations`, `user_session_state`, `message_attachments`, `organizations`, `workspace_member_removals`). New helper `is_jti_denied_from_jwt()` reads `current_setting('request.jwt.claims', true)::jsonb->>'jti'` — first such predicate in the codebase; flagged for architecture-strategist + data-integrity-guardian review. Belt-and-suspenders USING + WITH CHECK on every policy. GRANT EXECUTE ON `is_jti_denied(uuid)` TO authenticated added so the wrapped reader can be transitively called under the authenticated role context (jti-enumeration oracle acknowledged: boolean-only return + 2^128 UUID space → practical enumeration infeasible). Operator CLI at `apps/web-platform/scripts/revoke-jti.ts` (mirrors `byok-revoke.ts` shape: bun shebang, named-flag argv, createServiceClient, post-RPC re-read sanity, dev/prd URL stdout disclosure; JWT-scope-only — does NOT terminate the underlying Supabase session). New WSMessage variant `revocation_notice {reason, deniedAt}` discriminates the generic "Authentication unavailable; retry shortly" toast into "Your session was revoked. Reason: X. Contact support." when the ws-handler `tenantFor` catch site sees `RuntimeAuthError.cause === 'denied_jti'`; await-ed before ws.close() so the notice is delivered before the socket closes. No Article 30 register amendment (PA1 already covers `denied_jti` from PR-B; jti = random UUID, not personal data; founder_id already in PA1 scope). Verify-068 sentinel asserts 31 invariants (3 functions present + SECURITY DEFINER + STABLE; 21 RESTRICTIVE policies; service-role-only revoke_jti; authenticated GRANT matrix on my_revocation_status + is_jti_denied; anon-revoke matrix on all three new functions). Brand-survival threshold: `single-user incident`. Closes #3930 + #3932. -->

<!-- 2026-05-25: PR #4351 (feat-dsar-author-redaction-4319) — Art. 15(4) "rights of others" author-only message redaction in the DSAR exporter at `apps/web-platform/server/dsar-export.ts`. In shared workspace conversations (post-mig 059), messages authored by a non-subject within a subject-owned conversation now have all free-text + namespace-leaking columns nulled per the closed `MESSAGE_REDACT_FIELDS` set (13 columns: `content`, `tool_calls`, `usage`, `draft_preview`, `action_class`, `tier`, `source`, `owning_domain`, `urgency`, `trust_tier`, `source_ref`, `leader_id`, `template_id`). Raw `user_id` of the non-subject is replaced with a per-bundle salt-scoped pseudonym (`member_<hex12>`) via `pseudonymiseUserId(rawUserId, crypto.randomBytes(32))` — salt held in closure of `exportSqlTable`, NEVER persisted to manifest/audit/log emission (AC5 grep enforcement). Attachments cascade via allowlist semantic on `subjectAuthoredMessageIds` (orphan / foreign-author parents → fail-closed). Manifest schema 1.0.0 → 1.1.0 adds `redactions[{path, reason: "art-15-4-rights-of-others", count}]` field disclosing the redaction location + count to the subject per EDPB Guidelines 01/2022 §176. Legacy `user_id IS NULL` rows REDACTED under fail-closed default (`LEGACY_NULL_IS_SUBJECT = false`) per Phase 0.3 audit: the post-mig 059 `messages_workspace_member_insert` RLS policy gates on `is_workspace_member(workspace_id, auth.uid())` — NOT on `user_id` matching conversation owner — and every server INSERT path omits `user_id`. New CI sentinel test `apps/web-platform/test/dsar-message-redact-fields-sweep.test.ts` parses every migration for `public.messages` column additions and asserts each is classified in `MESSAGE_REDACT_FIELDS` or `MESSAGE_NON_REDACT_ALLOWLIST`; future free-text column additions trip CI until the reviewer classifies them. Legal disclosure prose updated in Privacy Policy + GDPR Policy §6.1.b Right of Access + DPD §5.3(l) + Article 30 PA-2 §(g) TOM (11) to enumerate the 13 redacted columns and reference the source-of-truth constant + sentinel test. Multi-agent review (11 agents): security-sentinel + data-integrity-guardian orthogonal concurrence expanded the field-list from 5 to 13 columns at review time (closed-list-under-redaction defect class — learning at `knowledge-base/project/learnings/2026-05-25-closed-field-list-must-classify-at-value-shape-not-column-name.md`); user-impact-reviewer APPROVED per AC9 of the plan. Out of scope: Art. 15 completeness for subject-authored messages in foreign-owned workspace conversations (#4358); Art. 5(2) accountability WORM extension on `dsar_export_audit_pii` for redaction events (#4359); pre-existing `enqueueExport.workspace_id` production bug surfaced by this PR's integration test (#4396). Brand-survival threshold: `single-user incident`; CPO sign-off operator-attested in PR body. No new sub-processor engaged. -->

<!-- 2026-05-25: PR #4290 (feat-byok-delegations-4232 / PR-A #4232) — `byok_delegations` WORM ledger added to DSAR_TABLE_ALLOWLIST in apps/web-platform/server/dsar-export-allowlist.ts as Art. 15+20 (the grantor explicitly authorised a workspace co-member as grantee to fund their BYOK runs under a per-day/per-hour USD cap — the row IS the Art. 6(1)(b) contract between grantor and grantee inside a shared workspace per migration 064 LAWFUL_BASIS header). OR-semantics over five actor columns (grantor_user_id, grantee_user_id, created_by_user_id, revoked_by_user_id, cap_updated_by_user_id) per per-row-WHERE lint AC30; one explicit `.eq()` chain per column in `apps/web-platform/server/dsar-export.ts` with id-set merge/dedupe so a row in which the user appears in multiple positions (e.g. grantor == created_by) is not double-counted. Article 30 PA-21 row (forthcoming in PR-B follow-up) covers the delegation activity; v1 substrate is gated by the `byok-delegations` feature flag (default-off in `lib/feature-flags/server.ts`) so production exposure is internal-only at PR-A merge — full Art. 17 cascade RPC (`art_17_anonymise` enum value reserved in mig 064 `revoked_reason` CHECK) and cost-writer route through `check_and_record_byok_delegation_use` ship in PR-B. Service-role allowlist extended for `apps/web-platform/server/byok-resolver.ts` (workspace + organization lookup BEFORE the BYOK lease opens, where the caller's tenant JWT is not yet established; callerUserId is server-derived per SS F3 invariant; the `resolve_byok_key_owner` RPC is SECURITY DEFINER search_path-pinned per cq-pg-security-definer-search-path-pin-pg-temp). Migration 064 carries `to_regclass` preconditions for `public.users` + `public.workspaces` per the #4338 schema-vs-ledger drift class. -->

<!-- 2026-05-19: PR-H #4066 (feat-daily-priorities-multi-source-3244) — Article 30 Register adds Processing Activity 17 (GitHub-sourced priority signals; PA-15 retained for LinkedIn per PR #4081, restored from origin/main during review F3 fix). Legal basis Art. 6(1)(f) legitimate interest; sub-processor GitHub Inc. (Microsoft Corporation); render-time redactGithubSourcedText is the load-bearing Art. 14 minimization gate per plan TR6 amendment. CVE / secret-scan draft_preview summary stripped server-side (review P2). New audit ledger audit_github_token_use + record_github_token_use SECURITY DEFINER RPC + WORM trigger + anonymise_audit_github_token_use Art. 17 cascade (migration 052). New webhook delivery dedup processed_github_events (autovacuum + 30-day partition rotation retention). Drafts surfaced display-only with Cache-Control: private, max-age=60 on the Today endpoint. /dashboard/audit/github ships as founder-facing read surface (empty-state pre-PR-H+1 #4098). DPD adds GitHub Inc.; Privacy Policy adds "Data from third parties"; AUP adds card-screenshot-redaction clause. Counsel review operator-attested at knowledge-base/legal/audits/2026-05-counsel-review-4066.md (precedent: PR #4081 / #4051). -->

<!-- 2026-05-19: PR #4065 (feat-trust-tier-pr-h-external-classes / PR-H #4077) — `action_sends` WORM ledger added to DSAR_TABLE_ALLOWLIST in apps/web-platform/server/dsar-export-allowlist.ts as Art. 15-only (founder did not "provide" the signature row; platform-generated evidence of their click). Body and recipient stored as SHA-256 hashes only (raw values never persisted). WORM trigger + anonymise_action_sends RPC handle Art. 17 erasure separately via server/account-delete.ts (runs BEFORE anonymise_scope_grants per FK ordering). Article 30 PA-16 row records the activity; mig 051 forward + verify ship the DB-side TOMs (UNIQUE constraints on action_sends.message_id and scope_grants active-grant pair, enum-absence CHECK on messages.action_class, SECURITY DEFINER search_path pin assertions). Privacy Policy §8.3 + DPD §2.3(r)/(s) + T&C §3a.2 updated (TC_VERSION 2.0.0 → 2.1.0). -->
<!-- 2026-05-19: PR #4051 (feat-legal-linkedin-page-4051) — Legal-track artifacts gating Phase 5.4 of the #4046 LinkedIn Community Management API re-application runbook (K-bis appeal upload). New LIA at knowledge-base/legal/legitimate-interest-assessments/2026-05-19-linkedin-org-page-lia.md (Art. 6(1)(f) three-part test for LinkedIn Page operation + aggregate Page Insights consumption). Privacy Policy §4.10 + §5.12 + §5.13 + §6 + §7 + §8.1 + §10 extended (Art. 13(1)(e) recipient disclosure for LinkedIn Ireland + Microsoft Ireland; Art. 17 carve-out for LinkedIn-published content per EDPB Guidelines 5/2019). DPD §2.3(p) + §4.2 + §6.4 + §10.3 mirror the Privacy Policy changes. Article 30 PA15 lawful-basis (ii) cross-reference promoted from forward-looking to live LIA path. Brand-survival threshold: single-user incident (CLO + CPO sign-off carried forward from #4046 brainstorm). Counsel review pending across LIA + Privacy Policy + DPD as a single bundle. -->
<!-- 2026-05-18: PR (legal-amend-aup-4-8-neural-data-d-bundle) — AUP §4.8 enumeration extended in response to CLO attestation on #3998 (PR #3988 follow-through). Adds (g) neural data per Cal. Civ. Code §1798.140(ae)(1)(G) (SB 1223, 2024) and (h) (D)-bundle remainder (racial/ethnic origin, religious/philosophical beliefs, union membership) for explicit notice-and-choice clarity. Catch-all paragraph retained as belt-and-suspenders. No regulatory drift, no TC_VERSION bump, no Article 30 register change (PA2 special-categories cell already cross-references the AUP). Closes #3998 (CLO attested + amendment landed). -->
<!-- 2026-05-18: PR #3988 (feat-one-shot-issue-3921-aup-art9-ccpa-spi-warning) — AUP §2 scope broadened to name app.soleur.ai + chat-attachments. New §4.7 prohibits GDPR Art. 9 (special categories) and Art. 10 (criminal convictions) submissions via prompts or attachments. New §4.8 prohibits CCPA SPI (Cal. Civ. Code §1798.140(ae)) via same surfaces. Closes #3921 and PR-D PA2 line-62 follow-up note (note itself removed in next PA2 amendment). No T&C / Privacy Policy / GDPR Policy / DPD changes; no TC_VERSION bump (AUP is incorporated by reference under T&C §9). -->
<!-- 2026-05-17: PR #3922 (feat-pr-e-audit-byok-jti-deny) — PR-E of the Soleur server-side agentic runtime track closes the Art. 5(2) accountability gap. is_jti_denied(jti) deny-list reader wired into the JWT-mint boundary at lib/supabase/tenant.ts; CI-enforced writer-sweep over every runWithByokLease(...) call site asserts persistTurnCost coverage; audit_byok_use WORM trigger pinned by integration test. No new migration (037 primitives shipped via PR-B #3395). No Article 30 register amendment (deny-list table in PA1 auth scope; jti is random UUID, not personal data; Sentry mirror covered by PA2 from PR-D). 7 deferred-scope-out follow-ups filed (#3928–#3934) for residual surfaces (RLS-side predicate, admin revoke_jti RPC, in-flight client invalidation, etc.). Note: PR-E plan's AC18 referenced a non-existent "Art. 5(2) audit-writer gap" Active Items row (forward-looking assumption that an earlier PR had authored it); this row is added directly to Completed Compliance Work instead. #3887 was closed manually 3 min before PR-E merge. -->
<!-- 2026-05-14: PR #3744 (feat-soleur-managed-deploy-substrate-3723) — Phase 0 legal pre-flight for the multi-tenant deploy substrate. New artifacts: ToS-research (4-provider verdict sentinels), LIA (Art. 6(1)(f) three-part test), ADR-030 (substrate architecture), gdpr-gate report (zero Critical), Article 30 register Processing Activity 10 row. Privacy-policy / DPD sub-processor disclosure for non-Soleur tenants explicitly scoped OUT of v1 — re-evaluation trigger: first non-Soleur tenant onboarding. Multi-tenant trajectory means each new non-Soleur tenant deploy triggers DPA signing + Art. 28(4) flow-down obligations. -->
<!-- 2026-05-12: PR-A2 (#3603/#3648) — Privacy Policy §4.7 Art. 13(3) prior-disclosure refresh for messages.usage column (CC_PERSIST_USAGE=true in Doppler prd). Article 30 RoPA register created at knowledge-base/legal/article-30-register.md. Both draft — counsel review pending. -->
<!-- 2026-05-11: Compound Promotion Loop (#2720) registered as Active Item with DPIA candidacy note (Art. 35; defer formal assessment 4 weeks). -->

<!-- 2026-05-11: R15 mitigation for #2719 landed via #3543 (ruleset PUT to #14145388) -->
<!-- 2026-05-11: R15 follow-up D1 landed via PR #3555 (origin issue #3544) — daily bypass_actors audit closes the audit-log-only blind spot in #3543. Smoke-verified 2026-06-30 (Inngest manual trigger, live==canonical GREEN). -->
<!-- 2026-05-11: R15 follow-up D2 (#3545) landed — empirical audit confirmed CodeQL coverage on bot PRs is satisfied (neutral conclusion). No remediation needed. Runbook: knowledge-base/engineering/operations/runbooks/codeql-bot-coverage.md -->


# Legal Compliance Posture

Living status document for vendor DPAs, legal documents, and compliance action items. Domain leaders read this during assessment to avoid asserting stale or incorrect status.

## Legal Documents

| Document | Location | Last Updated | Status |
|----------|----------|-------------|--------|
| Terms & Conditions | `docs/legal/terms-and-conditions.md` | 2026-03-20 | Active |
| Privacy Policy | `docs/legal/privacy-policy.md` | 2026-07-02 | Active |
| Article 30 Register (RoPA) | `knowledge-base/legal/article-30-register.md` | 2026-07-02 | Draft (counsel review pending) |
| Cookie Policy | `docs/legal/cookie-policy.md` | 2026-03-20 | Active |
| GDPR Policy | `docs/legal/gdpr-policy.md` | 2026-07-02 | Active |
| Acceptable Use Policy | `docs/legal/acceptable-use-policy.md` | 2026-05-18 | Active |
| Data Protection Disclosure | `docs/legal/data-protection-disclosure.md` | 2026-07-02 | Active |
| LinkedIn Page LIA | `knowledge-base/legal/legitimate-interest-assessments/2026-05-19-linkedin-org-page-lia.md` | 2026-05-19 | Draft (counsel review pending) |
| Operator Inbox Triage LIA | `knowledge-base/legal/legitimate-interest-assessments/2026-06-11-operator-inbox-triage-lia.md` | 2026-06-11 | Draft (counsel review pending) |
| Statutory Response Catalog | `knowledge-base/legal/statutory-response-catalog.md` | 2026-06-11 | Draft (counsel review pending) |
| Disclaimer | `docs/legal/disclaimer.md` | 2026-03-20 | Active |
| Individual CLA | `docs/legal/individual-cla.md` | 2026-03-20 | Active |
| Corporate CLA | `docs/legal/corporate-cla.md` | 2026-03-20 | Active |

## Vendor DPA Status

Source: `knowledge-base/project/specs/feat-vendor-ops-legal/dpa-verification-memo.md`

| Vendor | DPA Status | Signed/Verified | Transfer Mechanism | Data Region | Notes |
|--------|-----------|----------------|-------------------|-------------|-------|
| Hetzner Online GmbH | SIGNED | 2026-03-19 | N/A (EU-only) | hel1 (Finland) + fsn1 (Germany) | Covers CX33 (web platform, incl. the 2nd web host web-2 CX33 — in `fsn1` since PR #6393 relocated it `hel1`→`fsn1` on 2026-07-13 for cross-DC failure resilience) + the git-data host CAX11 (multi-host /workspaces split, #5274, ADR-068). All same Hetzner account, all EU regions (`var.web_hosts` is validation-pinned to `nbg1`/`fsn1`/`hel1`) — no re-sign required (host-count- and EU-DC-agnostic, no new sub-processor / no new third-country transfer). **[2026-07-17 (Ref #6538/#6463): web-2 (`fsn1`) retirement recorded.** The second web host is being decommissioned; post-retirement the web-platform compute plane is served solely from `hel1` (Helsinki). No re-sign, no sub-processor change, no third-country-transfer change — the DPA is host-count- and EU-DC-agnostic and the surviving `hel1` host is under the same account/AVV. `fsn1` retained above for the audit trail (web-2 occupied it from 2026-07-13 per #6393; retirement recorded 2026-07-17 under #6538, physical decommission on the supervised operator apply). Unrelated: the Better Stack Logs `eu-fsn-3` (Falkenstein) row below is a distinct processor and is unaffected.]** |
| Supabase Inc | SIGNED | 2026-03-19 | N/A (EU-only) | eu-west-1 (Ireland) | DPA version: August 5, 2025. SCCs Module 2+3. Governing law: Irish. `usage` jsonb column added to `messages` table 2026-05-12 (PR #3648); processing-activity-bound DPA scope covers column-level extensions without re-signing |
| Stripe Inc | AUTO | 2026-03-19 | EU-US DPF + SCCs | US-based | Automatic via Services Agreement. SAQ-A eligible (PCI) |
| Cloudflare Inc | AUTO | 2026-03-19 | DPF + SCCs + CBPR | Global CDN | Self-executing via Self-Serve Agreement |
| Resend Inc | AUTO | 2026-04-13 (inbound scope amendment 2026-06-11, #5103; outbound cold-send scope amendment 2026-06-15, #5325) | DPF + SCCs | US-based | Automatic DPA via Terms of Service (Section 7: Data Processing). **Three roles.** (i) Outbound transactional email for review gate notifications, workspace invite notifications, invite acceptance confirmations, and DSAR export notifications (from `soleur.ai`). (ii) **Inbound email custody (PA-27, feat-operator-inbox-delegation #5103):** Resend Inbound receives all mail Sieve-forwarded from `ops@soleur.ai` and is the **custodian of inbound raw third-party mail** — it retains received content (full body, headers, attachments) for **30 days on all plans, with NO deletion API** (gdpr-gate Chapter V finding, verified 2026-06-10): Jikigai cannot shorten the window or honor erasure against that copy faster than expiry. Webhook delivery to us is metadata-only; bodies are fetched transiently and never persisted (no body column — migration 102). Disclosed in Article 30 PA-27, Privacy Policy §4.13/§5.9, DPD §2.3(aa)/§4.2; accepted residual (c) in the DPIA screening memo `knowledge-base/legal/audits/2026-06-11-dpia-screening-operator-inbox-triage.md`. (iii) **Outbound cold-email custody (PA-28, feat-agent-native-outbound-email #5325):** human-approval-gated 1:1 cold outreach sent from the dedicated `outbound.soleur.ai` subdomain (eu-west-1; SPF/DKIM/DMARC p=quarantine→reject) — a separate sender-reputation stream, distinct from the `soleur.ai` apex transactional sender (`notifications@soleur.ai`); same registrable domain, distinct subdomain. Resend retains sent content **30 days, no deletion API** (same Chapter V finding). At rest Jikigai stores only a keyed `recipient_hash` (HMAC, pepper in Doppler) + body hashes — never the plaintext recipient or body (migration 104). No LLM on this leg. Disclosed in Article 30 PA-28, LIA `2026-06-15-outbound-email-authority-lia.md` |
| Doppler Inc | AUTO (MSA + DPA addendum — verification pending) | 2026-05-05 | EU-US Data Privacy Framework | US-based | Holds GH_APP_DRIFTGUARD_PRIVATE_KEY_B64 (brand-survival credential — single-user incident threshold). RBAC review tracked in #3228. PR #3224 introduces the drift-guard secret; Doppler→GH Actions sync is the documented bootstrap path |
| Google LLC (osv.dev) | AUTO | 2026-05-10 | SCC-equivalent (Google Cloud DPA) | US-based | OSV.dev queried by `skill-security-scan` for supply-chain advisory checks (#2719). Package metadata only (name, ecosystem, version) — never operator-identifying data — is sent. Per gdpr-gate `GDPR-ChapterV-1`. |
| Anthropic PBC | AUTO | 2026-05-11 | SCCs M2+3 + UK IDTA + Swiss Addendum | US-based | DPA effective 2025-02-24 (`https://www.anthropic.com/legal/data-processing-addendum`), auto-incorporated via Commercial Terms § C ("Data Privacy") effective 2025-06-17. SCCs Modules 2+3 per § I.1 (Art. 46 GDPR). Governing law: Irish (DPA § A.1.c). Sub-processors: `https://trust.anthropic.com/subprocessors`. **Scope: Jikigai-keyed Anthropic API surface only** — `claude-code-action` workflows in this repo's CI, the compound-promotion-loop (#2720) clustering job, the weekly release digest cron's curation call (#5080, `cron-weekly-release-digest.ts` — input is sanitized published release notes only; emails/@handles/Co-Authored-By stripped per spec TR4; no operator-identifying data beyond already-published release-note text), and (scope amendment 2026-06-11, #5103) the **PA-27 email-triage summarizer** (one `messages.create` call per non-statutory inbound email to ops@soleur.ai; subject + sender + truncated/sanitized third-party body; 30-day default retention until the Zero-Retention amendment is signed — snapshot at `knowledge-base/legal/data-processing-agreements/anthropic.md`). Plugin/skill invocations under the user's own Anthropic API key are covered by `docs/legal/gdpr-policy.md` § 2.2 (Anthropic acts as independent controller/processor — Soleur does not intermediate). Single-user-incident threshold dependency for #2720 (per #2720 plan AC23/AC26). |
| GitHub Inc | PENDING | 2026-05-13 | SCCs (Module 2) + DPF | US-based | Sub-processor for operator GitHub App installations. Data categories: issue bodies, commit author emails, repo metadata, installation tokens. DPA at `https://github.com/customer-terms/customer-data-protection-agreement`, auto-incorporated via Customer Agreement. Retention: tied to GitHub's own lifecycle (60-day audit log per published policy). **Scope: cc-router in-process MCP tool surface** — `github_read_*` (6) + `github_trigger_workflow` + `github_push_branch` + `create_pull_request` (9 tools total). Phase 1 (#2909) ships the DPA row only; actual exposure of these tools via cc-router is Phase 2 (#3722, blocked-by #2939). Operator action pre-Phase-2: verify DPA acceptance at GitHub Settings, flip status `PENDING → AUTO`. Single-user-incident threshold dependency for #2909 / #3722. |
| Plausible Analytics (Plausible Insights OÜ) | PENDING | 2026-05-13 | N/A (EU-only — operator on plausible.io) OR SCCs (if self-hosted on US infra — verify at activation) | Estonia (plausible.io hosted EU) | Sub-processor for operator-bound analytics. Data categories: ephemeral IP processing with 24h hashed retention per Plausible DPA (IPs hashed at intake and discarded within 24h; no cookies, no persistent user identifiers). DPA at `https://plausible.io/dpa`. Retention per Plausible's documented event-retention policy. **Scope: legacy `agent-runner.ts` path only — cc-router is PERMANENTLY blocked from Plausible tools via `CC_ROUTER_TIER3_DENYLIST`** (3 tools share a single backend `PLAUSIBLE_API_KEY` with no per-user / per-site enforcement; exposing them via the router is cross-tenant by construction). Operator action: verify region (plausible.io EU-hosted vs self-hosted US infra) and flip status `PENDING → AUTO`. If self-hosted on US infra, Transfer Mechanism becomes SCCs. Single-user-incident threshold dependency. |
| Bullet Train Limited (Flagsmith) | AUTO via Flagsmith Terms of Service (the in-document **Data Processor Appendix** is the Art. 28 DPA mechanism — auto-incorporated on operator-side ToS acceptance; UK IDTA + EU SCCs Modules 2+3 are the distinct Art. 46 transfer instruments, both also incorporated by reference through the Data Processor Appendix per §11.2 of `data-processing-agreement-template.md`) | 2026-05-25 | UK IDTA + EU SCCs Modules 2+3 (belt-and-suspenders to EC adequacy decision for the UK; onward US sub-processors covered by SCCs Module 3 + DPF where available) | UK (Bullet Train Limited controller, registered England and Wales no. 12353266; registered office per Companies House: 66 Paul Street, London, EC2A 4NA; operating address: 86-90 Paul Street, EC2A 4NE) → global edge network (`edge.api.flagsmith.com`, AWS-hosted; region not pinned on managed-SaaS tier) | Sub-processor for the Web Platform runtime feature-flag identity-trait evaluation (PA-1 + PA-2). Operator passes `transient: true` on every `getIdentityFlags(identifier, { role, orgId }, true)` call as the data-min lever (opts out of Flagsmith server-side identity persistence). Single-control gating (updated 2026-05-26, PR #4481): Flagsmith `org-targeted` segment rule is the sole per-org gate; env-allowlist (`*_ALLOWLIST_ORG_IDS`) removed. `FLAG_*` env vars remain as Flagsmith outage fallback. Snapshot at `knowledge-base/legal/data-processing-agreements/flagsmith.md`. Sub-processor list at `https://www.flagsmith.com/gdpr-sub-processor-list` (last updated 13 October 2025). §6.1 30-day Customer notification clock NOT triggered today (zero executed customer DPAs per `tenant-dpa-register.md`); promotes into first executed Customer DPA's Schedule 2 per §6.5 single-source-of-truth resolution. Single-user-incident threshold dependency for umbrella #4456 (PR-1 #4455 — disclosure lands BEFORE PR-2 code that egresses `orgId` identity trait). |
| Better Stack s.r.o. | PENDING (sign Vendor DPA — operator action; AC15 escalates to `compliance/critical` follow-up if unsigned at PR-ready time) | 2026-05-22 | N/A (intra-EU; CZ controller → DE `eu-fsn-3` cluster) | Czech Republic (controller) → Falkenstein DE `eu-fsn-3` (Logs ingest) | Sub-processor for Logs role (vector→Better Stack journald + host_metrics). The heartbeat role is non-personal-data (opaque ping payload) and is recorded in DPD §2.3(m)(i). Source ID `2457081` (`soleur-inngest-vector-prd`) cluster-pinned 2026-05-22 via authenticated POST probe (eu-fsn-3 → 202; eu-nbg-2 → 401, confirming token cluster-binding). Pseudonymisation: `userIdHash` HMAC-SHA256 at the VRL boundary (`apps/web-platform/infra/vector.toml` `pii_scrub_*` transforms reusing `SENTRY_USERID_PEPPER` per ADR-029 §I4). CI gate: `validate-vector-config` in `apply-web-platform-infra.yml`. Substrate note: generic HTTP sink is the canonical vendor-recommended pattern; native `better_stack_logs` sink does not exist (Vector PR #19274 closed unmerged 2025-01-27). Strategic single-provider consolidation question tracked in #4296 (60-day re-decision). Single-user-incident threshold dependency for #4273. |

## Vendored Code Provenance

Source: `knowledge-base/engineering/policies/content-vendoring.md`

The `gdpr-gate` skill incorporates upstream-vendored detection rules under permissive license. Each lifted bundle is governed by the content-vendoring policy: pinned to a commit, integrity-checked at pre-commit, drift-detected weekly, and runtime-monitored for staleness (>30d STDOUT banner, >90d POSTURE_FAIL line).

| Upstream | License | Pinned Commit | Lifted Files | Last Verified | Status |
|---|---|---|---|---|---|
| github.com/goSprinto/compliance-skills | MIT | `7b58d68` | 5 (gdpr-gate `references/`) | 2026-05-10 | active |

NOTICE-of-record: `plugins/soleur/skills/gdpr-gate/NOTICE` (YAML frontmatter is the canonical machine-readable form). Drift-detection workflow: `.github/workflows/scheduled-content-vendor-drift.yml`. Operator runbook: `knowledge-base/engineering/operations/runbooks/vendor-pin-drift-resolution.md`.

## Active Compliance Items

<!--
Row schema (canonical for gdpr-gate critical-finding handshake — operator-acknowledged write only; the gate NEVER writes here directly):

| Item                       | Issue        | Status              | Deadline | Notes                                                            |
|----------------------------|--------------|---------------------|----------|------------------------------------------------------------------|
| <one-line summary>         | #<number>    | OPEN | IN-PROGRESS | <date>   | <check_id from gdpr-gate, e.g. GDPR-Art-9; remediation context>  |

Contract (mirrors plugins/soleur/skills/gdpr-gate/SKILL.md §"Critical-finding escalation flow"):
- The `clo` agent reads this section during legal posture assessments.
- `/soleur:gdpr-gate` Critical findings prompt the operator to (1) `gh issue create --label compliance/critical`, (2) append a row here referencing the issue number + check_id, (3) commit with `compliance: register Art. 9 finding for #<issue>`.
- `/soleur:ship` Phase 5.5 gdpr-gate critical-finding-acknowledgment gate verifies every PR-referenced `compliance/critical` issue has a row here before merge.
- Status moves OPEN → IN-PROGRESS → resolved (move to Completed Compliance Work).
-->

| Item | Issue | Status | Deadline | Notes |
|------|-------|--------|----------|-------|
| T&C blanket statement contradictions | #736 | OPEN | - | Identified during #670 review |
| Skill-install advisory gate | #2719 | OPEN | - | Single-user-incident threshold; EU jurisdiction. Verdict naming `LOW-RISK \| REVIEW \| HIGH-RISK` with mandatory advisory disclaimer (CLO requirement). Override = structured artifact under `knowledge-base/engineering/security/skill-overrides/` (GDPR Art. 32 evidence; retention = repo lifetime). PII redaction (email/IPv4/IBAN) at scan time per `GDPR-DataMin-1`. Self-defense: SHA-pinned rule pack, OSV untrusted-input handling, fail-loud self-test. R15 mitigation landed via #3543 on 2026-05-11 — `skill-security-scan PR gate` is a required check on the `CI Required` ruleset (#14145388); admin-merge bypass auto-files a `compliance/critical` issue via post-merge audit. R15 follow-up D1 landed via PR #3555 on 2026-05-11 (origin issue #3544) — a daily Inngest cron (`cron-ruleset-bypass-audit`, 06:13 UTC; superseded the original `scheduled-ruleset-bypass-audit.yml` GHA workflow) audits the live `CI Required` ruleset (24h worst-case detection window) and files a `compliance/critical` issue routed to CLO + CPO on drift. Smoke-verified 2026-06-30 (#3570/#3571): live==canonical GREEN, manual trigger `cron/ruleset-bypass-audit.manual-trigger` dispatched HTTP 202, no spurious issue filed. The ruleset's source of truth is Terraform (`infra/github/ruleset-ci-required.tf`); canonical JSON snapshots are kept in lockstep with it by the `T-rsc-9` sync gate. **2026-06-30 (#4397/#5759): the GHA→Inngest migration had dropped `required_status_checks` + `enforcement` drift detection and auto-close-on-green; all three restored in `cron-ruleset-bypass-audit.ts`** (now audits `bypass_actors` widening, `required_status_checks` un-requiring, and `enforcement != active`), unit-covered by `cron-ruleset-bypass-audit.test.ts`. Runbook: `knowledge-base/engineering/operations/runbooks/ruleset-bypass-drift.md`. |
| Compound Promotion Loop (Layer 2 self-healing) | #2720 | OPEN | - | Single-user-incident threshold. **ENABLED 2026-07-06 (#6039)** — operator dogfood; `promotion-config.yml` `enabled: true`; loop opens human-gated draft PRs (never auto-merges). Plan-time gdpr-gate findings folded as AC23/AC26/AC27 (Anthropic DPA row present — #3594 CLOSED; DPIA candidacy LC-04; synthesized-PII fixtures). **DPIA candidate (Art. 35)** — the week-4 re-evaluation clock STARTED at enable (2026-07-06); assess cluster count, false-positive rate, operator merge ratio ≥28d after enable. Enrolled as a soak follow-through (`scripts/followthroughs/dpia-reeval-compound-promote-2720.sh`, earliest 2026-08-03) so the re-eval cannot rot. Append-only audit log: `knowledge-base/project/learnings/promotion-log.md`. Runbook: `knowledge-base/engineering/operations/runbooks/compound-promote-runbook.md`. |
| Anthropic processor DPA row missing from Vendor DPAs | #3594 | CLOSED (#3596) | 2026-05-11 | Resolved — Anthropic processor row added to the Vendor DPA Status table (present at line 81; DPA effective 2025-02-24, SCCs M2+3 + UK IDTA + Swiss Addendum, auto-incorporated via Commercial Terms § C). This unblocked the compound-promotion-loop enable (#6039); the ship-blocker on #2720 (AC23) is satisfied. |
| DSAR Art. 15 + Art. 20 self-serve endpoint | #3637 | IN-PROGRESS | - | Single-user-incident threshold (Art. 33 + Art. 34 notifiable if leak). Replaces manual `legal@jikigai.com` flow with a self-serve `/dashboard/settings/privacy` endpoint. ADR-028 captures the service-role + per-row WHERE + assertReadScope architecture (C1 rejection of per-user JWT-at-rest credential-vault risk class). 24-month retention on `dsar_export_audit_pii` PII; Art. 17 cascade anonymises before auth-delete. Cross-document gate `.github/workflows/legal-doc-cross-document-gate.yml` blocks PRs that touch `apps/web-platform/server/dsar-export.ts` without updating all four legal docs in lockstep. **Gate promoted from advisory to required-status-check on the `CI Required` ruleset via PR #4384 (closes #4384) — the auto-merge bypass that produced #4333 is closed by Terraform-managed `required_check { context = "enforce", integration_id = 15368 }` in `infra/github/ruleset-ci-required.tf`.** v1.1 deferrals tracked separately: read-only MCP status tool (D1), synchronous fast-path for accounts <10 MB (D2). |
| Sentry Monitors/Alerts split adaptation — migration audit + log-ingestion carve-out + Crons heartbeat for 8 scheduled workflows | #3814 | IN-PROGRESS | At first `reusable-release.yml` run after merge | Single-user-incident threshold. Sentry split "Alerts" into Monitors (detection) and Alerts (routing) in 2026; vendor's "no action required" is rejected because (a) auto-migration may have orphaned a Metric Alert from its paired routing rule (silent paging gap on auth burst), (b) new monitor classes (log-condition, custom-metric) push past the current Article 30 §(c) inventory unless explicitly carved out. Plan: `knowledge-base/project/plans/2026-05-15-feat-sentry-monitors-alerts-adapt-plan.md`. ADR-031 at `knowledge-base/engineering/architecture/decisions/ADR-031-sentry-as-iac.md` documents the Terraform onramp via `jianyuan/sentry` v0.15.0-beta2 with import-not-recreate posture for the 4 existing auth issue-alert rules. Migration audit (Article 30 §5(2) accountability evidence) — **dual-path** (PR #3811 review P1-H correction): operator runs (Phase 2.1 pre-import baseline + ad-hoc local re-runs) default to `knowledge-base/legal/audits/sentry-migration-audit-<YYYY-MM-DD>.md` (tracked in git history); CI release runs (`reusable-release.yml` Phase 2.2) override `AUDIT_OUT_DIR` to `${RUNNER_TEMP}/sentry-audit/` and upload the report as a per-release GitHub release asset keyed by the release tag. Both paths regenerate the report via `apps/web-platform/scripts/sentry-monitors-audit.sh`. See plan OQ1 for the dual-path rationale. GDPR-gate (Phase 6) PASS required pre-merge: zero changes to `apps/web-platform/server/sentry-scrub.ts` or `apps/web-platform/sentry.{client,server}.config.ts`; folds in #3236 (cross-workflow heartbeat for secret-touching scheduled workflows) by giving each of 8 scoped workflows a vendor-hosted dead-man's-switch (`scheduled-cf-token-expiry-check` deferred until its `schedule:` re-lands). Closes #3814 + #3236 manually after AC13–AC16 verified. Follow-ups filed at PR review time: #3828 (composite-action extraction for the multi-workflow check-in fan-out, contested-design) and #3829 (CI gate enforcing "new Sentry monitor type → `sentry-scrub.ts` must change" — currently operator-memory only; architectural-pivot; type/security because the failure mode is a GDPR Art. 33 notifiable breach). |
| Multi-tenant deploy substrate v1 (Soleur-as-tenant-zero) — non-Soleur tenant disclosure gate | #3723 | OPEN | At first non-Soleur tenant onboarding | Single-user-incident threshold. v1 ships substrate (audit-log migration 043 + manual provisioning runbook) for Soleur-as-tenant-zero only. **Privacy-policy / DPD sub-processor disclosure for non-Soleur tenants is explicit scope-out at v1** (substrate processes Soleur-operator metadata only, not `app.soleur.ai` end-user data). ADR-030 captures the per-tenant GH Actions OIDC + tenant-owned cloud accounts architecture. LIA at `knowledge-base/legal/legitimate-interest-assessments/2026-05-14-tenant-deploy-substrate-lia.md` (Art. 6(1)(f) three-part test). ToS-research at `knowledge-base/legal/tos-research/2026-05-14-tenant-account-provisioning-tos-research.md` (4 providers ALLOWED_WITH_CONDITIONS pending counsel re-read at first non-Soleur onboarding). gdpr-gate report at `knowledge-base/legal/gdpr-gate-report-2026-05-14.md` (zero Critical). Re-evaluation trigger: **first non-Soleur tenant onboarding** triggers (i) tenant DPA signing per Phase 2 runbook Step 0, (ii) Art. 28(4) sub-processor flow-down obligations, (iii) Privacy Policy + DPD update with the orchestration-plane audit log disclosure, (iv) full counsel re-read of all 4 ToS-research verdicts. |
| Team workspace multi-user — legal-doc lockstep gate dependency | #4229 | IN-PROGRESS | At Doppler flag-flip | Single-user-incident threshold. feat-team-workspace-multi-user (#4225) shipped 2026-05-21 with migrations 053–057 + workspace_members invite/remove RPCs + DSAR Art-15/17/20 extension over the 4 new workspace tables (Article 30 register PA-2 amendment for "workspace co-member" data category). **Legal scaffolding PR #4289 shipped 2026-05-22** (this PR): ToS 2.2.0 §3b Workspace Members + AUP §5.5 owner attestation + DPD §2.3(u) co-member data category + §4.2 processor carve-out + Privacy Policy §4.11 dual-perspective disclosure + §4.7 recipient note + Side Letter template + Side Letter register + operator-attested counsel-review audit (`knowledge-base/legal/audits/2026-05-counsel-review-4289.md`). [Updated 2026-05-26, PR #4481: env-allowlist (`TEAM_WORKSPACE_ALLOWLIST_ORG_IDS`, `BYOK_DELEGATIONS_ALLOWLIST_ORG_IDS`) removed from code and Doppler prd; Flagsmith `org-targeted` segment is the sole per-org gate. `FLAG_TEAM_WORKSPACE_INVITE=1` remains in Doppler prd as the Flagsmith outage fallback. Followthrough script `team-workspace-flag-flip-4284.sh` deleted (issue #4284 CLOSED).] AC-LEGAL-FLIP remaining precondition is **Doppler-only**: `FLAG_TEAM_WORKSPACE_INVITE=1` in prd. Rollback runbook: `knowledge-base/project/specs/feat-team-workspace-multi-user/rollback.md`. |
| DSAR departed-workspace-member coverage (Art. 15 completeness + Art. 17 cascade for new ledger) | #4230 / PR #4294 | IN-PROGRESS | At flag-flip ON | Single-user-incident threshold (Art. 33 + Art. 34 notifiable if leak). Closes the umbrella #4229 follow-up: once `remove_workspace_member` DELETEs a `workspace_members` row, the ex-member's identity-to-workspace linkage was gone and the DSAR export at `apps/web-platform/server/dsar-export.ts:609-630` silently omitted any workspace they had left. **Approach A** — UNION current memberships with historical `workspace_member_attestations` to recover `workspaceIds`; symmetric attestation export via `.or(invitee.eq.X,inviter.eq.X)` + two-arm `assertReadScope` (Kieran P1-1) so departed members' INVITER-side rows are also recovered. **Approach B** — new `workspace_member_removals` WORM ledger captures `(removed_user_id, removed_by_user_id, removed_at, workspace_id)` for every removal; `remove_workspace_member` SECURITY DEFINER RPC appends the row inside the same body as the DELETE (atomic — FK violation rolls back DELETE per AC2). Migration 063 mirrors 058's WORM-trigger + REVOKE matrix + anonymise-RPC patterns verbatim; DELETE bypass is row-state-gated (`removed_at < now() - interval '36 months'`) per learning 2026-05-15-worm-trigger-blocks-pg-cron-retention-sweep.md (pg_cron runs as `postgres`; role-gated bypasses silently fail). Cascade-order extension at `account-delete.ts` step 3.905 (between `anonymise_workspace_member_attestations` and `anonymise_workspace_members`) — load-bearing for Art. 17 on the new ON DELETE RESTRICT FKs. workspace_id FK is **ON DELETE SET NULL** (post-DISSENT-flip inline fix) — admits orphan-org cleanup; WORM trigger permits the implicit NOT NULL → NULL transition on workspace_id only. Sister-table defect on 058 (workspace_member_attestations.workspace_id RESTRICT) filed at #4329 (pre-existing-unrelated; CONCUR'd by reviewer). **36-month retention** deviates from 24-mo PA-PII envelope; rationale in ADR-039 §Retention (Art. 82(2) limitation horizon shortest-jurisdiction floor = DE 3y). PA-20 row added to `article-30-register.md`. Runbook for accountless ex-members at `knowledge-base/legal/runbooks/dsar-accountless-ex-member.md` (Art. 12(6) ID-verification template + 30-day SLA per Art. 12(3)). **Scope-split:** author-only message redaction (Art. 15(4) rights-of-others predicate) tracked at #4319 — affects ALL DSARs, not departed-member-specific; gated on PR #4289 ready-for-review for the Art. 13(2)(b) disclosure dependency. ADR-039 records the WORM-ledger invariant + cascade-order requirement + RLS deviation (departed members cannot read their own removal row via `is_workspace_member` — DSAR service-role read only). Legal text rides PR #4289 (legal scaffolding, now shipped); PA-20 + this row + accountless runbook ride this PR. Brand-survival threshold: `single-user incident`. `user-impact-reviewer` mandatory at PR review. Operator action required at flag-flip: verify PR #4294 merged + AC9 (web-platform-release.yml migrate applied 062 to prd) confirmed. |
| Sentry residency cleanup (Phase A2 / Branch C) — ~~phantom-ingest after-the-fact §5(2) disclosure~~ duplicate-org cleanup + atomic DSN swap + audit-gate destination-controllability expansion | #3861 | IN-PROGRESS | n/a (Art 33 procedural gate retracted — see UPDATE) | Single-user-incident threshold. 3-PR series following A1 (#3863, merged 2026-05-15): PR-α #3904 (legal+docs deadline gate — ships ≤2026-05-18: PA8 §(d) recipient-drift disclosure (placeholders backfilled in PR-β #3945) + ADR-031 stale-URL fix + new `## Cluster / Host Glossary` section + this row), PR-β #3945 (runtime atomic DSN swap + IaC + sentry-monitors-audit.sh 4-gate expansion + new `.github/workflows/sentry-audit-gate.yml` CI required-check + PA8 placeholder backfill), PR-γ #3946 (US shadow-org teardown + 2 Sentry support tickets + PIR Phase 8 flip + W1/W2/W5 follow-up issues). Disclosure of record: PIR `knowledge-base/engineering/operations/post-mortems/sentry-phantom-ingest-destination-unreachable-postmortem.md` (Art 34 self-controller-self-processor framing — 10 operator-adjacent accounts + zero arms-length external signups during phantom-ingest window 2026-03-28 → 2026-05-16, verified by SQL count + operator categorization at PR-α merge time 2026-05-17). Plan: `knowledge-base/project/plans/2026-05-16-feat-sentry-residency-a2-branch-c-plan.md`. Brainstorm: `knowledge-base/project/brainstorms/2026-05-16-sentry-residency-a2-branch-c-brainstorm.md`. Art 33 CNIL filing deferred pending Sentry support response per brainstorm Decision #10 (T-24h buffer 2026-05-18T12:50Z; T-2h fallback 2026-05-19T10:50Z); operator files directly with PR-α #3904 cited as evidence-in-flight if PR-α slips past the deadline. PR-α prose drift-disclosure precedent is first-of-its-kind for after-the-fact residency-drift §5(2) wording (brainstorm Capability Gap #2 — CLO sign-off load-bearing). Audit-gate triple-expansion (org-reachable + project-scope + write-probe + DSN-org-id-match) is the Art 25 design-level recurrence-prevention control. Closes #3861 manually after AC1–AC16 verified across PR-α/β/γ. **[2026-05-19 UPDATE: The "phantom-ingest after-the-fact §5(2) disclosure" framing above is SUPERSEDED. Sentry support replies 2026-05-19 (billing + Rodolfo forensics) confirm both `jikigai` (org ID `4511123328466944`) and `jikigai-eu` (org ID `4511404939345920`) are operator-owned EU-database orgs throughout. The 2026-03-28 → 2026-05-16 401/403 responses are reattributed to an internal `SENTRY_AUTH_TOKEN` token-membership boundary, not non-ownership. No third-party recipient occurred. No Art. 33 / Art. 34 notification was warranted; the Art 33 CNIL filing referenced above is retracted. Probe report: `knowledge-base/legal/audits/2026-05-19-sentry-token-scope-probe-divergence.md`. Corrective PR series: PR-1a #4044 (PIR reopen + Art-33 supersede), PR-1b #4102 (probe + divergence appendix + leak learning), PR-2 (corpus sweep across PA8 §(d) + PIR Phase 9 + ADR-031 + this row). Operator-side cleanup 2026-05-21: Sentry support canceled the duplicate `jikigai` org and transferred $29 credit to `jikigai-eu`; runtime token now HTTP 403 against canceled slug; 10 active monitors on `jikigai-eu` (HTTP 200, 0 disabled). See PIR §Phase 9 Gate-3b Correction + PA8 §(d) UPDATE block for the load-bearing record.]** |
| Customer-facing DPA template (pre-draft) + DPD §2.1b(a) controllership clarification | #4330 | OPEN | At first B2B trigger event (publish-time) | Single-user-incident threshold (carry-forward from #4289). DPA template pre-draft at `knowledge-base/legal/data-processing-agreement-template.md`. **DPA template drafted (knowledge-base only); publish + counsel-review when first B2B prospect requests.** Status: `DEFERRED-ARTIFACT-ONLY` — the template lives under `knowledge-base/legal/` and is NOT served at `docs/legal/data-processing-agreement.md` until a trigger event fires. Three trigger conditions (verbatim from issue body): (a) first B2B prospect asks "Do you have a DPA?"; (b) first paying customer organization invites employees as Workspace Co-Members under `FLAG_TEAM_WORKSPACE_INVITE` (the team-workspace dogfood expands beyond Jikigai-internal Jean+Harry); (c) first EU customer requests SCCs for non-EU sub-processors (Stripe US, Resend US, Sentry onward US). On trigger fire: (i) publish DPA template by copying to `docs/legal/data-processing-agreement.md` + Eleventy mirror at `plugins/soleur/docs/pages/legal/data-processing-agreement.md`; (ii) invoke external counsel review and write counsel-review audit at `knowledge-base/legal/audits/<YYYY-MM>-counsel-review-dpa-template-<PR>.md`; (iii) bump `TC_VERSION` in `apps/web-platform/lib/legal/tc-version.ts` IF the customer-DPA cross-reference adds language to ToS §3b.4 supersession trigger; (iv) write the first row of a customer-DPA execution register on counter-signature (NOT `tenant-dpa-register.md`, which is scoped to the non-Soleur deploy-substrate flow #3723 — either create `customer-dpa-register.md` with a parallel schema, or amend the existing register to be dual-purpose); (v) regenerate Schedule 2 sub-processor table from then-current DPD §4.2; (vi) initiate SOC 2 engagement within 90 days per DPA §10.3 commitment. DPD §2.1b(a) team-workspace carve-out clarification shipped in this PR (canonical + Eleventy mirror); no contradiction with ToS §3b.1 (the two sections describe different processing activities — default-user platform-level vs team-workspace sub-case under `FLAG_TEAM_WORKSPACE_INVITE`). Sub-processor list externalization (Vercel `security.vercel.com` precedent) tracked separately. Closes #4330. |
| Sub-processor list public page (Vercel `security.vercel.com` precedent) | #4350 | OPEN | At first DPA publish trigger event | Deferred from #4330. Vercel publishes its always-current sub-processor list at `security.vercel.com`; Linear at `linear.app/dpa`; Soleur today maintains the Vendor DPA Status table at `knowledge-base/legal/compliance-posture.md` (this file), which is NOT publicly addressable. Until a publicly addressable sub-processor list exists, the customer-facing DPA template Schedule 2 is an inline snapshot regenerated at each DPA publish. Re-evaluate at first DPA publish trigger event (one of the three trigger conditions in the #4330 row above). |
| Attachments workspace-shared storage RLS + cascade pseudonymisation (PR-2) | #4318 / PR #4417 | IN-PROGRESS | At `TEAM_WORKSPACE_INVITE_ENABLED` flag flip | Single-user-incident threshold. Mig 068 widens `storage.objects` SELECT policy for `chat-attachments` to admit workspace co-members via SECURITY DEFINER helper `is_attachment_path_workspace_member` deriving `workspace_id` from `conversations.workspace_id`. Split policy preserves write narrowing (own-folder INSERT/UPDATE/DELETE per `2026-04-18-rls-for-all-using-applies-to-writes.md`). Cascade pseudonymisation (`_anonymise_authored_messages_internal` → `messages.user_id = NULL`, FK-safe per Phase 0 finding E-1) folded inside `remove_workspace_member` RPC atomic body AND called via `anonymise_departed_user_across_workspaces` from `account-delete.ts` step 3.901. PA-2 §(d) recipients + §(g) TOMs (12)+(13) amended in this PR. **DSAR co-uploader enumeration resolved by PR #4486 (#4445):** manifest schema 1.2.0 adds co-uploader manifest entries with `redacted:true, redaction_reason:art-15-co-uploader, uploader_pseudonym:"member_<hex12>"`; bytes NOT included in the ZIP per Art. 15(4). Post-cascade NULL-uploader rows render as `former-member` sentinel for Art. 15 completeness vs co-member chat-UI visibility. **Storage cleanup step 3.9015 resolved by PR #4486 (#4444).** Plan: `knowledge-base/project/plans/2026-05-25-feat-attachments-workspace-shared-pr2-plan.md`. Phase 0 worklog (PROBE-A..D + R-9 spike empirical) at `knowledge-base/project/specs/feat-attachments-rls-bundle-pr2-4318/phase-0-worklog.md`. Post-apply verify sentinel at `apps/web-platform/supabase/verify/068_attachments_workspace_shared.sql` runs automatically via `web-platform-release.yml#verify-migrations`. |
| Operator inbox triage (PA-27) — vendor-window + residual-risk follow-through | #5103 | OPEN | At first non-operator data subject as item owner OR first residual materialising | Single-user-incident threshold. Tracks the PA-27 accepted posture: (i) **Resend inbound 30-day retention with NO deletion API** (Chapter V finding — re-evaluate if Resend ships a delete API or extends the window; DPIA residual (c)); (ii) **Anthropic Zero-Retention amendment unsigned** — third-party mail content sent by the PA-27 summarizer sits in Anthropic's 30-day default window until the operator signs (same operator step as PA-22 (f), now ALSO load-bearing for involuntary third-party data subjects); (iii) DPIA residuals (a) Art. 9-in-summary and (b) prompt-injection-distorted summaries are accepted at single-operator scale only — any scale expansion or any pipeline write/act authority re-opens the screening (`knowledge-base/legal/audits/2026-06-11-dpia-screening-operator-inbox-triage.md` §4); (iv) **self-hosted Inngest event store has NO automatic deletion** (Inngest self-hosting docs; no retention flag in the `inngest start` ExecStart, `apps/web-platform/infra/inngest-bootstrap.sh`) — `subject`+`sender` event payloads persist until operator-side store maintenance; implement or confirm a periodic event-store purge bound (same store as DPD §2.3(o), whose prior "30-day rolling" figure was corrected 2026-06-11) ; tracked in #5150. Counsel review of the full bundle (PA-27 + LIA + DPIA memo + statutory catalog + policy lockstep) pending as a single bundle. |
| Storage-object lifecycle on workspace deletion (gates `TEAM_WORKSPACE_INVITE_ENABLED` flag flip) | #4444 | IN-PROGRESS | At first FK chain relaxation from `ON DELETE RESTRICT` OR proposed flag flip | Single-user-incident threshold. Filed by PR #4417 per OQ#3 in the attachments workspace-shared plan §Reconciliation R-10. Storage cleanup step 3.9015 added by PR #4486 (#4444) in `server/account-delete.ts` — deletes orphan Storage objects in `chat-attachments` for the departing user's conversations. Workspace deletion itself still has no cascade for Storage objects — FK chain `workspaces.id ← workspace_members/conversations/messages` is `ON DELETE RESTRICT` so workspace deletion is a non-flow today. Dormant in prd (zero multi-user workspaces). Re-evaluate when any FK in the chain is relaxed OR `TEAM_WORKSPACE_INVITE_ENABLED` is proposed for any non-jikigai workspace. |
| Multi-host cross-tenant write threat-model (TS-1) | #5274 | OPEN | At #5274 Phase-3 GA (soak-gated) | Single-user-incident threshold. Operator-acknowledged Critical gating the 3.D cutover: a second web host (web-2) writing per-workspace git data raised a cross-tenant write threat class. Enforcement SHIPPED in code via the Phase 3.C write-boundary sentinel (D2 write sentinel + membership-gated git-data fetch authz, PR #5893). Status remains OPEN because the GA flip is soak-gated. **Closes at #5274 Phase-3 GA (soak-gated).** |
| Multi-host EU-location pin + CI gate (T-1) | #5274 | OPEN | At #5274 Phase-3 GA (soak-gated) | Single-user-incident threshold. Operator-acknowledged Critical gating the 3.D cutover: both web hosts + the git-data host must stay pinned to an EU Hetzner DC (`var.web_hosts` validates against `nbg1`/`fsn1`/`hel1`; web-2 has been `fsn1` since PR #6393 — the pin is EU-region, NOT hel1-specific) so no new third-country transfer is introduced. Enforcement SHIPPED in Phase 3.A (EU-location pin + CI gate). Status remains OPEN because the GA flip is soak-gated. **Closes at #5274 Phase-3 GA (soak-gated).** |
| Multi-host Art. 17 bare-repo erasure wrapper (DL-1) | #5274 | OPEN | At #5274 Phase-3 GA (soak-gated) | Single-user-incident threshold. Operator-acknowledged Critical gating the 3.D cutover: Art. 17 erasure must reach the bare per-workspace git data on the git-data host. Erasure wrapper SHIPPED in Phase 3.A; the application call that invokes it ships in Phase 3.D. Status remains OPEN because the GA flip is soak-gated. **Closes at #5274 Phase-3 GA (soak-gated).** |
| TS-1 cross-tenant write threat-model (multi-host /workspaces) | #5274 | OPEN | At #5274 Phase-3 GA (soak-gated) | Single-user-incident threshold. Operator-acknowledged Critical gating the 3.D GA flip. Cross-tenant write to another workspace's git data on the shared multi-host substrate — enforcement SHIPPED as the 3.C per-workspace write-boundary sentinel (membership-gated git-data write authz + D2 write sentinel, PR #5893); GA flip is soak-gated. Closes at #5274 Phase-3 GA (soak-gated). |
| T-1 EU-location pin + CI gate (git-data + web hosts pinned to an EU DC: nbg1/fsn1/hel1) | #5274 | OPEN | At #5274 Phase-3 GA (soak-gated) | Single-user-incident threshold. Operator-acknowledged Critical gating the 3.D GA flip. Third-country-transfer risk from a host drifting out of the EU (the pin admits nbg1/fsn1/hel1; web-2 is fsn1 since PR #6393) — enforcement SHIPPED in 3.A (EU-location pin + CI location gate). No new sub-processor / no new third-country transfer (same Hetzner EU account/region). Closes at #5274 Phase-3 GA (soak-gated). |
| DL-1 Art. 17 bare-repo erasure wrapper (per-workspace git-data erasure) | #5274 | OPEN | At #5274 Phase-3 GA (soak-gated) | Single-user-incident threshold. Operator-acknowledged Critical gating the 3.D GA flip. Art. 17 erasure of per-workspace bare-repo git data on the git-data host — the erasure wrapper SHIPPED in 3.A; the app-side call wiring SHIPS in 3.D. GA flip is soak-gated. Closes at #5274 Phase-3 GA (soak-gated). |

## Completed Compliance Work

| Item | Issue/PR | Completed | Notes |
|------|----------|-----------|-------|
| AUP/T&C autonomous command execution residual-risk disclosure (TC_VERSION 2.2.1 → 2.3.0) | #4952 (ref #4949) / PR #4954 | 2026-06-04 | Single-user-incident threshold. AUP §5.7 + §2 clause and T&C §3a.7 + §10.4 + §9 bullet disclose the Web Platform agent's autonomous (auto-run) shell-command surface shipped by #4949 (default-ON, first-run owner consent soft-gate), to CLO bar: blocklist illustrative-not-exhaustive, non-blocked command can auto-run and change/delete files, mitigations (git-backed recovery + visible-in-chat) named as mitigations not guarantees, own-workspace-vs-third-party-sends Art. 22 distinction. Substantively consistent with the in-product `AUTONOMOUS_DISCLOSURE_COPY` LOCKED COPY banner (no second source of truth). Tier-1 material T&C change (new §10.4 disclaimer-of-warranty text) → `TC_VERSION` bump forcing `/accept-terms` re-acceptance; `TC_DOCUMENT_SHA` + AUP SHA repinned on final bytes; seed-script TC_VERSION parity (`seed-dev-users.sh` + `seed-qa-user.sh` → 2.3.0, `check-tc-document-sha.sh` Step 2.5). Eleventy mirrors in 3-way lockstep (heading-sequence + Last-Updated date + T&C body-equivalence green). No Article 30 amendment — the interactive Concierge chat runtime (cc-dispatcher / permission-callback, persisting tool-call metadata) is already registered as PA-2 (Conversation Data); autonomous command execution is a tool-call on the operator's own connected workspace within it (no new purpose / data category / recipient / sub-processor). The removed per-command approval step is a workspace-safety control on the operator's own systems, not a registered PA-2 Art. 32 §(g) TOM, so no limb amendment. NOT PA 21/22 (those are the separate Inngest agent.spawn TodayCard leader-prompt runtime). gdpr-gate: no regulated-path diff, zero Critical/Important. CPO sign-off (plan) + CLO sign-off (Tier-1) operator-attested in PR body. No new sub-processor. |
| BYOK delegation acceptance modal mount restored (SOL-49) | SOL-49 / PR #4778 | 2026-06-02 | UX-only fix. `apps/web-platform/components/settings/delegation-acceptance-modal.tsx` (created PR #4508, extended PR #4627) was orphaned — never mounted in any parent. Grantees with a pending delegation saw the "Pending acceptance" text but had no clickable surface to record consent. The SQL gate (mig 075 `current_byok_side_letter_version()` + acceptance-EXISTS clause) and the WORM `byok_delegation_acceptances` table (PR #4508) were intact throughout — no leases issued without consent, no lawful-basis chain bypass. Fix wires the existing modal into `app/(dashboard)/dashboard/chat/layout.tsx` via the `DelegationBanner`, gated on `acceptance.accepted === false || withdrawn === true` (3-state enum: never-accepted / active / withdrawn). All three modal callbacks (`onAccepted` / `onDeclined` / `onWithdrawn`) close the modal AND call `router.refresh()` so the server-component re-fetch resolves the unmount condition. Modal POST handlers gained explicit `catch` blocks mirroring to Sentry via `reportSilentFallback` per `cq-silent-fallback-must-mirror-to-sentry`. Modal container gained `role="dialog"` + `aria-modal="true"` + `aria-labelledby` (a11y). No new sub-processor; no Article 30 register amendment; no DPD / Privacy Policy / AUP text change. Brand-survival threshold: `single-user incident` (carry-forward from #4625 brainstorm). |
| LinkedIn org-page legal-track (LIA + Privacy Policy + DPD + jikigai.com privacy-policy redirect) | #4051 / PR #4081 | 2026-05-19 | Brand-survival threshold: single-user incident (carried forward from #4046 brainstorm). New LIA at `knowledge-base/legal/legitimate-interest-assessments/2026-05-19-linkedin-org-page-lia.md` (Art. 6(1)(f) three-part test for LinkedIn Page operation + aggregate Page Insights consumption; K-bis transfer scope-out as Art. 6(1)(c) cross-reference). Privacy Policy extended with §4.10 / §5.12 LinkedIn Ireland Unlimited Company / §5.13 Microsoft Ireland Operations Ltd / §6 dual-basis / §7 retention / §8.1 Art. 17 LinkedIn-published-content carve-out (EDPB Guidelines 5/2019) / §10 international-transfers. DPD §2.3(p) + §4.2 + §6.4 + §10.3 mirror in structured form. Article 30 PA15 lawful-basis (ii) cross-reference promoted from forward-looking to live LIA path; PA15 closing paragraph rewritten resolved-state. Folded-in infra: `cloudflare_ruleset.jikigai_com_redirects` on aliased `cloudflare.jikigai_com` provider (jikigai.com/legal/privacy-policy 301 → soleur.ai/pages/legal/privacy-policy.html). Counsel sign-off: operator-attested (Jean Deruelle, Jikigai SARL gérant) at `knowledge-base/legal/audits/2026-05-counsel-review-4051.md`; external counsel re-review triggers recorded in the audit file (first Page Insights API call OR first non-Soleur tenant onboarding). Operator action required at apply time (post-merge): expand `cf_api_token_jikigai_com` Cloudflare API token scope from `Zone:DNS:Edit on jikigai.com` to `Zone:DNS:Edit + Zone:Ruleset:Edit on jikigai.com`; apply ruleset via `terraform apply -target=cloudflare_ruleset.jikigai_com_redirects`; update LinkedIn Developer app 229658411 privacy-policy URL to `https://jikigai.com/legal/privacy-policy`; proceed with #4046 Phase 5.4 K-bis appeal at `https://www.linkedin.com/help/linkedin/ask/dsapi`. |
| W7 DSAR cohort audit (cc-soleur-go disclosure-asymmetry window) | #3603 / PR #3662 | 2026-05-12 | Backward-looking Art. 15 cohort audit for window 2026-05-05 → 2026-05-11 (PR #3286 first ship through PR-A1 AC11 verification). Four channels: GitHub `label:legal` (programmatic), `legal@jikigai.com` inbox + Linear `Art. 15 OR DSAR` + Discord support (operator-manual completeness flags). **Result: zero Art. 15 / DSAR requests in window.** No supplementary disclosure required. Evidence: `knowledge-base/legal/audits/2026-05-12-w7-dsar-cohort-audit.md` (load-bearing per CLO Q4 — null result is the audit answer). Operator-amendment posture documented for post-merge discovery. |
| `CC_PERSIST_USAGE=true` operator decision + PR-C disclosure-side close | #3603 / PR #3662 | 2026-05-12 | Operator confirmed at PR-C plan-time that the flip to `CC_PERSIST_USAGE=true` in both `prd` and `prd_scheduled` Doppler configs was a **deliberate decision** taken with PR-C disclosure in flight — not a flag-flip-by-mistake. PR-C #3662 closes the disclosure side: Privacy Policy §4.7, DPD §2.3(i), GDPR Policy §3.7 + §10 #10 each carry the `usage` jsonb disclosure. SIGKILL persistence-limitation transparency sentence added to Privacy §4.7 (and Notes in GDPR §10 #10) with cross-reference from Privacy §8.1. Not an Art. 33-notifiable incident; planned-and-known operator-decision audit trail. Re-evaluation deferred to post-OP3 (after umbrella close). |
| Privacy Policy §4.7 refresh — `messages.usage` disclosure (Art. 13(3)) | #3603 / PR #3648 | 2026-05-12 | Disclosed cc-narrowed per-turn cost telemetry (`{ cost_usd }`) on Concierge (`/soleur:go`) surface; `CC_PERSIST_USAGE=true` flipped on in Doppler `prd` 2026-05-12. Legacy `agent-runner` `UsageSnapshot` shape on aborted turns unchanged. Data-minimisation rationale: Art. 5(1)(c). Legal basis, recipients, retention, and TOMs inherited from parent conversation row (Art. 6(1)(b); Supabase eu-west-1 + Hetzner hel1; RLS on `conversation_id`; cascade-delete on account erasure). Sync committed to `plugins/soleur/docs/pages/legal/privacy-policy.md`. Counsel review pending. |
| Article 30 Register (RoPA) — initial draft | #3603 / PR #3648 | 2026-05-12 | Initial draft at `knowledge-base/legal/article-30-register.md` covering 8 processing activities (account/auth, conversation data incl. `messages.usage`, billing, KB sharing, web push, newsletter, CLA signatures, operational telemetry/breach-detection). Vendor mapping consolidated. Counsel review pending — outstanding items: controller legal form (SAS vs SARL), Web Push transfer characterisation, P0 mirror retention envelope, Art. 30(5) micro-enterprise derogation confirmation. PA8 §(f) Hetzner pino retention concretised in PR #3751 (2026-05-13). DPD §(m) operational telemetry & breach detection user-facing entry added in PR #3708 (2026-05-13), mirroring PA8 §(c)/§(d)/§(e)/§(f) in user-readable prose with Sentry §4.2 row + §6.4 intra-EU SCC bullet. PR also backfilled §(l) DSAR self-serve export to the Eleventy mirror (canonical-vs-mirror drift fix; pre-existing). |
| Supabase DPA update (Braintrust sub-processor) | #1056 / PR #1298 | 2026-04-07 | Supabase confirmed (Tracy Lane, 2026-04-07): Braintrust tracing disabled for EU-hosted projects (eu-west-1). No cross-border transfer. No data sent to Braintrust. No re-signing required — existing DPA (signed 2026-03-19) remains in effect. Sub-processor accepted via Section 6.5 notification mechanism. |
| Sentry residency cleanup (Phase A1) | #3861 / PR #3863 | 2026-05-15 | Probe confirmed DE-bound ingest; tfstate captured wrong-cluster US shadow. A1 stops wrong-shape §5(2) evidence regen; A2 cluster surgery follows on operator billing prereqs. |
| Per-action-class scope-grant ledger + DSAR Art. 15+20 coverage (PR-G) | #3947 / PR #3984 | 2026-05-18 | Migration 048 adds `public.scope_grants` (WORM, RLS founder-scoped, `grant_action_class` / `revoke_action_class` / `anonymise_scope_grants` SECURITY DEFINER RPCs). `scope_grants` added to `DSAR_TABLE_ALLOWLIST` (Art. 15+20, `founder_id` owner) so the self-serve export bundles each user's consent chain. WORM trigger uses structural-shape bypass (founder_id non-NULL → NULL, all other columns unchanged) — supersedes 043/044's broken `current_user = 'service_role'` role-gate pattern (see learning `2026-05-18-worm-trigger-bypass-role-check-fails-under-postgrest-routing.md`). Stripe `invoice.payment_failed` webhook gates `inngest.send` on `isGranted("finance.payment_failed")`. AUP §5.4 amended for automated agent actions. Brand-survival threshold: `single-user incident`. |
| PR-G post-merge gates (cohort onboarding flag flip → live) | #3947 / PR #3984 | 2026-05-19 | POST-1 (migrations 048+049 applied to prd) verified by `web-platform-release.yml` at sha `a6416127` (`migrate` + `verify-migrations` green). POST-2 (operator scope_grant seeded in prd at `finance.payment_failed`/`draft_one_click`) via Playwright; persistence confirmed by reload ("Active at Draft, one click since 5/19/2026"). **POST-2 part 2 (preview-env Stripe smoke) skipped** with justification: substrate is self-hosted on Hetzner per ADR-030 (no Vercel preview deploys); TR3 precondition + grant-path tests in CI cover the predicate-wiring drift the smoke was meant to catch. POST-4 (`SOLEUR_FR5_ENABLED=true` in Doppler prd) at 10:39:58Z; pre-flip read `false` → set → verify probe `true`. Container restart at 12:59:20Z via deploy-webhook redeploy of `v0.94.0` (Doppler env baked at container start per `apps/web-platform/infra/cloud-init.yml`; runtime hot-reload not supported, redeploy required to pick up Doppler changes — learning to capture). POST-5 (synthetic `invoice.payment_failed` smoke) at 13:05:33Z; HTTP 200 + `{"received":true}`; dedup row `evt_smoke_post5_1779195932` landed in `processed_stripe_events`. Smoke proved webhook alive on restarted container, signature verified with prd `STRIPE_WEBHOOK_SECRET`, new `SOLEUR_FR5_ENABLED=true` code path executed without crash. **Runtime grant-path gap (documented):** operator has no `stripe_customer_id` in prd, so the full flag → customer → grant → `inngest.send` runtime path was NOT exercised end-to-end. Covered by TR3 + grant-path unit tests in CI on the merged code; deferred to first real Stripe failed-payment event from a founder with both `stripe_customer_id` AND active `scope_grant`. POST-7 (`/soleur:postmerge`) at 13:08Z: prd `/health` returned `status:ok, version:0.94.0, build_sha:a6416127, supabase:connected, uptime:511s` (uptime consistent with the redeploy). UI bug surfaced during POST-2: stale "Not authorized" status text after optimistic-UI authorize click — filed as #4048 (cosmetic, non-blocking). POST-3 (CPO sign-off + first dogfood founder selection) operator-paced; not blocking the flag flip per K14 (per-grant deny-by-default predicate at `route.ts:437` is the load-bearing tenant gate — env flag is now a global kill-switch only). Brand-survival threshold: `single-user incident`. |
| Cross-process JWT-deny RLS enforcement + admin revoke surface (PR-E follow-up) | #3930 + #3932 / PR #4418 | 2026-05-25 | Single-user-incident threshold (carry-forward from PR-E #3922). Migration 068 closes the cross-process stolen-JWT gap PR-E (Node-process-local consumer at `apps/web-platform/lib/supabase/tenant.ts` `denyProbe`) left open. Three new SECURITY DEFINER functions — `revoke_jti(uuid, uuid, text)` (service-role-only writer; ON CONFLICT DO NOTHING for operator-retry idempotency), `my_revocation_status()` (authenticated reader; jti deliberately omitted per #3930 jti-enumeration mitigation), `is_jti_denied_from_jwt()` (STABLE wrapper used by RLS predicate; reads `current_setting('request.jwt.claims', true)::jsonb->>'jti'`) — all pin `SET search_path = public, pg_temp` per `cq-pg-security-definer-search-path-pin-pg-temp`. 21 RESTRICTIVE `<table>_jti_not_denied` policies on every tenant table (19 original + `organizations` + `workspace_member_removals` added at review time after empirical re-derivation caught the gap); AND-combined with existing PERMISSIVE policies. `GRANT EXECUTE ON is_jti_denied(uuid) TO authenticated` added so the transitive call under authenticated context succeeds (jti-enumeration oracle acknowledged: boolean-only return + 2^128 UUID space makes practical enumeration infeasible; documented in mig 068 §4 docblock). Operator CLI `apps/web-platform/scripts/revoke-jti.ts` (bun shebang, named-flag argv, createServiceClient, dev/prd Supabase URL stdout disclosure per `hr-dev-prd-distinct-supabase-projects`, post-RPC re-read sanity; JWT-scope-only — does NOT terminate the underlying Supabase session). New `revocation_notice` WSMessage variant discriminates the generic "Authentication unavailable; retry shortly" toast into "Your session was revoked. Reason: X." when ws-handler's `tenantFor` catch site sees `RuntimeAuthError.cause === 'denied_jti'` — emission is now `await`-ed before `ws.close()` (review fix; was `void` which raced the close). Verify-068 sentinel passes 31/31 against dev (28 forward + 3 anon-revoke matrix added at review). Forward+rollback dance applied + rolled back in a single transaction — clean reverse confirmed. No Article 30 register amendment (PA1 already covers `denied_jti` from PR-B; jti = random UUID, not personal data; founder_id already in PA1 scope). Art. 32(1)(b) "ongoing confidentiality" strengthened — was Node-process-local, now extends across process boundaries (PostgREST). Closes the 2-of-7 deferred-scope-outs from PR-E (#3928, #3929, #3931, #3933, #3934 stay open per their re-evaluation triggers). |
| Art. 5(2) audit-writer + is_jti_denied consumer (PR-E) | #3887 / PR #3922 | 2026-05-16 | Migration 037's `audit_byok_use` WORM + `write_byok_audit` SECURITY DEFINER RPC + `is_jti_denied` reader (shipped via PR-B #3395) wired to a Node-process-local consumer at `apps/web-platform/lib/supabase/tenant.ts` (cache-hit + cache-miss deny-probe with Sentry mirror via `mirrorWithDebounce`). CI-enforced writer-sweep over `runWithByokLease(...)` call sites under `apps/web-platform/server/**`. WORM integration test pins UPDATE/DELETE-raises-P0001. No Article 30 register amendment (deny-list in PA1 auth scope; jti = random UUID, not personal data; Sentry mirror in PA2 from PR-D). **Residual surfaces tracked as deferred-scope-out: #3932** PostgREST-side RLS predicate (closes cross-process stolen-JWT exposure), **#3930** admin `revoke_jti` RPC (founder-level revocation), **#3933** in-flight `SupabaseClient` invalidation hook, **#3928/#3929/#3931/#3934** (TTL sweep / per-sub-call audit / deny-RPC cache / synthetic-fixture sweeper). Plan: `knowledge-base/project/plans/2026-05-16-feat-pr-e-audit-byok-jti-deny-plan.md`. Brand-survival threshold: `single-user incident`. CPO sign-off captured at deepen-plan; user-impact-reviewer invocation at /soleur:review time cross-reconciled two contested architectural P1s into the residual-scope-out filings above. |
| Workspace membership audit log (Article 30 PA-20 + DPD/Privacy/GDPR) | #4231 / PR #4287 | 2026-05-22 | Single-user-incident threshold. Migration 063 adds `public.workspace_member_actions` — append-only audit ledger of workspace membership mutations (add / remove / role-change) on `public.workspace_members`. AFTER trigger writes one row per mutation, capturing actor via the `workspace_audit.actor_user_id` session GUC set by `invite_workspace_member` + `remove_workspace_member` RPCs (re-CREATEd in mig 063 to prepend the GUC `set_config` call). Pure-reject WORM trigger on UPDATE/DELETE; bypass via `SET LOCAL session_replication_role='replica'` in the three SECURITY DEFINER wrapper RPCs (`anonymise_workspace_member_actions`, `purge_workspace_member_actions`, in-migration backfill) per mig 037 + mig 051 precedent (NEVER `current_user='service_role'` per learning `2026-05-18-worm-trigger-bypass-role-check-fails-under-postgrest-routing.md`). Owner-only reader RPC `list_workspace_member_actions` with keyset cursor `(created_at, id)` (review-found tiebreak fix); JOIN traversal `organizations.owner_user_id` → `workspaces.organization_id` returns empty for non-owners (no table-existence leak). RLS-zero policies + named-role REVOKE matrix per learning `2026-05-06-supabase-default-privileges-defeat-revoke-from-public`. 7-year retention via pg_cron `workspace-member-actions-retention` daily at 04:00 UTC invoking `purge_workspace_member_actions()` wrapper; observability via `cron.job_run_details` + `RAISE LOG 'audit_retention_purge'` routed to Supabase logs → Vector → Better Stack (monitor provisioning deferred to flag-flip-prep PR per runbook `knowledge-base/engineering/operations/runbooks/cron-retention-monitor.md`). Article 17 erasure cascade via `anonymise_workspace_member_actions(p_user_id)` step 3.93 in `server/account-delete.ts` (between `anonymise_organization_membership` 3.92 and `auth.admin.deleteUser` 4); cascade DELETE at 3.91 does NOT create new audit rows because mig 063 re-CREATEs `anonymise_workspace_members` to bypass the AFTER trigger via replica-role (eliminates orphan-PII window). `workspace_member_actions` added to `DSAR_TABLE_ALLOWLIST` with OR-semantics via new `additionalOwnerFields` schema field (`actor_user_id` + `target_user_id`); DSAR worker reads via two `.eq()` chains merged + deduped by row id; per-row-where lint extended with inverse assertion (every declared owner column must have ≥1 read chain — review-found Art. 15 silent-drop regression class). Sentinel sweep script `apps/web-platform/scripts/check-workspace-members-write-sites.sh` (TR9) enumerates every `workspace_members` write site and asserts each routes through a GUC-setting RPC or documented admin-tool/fixture path; wrapped in vitest for standard CI. Article 30 PA-20 records the activity (Art. 6(1)(c) accountability + Art. 6(1)(f) operational integrity; LIA balancing test favors processing — internal-use-only, 7y bounded, data subjects retain Art. 15/17/20 rights, audit protects subjects from controller misconduct; purpose-limitation clause prohibits product-analytics / sales / ML / feature decisions). DPD §Last Updated + Privacy Policy §Last Updated + GDPR Policy §5.3 cascade prose all updated in lockstep. Internal jikigai workspaces only at v1 (gated by `FLAG_TEAM_WORKSPACE_INVITE`); customer-facing public-disclosure deferred to flag-flip-prep PR (T-01 / T-02 GDPR-gate fold-ins). Multi-agent review (12 agents): net SHIP, 7 inline P1/P2 fixes drained (trigger UPDATE attestation carry, cron DO/EXCEPTION wrap, backfill parity ASSERT, lock_timeout, keyset cursor tiebreak, inverse lint, sentinel TS_WRITES allowlist routing); architecture P1 substrate-divergence ADR deferred as already-shipping-architecture; architecture P2 reader org-owner-vs-workspace-owner deferred to flag-flip multi-owner migration. Counsel review: operator-attested in this PR. CPO sign-off: carry-forward from parent #4229 Decision 10 (workspace_member_actions narrowed v1 within Decision 10's bounded surface). Migration apply deferred per migration-checklist.md (`## prd apply — pending`) — applies post-merge via `web-platform-release.yml#migrate`. Brand-survival threshold: `single-user incident`. |
| PR-I template authorizations + first-send-IS-auth + Art-17 cascade (Article 30 PA-18 + DPD/Privacy/GDPR/AUP) | #4078 / PR #4213 | 2026-05-21 | Single-user-incident threshold. Migration 053 adds `public.template_authorizations` (WORM, RLS owner-select only — no owner-insert; writes route exclusively through SECURITY DEFINER `authorize_template` RPC). Three SECURITY DEFINER RPCs (`authorize_template` / `revoke_template_authorization` / `anonymise_template_authorizations`) use `SET LOCAL session_replication_role = 'replica'` for WORM bypass per mig 051 §(h) precedent (NEVER `current_user='service_role'` per learning `2026-05-18-worm-trigger-bypass-role-check-fails-under-postgrest-routing.md`). NOT NULL bounded columns: 100-send max, 90-day expiry, 30-day soft re-confirm (provisional defaults; calibration deferred to #4217). 8-value `revocation_reason` CHECK enum (paired-null with `revoked_at`). Partial-UNIQUE active index on `(founder_id, template_hash) WHERE revoked_at IS NULL`. First-send-IS-authorization pattern: the Send click on a labeled `draft_one_click` button IS the Art. 7(3) "specific" + "informed" consent act; send route auto-calls `authorize_template` on no-existing-row then proceeds to `writeActionSend` in the same request. `isTemplateAuthorized` predicate runs AFTER `isGranted` for `tierRequiresTemplateAuth(grant.tier) === true` tiers only (compile-time-exhaustive switch over ActionClassTier); fail-closed on DB exception (PredicateException → 500 + Sentry kind:template_predicate_timeout); auto-revoke best-effort side effect on expired/quota_exhausted keeps the scope-grants UI honest. Founder revoke surface at `/dashboard/settings/scope-grants` (new section + per-row Revoke button); `revoke_template_authorization` RPC gates `reason='founder_revoked'` when `auth.uid() IS NOT NULL` (operator/cascade callers retain full enum access). `template_authorizations` added to `DSAR_TABLE_ALLOWLIST` (Art. 15+20, `founder_id` owner). Article 17 cascade extends `server/account-delete.ts` step 3.83 BETWEEN `anonymise_action_sends` and `anonymise_scope_grants` — SEMANTIC ordering (Art. 5(2) attribution: `dsr_erasure` set on child rows BEFORE parent grant's founder_id nulled), NOT FK-driven. Article 30 PA-18 records the activity (plan §Phase 10.1 referenced PA-16 but that slot was already taken by `action_sends`; PA-18 used to preserve register ordering). DPD §2.3(t) + Privacy Policy §8.3 extension + GDPR Policy §5.3 cascade prose + AUP §5.4 (`policy_violation` revocation outcome) all updated in lockstep. Eleventy mirrors of DPD/Privacy/AUP carry dual-date update (May 21, 2026 in both hero `<p>` and body `**Last Updated:**`). ADR-035 records the code-static template registry decision; folds the un-revocability rationale (former ADR-036) into DPD §2.3(t) and the two-probe ordering rationale (former ADR-037) into ADR-035 §Consequences (e). Multi-agent review (6 agents at single-user-incident threshold): net SHIP, 1 P1 (RLS owner-insert policy admitting direct INSERT bypass) fixed inline, 12 P2 (incl. RPC reason gate, predicate race fix, exhaustive tier switch, scope-grants JOIN belt-and-suspenders, DENY_REASON_COPY typing) fixed inline, 1 architectural-pivot scope-out filed at #4236 (cross-cutting self-DSAR ceremony for 5 anonymise_* RPCs — migs 043/044/048/051/053). Counsel review: operator-attested in this PR. **Splits filed:** #4216 (PR-I+1 classifier-feedback retroactive UI), #4217 (bound calibration). Brand-survival threshold: `single-user incident`. CPO sign-off captured in this PR. |
| PR-H multi-source priority signals (Article 30 PA-17 + DPD/Privacy/AUP) | #3244 / PR #4066 | 2026-05-19 | Single-user-incident threshold. Article 30 PA-17 (GitHub-sourced priority signals) added (PA-15 LinkedIn restored after collision fix); DPD + Privacy Policy + AUP amended with the GitHub Inc. sub-processor reaffirmation, render-time `redactGithubSourcedText` Art. 14 minimization gate, card-screenshot-redaction operator advisory. Migration 052 ships `audit_github_token_use` ledger + `record_github_token_use` SECURITY DEFINER RPC + WORM trigger + `anonymise_audit_github_token_use` Art. 17 cascade. Webhook delivery dedup via `processed_github_events`. Founder-facing read surface at `/dashboard/audit/github` (empty-state until PR-H+1 wires the writer). Counsel review: operator-attested per `knowledge-base/legal/audits/2026-05-counsel-review-4066.md` — five artifacts signed off (Article 30 register, DPD, Privacy Policy, AUP, audit-ledger Art. 17 cascade) in lieu of external counsel review for v1 (Soleur-as-tenant-zero posture). External counsel re-review trigger: first non-Soleur tenant onboarding OR first founder installing the GitHub App with regulated-data repositories. Post-merge operator runbook: `terraform apply` for `infra/github-app.tf` + `kb-drift.tf` + `alerts-github-webhook.tf`; Doppler `prd_kb_drift_walker` bootstrap; GitHub App creation via committed manifest (#4115) + webhook URL wiring. PR-H+1 (#4098) tracks the per-Octokit audit writer + send/edit/discard button wiring + audit dashboard population. |
| Web platform vendor DPA review | #670 / PR #732 | 2026-03-18 | All 4 vendor DPAs signed/verified. Expense ledger updated. Legal docs updated |
| BYOK delegation PR-B legal scaffolding (Side Letter, DPD §2.3(w), AUP §5.6, PA-23) | #4232 / PR #4508 | 2026-05-26 | Delegation Consent Side Letter template v1.0.0 (distinct from workspace Side Letter); DPD §2.3(w) delegated-credential prompt routing disclosure; AUP §5.6 delegation responsibilities; Article 30 PA-23 (consent + cost telemetry); migration 074 `byok_delegation_acceptances` consent table. All gated by `BYOK_DELEGATIONS_ENABLED` flag (default OFF). No new sub-processor. Brand-survival threshold: single-user incident. |
| BYOK delegation consent ENFORCEMENT (gate + withdrawal + server-owned version) | #4625 / PR #4627 | 2026-05-29 | Single-user-incident threshold. Closes the live joint-controllership exposure left by PR-B: `resolve_byok_key_owner` activated a delegation WITHOUT checking acceptance (a grantee's prompts processed under the grantor's key, grantor seeing cost telemetry, BEFORE consent). Migration 083 adds `current_byok_side_letter_version()` (SQL source of truth, == TS `BYOK_SIDE_LETTER_VERSION`, CI parity gate) and an acceptance-EXISTS clause to the resolver (gate-in-SQL — zero TS call-site changes, 064 TOCTOU intact, own-key short-circuit preserved). The accept route stops trusting client-supplied `side_letter_version` and stamps the server constant. Migration 084 adds the `byok_delegation_withdrawals` WORM ledger (NO UNIQUE — append-only + Art. 17 multi-row safety; `user_id` NULLABLE for anonymise) + `withdraw_byok_delegation_consent(p_delegation_id)` grantee-only SECURITY DEFINER RPC (derives `auth.uid()`, no `p_user_id` — SS-F3) + a second resolver clause (version-agnostic `NOT EXISTS(withdrawal newer than latest acceptance)`, COALESCE+`>=` closes NULL/tie fail-open) + a per-turn consent re-gate in `check_and_record_byok_delegation_use` that stops in-flight billing within one turn and debits the grantee (`consent_withdrawn` attribution; ADR-040 sibling decision). Withdrawal added to DSAR Art. 15+20 + Art. 17 cascade (`anonymise_byok_delegation_withdrawals` step 5.12). DPD §2.3(w) + Privacy Policy + GDPR Policy §5.3 updated in lockstep; Delegation Consent Side Letter rewritten as versioned in-app Art. 26 arrangement; migration 074 header Art. 6/26 conflation corrected. All gated by `BYOK_DELEGATIONS_ENABLED` (default OFF); flag flip is CLO-sign-off-gated (post-merge, AC10/AC11). No new sub-processor. |
| Vendor checklist gate added | #670 / PR #732 | 2026-03-18 | PR template and constitution updated with vendor compliance section |

## How to Update This Document

- When a DPA is signed, updated, or revoked: update the Vendor DPA Status table
- When a new compliance item arises: add to Active Compliance Items with issue reference
- When a compliance item is resolved: move from Active to Completed
- Update `last_updated` frontmatter date on every change
