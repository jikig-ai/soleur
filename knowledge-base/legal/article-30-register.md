---
title: "Article 30(1) Register of Processing Activities"
type: article-30-register
jurisdiction: FR, EU
version: 0.1.0-draft
last_reviewed: 2026-05-15
controller: "Jikigai SARL (France; 25 rue de Ponthieu, 75008 Paris)"
dpo: "None designated — Soleur is below the GDPR Art. 37(1) mandatory-designation thresholds; designation remains optional"
contact: "legal@jikigai.com"
status: draft-requires-counsel-review
---

> **DRAFT — This document was generated by AI and requires professional legal review before use. It does not constitute legal advice.**

# GDPR Article 30(1) Record of Processing Activities (RoPA)

This is the canonical Register of Processing Activities maintained by **Jikigai** in its capacity as **data controller** for the Soleur project, as required by Article 30(1) of Regulation (EU) 2016/679 (GDPR). It is the working document a supervisory authority (in our case the CNIL, France) would request during an audit under Article 58(1)(a).

## 0. Controller Identification (Art. 30(1)(a))

| Field | Value |
|---|---|
| Controller name | Jikigai SARL |
| Legal form | SARL (Société à responsabilité limitée), France |
| Registered office | 25 rue de Ponthieu, 75008 Paris, France |
| Controller contact | legal@jikigai.com |
| Representative (Art. 27) | Not applicable — controller is established in the Union |
| DPO (Art. 37) | None designated. Designation is **not mandatory**: Soleur does not carry out (i) processing as a public authority, (ii) large-scale systematic monitoring of data subjects as a core activity, or (iii) large-scale processing of Art. 9/10 special-category data. Threshold to be re-assessed quarterly. |
| Joint controllers (Art. 26) | None |
| Register custodian | Soleur Chief Legal Officer role (CLO agent) — review cadence: quarterly + on any new processing activity / new sub-processor / new third-country transfer |

**In-scope surfaces:** Soleur Web Platform (`app.soleur.ai`); Soleur documentation site (`soleur.ai`); Soleur GitHub repository (`jikig-ai/soleur`). The locally-installed Soleur Claude Code plugin is **out of scope** of this register — it processes no personal data on Jikigai infrastructure and Jikigai is not a controller for it (see `docs/legal/privacy-policy.md` §4.1).

**Out-of-scope (independent controllers):** Anthropic (when the end-user supplies their own Anthropic API key under BYOK); GitHub (repository hosting); the user's own Claude Code installation.

---

## Processing Activity 1 — Account & Authentication

| Art. 30(1) limb | Entry |
|---|---|
| **(b) Purposes** | Create, authenticate, and maintain end-user accounts on `app.soleur.ai`; OAuth identity federation and account-linking on verified email; provisioning of per-user workspaces; secure storage of user-supplied (BYOK) third-party API keys. |
| **(c) Categories of data subjects** | End-users of the Web Platform (registered account holders). |
| **(c) Categories of personal data** | Email address; hashed password (bcrypt via Supabase GoTrue); OAuth provider user ID, display name, profile picture URL (Google / Apple / GitHub / Microsoft); session tokens (JWT); MFA enrolment data where enabled; user-supplied third-party LLM API keys (BYOK), stored encrypted with AES-256-GCM. |
| **Special categories (Art. 9 / 10)** | None. |
| **Lawful basis** | Art. 6(1)(b) — performance of the Terms of Service contract. |
| **(d) Recipients** | Supabase Inc (processor — auth + database); Hetzner Online GmbH (processor — hosting plane on which app servers run); Cloudflare Inc (processor — TLS termination, edge proxy). |
| **(e) Third-country transfers + safeguards** | **No transfer.** Supabase project deployed to `eu-west-1` (Dublin, Ireland). Hetzner CX33 in `hel1` (Helsinki, Finland). Cloudflare TLS termination is global CDN — international transfers covered by EU-US DPF certification + SCCs + Global CBPR. |
| **(f) Retention** | Account data retained for the life of the active account; deleted on user-initiated deletion (cascade delete via Supabase FK). Encrypted BYOK API keys deleted on workspace deletion. Session tokens expire per Supabase auth defaults. |
| **(g) TOMs (Art. 32)** | (1) TLS 1.2+ in transit. (2) Encryption at rest at storage layer (Supabase managed). (3) BYOK keys encrypted application-side with AES-256-GCM before persistence. (4) Supabase Row-Level Security policies enforce per-`user_id` isolation on `public.users` (`auth.uid() = id`) and `public.api_keys` (`auth.uid() = user_id`) — RLS enabled in migration `001_initial_schema.sql`. (5) **Per-user JWT mint via `getFreshTenantClient(userId)`** for tenant-scoped reads/writes against `users` and `api_keys` — PR-C #3854 migrated ~34 call sites across `apps/web-platform/server/` away from RLS-bypassing `createServiceClient()` so RLS is evaluated under the requesting user's identity (layered control implementing Art. 25(1) data-protection-by-design: the JWT-scoped client and the RLS predicate must both be defeated to cross a tenant boundary, satisfying Art. 32(1)(b) ongoing confidentiality). (6) **BYOK plaintext-key zeroize-on-finally via `runWithByokLease(userId, async (lease) => …)`** in `cc-dispatcher.realSdkQueryFactory` (`server/cc-dispatcher.ts:889`) — plaintext API keys reside in V8 heap only within the lease's `AsyncLocalStorage` scope and are wiped by `zeroize(buf)` (`server/byok.ts:120`) in `finally` on success **or** throw — added PR-C #3854 / Closes #3392. (7) `.service-role-allowlist` + CODEOWNERS pin restricts residual service-role usage to permanent exceptions only (e.g., `auth.getUser` handshake, `sum_user_mtd_cost` RPC, attachments injection); allowlist shrunk 16 → 14 entries by PR-C #3854. (8) bcrypt password hashing (GoTrue). (9) MFA available. (10) Audit logging via Supabase + pino. (11) Signed authentication cookies (HttpOnly, Secure, SameSite). (12) **Fail-open detection on tenant-client mint** in `refreshSubscriptionStatus.tenantFor` (`apps/web-platform/server/ws-handler.ts:111` helper; `:565` caller) — when the per-user JWT mint returns `null` for a suspended/downgraded user, `reportSilentFallback` mirrors the event to Sentry and the 60 s cap-drift self-evict tick bounds the window in which a user could retain stale `users.subscription_status` / `users.plan_tier` entitlement to ≤ 60 s; added PR-C #3854 (billing-adjacent single-user-incident control per PR #3854 user-impact matrix). |

---

## Processing Activity 2 — Conversation Data (Messages, Turns, Usage Telemetry)

| Art. 30(1) limb | Entry |
|---|---|
| **(b) Purposes** | Provide conversational-AI interactions with Soleur domain-specific agents; persist user prompts, assistant responses, and tool-call metadata so users can resume, audit, and export their work; preserve aborted-turn outputs so users have a faithful record of what was produced and billed; record per-turn cost telemetry for usage transparency and billing-dispute resolution. |
| **(c) Categories of data subjects** | End-users of the Web Platform. |
| **(c) Categories of personal data** | Conversation metadata (domain leader, status, timestamps); message content (`messages.content`) containing user prompts and assistant responses — content **may incidentally contain personal data the user chooses to provide** (free-text input); tool-call payload metadata; aborted-turn status markers; per-turn cost telemetry (`messages.usage` jsonb column, see below). **Attachments (PR-D #3244 §4, 2026-05-16):** `message_attachments` rows (`filename`, `content_type`, `size_bytes`, `storage_path`) FK'd to `messages.id`, plus user-uploaded file content stored in the private `chat-attachments` Storage bucket — image (PNG / JPEG / GIF / WebP) and PDF up to 24 MB. File content **may incidentally contain personal data and Art. 9 special-category data the user chooses to upload** (e.g., a brand-guide PDF that references health-related operations, an incorporation document including beneficial-owner identifiers). Filenames are sanitised (path-separator strip + C0/DEL + U+2028/U+2029 strip + 255-char cap) before persistence so a crafted filename cannot smuggle a forged "another attached file" line into the assistant's prompt context. |
| **`messages.usage` column — added by PR #3648 (2026-05-12)** | New `jsonb` column on the `messages` table populated for cc-router rows when the `CC_PERSIST_USAGE` Doppler flag is `true` in `prd` (current state: enabled). **Shape on cc-router complete turns: `{ cost_usd: number }`** — minimised to cost-only per **Art. 5(1)(c) (data minimisation)**. The legacy agent-runner path emits a full `UsageSnapshot` on aborted turns only. No additional personal data is introduced by this column; it is per-turn telemetry already attributable via the parent `messages` row. |
| **Special categories (Art. 9 / 10)** | None systematically. Users are advised not to submit Art. 9 data via prompts or attachments (see Acceptable Use Policy); incidental ingress through either `messages.content` or `chat-attachments` content is treated as Art. 9 if discovered and triggers the `gdpr-gate` regulated-data surface rule (`hr-gdpr-gate-on-regulated-data-surfaces`). PR-D follow-up: explicit AUP warning for attachment Art. 9 upload (tracked separately). |
| **Lawful basis** | Art. 6(1)(b) — performance of the Terms of Service. For `messages.usage` cost telemetry: same contractual basis (necessary for billing transparency and Terms § 5.5 partial-turn consumption record). Attachments are sub-objects of a conversation (same lawful basis, same controller, same retention cascade) — no separate processing activity. |
| **(d) Recipients** | Supabase Inc (processor — `conversations` + `messages` + `message_attachments` tables + `chat-attachments` Storage bucket); Hetzner Online GmbH (processor — application compute, including transient workspace mkdir/write of downloaded attachment bytes during a turn); Cloudflare Inc (processor — edge proxy in front of `app.soleur.ai`). |
| **(e) Third-country transfers + safeguards** | **No transfer of message bodies or attachment bytes.** Supabase EU region (eu-west-1, Ireland) holds both relational rows and Storage bucket objects; Hetzner Finland holds transient per-conversation workspace files. Cloudflare edge metadata (request headers / IP) crosses borders under DPF + SCCs + CBPR. Signed-URL presign / fetch traffic is end-to-end TLS to the EU Storage origin (Cloudflare proxies do not see attachment bytes). |
| **(f) Retention** | Conversations and messages retained while the user account is active. Cascade-deleted on account deletion or per-conversation deletion request. Aborted-turn rows follow the parent-conversation retention. Erasure requests under Art. 17 honoured at row granularity. **Attachments:** `message_attachments` rows cascade with `messages` (FK `ON DELETE CASCADE` on `message_id`); messages cascade with `conversations` (FK `ON DELETE CASCADE` on `conversation_id`); Storage objects in `chat-attachments` are explicitly removed in `account-delete.ts:152` and quarantined per the per-conversation deletion path. Storage bytes do NOT cascade via FK — the application removal step is load-bearing. |
| **(g) TOMs (Art. 32)** | (1) TLS in transit. (2) Encryption at rest (Supabase managed). (3) Supabase Row-Level Security policies scoping `public.messages`, `public.conversations`, `public.team_names`, and `public.user_concurrency_slots` to the owning `user_id` (FK-join policy on `messages` via `conversations.user_id`); RLS enabled in migrations `001_initial_schema.sql` (messages, conversations), `018_team_names.sql`, and `029_plan_tier_and_concurrency_slots.sql` (user_concurrency_slots). (4) **Per-user JWT mint via `getFreshTenantClient(userId)`** for tenant-scoped reads/writes against `messages`, `conversations`, `team_names`, `user_concurrency_slots` — PR-C #3854 migrated call sites across `session-sync`, `api-messages`, `conversation-writer`, `conversations-tools`, `lookup-conversation-for-path`, `current-repo-url`, `kb-document-resolver`, `kb-route-helpers`, `ws-handler`, and `cc-dispatcher` from RLS-bypassing `createServiceClient()` to per-user JWT clients so RLS evaluates under the requesting user's identity (layered control implementing Art. 25(1) data-protection-by-design: the JWT-scoped client and the RLS predicate must both be defeated to cross a tenant boundary, satisfying Art. 32(1)(b) ongoing confidentiality). (5) **Write-boundary `assertWriteScope(userId, conversationId)` sentinel** in `cc-dispatcher` (`server/cc-dispatcher.ts:521` definition; `:1382` call site) — fail-closed runtime assertion that halts user-message persistence when the dispatch scope does not match the expected `userId`/`conversationId`; added PR-C #3854. (6) Data minimisation enforced on `messages.usage` (cc-router stores `cost_usd` only — not token counts, not prompt text). (7) Append-only audit trail via Supabase logs. (8) Cost-only schema reviewed against Art. 5(1)(c) before PR #3648 merge. (9) Cross-tenant RLS-deny integration tests per file × table tuple under `apps/web-platform/test/server/*.tenant-isolation.test.ts` (gated `TENANT_INTEGRATION_TEST=1`) — added PR-C #3854 to detect regression of the per-user-JWT path. (10) **Attachment-storage isolation (PR-D #3244 §4, 2026-05-16):** per-user folder prefix `${userId}/${conversationId}/` enforced at the Storage object name boundary; `storage.objects` RLS policies for `bucket_id='chat-attachments'` scoped by `(storage.foldername(name))[1] = auth.uid()::text` — migration 019 (SELECT) + migration 045 (INSERT/UPDATE/DELETE), load-bearing post-PR-D; defense-in-depth application-layer path-prefix validation at `apps/web-platform/server/attachment-pipeline.ts:83-86` (rejects path-traversal and out-of-prefix references); content-type allowlist via `ALLOWED_ATTACHMENT_TYPES` (image/PNG, image/JPEG, image/GIF, image/WebP, application/PDF); filename sanitisation (strip C0/DEL + U+2028/U+2029 + path separators; cap 255); uploads via service-role presigned URL (`createSignedUploadUrl`) with conversation-ownership verification at the presign route; `message_attachments` RLS policy joining `messages.conversation_id → conversations.user_id = auth.uid()` (migration 019 SELECT + migration 045 INSERT); cross-tenant `attachment-pipeline.tenant-isolation.test.ts` deny tests added PR-D #3244 (gated `TENANT_INTEGRATION_TEST=1`); `cc-dispatcher.ts` removed from `.service-role-allowlist` (14 → 13 PERMANENT entries) by PR-D. |

---

## Processing Activity 3 — Subscription & Billing

| Art. 30(1) limb | Entry |
|---|---|
| **(b) Purposes** | Manage paid-tier subscriptions for the Web Platform; collect payment; issue invoices; comply with French tax law. |
| **(c) Categories of data subjects** | Paying end-users of the Web Platform. |
| **(c) Categories of personal data** | Customer email address; Stripe customer ID; subscription status; plan + price metadata; invoice IDs. **Card data never reaches Jikigai infrastructure** — handled exclusively by Stripe Checkout (PCI SAQ-A scope). |
| **Special categories** | None. |
| **Lawful basis** | Art. 6(1)(b) — contract performance; Art. 6(1)(c) — legal obligation (FR Code de commerce Art. L123-22, 10-year accounting record retention). |
| **(d) Recipients** | Stripe Inc (processor — payment processing); Supabase Inc (processor — stores subscription status/metadata only). |
| **(e) Third-country transfers + safeguards** | Stripe processes in the **US** — covered by EU-US Data Privacy Framework (adequacy decision) + Standard Contractual Clauses Module 2 as supplementary safeguard. Stripe is PCI DSS Level 1 certified. |
| **(f) Retention** | Subscription status retained while account active. Invoice and payment records retained **10 years** per FR Code de commerce Art. L123-22. |
| **(g) TOMs** | TLS 1.2+ in transit; Stripe Checkout (server-side session, client-side redirect) keeps PAN/CVC out of Jikigai scope; Stripe webhook signatures (HMAC) verified; subscription metadata isolated in Supabase under RLS. |

---

## Processing Activity 4 — Knowledge-Base Document Sharing (Public Share Links)

| Art. 30(1) limb | Entry |
|---|---|
| **(b) Purposes** | Allow authenticated users to share individual knowledge-base documents via public URLs; serve shared documents to unauthenticated viewers; allow revocation. |
| **(c) Categories of data subjects** | (i) Sharing users (account holders); (ii) unauthenticated viewers of shared links. |
| **(c) Categories of personal data** | **Sharing user:** document ID, sharing `user_id`, creation timestamp, share token. **Viewer:** IP address, user-agent, request timestamp (server access logs only — no cookies set for unauthenticated viewers; no analytics). |
| **Special categories** | None. |
| **Lawful basis** | Art. 6(1)(b) — contract performance for share-link records (the user activated the feature); Art. 6(1)(f) — legitimate interest for viewer access logs (infrastructure security, abuse prevention). |
| **(d) Recipients** | Supabase Inc (processor — share-link records); Cloudflare Inc (processor — access logs at edge); Hetzner Online GmbH (processor — origin logs). |
| **(e) Third-country transfers** | Cloudflare edge processing — DPF + SCCs + CBPR. Origin (Supabase, Hetzner) EU-only. |
| **(f) Retention** | Share-link records retained while the link is active; deleted on owner revocation or account deletion. Cloudflare access logs follow Cloudflare retention defaults (rolling window). |
| **(g) TOMs** | TLS in transit; `noindex` meta tag on shared pages; share tokens are cryptographically random; instant-revocation supported (post-revocation requests return error); no tracking/analytics on shared-page viewers. |

---

## Processing Activity 5 — Web Push Notifications

| Art. 30(1) limb | Entry |
|---|---|
| **(b) Purposes** | Deliver browser push notifications to authenticated users when an AI agent requires their input (review-gate events) and the user is not actively connected to the Web Platform. |
| **(c) Categories of data subjects** | End-users who have explicitly granted browser-level notification permission. |
| **(c) Categories of personal data** | Push subscription endpoint URL (from the browser's push service); encryption keys `p256dh` and `auth`; created-at and last-used timestamps; association to `user_id`. |
| **Special categories** | None. |
| **Lawful basis** | Art. 6(1)(a) — consent (granted via the browser permission prompt; revocable in browser settings at any time). |
| **(d) Recipients** | Supabase Inc (processor — stores subscription records); the user's browser's **push service** (Apple APNs, Google FCM/Web Push, Mozilla autopush, Microsoft WNS) — acts as an independent infrastructure intermediary keyed by the user's browser, not contracted by Jikigai. Encrypted payloads only (RFC 8291 message encryption). |
| **(e) Third-country transfers** | Push-service endpoints resolve to provider infrastructure (typically US-based for FCM/APNs/WNS). Jikigai transmits **only an encrypted payload** to a user-controlled endpoint; the user's own browser/device vendor governs that infrastructure. Documented as user-directed transmission rather than a Jikigai-controlled transfer; counsel to confirm characterisation. |
| **(f) Retention** | Retained while account active. Expired/invalid endpoints (HTTP `410 Gone` from the push service) auto-deleted. Cascade-deleted on account deletion. |
| **(g) TOMs** | Payload encryption per RFC 8291 (p256dh + auth secret); VAPID identification of the application server; subscriptions scoped to `user_id` via Supabase RLS; revocation honoured via browser settings. |

---

## Processing Activity 6 — Newsletter Subscription

| Art. 30(1) limb | Entry |
|---|---|
| **(b) Purposes** | Send periodic newsletter emails about Soleur updates, features, and content to opted-in subscribers. |
| **(c) Categories of data subjects** | Newsletter subscribers (may or may not be Web Platform users). |
| **(c) Categories of personal data** | Email address (actively provided); IP address, referrer URL, subscription timestamp, browser/device metadata (auto-collected by Buttondown at subscription). |
| **Special categories** | None. |
| **Lawful basis** | Art. 6(1)(a) — consent (double opt-in via Buttondown confirmation email) for the email address; Art. 6(1)(f) — legitimate interest (service operation, abuse prevention) for the auto-collected technical metadata. |
| **(d) Recipients** | Buttondown (processor); Buttondown sub-processors (current list at `buttondown.com/legal/subprocessors`). |
| **(e) Third-country transfers + safeguards** | Buttondown is **US-based**. Transfers under EU Standard Contractual Clauses (Commission Implementing Decision (EU) 2021/914, Module 2 — Controller-to-Processor), incorporated by reference into Buttondown's DPA. Buttondown DPA applies to all plan tiers including free. |
| **(f) Retention** | Email retained until unsubscribe; aggregate anonymised retained per Buttondown defaults. Technical metadata per Buttondown's retention practices. |
| **(g) TOMs** | TLS in transit; double opt-in confirmation (consent verification); unsubscribe link in every email (consent withdrawal); no subscriber-data sharing for advertising. |

---

## Processing Activity 7 — Contributor License Agreement Signatures

| Art. 30(1) limb | Entry |
|---|---|
| **(b) Purposes** | Maintain an enforceable, auditable record of contributor IP-license grants to protect the integrity of Soleur's licensing framework. Both Individual CLA and Corporate CLA records are in scope. |
| **(c) Categories of data subjects** | Individual contributors signing the ICLA; authorised signatories of corporations signing the CCLA. |
| **(c) Categories of personal data** | GitHub username; signature timestamp; pull-request reference associated with the signing event. For Corporate CLA: signatory name + corporate email + corporate identity. |
| **Special categories** | None. |
| **Lawful basis** | Art. 6(1)(f) — legitimate interest in maintaining an enforceable record of IP-license grants. Balancing test: (i) data is publicly disclosed by the signer through the act of contributing, (ii) processing is the minimum necessary to evidence the grant, (iii) signer is informed of the record at signing time, (iv) no automated decision-making. |
| **(d) Recipients** | GitHub Inc (the `cla-signatures` branch is hosted in the public GitHub repository — i.e. record is public-by-design). |
| **(e) Third-country transfers** | GitHub is US-based — transfers under GitHub's published SCCs / DPF mechanisms (see GitHub's Global Privacy Practices). |
| **(f) Retention** | **Indefinite**. License grants under the CLA are irrevocable and survive any withdrawal of the signature record. Art. 17 erasure of the record will be honoured on request, but grants made under the CLA before deletion continue in full effect (this carve-out is stated in the CLA itself — see `docs/legal/individual-cla.md` and `docs/legal/corporate-cla.md`). |
| **(g) TOMs** | Public-by-design append-only record on a dedicated git branch; signature integrity protected by git's content-addressed commit hashes; access control over write path via GitHub branch protections. |

---

## Processing Activity 8 — Operational Telemetry & Breach-Detection Logs

| Art. 30(1) limb | Entry |
|---|---|
| **(b) Purposes** | (i) Detect, investigate, and respond to security incidents; (ii) anchor the **Art. 33 72-hour breach-notification clock** with auditable first-observed-at timestamps; (iii) operate the Soleur cc-dispatcher's `mirrorP0Deduped` cross-tenant + W4-orphan P0 mirror pipeline that flags severity-P0 anomalies for the on-call CLO/CTO rotation; (iv) general application error monitoring; (v) operate the secret-leak audit pipelines on infrastructure plumbing (Doppler, drift-guard). |
| **(c) Categories of data subjects** | End-users of the Web Platform (incidental presence in error context, traces, and P0 mirror anchors). |
| **(c) Categories of personal data** | (i) **Sentry:** error messages and stack traces — may incidentally include `user_id`, request paths, request headers. User identifiers passed through the centralised silent-fallback helpers (`reportSilentFallback`, `warnSilentFallback`, `mirrorP0Deduped` in `apps/web-platform/server/observability.ts`) are HMAC-SHA256-pseudonymized at the emit boundary (Recital 26 — controller cannot re-identify from the hash without the server-side pepper held in Doppler, not shared with the processor). Server-side direct `Sentry.captureException` / `Sentry.captureMessage` payloads are sanitised through the `apps/web-platform/server/sentry-scrub.ts:scrubRecursive` boundary which renames top-level and nested `userId`/`user_id` keys to `userIdHash` via the shared `hashUserIdValue` primitive (ADR-029 §I4). Authenticated request handlers bind `Sentry.getCurrentScope().setUser({id: hashUserIdValue(user.id)})` at the per-user-rate-limit HOC (`apps/web-platform/server/with-user-rate-limit.ts`) and inline at each of the eleven helper-migration sites (canonical inventory: header comment block in `apps/web-platform/server/sentry-scrub.ts`), each wrapped in `Sentry.withIsolationScope(...)` so the binding cannot bleed across requests under the custom-server boot path. On the client side, `apps/web-platform/lib/client-observability.ts` (the `"use client"`-importable mirror of the server helper) **strips** `userId` / `user_id` / `email` keys from `extra` at the helper boundary before calling Sentry — the browser bundle cannot carry the server pepper, so structural strip + Sentry `beforeSend` backstop (`apps/web-platform/sentry.client.config.ts` `stripUserContextFromEvent` over `event.user`, `event.extra`, `event.contexts`, `event.breadcrumbs[*].data`) replace pseudonymization on this surface. A `piiStripped` sentinel marks events where a regression was caught. The cross-tenant DSAR-export breach-detection path (`mirrorCrossTenantViolation` in `apps/web-platform/server/observability.ts`) emits SHA-256-pseudonymous identifiers (`offendingUserIdHash` + `expectedUserIdHash`, salt-keyed via `SOLEUR_SENTRY_PII_SALT`) for breach anchoring under Art. 33; the field names are intentionally domain-distinct from the HMAC-pseudonymous `userIdHash` emitted on the pino/helper path. (ii) **pino stdout (Hetzner-resident, EU-only):** structured app logs — `conversation_id`, request metadata, and a `userId` field. From the deployment timestamp of #3698 forward, the pino `formatters.log()` rename hook in `apps/web-platform/server/logger.ts` renames top-level `userId` / `user_id` keys to `userIdHash` on every `logger.{error,warn,info,debug}` emission across `apps/web-platform/server/**` and `apps/web-platform/app/**` (HMAC-SHA256 with a server-side pepper held in Doppler and not shared with the processor — Recital 26 pseudonymisation, retained as pseudonym to support breach-investigation linkage under (b)(ii)). Nested keys (e.g. `extra.userId`, properties attached to Error instances) are not transformed by design — the boundary contract documented in `ADR-029` (rename-at-boundary) constrains the renamer to top-level keys, and the CI gate at `.github/workflows/pr-quality-guards.yml#userid-bypass-lint` blocks new direct-emission sites that would bypass it. Under the documented fail-safe path (rename-helper exception inside `formatters.log`), the log line is preserved with raw `userId` to maintain incident-response context, surfaced once per worker via an operator `console.warn` signal; runtime degradation is operator-detectable rather than silent. Pre-deployment historical lines remain on the Hetzner host until they age out per the retention window in (f); no off-host copies are taken. Sentry-event payloads from the helper boundary (`reportSilentFallback` / `warnSilentFallback` / `mirrorP0Deduped`) are pseudonymized as in (i); symmetric direct-capture coverage is enforced at the `apps/web-platform/server/sentry-scrub.ts:scrubRecursive` boundary which renames `userId`/`user_id` keys (case-insensitive) to `userIdHash` regardless of nesting depth, complementing the pino `formatters.log` top-level boundary. (iii) **cc-dispatcher P0 mirrors (`mirrorP0Deduped` cross-tenant + W4-orphan paths) and `reportSilentFallback`/`warnSilentFallback`:** `userIdHash` (pseudonymous identifier), `conversationId`, `first_observed_at` ISO timestamp — minimised to identifiers + clock anchor. (iv) **Sentry monitor classes processed (post-2026 split):** issue alerts (the (i) channel above) and **cron monitor check-ins** — vendor-hosted heartbeat for the 9 scoped scheduled GitHub Actions workflows (slug, status `in_progress\|ok\|error`, ISO timestamp; no pseudonymous identifier — workflow checks are job-scoped, not user-scoped — operational metadata, not Art. 4 personal data). **Sentry log ingestion (Logs product) is NOT enabled and no application log content is forwarded to Sentry**; if a future change introduces a Sentry log channel, this cell, DPD §2.3(m), Privacy Policy §5.10, GDPR Policy operational-telemetry bullet, and `apps/web-platform/server/sentry-scrub.ts` must be extended in advance. Migration audit evidence (Article 30 §5(2) accountability): `knowledge-base/legal/audits/sentry-migration-audit-<YYYY-MM-DD>.md`, regenerated at each `reusable-release.yml` run. |
| **Special categories** | None systematically. |
| **Lawful basis** | Art. 6(1)(f) — legitimate interest in service security and integrity, balanced against the data-subject interest in confidentiality (mitigated by tight access scoping and short retention); Art. 6(1)(c) — legal obligation for the **Art. 33 (breach notification)** clock-anchor evidence captured by `first_observed_at`. |
| **(d) Recipients** | **Current recipients:** Sentry (processor — DE region, **Functional Software GmbH**); Hetzner (processor — captures stdout); the on-call Jikigai engineering/CLO rotation (internal). **Recipient-drift after-the-fact disclosure (Article 30(5) accountability evidence — phantom-ingest destination-unreachable window 2026-03-28 → 2026-05-16):** For 49 days the runtime `SENTRY_DSN` plus the 9 scheduled-workflow cron-checkin secret triple (`SENTRY_INGEST_DOMAIN` + `SENTRY_PROJECT_ID` + `SENTRY_PUBLIC_KEY`) resolved to an unowned third-party Sentry organization (org ID `4511123328466944`) on the EU ingest cluster rather than a Jikigai-controllable processor; no Sentry-side admin reachability existed for that organization (audit-token POSTs against `eu.sentry.io/api/0/organizations/4511.../` returned 401 throughout the window). Ten accounts existed in the prd `auth.users` table during the window — all operator-adjacent (founder accounts on `jikig.com` / `soleur.ai`; team accounts on `jikigai.com` / `soleur.ai`; internal QA / `ux-audit-bot@jikigai.com` / test accounts on `soleur.dev` and `example.com`; and two `gmail.com` test signups confirmed by the operator at PR-α merge time as friends-of-team / pre-team-account test of the external signup flow — none of which constituted arms-length external usage). Zero arms-length external signups occurred during the window; the operator-controller and operator-adjacent accounts (all under operator instruction or contractual relationship) were the only data subjects. SQL verification: `select count(*) from auth.users where created_at between '2026-03-28' and '2026-05-16'` returned 10 against the prd Supabase project at PR-α merge time 2026-05-17 (run via Doppler `DATABASE_URL_POOLER`); per-row email categorization recorded (with PII redacted to domain + role) in the committed audit at `knowledge-base/legal/audits/2026-05-17-sentry-phantom-ingest-window-auth-users-audit.md`. PIR `knowledge-base/engineering/ops/runbooks/sentry-phantom-ingest-destination-unreachable-postmortem.md` is the disclosure of record under the self-controller-self-processor framing for Art. 34. Recipient restored to a Jikigai-controllable DE-region Sentry organization (Functional Software GmbH, `eu.sentry.io`, org slug `jikigai-eu`) by PR #3945 merged 2026-05-17 (PR-β of Branch C; parent issue #3861, PR-α #3904). Recurrence-prevention control (introduced by PR #3945): a 4-gate destination-controllability check in `apps/web-platform/scripts/sentry-monitors-audit.sh` (org-reachable + project-scope + write-probe + DSN-org-id-matches-token-org-id) invoked on every Sentry-touching diff (`.github/workflows/sentry-audit-gate.yml`) and at deploy (`.github/workflows/reusable-release.yml`), additive to the pre-existing DSN cluster-substring residency-mismatch check at `apps/web-platform/scripts/sentry-monitors-audit.sh:60` — a drift-detection gate, not a continuous-controllability guarantee. |
| **(e) Third-country transfers + safeguards** | **Sentry processed in DE region** under SCCs (Sentry's standard EU-region terms). pino stdout never leaves Hetzner Finland. No third-country transfer for the breach-detection plane. |
| **(f) Retention** | Sentry: 90 days default (rolling). pino stdout: **json-file Docker driver, 30 MB rolling per container (max-size=10m × max-file=3)** pinned in `apps/web-platform/infra/cloud-init.yml:303-310`. Observed daily log volume: __TBD_OBSERVED_VOLUME__ (post-merge operator measurement; see runbook `knowledge-base/engineering/ops/runbooks/recover-userid-from-pino-stdout.md`). **Re-verification triggers:** annual review; on any change to `apps/web-platform/infra/cloud-init.yml` (daemon.json block); on any introduction of an off-host log shipper (promtail/vector/fluent/filebeat/rsyslog); on container restart policy change affecting log path. **No off-host copies are taken.** P0 mirror records: retained for the **statutory 72-hour clock window + investigation-completion buffer**; documented retention to be tightened by counsel review (target ≤ 12 months unless an open investigation extends). **Note on Art. 17 (erasure):** the cc-dispatcher mirror and silent-fallback emissions carry `userIdHash` (pseudonymous under GDPR Recital 26). Sentry and Hetzner cannot re-identify the data subject without the Doppler-held server pepper. On user-account deletion, the hashed identifier is allowed to age out per processor retention; no active processor-side erasure call is required. |
| **(g) TOMs** | Access restricted to on-call rotation + CLO; Sentry user-context scrubbing rules for known PII fields; secrets and tokens redacted before logging; pino structured-log redaction filters; P0 mirror payloads carry **only** identifiers + first-observed-at clock anchor (no message bodies); audit trail of who accessed P0 mirrors is itself logged. |

---

## Processing Activity 9 — cc-soleur-go Router In-Process MCP Tool Surface (Phase 1 status: not exposed)

| Art. 30(1) limb | Entry |
|---|---|
| **(b) Purposes** | Recorded in advance per #2909 V2-13 Phase 1 (deny-by-default scaffolding). Phase 1 ships zero tool promotions; the legacy `agent-runner.ts startAgentSession` path retains the in-process `soleur_platform` MCP server, while the cc-soleur-go router (`cc-dispatcher.ts realSdkQueryFactory`) passes `mcpServers: readCcMcpAllowlist()` returning `{}` by default. Phase 2 (#3722, blocked-by Stage 6 #2939) tracks the actual promotion of 8 read-only Tier 1 candidates. Plausible tools (3) are PERMANENTLY blocked from the cc-router via `CC_ROUTER_TIER3_DENYLIST` (shared service-token cross-tenant credential by construction). |
| **(c) Categories of data subjects** | End-users of the Web Platform (when Phase 2 promotion occurs). |
| **(c) Categories of personal data** | **Phase 1: none — no tool surfaces personal data via cc-router.** Phase 2 candidates (per-tool, deferred to #3722 plan): **kb_share family** (user-authored KB entries, share tokens, share metadata); **conversations_lookup** (cross-conversation aggregation by `user_id` — closure-bound to requesting user); **GitHub tools (9)** — operator GitHub App installation tokens, issue bodies, commit author emails, repo metadata; **Plausible (3)** — PERMANENTLY blocked from cc-router. |
| **Special categories** | None systematically. |
| **Lawful basis** | Phase 1: N/A (nothing processed). Phase 2 candidates (per-family, deferred): kb_share + conversations_lookup → Art. 6(1)(b) contract; GitHub tools → Art. 6(1)(f) legitimate interest with balancing test documented at #3722 plan time. |
| **(d) Recipients** | Phase 1: none. Phase 2 recipients (deferred): GitHub Inc (US sub-processor); Plausible Insights OÜ (EU sub-processor — but PERMANENTLY blocked from cc-router, listed for legacy-path completeness). |
| **(e) Third-country transfers + safeguards** | Phase 1: none. Phase 2 (deferred): GitHub → DPF + SCCs Module 2 (US); Plausible → N/A if plausible.io EU-hosted, SCCs if self-hosted on US infra. |
| **(f) Retention** | Phase 1: N/A. Phase 2: tied to underlying stores (`conversations`, `messages`, `kb_shares` — covered by retention policies of Processing Activities 1, 2, 4) and to third-party processor lifecycles (GitHub 60-day audit log; Plausible per their published policy). |
| **(g) TOMs** | Phase 1: deny-by-default in code via `readCcMcpAllowlist()` (returns `{}` for empty env, throws on `CC_ROUTER_TIER3_DENYLIST` short-names); FR2 Sentry mirror at SDK iterator hook (`feature: "cc-mcp-tier"`) so unregistered-tool invocations are observable. Phase 2: per-tool TOMs documented at promotion time. |

---

## Processing Activity 10 — Tenant deploy substrate (orchestration-plane meta-audit log)

| Art. 30(1) limb | Entry |
|---|---|
| **(b) Purposes** | Orchestrate deploys on behalf of Soleur-operators authoring projects via Soleur agents; record evidence of Art. 28(3) sub-processor instructions (per-event attribution of "Soleur agent triggered deploy for founder X on tenant repo Y at time T"); evidence Art. 32 TOMs (RLS zero-policies, WORM trigger, retention sweep) per the orchestration plane's audit-log substrate. v1 single-tenant (Soleur-as-tenant-zero); multi-tenant trajectory captured in ADR-030. |
| **(c) Categories of data subjects** | Founder-operators (Soleur users acting as tenant operators). NOT end-users of tenant apps (their data does **not** flow through this substrate at v1). |
| **(c) Categories of personal data** | `founder_id` (UUID, pseudonymous controller-side identifier — FK `auth.users(id)` with `ON DELETE RESTRICT`); `target_repo` (text, charset+length-CHECK-constrained — Art. 5(1)(c) minimisation + log-injection defense); `target_workflow` (same); `gh_run_id` (bigint, non-PII GitHub workflow run identifier); `oidc_jti` (text, length-bounded per RFC 7519 §4.1.7 — non-PII GitHub OIDC JWT identifier); `trigger_outcome` (4 enum values: `queued` / `succeeded` / `failed` / `timeout`); `event_at` + `retention_until` (timestamps). No request bodies. No workflow payloads. No IP addresses. No end-user data. |
| **Special categories (Art. 9 / 10)** | None. |
| **Lawful basis** | Art. 6(1)(f) — legitimate interest. Three-part test documented at `knowledge-base/legal/legitimate-interest-assessments/2026-05-14-tenant-deploy-substrate-lia.md`: (i) **purpose** = operate the multi-tenant deploy substrate's orchestration plane + evidence Art. 28(3) sub-processor instructions; (ii) **necessity** = no less intrusive alternative (anonymous logs cannot evidence Art. 28(3)(a) at per-event granularity); (iii) **balancing** = data subject is the actor triggering the recorded event, data is low-sensitivity operational metadata, reasonable expectation of a record is overwhelming for any CI/CD platform. |
| **(d) Recipients** | Soleur internal (service_role only — REVOKE from PUBLIC/anon/authenticated + GRANT EXECUTE only to service_role on the writer RPC). **No third-party recipients at v1.** Sub-processors providing the trigger surface itself (GitHub, Doppler, Hetzner, Cloudflare) are recorded in their existing Processing Activity rows; they do not receive the contents of `public.tenant_deploy_audit`. |
| **(e) Third-country transfers + safeguards** | **No transfer.** Data resides in Supabase eu-west-1 (Ireland). No third-country transfer for the meta-audit-log substrate. |
| **(f) Retention** | **12 months** via the `tenant-deploy-audit-retention` pg_cron sweep (Art. 5(1)(e) storage limitation). Inline migration comment `-- RETENTION: 12 months via tenant-deploy-audit-retention pg_cron`. Aligned with Processing Activity 8 P0 mirror precedent (≤12mo absent open investigation). Art. 17 cascade `anonymise_tenant_deploy_audit(p_founder_id uuid)` UPDATEs `founder_id = NULL` (preserves row count and audit-trail integrity; does not DELETE) — runs BEFORE `auth.users` deletion per `ON DELETE RESTRICT` FK ordering. |
| **(g) TOMs (Art. 32)** | (1) **RLS with zero policies** on `public.tenant_deploy_audit` — service_role-only access control. (2) **WORM via BEFORE UPDATE/DELETE triggers** raising `P0001` except during the Art. 17 anonymise GUC window (`app.tenant_deploy_anonymise_in_progress` + `current_user = 'service_role'`) — integrity. (3) **SECURITY DEFINER writer RPC** with `SET search_path = public, pg_temp` (public FIRST per `cq-pg-security-definer-search-path-pin-pg-temp`) — injection defense. (4) **`::add-mask::` on Doppler/OIDC secrets** in workflows — confidentiality at log layer. (5) **`tenant-deploy-audit-retention` pg_cron sweep** — Art. 5(1)(e) storage limitation. (6) **GitHub Environments + required reviewers** on tenant repos — organizational TOM (load-bearing per external research; caps `workflow_dispatch` blast radius). (7) **`hetznercloud/tps-action` short-lived tokens** + **Doppler Service Account Identity OIDC** + **scoped CF account-API tokens** — pseudonymisation/minimisation of long-lived secrets. (8) **CHECK constraints** on `target_repo` + `target_workflow` charset/length — Art. 5(1)(c) data minimisation. (9) **`ON DELETE RESTRICT` FK** forces Art. 17 anonymise to run before auth-user deletion. (10) **REVOKE-from-named-roles + GRANT-only-to-service_role** per `2026-05-06-supabase-default-privileges-defeat-revoke-from-public.md`. (11) **Two-claim OIDC subject binding** (`repository_owner:<tenant-org>` + `environment:production`) — least-privilege trust scope. |

---

## Processing Activity 11 — Consent Records (T&C Acceptance Audit Ledger)

| Art. 30(1) limb | Entry |
|---|---|
| **(b) Purposes** | GDPR Art. 7(1) demonstrability: persist evidence that each Web Platform user accepted a specific version of the Terms & Conditions at a specific time, against a specific document content fingerprint. Without this ledger, re-acceptance of a new `TC_VERSION` would overwrite the prior `users.tc_accepted_at` / `users.tc_accepted_version` columns and destroy the prior-version evidence. Brand-survival single-user-incident class. |
| **(c) Categories of data subjects** | Web Platform users (registered account holders). NOT plugin-only users (the Plugin operates locally and does not create Web Platform consent records). |
| **(c) Categories of personal data** | `user_id` (UUID, pseudonymous controller-side identifier — FK `public.users(id)` with `ON DELETE RESTRICT`; nullable to support Art. 17 anonymisation); `version` (text, length-CHECK-constrained 1-32 — the `TC_VERSION` accepted); `document_sha` (text, 64-char hex CHECK — SHA-256 of `docs/legal/terms-and-conditions.md` at acceptance time; non-PII content fingerprint); `accepted_at` (timestamp); `ip_hash` (text, length-CHECK 1-128, NULLABLE — reserved for forward-compat per the LIA pending in #3855); `user_agent` (text, length-CHECK 1-512, NULLABLE — same forward-compat reservation); `retention_until` (timestamp). |
| **Special categories (Art. 9 / 10)** | None. |
| **Lawful basis** | Art. 6(1)(b) — performance of contract (the user's acceptance of the T&C is itself the contract); Art. 7 — evidence of consent grant. Retention required for demonstrability is a regulatory obligation under Art. 7(1) ("the controller shall be able to demonstrate"). |
| **(d) Recipients** | Soleur internal (service_role only — REVOKE from PUBLIC/anon/authenticated + GRANT EXECUTE only to service_role on `accept_terms` and `anonymise_tc_acceptances` RPCs). **No third-party recipients.** No outward disclosure absent regulatory demand. |
| **(e) Third-country transfers + safeguards** | **No transfer.** Data resides in Supabase `eu-west-1` (Ireland). |
| **(f) Retention** | **7-year envelope** via `tc_acceptances.retention_until` column default `now() + interval '7 years'`. Art. 5(1)(e) storage limitation. Rationale: French civil-litigation general limitation is 5 years (Code civil Art. 2224), B2B commercial obligations and tax-adjacent records are 10 years (Code de commerce Art. L123-22) — 7 years sits within the GDPR-defensible middle. **v1 ships WITHOUT the `pg_cron` retention sweep** (0 beta users + 7-year window means no row qualifies for deletion before 2033). Sweep tracked as a deferred-scope-out follow-on. Art. 17 cascade `anonymise_tc_acceptances(p_user_id uuid)` UPDATEs `user_id = NULL` (preserves row count + audit-trail integrity; does not DELETE) — runs BEFORE `auth.users` deletion per `ON DELETE RESTRICT` FK ordering. |
| **(g) TOMs (Art. 32)** | (1) **RLS with zero policies** on `public.tc_acceptances` — service_role-only access control. (2) **WORM via BEFORE UPDATE/DELETE triggers** raising `P0001` except during the Art. 17 anonymise GUC window (`app.tc_acceptances_anonymise_in_progress` + `current_user = 'service_role'`) — integrity. (3) **SECURITY DEFINER RPCs** (`accept_terms`, `anonymise_tc_acceptances`) with `SET search_path = public, pg_temp` per `cq-pg-security-definer-search-path-pin-pg-temp`. (4) **`UNIQUE(user_id, version)` + `ON CONFLICT DO NOTHING`** RPC-side idempotency — prevents fabricated duplicate consent rows. (5) **Hand-edited `TC_DOCUMENT_SHA` literal + CI guardrail** (`.github/workflows/ci.yml :: tc-document-sha-guard`) — content-integrity gate that prevents accepting a stale document SHA after T&C edits without operator-attested bump policy review. (6) **CHECK constraints** on `document_sha ~ '^[0-9a-f]{64}$'`, `version` length, `ip_hash` length, `user_agent` length — Art. 5(1)(c) data minimisation + input validation. (7) **`ON DELETE RESTRICT` FK** forces Art. 17 anonymise to run before auth-user deletion. (8) **REVOKE-from-named-roles + GRANT-only-to-service_role** per `2026-05-06-supabase-default-privileges-defeat-revoke-from-public.md`. (9) **Middleware fail-closed on tcError** — redirects non-exempt paths to `/accept-terms?error=db_unavailable` on Supabase outage, mirrored to Sentry (no silent admit). (10) **WebSocket mid-session re-check** of `users.tc_accepted_version` against `TC_VERSION` for gated inbound message types with 30s in-process cache — bounds stale-consent agent traffic to ≤ 30 s after operator-initiated bump. |

---

## Processing Activity 12 — GitHub branch-protection state custody (CI policy substrate)

| Art. 30(1) limb | Entry |
|---|---|
| **(b) Purposes** | Operate the CI-policy substrate that gates merges to `main` (required status checks + branch-protection bypass-actor allowlist) on `github.com/jikig-ai/soleur`. Provide an auditable IaC trail of every change to the branch-protection policy via the union of (i) Terraform state in R2 (`github/terraform.tfstate`), (ii) git commit history under `infra/github/`, and (iii) GitHub's first-party audit log of the API caller. Closes the failure mode surfaced by PR #3886 (a `lint fixture content` regression merged because the check was not in the ruleset's required set). |
| **(c) Categories of data subjects** | Soleur engineers (operator personas) who hold the fine-grained PAT used by `terraform apply`. NO end-users of any Soleur surface are subjects of this activity. |
| **(c) Categories of personal data** | None of substance. The processed artifacts are policy metadata: required-status-check `context` strings (workflow job names — public ABI), `integration_id` numerics (GitHub App installation IDs), `bypass_actors` shape (`actor_type` + role-id), `strict_required_status_checks_policy` boolean. The GitHub PAT bearer is the sole identifier reachable from this root and is operator-internal (not user-facing). The PAT itself is Doppler-resident at rest and never written to state plaintext (provider marks `token` sensitive). |
| **Special categories (Art. 9 / 10)** | None. |
| **Lawful basis** | Art. 6(1)(f) — legitimate interest (operate the CI substrate that gates merges). Inline LIA (no separate file under `knowledge-base/legal/legitimate-interest-assessments/`) — divergence from PA10's file-reference posture is intentional because there is no personal data to balance against, only operator-attributable apply events. (i) **purpose** = enforce the merge gate that prevents regression-class events like #3886 from landing on `main`; (ii) **necessity** = there is no less intrusive alternative — the GitHub Rulesets API is the only authoritative surface for required-check enforcement; (iii) **balancing** = the only data subject is the operator running `terraform apply`, the data is policy metadata, and the operator's expectation of an audit trail for branch-protection mutations is overwhelming for any IaC platform. |
| **(d) Recipients** | Soleur internal (operator only). GitHub holds the live ruleset state as a sub-processor of the `jikig-ai` GitHub org's IaC; the ruleset configuration itself is public via `gh api repos/jikig-ai/soleur/rulesets/14145388` (branch-protection rules are publicly inspectable on the repo's `/rules` page). No third-party recipients. |
| **(e) Third-country transfers + safeguards** | GitHub Inc. (US) — sub-processor under the existing GitHub Enterprise SCC + DPA already recorded in the Vendor / Sub-Processor Mapping section below for PA-* entries that touch GitHub. The PAT itself is Doppler-resident (EU) until apply time and is transmitted to the GitHub REST API at apply (TLS in transit). R2 state custody is Cloudflare-EU (existing PA8 / ADR-006 posture). |
| **(f) Retention** | Indefinite while the policy is in effect. The Terraform state file in R2 is the source-of-truth; prior versions are retained per R2 bucket versioning (rollback path documented in `infra/github/README.md` Phase 5). Rotation events for the PAT (90-day cadence) live in the Terraform commit history; no separate audit log. **Art. 5(1)(e) (storage limitation) compatibility:** indefinite retention is appropriate because no personal data is processed — the storage-limitation principle constrains *how long personal data can be kept*, and PA12 processes only policy metadata + operator-attributable apply events. |
| **(g) TOMs (Art. 32)** | (1) **Fine-grained PAT scoped to single repo** (`jikig-ai/soleur`) with `Administration: Read+Write` only — least privilege per OWASP minimum-privilege guideline. (2) **90-day PAT rotation cadence** documented in `infra/github/README.md` §Rotation with a +75-day calendar reminder. (3) **PAT Doppler-resident** (`prd_terraform/GH_RULESET_PAT`) — never committed to git or written to state plaintext (`integrations/github` provider marks `token` sensitive). (4) **R2 backend versioning + TLS** (ADR-006) — state-file confidentiality + rollback substrate. (5) **`terraform plan` as mandatory pre-apply gate** with the canonical `terraform show -json | jq` diff probe (Phase 2.3 of the runbook) — operator-attested per `hr-menu-option-ack-not-prod-write-auth`. (6) **`integration_id` pinned in Terraform config** (15368 GitHub Actions / 57789 CodeQL) — drift of an integration_id at apply time is a signal of upstream provider change and halts the plan. (7) **Provider pinned to `~> 6.10`** per repo-local learning `2026-03-19-github-ruleset-stale-bypass-actors.md`; `.terraform.lock.hcl` committed (currently `6.12.1`). (8) **No CI auto-apply** — apply is operator-only via the runbook; the auto-apply patterns in `apply-deploy-pipeline-fix.yml` / `apply-sentry-infra.yml` do NOT apply here because the ruleset mutation is a write-to-prod-policy event requiring human attestation. |

---

## Processing Activity 13 — Autonomous-draft runtime (Inngest trigger + CFO function — PR-F #3244)

| Art. 30(1) limb | Entry |
|---|---|
| **(b) Purposes** | Operate the server-side autonomous-draft runtime that converts a Stripe `invoice.payment_failed` event into a CFO-domain customer-response draft surfaced on the founder's `/dashboard` Today section. Draft-only (no send) — the founder decides whether to send, edit, or discard each draft. PR-F (#3244) ships substrate + one trigger; PR-G (#3947) gates cohort exposure. Brand-survival threshold: `single-user incident` (CPO sign-off carried from `2026-05-17-pr-f-inngest-trigger-layer-brainstorm.md`). |
| **(c) Categories of data subjects** | Founder-operators (Soleur users on a paid Stripe plan whose `invoice.payment_failed` fires the trigger). NOT end-users of tenant apps. Customer email addresses received from Stripe are hashed at the webhook boundary (sha256) BEFORE the Inngest envelope is signed — the customer is NOT a data subject of this activity. |
| **(c) Categories of personal data** | `founderId` (UUID, pseudonymous controller-side identifier — `public.users.id`); `invoiceId` (Stripe identifier, non-PII); `customerEmailHash` (sha256 hex, irreversible); `amount` (int, cents); `currency` (3-char ISO); `failureCode` (Stripe-defined enum, non-PII); plus the `messages` row written by the CFO function — `tier='external_brand_critical'`, `status='draft'`, `source='stripe'`, `owning_domain='cfo'`, `draft_preview` (model-generated text), `urgency`, `trust_tier='draft_one_click'`. Cost telemetry rows in `audit_byok_use` (`token_count`, `unit_cost_cents`, `ts`) are written by the per-turn cap-check (lands fully wired in PR-G). |
| **Special categories (Art. 9 / 10)** | None. Stripe customer email is hashed before persistence to either the Inngest store or the messages table; no cleartext email enters this processing path. |
| **Lawful basis** | Art. 6(1)(b) — performance of contract (the founder's paid subscription includes the autonomous-draft runtime as a feature of the contract); Art. 6(1)(f) — legitimate interest for the cost-telemetry (`audit_byok_use`) writes that demonstrate per-tenant BYOK cap enforcement. Three-part test for the LI prong: (i) **purpose** = operate the per-tenant kill-switch that bounds Anthropic API spend per founder per hour; (ii) **necessity** = no less intrusive alternative (anonymous cost logs cannot evidence per-tenant cap enforcement at audit time); (iii) **balancing** = data subject is the founder, data is operational metadata + their own API spend, reasonable expectation of an audit trail is overwhelming for any BYOK-billed substrate. |
| **(d) Recipients** | Soleur internal (service_role for the CFO function's tenant writes through user-scoped JWT; tenant-client RLS for `/api/dashboard/today` reads). **No third-party recipients.** Anthropic is the recipient of the BYOK prompt + completion API calls under the founder's own API key — the founder controls that bilateral relationship via BYOK; Soleur does not transmit founder data to Anthropic outside of the founder's lease scope. **Self-hosted Inngest** server eliminates the sub-processor cycle that Inngest Cloud would have created (rationale in ADR-030 §Decision). |
| **(e) Third-country transfers + safeguards** | **No transfer.** Inngest substrate is self-hosted on Hetzner (eu-central; existing Hetzner row in the Vendor / Sub-Processor Mapping). Self-hosted binary persists state in SQLite on the same Hetzner host; bound to `127.0.0.1` (no public attack surface). Supabase eu-west-1 (Ireland) for `messages` + `audit_byok_use`. Stripe webhook payload (US) is already covered by Processing Activity 3 (Subscription & Billing); the bridge to Inngest stays within EU residency. |
| **(f) Retention** | `messages` draft rows: founder controls — Send/Edit/Discard actions land in PR-G. Discarded drafts hard-delete; sent drafts move to existing message-retention envelope under Processing Activity 2. `audit_byok_use` rows: existing 12-month sweep (Processing Activity 8 P0 mirror precedent). Inngest event store (SQLite): 30-day rolling window via `inngest start --history-retention` (operator-managed; not personal-data-bearing at v1 because Stripe customer email is hashed BEFORE the envelope is signed). Art. 17 cascade for `messages` is via the existing PA-2 anonymisation path. |
| **(g) TOMs (Art. 32)** | (1) **"Drafts everywhere, sends nowhere" enforced by `messages_external_tier_status_check` CHECK constraint** (migration 046) — DB-level invariant that any future code attempting `INSERT … status='sent'` on an external-tier row is rejected with `ERRCODE=23514`. Widening requires explicit `DROP + CREATE` — caught at PR review. (2) **Per-tenant hourly cost cap** via `record_byok_use_and_check_cap()` plpgsql RPC with leading `SELECT … FOR UPDATE` on `public.users` — closes the TOCTOU race two concurrent BYOK turns at cap-boundary would otherwise hit. (3) **BYOK lease opens INSIDE each step.run** — ALS context is local to the Inngest step boundary; the SDK's sync-escape check (`byok-lease.ts:133-139`) fails closed if the lease is captured outside its scope. (4) **JWT mint per step** via `getFreshTenantClient(founderId)` — never cached across step boundaries; the `is_jti_denied` deny-list (PR-D #3883) gates the mint. (5) **Single-pass verify-stripe-state invariant** — verify lives in the CFO function body, NOT a step.run-checkpointed result; any 6h-deadlettered retry re-enters from the top and re-verifies (closes stale-after-verify failure mode). (6) **Schema-gate as non-throwing step.run** returning `{deadletter:true}` — schema-version mismatches do NOT consume a retry budget. (7) **Inngest signature-verify required at startup** — `INNGEST_SIGNING_KEY` is mandatory at module load; route returns 401 on missing/malformed/stale signature BEFORE any function dispatches. (8) **Customer email hashed at webhook boundary** (sha256, irreversible) BEFORE the Inngest envelope is signed — no cleartext email enters the Inngest store or stdout logs. `payment_method` dropped entirely. (9) **`SOLEUR_FR5_ENABLED=false` default at merge** — dark-launch flag; operator flips after the self-hosted Inngest server health-checks. Stripe redelivery (up to 3 days) preserves at-least-once. (10) **`processed_stripe_events` unique-index dedup** at the webhook (migration 030) — backstops Inngest's 24h dedup window for Stripe retries beyond 24h. (11) **Inngest server bound to `127.0.0.1`** via systemd `--host 127.0.0.1` — no public ingress; forged-event attack surface eliminated. (12) **Page-level disclosure banner** (`RUNTIME_COST_DISCLOSURE` constant in `lib/legal/disclosures.ts`) rendered above the Today section — Art. 13 transparency that autonomous drafts incur BYOK Anthropic API cost. |

ADR-030 (`knowledge-base/engineering/architecture/decisions/ADR-030-inngest-as-durable-trigger-layer.md`) records the substrate decision (self-hosted Inngest OSS on Hetzner over Inngest Cloud) and the load-bearing invariants (I1–I5) backing TOMs (3), (5), (7), (1).

---

## Processing Activity 14 — Per-tenant scope grants and audit viewer (PR-G #3947)

| Art. 30(1) limb | Entry |
|---|---|
| **(b) Purposes** | Operate the per-tenant authorization substrate that gates the autonomous-draft runtime (Processing Activity 13). One founder, one row per (action class, point-in-time): the founder authorizes Soleur to act on their behalf for a specific action class (e.g., `finance.payment_failed`) at one of three tiers (`auto`, `draft_one_click`, `approve_every_time`); the webhook predicate produces no Inngest event in the absence of an active grant. Companion `/dashboard/audit` viewer renders the resulting ledger (`audit_byok_use` rows + Inngest run history) per Art. 15 right of access. Brand-survival threshold: `single-user incident`. PR-G #3947 / draft PR #3984; ADR-033. |
| **(c) Categories of data subjects** | Founder-operators (Soleur users on the Web Platform). NOT end-users of tenant apps. The scope_grants ledger records the founder's authorization decisions; the audit viewer renders the founder's own runtime history (RLS self-select bounds the read to the authenticated founder). |
| **(c) Categories of personal data** | `scope_grants`: `id` (UUID), `founder_id` (UUID, FK to `public.users(id)` — nullable so Art. 17 anonymise can null it without DELETE), `action_class` (text, ≤64 chars; PR-G ships `finance.payment_failed` only), `tier` (one of `auto` / `draft_one_click` / `approve_every_time`), `granted_at`, `revoked_at`, `revoked_reason` (text, ≤256), `created_at`. Inngest envelope tier pin: `event.data.tier` (the grant tier active when the trigger fired — preserved on the event so the audit ledger renders consent-at-time). Audit viewer reads: `audit_byok_use` rows (existing Processing Activity 8) and Inngest run history (via server-only proxy at `/api/dashboard/runs`; raw `customerId` masked server-side; `authorizing_event` rendered only as the redacted summary). |
| **Special categories (Art. 9 / 10)** | None. No special-category data enters this substrate; the scope grants record consent state, not the underlying decision content. |
| **Lawful basis** | Art. 6(1)(b) — performance of contract (the scope grants ledger is the substrate by which the founder's contract performance authorizes Soleur to act on their behalf for specific action classes). Art. 22(2)(c) explicit consent applies for the `auto` tier (second-click acknowledgement at `/dashboard/settings/scope-grants` records the explicit consent required for solely-automated decision-making with legal/significant effects). Art. 22(3) human-review affordance inlined on every `/dashboard/audit` Inngest row (mailto:legal@jikigai.com) and via the email channel; ToS §3a.6 and Privacy Policy §8.3 disclose the right. |
| **(d) Recipients** | Soleur internal: founder-scoped Supabase reads via cookie-scoped RLS at `/dashboard/settings/scope-grants` and `/dashboard/audit`; service-role at the webhook predicate (the webhook has no founder JWT — `.eq("founder_id", founderId)` IS the tenant filter, not belt-and-suspenders). **No third-party recipients.** No new sub-processors introduced by PR-G (the Inngest substrate remains self-hosted per ADR-030; Anthropic remains the founder's bilateral BYOK relationship). |
| **(e) Third-country transfers + safeguards** | **No transfer.** `scope_grants` lives in `public` schema on Supabase eu-west-1 (existing Supabase row in the Vendor / Sub-Processor Mapping). Audit viewer reads stay within EU residency: BYOK rows from Supabase eu-west-1, Inngest run history from the self-hosted Inngest server on Hetzner (eu-central, bound to `127.0.0.1`). |
| **(f) Retention** | `scope_grants` rows: **append-only, retained indefinitely** as a consent record (the substrate of the founder's authorization to act on their behalf). Tier change does NOT update or delete — re-grant inserts a new row, marks the previous row `revoked_at = now(), revoked_reason = 'tier_change'`. Revocation flips `revoked_at` only. Art. 17 erasure cascade via `anonymise_scope_grants(p_user_id)` SECURITY DEFINER RPC: sets `founder_id = NULL` on the user's rows (GUC + role-check trigger bypass); the row's tier history persists with a null discriminator. The cascade runs BEFORE `anonymise_tc_acceptances` at `server/account-delete.ts` (FK ordering — both target `public.users(id)` with `ON DELETE RESTRICT`). Inngest envelope tier pin: governed by Processing Activity 13's 30-day SQLite window. |
| **(g) TOMs (Art. 32)** | (1) **Append-only WORM ledger** — `scope_grants_no_mutate()` trigger on UPDATE rejects any change to non-revoke columns (`founder_id`, `action_class`, `tier`, `granted_at`, `created_at` and any previously-set `revoked_at`/`revoked_reason`) with `ERRCODE = P0001`; DELETE rejected unconditionally except via the GUC-gated anonymise bypass for the Art. 17 cascade. (2) **RLS self-select** — `scope_grants_owner_select` policy `auth.uid() = founder_id`; no INSERT/UPDATE/DELETE policy (writes routed exclusively through SECURITY DEFINER RPCs). (3) **SECURITY DEFINER RPC discipline** — `grant_action_class`, `revoke_action_class`, `anonymise_scope_grants` all pin `SET search_path = public, pg_temp`; explicit `REVOKE EXECUTE FROM PUBLIC, anon` + `GRANT EXECUTE TO authenticated` (grant/revoke) or `service_role` (anonymise). (4) **Per-grant deny-by-default at the webhook** — `apps/web-platform/app/api/webhooks/stripe/route.ts:437` requires `isGranted(supabase, founderId, actionClass)` to return non-null AND `!isDenied(actionClass)` before `inngest.send`. The DB-level grant lookup is the load-bearing tenant gate (closes the K14 flip-in-PR-G risk vector). (5) **Consent-at-time pinning** — the Inngest envelope carries `data.tier = grant.tier`; the CFO function writes `messages.trust_tier` from the envelope, not from the current ledger state. A founder revoking after a trigger fires does NOT alter the recorded tier of the in-flight run. (6) **Server-only Inngest API credential** — `INNGEST_SIGNING_KEY` is bundled into the `/api/dashboard/runs` route only; lint test at `test/lint/inngest-key-server-only.test.ts` (TR7) grep-asserts the key is absent from `app/(dashboard)/**` and `components/**`. (7) **Belt-and-suspenders RLS filter** — every cookie-scoped query in the founder UI (audit viewer's BYOK section, scope-grants settings page) additionally pins `.eq("founder_id", user.id)` so any future RLS loosening still fails closed. (8) **Pessimistic UI + auto-tier acknowledgement** — `/dashboard/settings/scope-grants` UI does NOT optimistically update; selecting `auto` reveals an inline acknowledgement checkbox that gates submit (Art. 22(2)(c) explicit-consent UI primitive at single-user-incident threshold). (9) **Art. 22(3) "Request human review" affordance** inlined on every `/dashboard/audit` Inngest row (mailto:legal@jikigai.com) and via the email channel — ToS §3a.6 and Privacy Policy §8.3. (10) **Audit-viewer customer-id masking** — raw `customerId` masked server-side in `lib/inngest/list-runs.ts` (`maskCustomerId`); the masked summary is rendered exclusively by `components/audit/redacted-event-summary.tsx` (AC8 single-renderer invariant). |

ADR-033 (`knowledge-base/engineering/architecture/decisions/ADR-033-per-tenant-scope-grants.md`) records the substrate decision (Supabase WORM ledger over Doppler / in-process registry / JWT claim / global flag alone) and links to ADR-030 for the autonomous-draft substrate this gates.

---

## Vendor / Sub-Processor Mapping (cross-cut of (d) + (e) + (g))

| Vendor (role) | Region | DPA status | Transfer mechanism | Activities | Notes |
|---|---|---|---|---|---|
| **Supabase Inc** (processor) | `eu-west-1` Ireland | SIGNED 2026-03-19 (DPA v. 2025-08-05; SCCs M2+3) | N/A — EU-only | 1, 2, 3 (subscription metadata), 4, 5, 11 | Braintrust tracing disabled for EU projects (see compliance-posture.md Completed Work). |
| **Hetzner Online GmbH** (processor) | `hel1` Helsinki, Finland | SIGNED 2026-03-19 (AVV) | N/A — EU-only | 1, 2, 4, 8 (origin logs) | CX33 hosts the Web Platform compute plane. |
| **Stripe Inc** (processor) | US | AUTO via Services Agreement (verified 2026-03-19) | EU-US DPF + SCCs M2 | 3 | PCI DSS Level 1; Jikigai integration is SAQ-A. |
| **Cloudflare Inc** (processor) | Global CDN | AUTO Self-Serve (verified 2026-03-19) | DPF + SCCs + CBPR | 1, 2, 4, 5 (edge proxy) | TLS termination + DDoS protection. |
| **Resend Inc** (processor) | US | AUTO via ToS § 7 (verified 2026-04-13) | EU-US DPF + SCCs | (review-gate transactional email — adjunct to PA 5) | Transactional review-gate emails only — listed for completeness; not its own register activity because it is a downstream channel of PA 5's notification purpose. |
| **Sentry (Functional Software GmbH)** (processor) | DE | SCCs (standard EU-region terms) | DE region, intra-EU | 8 | Error monitoring with user-context scrubbing. |
| **Buttondown** (processor) | US | DPA in force (all tiers, free included) | SCCs M2 | 6 | Double opt-in newsletter. |
| **GitHub Inc** (independent processor for repo / public-disclosure venue for CLAs; sub-processor for cc-router MCP tool surface; processor for branch-protection state custody) | US | Standard GitHub terms; Customer DPA per `https://github.com/customer-terms/customer-data-protection-agreement` (#2909) | DPF + SCCs Module 2 | 7, 9, 12 | `cla-signatures` branch is public-by-design (PA 7). cc-router scope (PA 9) Phase 1 status: not exposed — Phase 2 (#3722) tracks promotion of 9 GitHub MCP tools. PA 12 is the `infra/github/` Terraform root managing ruleset 14145388; branch-protection state is public-by-API. |
| **Plausible Insights OÜ** (sub-processor — legacy `agent-runner.ts` only; PERMANENTLY blocked from cc-router) | Estonia (plausible.io EU-hosted) — verify region at activation if self-hosted on US infra | AUTO via DPA at `https://plausible.io/dpa` (#2909 — pending operator verification) | N/A if EU-hosted; SCCs if US-hosted self-managed | 9 (legacy path only) | 3 tools (`plausible_create_site/add_goal/get_stats`) share a single backend `PLAUSIBLE_API_KEY`. Cross-tenant credential by construction — `CC_ROUTER_TIER3_DENYLIST` (`apps/web-platform/server/tool-tiers.ts`) permanently blocks router exposure. IP-pseudonymized event data per Plausible's published "no personal data" posture. |
| **Anthropic PBC** (independent controller/processor — out of scope of this register for BYOK) | US | AUTO via Commercial Terms § C (DPA effective 2025-02-24; SCCs M2+3 + UK IDTA + Swiss Addendum) | DPF + SCCs M2+3 + UK IDTA + Swiss Addendum | — | In scope **only** for Jikigai-keyed Anthropic API surfaces (`claude-code-action` CI, compound-promotion-loop #2720). End-user BYOK plugin traffic is not intermediated by Jikigai (see privacy-policy.md §5.1). |

---

## Cross-Cutting Technical & Organisational Measures (Art. 32)

- **Encryption in transit:** TLS 1.2+ enforced everywhere; HSTS on `app.soleur.ai`.
- **Encryption at rest:** Supabase storage layer encryption; BYOK third-party API keys additionally encrypted at the application layer with AES-256-GCM before persistence.
- **Access control:** Supabase Row-Level Security on every multi-tenant table; per-`user_id` isolation; principle of least privilege for service-role keys.
- **Authentication:** bcrypt (Supabase GoTrue); JWT-based sessions; HttpOnly+Secure+SameSite cookies; MFA available; OAuth federation with verified-email account linking.
- **Secrets management:** Doppler-managed environment variables; rotation procedure documented; `CC_PERSIST_USAGE` and similar flags governed via Doppler `prd` config.
- **Audit logging:** Supabase audit logs; pino structured logs (redacted); cc-dispatcher P0 mirror trail for Art. 33 clock anchoring.
- **Data minimisation:** `messages.usage` jsonb column constrained on cc-router to `{ cost_usd: number }` only, satisfying Art. 5(1)(c).
- **Resilience:** Hetzner backups; Supabase point-in-time recovery.
- **Vendor governance:** Quarterly DPA-status review against `knowledge-base/legal/compliance-posture.md`; new sub-processors registered before activation.
- **Incident response:** Art. 33 72-hour clock anchored by `first_observed_at` ISO timestamps in the cc-dispatcher P0 mirror pipeline; CNIL notification template held by CLO; data-subject communication path under Art. 34 documented.
- **Developer-side controls:** `gdpr-gate` skill enforces regulated-data surface rules pre-merge (`hr-gdpr-gate-on-regulated-data-surfaces`); plan-time GDPR findings folded into PR acceptance criteria; vendor-DPA checklist gate in PR template.

---

## Register Maintenance

- **Review cadence:** Quarterly + event-driven (new processing activity, new sub-processor, new third-country transfer, new technology, new lawful basis, supervisory-authority engagement).
- **Custodian:** Soleur CLO role.
- **Source-of-truth references:**
  - `knowledge-base/legal/compliance-posture.md` — vendor DPA status, active compliance items.
  - `docs/legal/privacy-policy.md` — outward-facing disclosures (must remain consistent with this register).
  - `docs/legal/gdpr-policy.md` — GDPR-specific disclosures and data-subject rights procedure.
  - `apps/web-platform/supabase/migrations/` — schema authority for personal-data column inventory.
- **Outstanding counsel-review items:**
  1. ~~Confirm exact legal form of Jikigai~~ — **resolved 2026-05-12: Jikigai SARL (Société à responsabilité limitée), confirmed by founder.**
  2. Confirm Art. 30(5) micro-enterprise derogation analysis (Soleur is below 250 employees but processes data not "occasionally" — Art. 30(1) obligation stands; derogation explicitly does **not** apply).
  3. Confirm characterisation of Web Push transmission as user-directed vs controller-initiated transfer (PA 5).
  4. Confirm retention envelope for cc-dispatcher P0 mirror records (PA 8) — target ≤ 12 months absent open investigation.
  5. Confirm Anthropic-DPA register-row gap (Active Compliance Item #3594) is closed before this register is finalised.
  6. Confirm characterisation of the per-user JWT mint (`getFreshTenantClient`) + RLS as a layered Art. 25(1) data-protection-by-design control across PA1 and PA2 (introduced 2026-05-16 by PR-C #3854 follow-through #3879). Confirm that "defeating both layers" is the correct adversary model for the TOM cell rather than treating per-user JWT as a standalone Art. 32(1)(b) control.

> **Related documents:** This Article 30 register references the Privacy Policy, GDPR Policy, Data Protection Disclosure, and Cookie Policy. Consistency with `docs/legal/privacy-policy.md` and `docs/legal/gdpr-policy.md` must be re-verified on every update. For the processor-relationship characterisation of locally-installed plugin usage, see `docs/legal/data-protection-disclosure.md` (Soleur plugin is **not** a processor relationship — Jikigai does not process data on behalf of plugin users).

---

> **DRAFT — This document was generated by AI and requires professional legal review before use. It does not constitute legal advice.**
