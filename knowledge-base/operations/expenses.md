---
last_updated: 2026-08-06
---

# Expenses

## Recurring

| Service | Provider | Category | Amount | Status | Renewal Date | Notes |
|---------|----------|----------|--------|--------|--------------|-------|
| GitHub Copilot | GitHub | dev-tools | 10.00 | active | 2026-03-14 | Business plan |
| Hetzner CX33 (web-1) | Hetzner | hosting | 9.17 | active | 2026-04-01 | 4 shared vCPU x86, 8 GB RAM, 80 GB SSD, hel1 (web platform, `soleur-web-platform`). **Corrected 2026-07-16 (#6538): was 15.37 / "160 GB SSD" — the ledger priced the same cx33 SKU two ways (this row vs the registry row). Live Hetzner catalog: cx33 = EUR 8.49/mo net, 80 GB → USD ~9.17 at ~1.08 FX. VERIFY actual draw on the next Hetzner invoice.** DPA: must be signed via Hetzner Console <!-- estimate verify_by=2026-08-01 owner=cfo source="Hetzner invoice email in ops@soleur.ai" --> |
| Hetzner Volume (20 GB) | Hetzner | hosting | 1.24 | active | 2026-04-01 | Persistent storage for /workspaces, hel1 (web platform). **Amount corrected 2026-07-17 (#6602): was 0.88 — the ledger priced volumes at a stale ~$0.044/GB basis; live Hetzner API `/v1/pricing` gives `volume.price_per_gb_month.net` = EUR 0.0572/GB → 20 GB = EUR 1.144 → USD ~1.24 at ~1.08 FX, aligning the volume rows to the host rows' FX basis (#6589 gap).** <!-- estimate verify_by=2026-08-01 owner=cfo source="Hetzner invoice email in ops@soleur.ai" --> |
| Hetzner Volume (workspaces-luks, 20 GB) | Hetzner | hosting | 1.24 | active | 2026-07-19 | Additive LUKS-encryption target volume (`hcloud_volume.workspaces_luks`, id 106406962, `/dev/disk/by-id/scsi-0HC_Volume_106406962`), hel1 — first-provisioned 2026-07-19 (#6649, scoped `apply_target=workspaces-luks-cutover`). Attached ALONGSIDE the live plaintext `soleur-web-platform-data` volume (row above) as the ADR-119 two-copy state; currently **RAW/empty** — the real freeze/rsync/repoint cutover (#6604, `dry_run=false`, human-gated) has NOT run, so no user data lives here yet. Live Hetzner API `volume.price_per_gb_month.net` = EUR 0.0572/GB × 20 = EUR 1.144 → USD ~1.24 at ~1.08 FX. **Net-new +$1.24/mo ONLY during the transition** — post-cutover the plaintext 20 GB volume is wiped + decommissioned (DP-4), making this a replacement (no net-new), mirroring the git-data LUKS row. DPA: existing Hetzner DPA (same account, no new sub-processor). See #6649, #6604, ADR-119 <!-- estimate verify_by=2026-08-01 owner=cfo source="Hetzner invoice email in ops@soleur.ai" --> |
| Hetzner Primary IPv4 (web-1) | Hetzner | hosting | 0.54 | active | 2026-04-01 | Public IPv4 for the web-1 host (`soleur-web-platform`, hel1, IP 135.181.45.178 — verified live via Hetzner API `/v1/primary_ips` 2026-07-17). **Row added 2026-07-17 (#6602): the #6589 gap — web-1 had no Primary IPv4 line though web-2/inngest/grok each did; the API confirms all 5 hosts carry one billable IPv4.** ~EUR 0.50/mo → USD ~0.54 at ~1.08 FX. <!-- estimate verify_by=2026-08-01 owner=cfo source="Hetzner invoice email in ops@soleur.ai" --> |
| Hetzner CPX22 (web-2) | Hetzner | hosting | 21.05 | approved-not-billing | - | 2 vCPU AMD x86, 4 GB RAM, 80 GB local disk, hel1, private_ip 10.0.1.11 (`hcloud_server` via `for_each var.web_hosts`; `web-2 = { location = "hel1", private_ip = "10.0.1.11", server_type = "cpx22" }`). **TYPE REPINNED cx23 -> cpx22 2026-07-26 (#6966, ADR-143 addendum) — FORCED BY STOCK, not by sizing.** This row previously read CX23 / 5.93. The live probe 2026-07-26 ~18:05 UTC (`/v1/datacenters` -> `.server_types.available`) found the ENTIRE cx line (cx23 AND web-1's cx33) and the ENTIRE cax ARM line orderable in **0 of 3** EU DCs, so the prior row's claim that cx23 was "in stock in hel1" is void — and `main` HALTed on the `host_creates` tripwire until the repin, because web-2 was declared but unbornable (run 30207415503). **RATE:** cpx22 = **EUR 19.49/mo net** (live `/v1/server_types`, hel1, 2026-07-26) -> **USD ~21.05 at ~1.08 FX**, i.e. **+EUR 14.00/mo (~+USD 15.12)** vs the superseded cx23 line and 3.55x its price. **CAVEAT — the API returns IDENTICAL `net` and `gross` for this account, so the +EUR 14.00 delta is arithmetic on those values, NOT a VAT-adjusted quote. VERIFY the actual draw on the first Hetzner invoice that shows this host and reconcile if it differs.** **SIZING UNCHANGED:** cpx22 matches cx23 on cores and RAM exactly (2c/4g) and doubles local disk (40->80 GB); 30-day Better Stack `host_metrics` show web-1 peaks ~1.5 GB RAM / 0.48 load15 (ADR-143 D1), so 4 GB / 2 vCPU remains ample — this is a price change, not a resize. cpx12 (1c/2g, ~EUR 11.49) was the only cheaper orderable option and was rejected on headroom (75% RAM at measured peak, 1 vCPU, and a reboot-forcing resize needed at the GA flip). Serving-weight 0 (ADR-143 D2): NOT in the ingress rotation (dns.tf app record stays web-1-only) until the ADR-068 Phase-3 GA flip. Status `approved-not-billing`, NOT active: web-2 is NOT provisioned yet — birth is a gated `web-host-create` workflow_dispatch; excluded from current burn until then. **Billing begins at provision** (flip approved-not-billing -> active the same day). DPA: existing Hetzner DPA (same account, no new sub-processor). See #6966, #6459, ADR-143 <!-- estimate verify_by=2026-08-01 owner=cfo source="Hetzner invoice email in ops@soleur.ai (first invoice post-provision)" --> |
| Hetzner Primary IPv4 (web-2) | Hetzner | hosting | 0.54 | approved-not-billing | - | Public IPv4 for the fresh web-2 cattle standby host — fans out with the host via `for_each var.web_hosts` (ADR-143, #6459/#6919). ~EUR 0.50/mo → USD ~0.54 at ~1.08 FX. **PHANTOM until provision** — an IPv4 cannot exist without its host, and web-2's birth is a post-merge gated `workflow_dispatch` (see the CPX22 row above); excluded from current burn until then. **Billing begins at provision.** DPA: existing Hetzner DPA (same account, no new sub-processor). See #6459, PR #6919, ADR-143 <!-- estimate verify_by=2026-08-01 owner=cfo source="Hetzner invoice email in ops@soleur.ai (first invoice post-provision)" --> |
| Hetzner Volume (web-2, workspaces, 20 GB) | Hetzner | hosting | 1.24 | approved-not-billing | - | Persistent /workspaces block volume (`hcloud_volume.workspaces["web-2"]`, `var.volume_size` = 20 GB), hel1 — fans out with the host via `for_each var.web_hosts` (ADR-143, #6459/#6919). Live Hetzner API `volume.price_per_gb_month.net` = EUR 0.0572/GB × 20 = EUR 1.144 → USD ~1.24 at ~1.08 FX (same basis as the web-1 volume rows above). **PHANTOM until provision** — web-2's birth is a post-merge gated `workflow_dispatch` (see the CPX22 (web-2) row above); pre-GA-flip the sole /workspaces copy is web-1's volume, so this boots RAW/empty (a request to web-2 pre-flip hits empty /workspaces = workspace-gone, ADR-143). Excluded from current burn until provisioned; **billing begins at provision**. DPA: existing Hetzner DPA (same account, no new sub-processor). See #6459, PR #6919, ADR-143 <!-- estimate verify_by=2026-08-01 owner=cfo source="Hetzner invoice email in ops@soleur.ai (first invoice post-provision)" --> |
| Hetzner CPX22 (git-data) | Hetzner | hosting | 21.05 | approved-not-billing | - | 2 vCPU AMD x86, 4 GB RAM, hel1. Bare git-data store host for the multi-host /workspaces split (#5274 Phase 2, ADR-068). **TYPE REPINNED `cax11` → `cpx22` 2026-07-27 (#6570, ADR-068 addendum) — FORCED BY STOCK, not by sizing.** This row previously read the ARM `cax11` type at 4.10 / 2 vCPU ARM64-Ampere. The entire ARM `cax` line was orderable in **0 of 3 EU DCs** at both recorded probes (2026-07-15 and 2026-07-26), so this host could never be born on its declared type at all — that is #6570, and the repin buys orderability rather than capacity. **RATE:** cpx22 = **EUR 19.49/mo net** (live `/v1/server_types`, hel1) → **USD ~21.05 at ~1.08 FX**, the same figure as the web-2 and inngest cpx22 rows; **+EUR 13.50/mo (~+USD 14.60)** vs the voided ARM line — basis EUR 19.49 − **5.99**, the repo-canon cax11 rate (`variables.tf`, `zot-registry.tf`, the inngest row, ADR-143's probe table). **The prior figures in this row (`4.10` / `~EUR 3.79`) were themselves wrong** and are void, not a second basis; against the old Amount cell the swing reads +USD 16.95. **CAVEAT — the Hetzner API returns IDENTICAL `net` and `gross` for this account, so that delta is arithmetic on those values, NOT a VAT-adjusted quote. VERIFY the actual draw on the first Hetzner invoice that shows this host and reconcile if it differs.** **SIZING CONFIRMED, and the claim qualified (#6982, ADR-068 D-SIZE):** 2 vCPU / 4 GB stays. A bare-repo git store is neither CPU- nor RAM-bound **in steady state**, but git's DEFAULTS made it burst-bound (`receive.autogc` ON so every push could trigger an inline server-side repack, `pack.windowMemory` unlimited on a no-swap box, `gc.autoDetach` hiding the OOM from the pushing client). #6982 removes that regime, so the claim is now an ENFORCED INVARIANT rather than an assertion. Not downsized to `cpx12` (−$103.68/yr) because the correction path is a DESTRUCTIVE `git-data-host-replace` of the host holding every user's source code. **PHANTOM ROW — corrected 2026-07-15 (#6453): this host has NEVER existed.** It was marked `active` on the expectation that it would provision on its PR's merge; it did not, so the row had been overstating burn. Status flips to `active` only when `hcloud server list` actually shows it — re-verified absent from Terraform state 2026-07-27. **Birth remains blocked, but no longer by the type:** there is no birth route at all (**#6977** — `git-data-host-replace` hard-aborts on a first CREATE, and no `git-data-host-create` target exists). Separately, ADR-115 **excludes git-data from the reboot primitive** until its `luksOpen` is reboot-safe (it lives in `runcmd`, no `crypttab` exists, and the fstab entry carries `nofail`) — a standing constraint on REBOOTS, not a birth blocker. GA trigger #5274 is "Post-MVP / Later". DPA: covered by the existing Hetzner DPA (same account, no new sub-processor). See #6570, #6977, ADR-068 <!-- estimate verify_by=2026-08-01 owner=cfo source="Hetzner invoice email in ops@soleur.ai (first invoice post-provision)" --> |
| Hetzner Primary IPv4 (git-data) | Hetzner | hosting | 0.54 | approved-not-billing | - | Egress-only public IPv4 for the git-data host (apt/GitHub during cloud-init; a no-public-IP host has no NAT). Public ingress is deny-all by firewall. **PHANTOM ROW — corrected 2026-07-15 (#6453):** an IPv4 cannot exist without its host, and the git-data host has never been born (row above). ~EUR 0.50/mo when it exists — VERIFY on invoice |
| Hetzner Volume (git-data, 10 GB, plaintext) | Hetzner | hosting | 0.62 | approved-not-billing | - | **MISSING ROW — added 2026-07-27 (#6982).** The birth `-target` set creates **TWO** 10 GB volumes (`hcloud_volume.git_data` AND `hcloud_volume.git_data_luks`) and this ledger booked only the LUKS one, so the git-data birth was under-booked by one volume. This is the **PLAINTEXT** `soleur-git-data-store` volume, and per `git-data-bootstrap.sh` (`REPO_ROOT=$GIT_DATA_ROOT/repositories`, `GIT_DATA_ROOT=/mnt/git-data`) it is the **LIVE store at birth** — the LUKS sibling is a mounted-but-empty cutover target until `GIT_DATA_STORE_ENABLED` flips. Live Hetzner API `volume.price_per_gb_month.net` = EUR 0.0572/GB x 10 = EUR 0.572 -> USD ~0.62 at ~1.08 FX. **PHANTOM until birth** — the host has never existed (see the CPX22 (git-data) row), so this stays `approved-not-billing`; #6982 ships hardening, not the birth. DPA: existing Hetzner DPA (same account, no new sub-processor). See #6982, #5274, ADR-068 <!-- estimate verify_by=2026-08-01 owner=cfo source="Hetzner invoice email in ops@soleur.ai (first invoice post-provision)" --> |
| Hetzner Volume (git-data, 10 GB, LUKS) | Hetzner | hosting | 0.62 | approved-not-billing | - | **PHANTOM ROW — corrected 2026-07-15 (#6453):** never provisioned; the git-data host it attaches to has never been born (see the CPX22 (git-data) row). The cutover prose below describes the DESIGNED lifecycle, not anything that has happened. Bare-repo block volume for the git-data store (objects/refs + the per-worktree fence sidecar/lock), hel1 (#5274). Live Hetzner API `volume.price_per_gb_month.net` = EUR 0.0572/GB x 10 = EUR 0.572 -> USD ~0.62 at ~1.08 FX (was 0.48, a stale rate corrected by #6982) — VERIFY on invoice. **#5274 Phase-3 (3.D) cutover:** converted to encryption-at-rest — the git-data store now lives on a fresh **LUKS-encrypted** block volume that replaces the original unencrypted git-data volume. Transient dual-existence during the cutover (old + new volume both attached while data is copied over); the old unencrypted volume is decommissioned post-cutover per DL-2. **"No net-new recurring cost" is STRUCK for the pre-cutover period (#6982).** That reasoning describes the POST-cutover steady state, and the #5274 Phase-3 cutover is Post-MVP — so from the birth until the flag flips BOTH volumes exist and bill. The birth `-target` set creates `hcloud_volume.git_data` AND `hcloud_volume.git_data_luks`; the plaintext sibling now has its own row above rather than being invisible. It becomes a replacement (no net-new) only when the plaintext volume is decommissioned post-cutover, mirroring how the workspaces-luks row states the same transition <!-- estimate verify_by=2026-08-01 owner=cfo source="Hetzner invoice email in ops@soleur.ai (first invoice post-provision)" --> |
| Hetzner CX23 (registry) | Hetzner | hosting | 5.93 | retired | 2026-08-01 | Dedicated amd64 host (`hcloud_server.registry`, **cx23**: 2 vCPU x86 / 4 GB RAM, **hel1**) for the self-hosted **zot** container registry + attached 60 GB `hcloud_volume` (`/var/lib/zot`). **Type + amount corrected 2026-07-16 (#6538): row read CX33 / 9.17, but #6497/#6463 reverted the host cx33→cx23 on 2026-07-16 after live telemetry showed the 8 GB was never needed (37 MB steady) and the OOM diagnosis was never confirmed (`variables.tf` `registry_server_type`). Verified live via the Hetzner API 2026-07-16: `soleur-registry` = cx23, hel1. cx23 = EUR 5.49/mo net → USD ~5.93 at ~1.08 FX — VERIFY actual draw on the next Hetzner invoice.** **SUPERSEDED-ON-APPLY 2026-08-06 (#7309): `var.registry_server_type` is now `cpx22` (row below). THIS ROW STAYS `active` AND STAYS AT 5.93 — the repin is a source change only; it schedules no `terraform apply`, so `soleur-registry` is still a live cx23 and still draws the cx23 rate. Flip this row to `retired` and the cpx22 row to `active` when, and only when, the host-replace actually runs — pre-dating that flip is the #6453 defect (a ledger reading ahead of billing overstates burn).** Historical context (the cx33 era): #6288: fix a zot restart-loop (~4/min) OOM-ing during the boot scan of the ~35 GB store on the prior 4 GB cx23 box (non-ENOSPC; SOLEUR_ZOT telemetry — restarts climbed past the gc window). The plan first targeted **cx32, which is not a real Hetzner type** — that phantom `-replace` destroyed the old nbg1 host then failed `server type cx32 not found` (GHCR-masked, prod /health 200). RESOLUTION: migrate the registry **nbg1→hel1** and use **cx33** (the real 8 GB member of the CX Intel line), the only cheap ≥8 GB option — cx33 = **EUR 8.49/mo net** (Hetzner API 2026-07-09; nbg1's cheapest 8 GB was cpx32 ~EUR 35/mo, ~6×; cx33 not offered in nbg1). vs prior cx23 ~EUR 5.49/mo = **+~EUR 3/mo**; USD ~9.17 at ~1.08 FX — VERIFY next invoice. hel1 is the same eu-central network zone + where the web/git-data/inngest hosts live. **LIVE:** the `registry-region-migrate` dispatch completed 2026-07-10 — the cx33/hel1 host is running (private_ip 10.0.1.30, `zot_restarts=0`, `mem_total_mb=7751`) and billing at the cx33 rate. The 35 GB store is a disposable GHCR mirror — the fresh hel1 volume re-fills from GHCR (row below). DPA: existing Hetzner DPA (same account, no new sub-processor). See issue #6288, #6122, ADR-096; disk-grow #6284, PR #6120 <!-- estimate verify_by=2026-08-01 owner=cfo source="Hetzner invoice email in ops@soleur.ai" --> |
| Hetzner CPX22 (registry) | Hetzner | hosting | 21.05 | active | 2026-08-10 | 2 vCPU AMD x86, 4 GB RAM, 80 GB local disk, hel1 (`hcloud_server.registry`, `var.registry_server_type = "cpx22"`). **TYPE REPINNED cx23 -> cpx22 2026-08-06 (#7309) — FORCED BY STOCK VOLATILITY, not by sizing and not by unorderability.** Read the measurement, not the headline: cx23 IS orderable in hel1-dc2 as of a live 2026-08-06 probe (`/v1/datacenters` -> `.server_types.available`, never `.supported`). What forced the move is that hel1-dc2 cx23 availability went **available -> unavailable -> available across twelve days, the first change inside 24 hours**: available 2026-07-15 (#6457) and 2026-07-25 (ADR-143's probe table, "YES — in stock"), unavailable 2026-07-26 (#6966, "0 of 3 EU DCs") and 2026-08-04 (#7280), available again 2026-08-06 (#7309). Two direction changes, no `deprecation` block on any of them. Series and sources single-sourced at `zot-registry.tf`, anchor "STOCK REALITY". A registry type change is a host REPLACE (`user_data` is ForceNew and the resource carries no `lifecycle.ignore_changes`), i.e. destroy-then-create with no capacity reservation, where a failed revert needs a SECOND create against the same supply while the sole pull path is dark (ADR-169). cpx22 was available at all three probes. **RATE:** cpx22 = **EUR 19.49/mo net** vs cx23 **EUR 5.49/mo net** = **+EUR 14.00/mo (+EUR 168/yr), 3.55x** on this host -> **USD ~21.05 at ~1.08 FX** (+USD ~15.12/mo). Operator accepted this cost explicitly before the change was authorized. **CAVEAT — the Hetzner API returns IDENTICAL `net` and `gross` for this account, so the delta is arithmetic on those values, NOT a VAT-adjusted quote. VERIFY the actual draw on the first invoice that shows this host.** **SIZING UNCHANGED:** cpx22 matches cx23 on cores and RAM exactly (2c/4g) and doubles local disk (40->80 GB), so the derived arch stays amd64 and zot's ADR-062 cgroup cap stays 3072m; the 60 GB store volume and its USD 3.71 (EUR 3.432) row are untouched (the store lives there, not on local disk). **FLIPPED TO ACTIVE 2026-08-10 (#7341).** The condition both rows stated explicitly has fired: `registry-luks-recut` ran (Actions run 31437037877) and the host-replace actually happened. Verified live via the Hetzner API, not inferred from the dispatch: `soleur-registry` id=161264730 is **type=cpx22**, created 2026-08-10T22:16:50Z, status=running, with a fresh 60 GB volume id=106585109 (the prior store id=106286457 was destroyed by the recut). So the cpx22 rate is drawn from 2026-08-10 and the cx23 row is retired the same day -- the two are a pair, and flipping one without the other would either double-count the host or drop it. **+EUR 14.00/mo (+USD ~15.12) net-new** on this host from that date. VERIFY against the first Hetzner invoice covering 2026-08-10 onward; the API returns identical `net` and `gross` for this account, so the delta remains arithmetic on those values rather than a VAT-adjusted quote. **The prior guidance below is kept, not deleted, because it is what made this flip verifiable rather than a judgement call:** **APPROVED-NOT-BILLING is load-bearing:** merging fires the per-PR `terraform apply`, but its `-target` set does not include `hcloud_server.registry` or anything depending on it, so that apply cannot reach this host; so nothing is drawn at the cpx22 rate until the guarded `registry-host-replace` / `registry-luks-recut` dispatch runs. DPA: existing Hetzner DPA (same account, same region, no new sub-processor). See #7309, #7287, ADR-096, ADR-169 <!-- estimate verify_by=2026-11-06 owner=cfo source="Hetzner /v1/server_types live probe 2026-08-06; reconcile against the invoice in ops@soleur.ai after the host-replace" --> |
| Hetzner Volume (registry, 60 GB) | Hetzner | hosting | 3.71 | active | 2026-08-01 | Persistent block volume (`hcloud_volume.registry`) for the self-hosted zot OCI store, mounted `/var/lib/zot`, **hel1** (migrated from nbg1, #6288). GROWN 30→60 GB (#6284, 2026-07-09) to fit the retained keep-set. **Amount corrected 2026-07-17 (#6602): was 2.64 (the ledger's stale ~$0.044/GB basis, EUR 2.44); live Hetzner API `/v1/pricing` = EUR 0.0572/GB → 60 GB = EUR 3.432 → USD ~3.71 at ~1.08 FX, aligning to the host rows' FX basis (#6589).** The #6288 nbg1→hel1 region migration is ForceNew on this volume — the nbg1 volume is destroyed + a fresh hel1 volume created; the store re-fills from GHCR (zot is a mirror), so no data migration. Broken out from the prior bundled `CAX11 (registry)` row, which folded a (then 30 GB) volume into its combined amount; the volume is live billing and must be tracked explicitly. DPA: existing Hetzner DPA (same account, no new sub-processor). See #6284, #6288, ADR-096 <!-- estimate verify_by=2026-08-01 owner=cfo source="Hetzner invoice email in ops@soleur.ai" --> |
| Hetzner Primary IPv4 (registry) | Hetzner | hosting | 0.54 | active | 2026-08-01 | Public IPv4 for the zot registry host (`soleur-registry`, hel1, IP 77.42.44.156 — verified live via Hetzner API `/v1/primary_ips` 2026-07-17). **Row added 2026-07-17 (#6602): the #6589 gap — registry had no Primary IPv4 line though web-2/inngest/grok each did; the API confirms all 5 hosts carry one billable IPv4.** ~EUR 0.50/mo → USD ~0.54 at ~1.08 FX. <!-- estimate verify_by=2026-08-01 owner=cfo source="Hetzner invoice email in ops@soleur.ai" --> |
| Hetzner CPX22 (inngest) | Hetzner | hosting | 21.05 | active | 2026-08-01 | Dedicated amd64 host (`hcloud_server.inngest`, cpx22: 2 vCPU AMD, 4 GB RAM, hel1) for the extracted single-host Inngest scheduler singleton (#6178, ADR-100). Provisioned **amd64 (cpx22, ~EUR 19.49/mo)** because the cheaper arm64 cax11 (~EUR 5.99/mo) was EU-wide out of stock at Phase-2 provision time; the host is now **dual-arch** (arch derived from `var.inngest_server_type`, mirroring the zot registry) so it can revert to cax11 when Hetzner Ampere restocks (host replace). Extracts the durable-trigger control plane off the co-located web host so exactly-one-scheduler is enforced by topology (unblocks active-active web). ~EUR 19.49/mo — VERIFY actual draw on the next Hetzner invoice. Provisioned on the operator's `apply_target=inngest-host` dispatch; **billing began at provision 2026-07-08** (flipped approved-not-billing → active then). DPA: covered by the existing Hetzner DPA (same account, no new sub-processor). No new Supabase cost — but **not** for the reason previously recorded here. This row claimed "the dark backend is a distinct DB on `soleur-inngest-prd`". **That is false** (corrected 2026-07-15): until the cutover flips it, `INNGEST_POSTGRES_URI` in Doppler `soleur-inngest/prd` resolves to username **`postgres.mlwiodleouzwniehynfz`** — i.e. the dark backend is the **soleur-dev** project's pooler, co-tenanting Inngest's tables with the app's dev schema (`apps/web-platform/infra/inngest.tf:234-235`; ADR-100 addendum 2026-07-15). The cost conclusion still holds (soleur-dev is an existing project on the same Pro plan, so no incremental Supabase charge), but the co-tenancy had a **security** consequence the wrong description hid: the dev anon key ships to browsers, so the 14 goose-created tables were anon-writable until the 2026-07-15 table-scoped lockdown (ADR-030 I8, `0002_dev_inngest_tables_lockdown.sql`). Post-cutover the tables are dropped and soleur-dev returns to a pure app project. See issue #6178, ADR-100, PRs #6180/#6209 <!-- estimate verify_by=2026-08-01 owner=cfo source="Hetzner invoice email in ops@soleur.ai" --> |
| Hetzner Volume (inngest, 10 GB) | Hetzner | hosting | 0.62 | active | 2026-08-01 | Redis AOF block volume (`hcloud_volume.inngest_redis`) for the dedicated Inngest host's durable queue/run-state — never tmpfs (a wiped queue drops armed reminders, #5450 trap), hel1 (#6178). **Amount corrected 2026-07-17 (#6602): was 0.48 — same stale ~$0.044/GB basis; live API EUR 0.0572/GB × 10 GB × ~1.08 FX = USD ~0.62.** Provisioned with the inngest host; **billing began at provision 2026-07-08** (flipped approved-not-billing → active at the dispatch) <!-- estimate verify_by=2026-08-01 owner=cfo source="Hetzner invoice email in ops@soleur.ai" --> |
| Hetzner CX23 (hermes-agent) | Hetzner | hosting | 5.93 | retired | 2026-07-15 | **RETIRED 2026-07-15 (#6453) — reclaimed to free a Hetzner server slot.** cx23 (2 vCPU x86, 4 GB, 40 GB local disk), **nbg1**, created 2026-05-27, destroyed 2026-07-15 after 49 days. ~EUR 5.49/mo + ~EUR 0.50/mo primary IPv4 = ~USD 5.93 at ~1.08 FX. **This row is added at retirement, not at provision — it never had one.** The host was NEVER in Terraform (`apps/web-platform/infra/*.tf` has zero `hermes-agent` references), had no private-net attachment, no DNS pointing at it, and zero repo references outside unrelated prose — so it was invisible to the ledger for its entire life (~USD 9.7 total shadow spend). Purpose never established: at reclaim it was steadily transmitting ~410 MiB/day outbound (avg 711 B/s out vs 448 B/s in, ~0.43 pps, CPU avg 1.9%) — a low-rate outbound producer reporting somewhere unknown. **NO ROLLBACK — snapshot deleted 2026-07-15 (operator decision).** A pre-destroy snapshot (`408787015`, 6.56 GB of the 40 GB disk) was taken, then **deleted the same day**: retaining an unaudited disk of unknown provenance without an expiry is continued processing (CLO), Hetzner images carry no TTL, and the enforcement mechanisms available were each worse than the risk (the follow-through sweeper is read-only by design and only *closes* trackers — it cannot delete; giving it a write-capable Hetzner credential would hand that credential to all 28 follow-through scripts, gated by an editable issue body). **Consequence, accepted knowingly: hermes-agent is unrecoverable.** If an unexplained failure surfaces whose cause traces to this host, there is nothing to restore from — treat that as the cost paid for not retaining unaudited data. Both primary IPs were `auto_delete=true`, so no orphaned IP billing. Detection gap filed as #6460 (fleet-capacity-audit). See #6453, PR #6457 |
| Hetzner CX33 (grok-dogfood) | Hetzner | hosting | 9.17 | active | 2026-08-01 | Operator dogfood host for headless Grok Build (#6545, `hcloud_server.grok_dogfood` id `151628547`, `cx33` **hel1**, IPv4 `89.167.98.209`). LIVE 2026-07-16. Hetzner API net **EUR 8.49/mo** → USD ~9.17 at ~1.08 FX — VERIFY first invoice. Public IP only (no private-net — review P1). Gated by `enable_grok_dogfood` / Doppler `ENABLE_GROK_DOGFOOD=true`. DPA: existing Hetzner DPA. Kill criteria in runbook `grok-build-hetzner-dogfood.md`. <!-- estimate verify_by=2026-08-01 owner=cfo source="Hetzner invoice email in ops@soleur.ai" -->
| Hetzner Primary IPv4 (grok-dogfood) | Hetzner | hosting | 0.54 | active | 2026-08-01 | Egress/SSH public IPv4 for live grok-dogfood host (#6545). ~EUR 0.50/mo — VERIFY on invoice. auto_delete with host. <!-- estimate verify_by=2026-08-01 owner=cfo source="Hetzner invoice email in ops@soleur.ai" -->
| xAI API (Grok 4.5 dogfood) | xAI | saas | 100.00 | active | - | Grok 4.5 API spend for operator dogfood measure suite (#6545). Soft ceiling **$100/mo** trial kill-switch (unchanged). First billable measure batch 2026-07-16 ≈ **$0.14** (3 runs; logs `/var/log/grok-dogfood/runs.jsonl` on host). Key operator-only 0600 secrets.env on host (not prd dump / not private trust plane). VERIFY monthly draw on xAI console. No product Concierge traffic in Phase 1.
| Hetzner GEX44 (grok-dogfood-gpu) | Hetzner | hosting | 198.72 | approved-not-billing | - | **Phase 2 open-weight GPU dogfood (#6546).** Robot dedicated GEX44 (RTX 4000 SFF Ada 20 GB VRAM, FSN1) — **not** Cloud/`hcloud`. Catalog ~**EUR 184/mo** excl. VAT → USD ~198.72 at ~1.08 FX — **VERIFY at order** on Robot configurator. Status stays `approved-not-billing` until host is live; flip `active` same day as order with `billable_from:` on #6546; flip `retired` same day as Robot cancel. Setup fee one-time (~€79–€114 — live configurator). IPv4 typically included on GEX44 — do not double-count. R&D not COGS. Approach A: Ollama loopback + Grok co-located; no private-net; no prd secrets. Default **1-week soak then destroy** unless re-approve; hard stop if no comparison table within **14 days** of first billable hour. Kill: spend ceiling, license fail, compromise, customer data, brand mislabel, SKU jump without re-approval. **Do not order without spend ack + live stock + license memo.** DPA: confirm Hetzner AVV covers Robot dedicated at order. Runbook: `grok-build-hetzner-dogfood.md` Phase 2. |
| Hetzner Primary IPv4 (inngest) | Hetzner | hosting | 0.54 | active | 2026-08-01 | Egress-only public IPv4 for the dedicated Inngest host (apt + the soleur-inngest-bootstrap OCI pull during cloud-init; a no-public-IP host has no NAT). Public ingress is deny-all by firewall + host-local nftables scopes the control API to web hosts (#6178, SEC-H2). ~EUR 0.50/mo — VERIFY on invoice. Provisioned with the inngest host; **billing began at provision 2026-07-08** (flipped approved-not-billing → active at the dispatch) <!-- estimate verify_by=2026-08-01 owner=cfo source="Hetzner invoice email in ops@soleur.ai" --> |
| Supabase Pro + Custom Domain | Supabase | saas | 35.00 | active | 2026-04-03 | Pro plan ($25/mo) + custom domain add-on ($10/mo). Custom domain: api.soleur.ai for branded OAuth callbacks |
| Supabase Inngest project (Micro compute) | Supabase | saas | 10.00 | active | 2026-06-17 | Dedicated EU project `soleur-inngest-prd` (ref pigsfuxruiopinouvjwy, eu-west-1) for the durable Inngest Postgres backend (#5450). Org is on Pro, so a 3rd project bills ~$10/mo Micro compute (Nano is Free-plan-only) — the plan's "free-tier-capable" assumption was false. Isolation rationale: breaks shared-fate + connection-budget contention with the main app's project (architecture review P1-1). Verify actual compute draw on the next invoice |
| Stripe | Stripe | payments | 0.00 | test-mode | - | Payment processing for web platform. Live costs: 2.9% + $0.30/charge (US), 1.5% + EUR 0.25/charge (EU cards). No monthly minimum |
| soleur.ai | Cloudflare | domain | 70.00 | active | 2028-02-16 | 2-year registration required for .ai TLD. Also proxies app.soleur.ai (A record to Hetzner CX33, free tier) |
| Plausible Analytics | Plausible | saas | 9.00 | active | 2027-03-28 | Growth plan, 10K pageviews, EUR 9/mo. Annual renewal |
| X API | X Corp | api | 0.00 | active | - | Free tier, pay-per-use; @soleur_ai account |
| X API Basic (DEFERRED) | X Corp | api | 100.00 | deferred | - | DEFERRED: $100/mo Basic tier for fetch-mentions/timeline. Trigger: first paying customer or $500 MRR. See #497 |
| Better Stack Responder (DEFERRED) | Better Stack | observability | 29.00 | deferred | - | DEFERRED: $29/user/mo Responder tier for on-call escalation policy (Slack/SMS/phone routing of the inngest-server heartbeat alert). Free tier is heartbeat + email-only. Trigger: first paying customer or first incident with user-visible latency from email-only routing. See #3960 |
| Sentry Team | Sentry | observability | 73.56 | active | 2026-06-17 | Error tracking + CSP report-uri + cron monitors for web platform. Org: jikigai-eu (eu.sentry.io, am3 Team plan tier, monthly billing, card on file), project: soleur-web-platform. $75/mo PAYG cap on top (onDemandMaxSpend) for cron-monitor seat overages — see #3958. Replaces former free-tier `jikigai` org, which was canceled 2026-05-21 and its $29 unused balance transferred as credit (see One-Time row). Context: #3861 residency reframe, #3962 PIR Phase 8 Gate 3b. 2026-06-11: PR #5161 backfilled 14 cron monitors (40 total active) — closes the void-heartbeat alerting gap from #5111's AC12. 2026-07-17 (#6589): Amount corrected 40.00 → 71.22 against a live API read — the row had carried an unverified estimate since 2026-06-11. Composition is monitor-count-driven and fixed, not an estimate. 2026-08-03 (#6808): re-measured live rather than incremented, which caught three stale facts the +$0.78 this PR adds would otherwise have hidden. (a) The PAYG cap is **$75**, not $50 — `onDemandMaxSpend: 7500`, `budgetMode: shared`. (b) Billable cron-monitor seats are **51**, not 49: `monitorSeats.usage = 52` against `reserved = 1`, live `onDemandSpendUsed = $39.78` (= 51 × $0.78). (c) Uptime is `usage = 5` against `reserved = 1` → 4 billable × $1.00 = $4.00. So the CURRENT true figure is $29 base + $43.78 PAYG = **$72.78**, and this PR's `workspaces-luks-verify` monitor takes it to $29 + $44.56 = **$73.56** (52 billable seats × $0.78 + $4.00). `monitorSeats.reserved = 1` and `uptime.reserved = 1` (live-verified), so every scheduled workflow adds an uncapped $0.78/mo — no purchasable reserved volume exists (getsentry/sentry#73359, closed unshipped). $44.56 against the $75 cap leaves ~39 monitors of headroom, not the ~9 the pre-#6808 row implied against the stale $50 figure; next cliff 2026-08-16 (onDemandPeriodEnd). At renewal, if PAYG cannot cover all active monitors, every monitor deactivates at once and check-ins are silently dropped — see #3958. Re-read with `doppler run -p soleur -c prd_terraform -- curl -sS -H "Authorization: Bearer $SENTRY_IAC_AUTH_TOKEN" https://eu.sentry.io/api/0/customers/jikigai-eu/ \| jq '{onDemandMaxSpend, onDemandSpendUsed, categories}'` rather than incrementing this cell |
| Better Stack | Better Stack | observability | 0.00 | free-tier | - | Uptime monitoring for app.soleur.ai/health (3-min interval) + status page at soleur-ai.betteruptime.com. Free tier: 10 monitors, 1 status page, email alerts. Upgrade trigger: first paying customer (custom domain, white-label, custom CSS). 2026-06-10: 80% log-quota warning (free tier 3 GB/mo logs, 3-day retention + 30 GB metrics). Root cause: Vector host_metrics 30s scrape = >99% of ingest (~196k rows/day). Remediated: 300s scrape + loop*/dm-* device excludes (PR #5105) + deleted leftover "Onboarding • Real-time flights" demo source (id 2327782) via Telemetry API. Decision: stay free tier ($0.00 unchanged); upgrade trigger unchanged (first paying customer). Ref #4296. 2026-06-10 second pass: AC12 verdict FAIL (198 rows per 300s scrape, ~57k rows/day projected vs 25k threshold — interval fix alone insufficient). Remediated again: dropped network collector + filesystem mountpoint allowlist (/, /mnt/data, /var/lib/vector), predicted ~19.9k rows/day (PR #5131). Verdict tracker #5110 |
| Buttondown | Buttondown | saas | 0.00 | free-tier | - | Newsletter platform. Free tier: 100 subscribers, custom sending domain, API access. Upgrade trigger: >100 subscribers ($9/mo Basic) |
| Doppler | Doppler | secrets-mgmt | 0.00 | free-tier | - | Secrets management. Developer plan (free). Account: <ops@jikigai.com>. 6 configs: dev, dev_personal, ci, prd, prd_scheduled, prd_terraform. Upgrade triggers: team growth, audit logs |
| LinkedIn | LinkedIn | social | 0.00 | active | - | Company page + personal profile for content distribution. Free tier API access via LinkedIn Marketing Developer Platform |
| Bluesky | Bluesky | social | 0.00 | active | - | @soleur.ai account for content distribution. AT Protocol API (free, no tier limits) |
| Resend | Resend | email | 20.00 | active | - | Transactional + cold-outbound email API. Upgraded free-tier → **Pro ($20/mo, 50K emails/mo)**. Trigger: the 2026-06-15 outbound-email cold-sending go-live (#5325, PR #5365) added a dedicated `outbound.soleur.ai` sending subdomain for agent cold outreach, isolated from the `notifications@soleur.ai` transactional stream; running a second Resend sending domain exceeds the free tier's 1-domain limit. (PR title says `mail.soleur.ai`; the merged runtime domain is `outbound.soleur.ai` — `mail.soleur.ai` is delegated to Buttondown and can't carry Resend DNS.) Pro upgrade is operator-driven/gated (restricted send-key can't self-upgrade). **Estimate — the flat published Pro price is $20/mo; the actual billed charge (first-invoice proration) + billing date are authoritative only from the Resend invoice, which the restricted send-key cannot read. Not opened this cycle (#6602, operator OPTION 1) — verify_by marker dated to the next monthly Resend invoice.** See ADR-060, compliance-posture PA-28 <!-- estimate verify_by=2026-08-16 owner=cfo source="Resend Pro invoice email in ops@soleur.ai" --> |
| Proton Mail Workspace Standard (2 users) | Proton | email | 14.00 | active | - | Custom-domain email for `@soleur.ai` mailboxes. Workspace Standard plan, 2 users, **billed ANNUALLY** (`~$7/user/mo annual` estimate = ~$168/yr for 2 users, amortized ÷12 like the Cloudflare domain row). Proton exposes no billing API and the annual invoice was not opened this cycle (#6602, operator OPTION 1), so the amortized $14/mo remains an estimate; verify_by is dated to the **annual renewal** (Proton was enabled 2026-04-28 via PR #3009 → renews ~2027-04-28) — a month-out date on an annually-billed row would noise-file every month for a figure that cannot be re-verified until renewal. DNS provisioned via Terraform PR #3009 (apex MX, SPF widening to `~all`, 3x DKIM CNAMEs, verification TXT). Receives + signs outbound from `@soleur.ai`. <!-- estimate verify_by=2027-04-28 owner=cfo source="Proton annual invoice email in ops@soleur.ai" --> |
| Anthropic API (cron-ux-audit) | Anthropic | api | 15.00 | active | - | Operator `ANTHROPIC_API_KEY` draw from the `cron-ux-audit` Inngest handler on the prod host. **Mechanism corrected 2026-07-30:** this row cited `.github/workflows/scheduled-ux-audit.yml` until now; that workflow was deleted in `5d3a1e11a` when ux-audit moved to Inngest, so the draw originates server-side from a Doppler-sourced key, not from GitHub Actions. The `15.00` (~$3-$12/run, ≤3 runs/month) is the figure measured under the deleted workflow and has not been re-derived since 2026-04-19. **Scope is this ONE cron** — the other 14 claude-spawning crons are ledgered separately under `Anthropic API (claude-eval cron fleet)` in dev-tools, because they are engineering/marketing automation rather than per-paying-user delivery. <!-- estimate verify_by=2026-08-30 owner=cfo source="doppler run -p soleur -c prd_terraform -- scripts/betterstack-query.sh --since 3d --grep SOLEUR_CLAUDE_COST, sum cost_usd for source=cron:cron-ux-audit and cadence-weight the 3-day window to monthly (ADR-108)" --> |
| Anthropic API (claude-eval cron fleet) | Anthropic | dev-tools | 0.00 | unmetered | - | R&D / dev-tooling (engineering + marketing automation — bug-fixer, daily-triage, roadmap-review, legal-audit, growth, content-generator, …), **NOT product COGS** — same basis as the Max seats and the #6538 `grok-dogfood` precedent. **15 claude-spawning crons** share `apps/web-platform/server/inngest/functions/_cron-claude-eval-substrate.ts` (count = crons importing the substrate AND `model-tiers`; a raw `grep -rl` over the substrate returns 21 files and is NOT the cron count — it includes a shared module, an event handler, two one-shots and a comment-only match). `cron-compound-promote` and `cron-weekly-release-digest` draw on the same key over HTTP without the substrate. Cost driver is the tier: `AUDIT_MODEL = claude-opus-5`, `EXECUTION_MODEL = SONNET_MODEL` (`server/inngest/model-tiers.ts`). **The `0.00` is UNMEASURED, not zero** — deliberately un-booked rather than guessed, so **any subtotal containing this row is a floor**. `unmetered` keeps it out of the operator-digest run-rate (that allowlist sums `active` only); absent beats wrong. **A figure IS derivable today** from the per-run `SOLEUR_CLAUDE_COST` markers (ADR-108) — no `ANTHROPIC_ADMIN_KEY` required; only the authoritative **org-total** is blocked on #6297. ADR-108 §Context records ~$430/mo on the shared key, a broader scope than this row (fleet + agent-runner/leader-loop sessions). Exhaustion is detected AT the wall by the hourly `cron-anthropic-credit-probe`, never predicted; the pre-exhaustion alert is #5692. <!-- estimate verify_by=2026-08-30 owner=cfo source="doppler run -p soleur -c prd_terraform -- scripts/betterstack-query.sh --since 3d --grep SOLEUR_CLAUDE_COST, GROUP BY source and cadence-weight the 3-day retention window to monthly per betterstack-log-query.md (ADR-108)" --> |
| Anthropic API (CI) | Anthropic | dev-tools | 0.00 | unmetered | - | R&D / dev-tooling (engineering accelerator, NOT product COGS) — same basis as the Max seats. CI surfaces that CAN draw `ANTHROPIC_API_KEY`: `fix-constraints-stage-a.yml` (its agent step fires only when the constraint gate is red — `claude-sonnet-5`, `--max-turns 20`; otherwise it pays only a 1-token preflight), plus `ci.yml`'s `plugin-root-propagation-gate` and `sandbox-canary-capture-gate`, which each run a real Claude turn inside `node:22-slim` when their path triggers match. **`unmetered`, not `accruing`:** THREE api-spend producers exist, not the two this row previously named. `fix-constraints-stage-a.yml` is **active** (417 runs since 2026-07-01) and correctly wires `Capture API spend` + `Upload API spend artifact` — but its agent step fires only on a red constraint gate (0 of 25 sampled runs), so it is a **zero-yield meter, not a missing one**. The two this row used to name emit nothing: `claude-code-review.yml` is `disabled_manually`, `test-pretooluse-hooks.yml` is `workflow_dispatch`-only. Genuinely unmetered: `ci.yml`'s `plugin-root-propagation-gate` and `sandbox-canary-capture-gate`, each running a real paid turn inside `node:22-slim` when its path trigger matches. The `0.00` therefore means "not measured", NOT "measured as zero" — [`../finance/api-spend-ledger.jsonl`](../finance/api-spend-ledger.jsonl) holds **0 records** and no `api-spend-*` artifact exists. Procedure (see its status banner): [`api-spend-reconciliation.md`](../engineering/operations/runbooks/api-spend-reconciliation.md). Ledger provenance: #5086, ADR-056; scheduled automation #5173. <!-- estimate verify_by=2026-08-30 owner=cfo source="gh api repos/jikig-ai/soleur/actions/artifacts --paginate, count artifacts whose name starts with api-spend; or the #6297 org-total once ANTHROPIC_ADMIN_KEY is mintable" --> |
| Claude Code Max 20x (seat 1) | Anthropic | dev-tools | 200.00 | active | 2026-05-01 | Max 20x tier ($200/mo/seat). Engineering tooling for Soleur development. Started 2026-02-01; seat 1 of 2 |
| Claude Code Max 20x (seat 2) | Anthropic | dev-tools | 200.00 | active | 2026-05-01 | Max 20x tier ($200/mo/seat). Engineering tooling for Soleur development. Started 2026-02-01; seat 2 of 2 |
| Cloudflare R2 (cla-evidence) | Cloudflare | storage | 0.00 | active | - | Off-site CLA signature archive, Governance object-lock, 10yr retention, region weur. Pay-per-use: $0.015/GB-mo + $0.36/M writes. Sub-cent/mo at realistic scale. See apps/cla-evidence/infra/. |
| Cloudflare Load Balancing | Cloudflare | hosting | 5.00 | approved-not-billing | - | Load Balancing add-on fronting `app.soleur.ai` for the multi-host GA blue-green ingress (ADR-068 §(b); runbook `cf-lb-a-record-gaplessness-verify.md`). ~$5/mo base tier (2 origins web-1+web-2 + 1 pool + 500k DNS queries/mo; +$5/additional origin, + DNS-query overage). **Operator pre-approved the recurring spend 2026-07-03** — NOT revenue-gated; goal is deploy-everything-then-flip-to-GA ASAP. Free-plan zone has no LB subscription until the LB is created; provisions via `apply-web-platform-infra.yml` as a count-gated `cloudflare_load_balancer` (`cf_load_balancing_enabled`). **Billing begins at provision** (status flips `approved-not-billing → active` in the GA infra-apply PR) — excluded from current burn until then. DPA: covered by existing Cloudflare relationship (same account, no new sub-processor). VERIFY exact base + query draw on the first Cloudflare invoice post-provision. See #5966, PR #5968 |

## Vendor account limits (facts supplied by the operator — no API path)

Not spend, but the ceilings that bound it. Recorded here because **nothing in this repo can read
them**: Hetzner's `GET /v1/limits` returns **404** on this account, so the vendor's own limits page is
the only source and no automated check can contradict a stale value. Treat every number below as an
operator-supplied fact with a decay date, never as derivable.

| Limit | Live value | Verified | Notes |
|---|---|---|---|
| Hetzner Cloud servers | **4 / 10** | 2026-07-26 | Counted per customer across all projects. Six free slots — enough for web-2 **and** git-data with headroom, so neither is slot-blocked and no vendor request is pending. This is the granted outcome of the 2026-07-15 cap-headroom workstream (raise 5 → 10, #6453 / PR #6457); the **granted value was never recorded back into the repo**, so the pre-raise "5" survived in downstream reasoning until 2026-07-26 and nearly drove two wrong calls: requesting a raise that already existed, and retiring `soleur-grok-dogfood` to "free a slot" — an irreversible `cx33` loss (cheapest orderable 8 GB replacement `cpx32`, 4.2x) for a slot already free six times over. Making this self-checking belongs to #6460. <!-- estimate verify_by=2026-10-26 owner=coo source="Hetzner Cloud limits page (no API endpoint)" --> |
| Hetzner dedicated vCPUs | 0 / 8 | 2026-07-26 | Only the `ccx*` line consumes this; the fleet is entirely shared-vCPU today. Relevant if a `ccx*` type is ever chosen for a host. <!-- estimate verify_by=2026-10-26 owner=coo source="Hetzner Cloud limits page (no API endpoint)" --> |
| Hetzner block-volume capacity | 110 / 1024 GB | 2026-07-26 | web-2's birth adds 20 GB. <!-- estimate verify_by=2026-10-26 owner=coo source="Hetzner Cloud limits page (no API endpoint)" --> |
| Hetzner primary IPv4 | 8 / 20 | 2026-07-26 | One per host, `auto_delete` with the host. <!-- estimate verify_by=2026-10-26 owner=coo source="Hetzner Cloud limits page (no API endpoint)" --> |
| Hetzner snapshots | 4 / 30 | 2026-07-26 | <!-- estimate verify_by=2026-10-26 owner=coo source="Hetzner Cloud limits page (no API endpoint)" --> |

**Not a limit but the same class of latent risk — RESTATED 2026-08-06 (#7309), because the
previous form was measured false.** It read: three running hosts sit on server types that can no
longer be ordered in any EU DC — web-1 (`cx33`), `soleur-grok-dogfood` (`cx33`), `soleur-registry`
(`cx23`). **One of those three legs is false; the other two stand.** Probed live 2026-08-06,
`.server_types.available`, 3 samples, per datacenter:

| | `cx23` | `cx33` | `cpx22` | `cax11` |
|---|---|---|---|---|
| `hel1-dc2` | ✓ | **✗** | ✓ | ✗ |
| `nbg1-dc3` | ✓ | ✓ | ✓ | ✗ |
| `fsn1-dc14` | ✓ | ✓ | ✓ | ✗ |

The `cx23` leg is false — it is orderable in `hel1-dc2` again (it was not on 2026-07-26, nor on
2026-08-04). **The two `cx33` legs are unchanged and still true:** web-1 and `soleur-grok-dogfood`
both run in `hel1`, where `cx33` is NOT orderable. Its ✓ in the other two datacenters buys them
nothing — a host cannot be rebuilt in a datacenter it does not occupy without also moving region,
which is a larger decision. **Read every probe against the DC the host actually runs in**; a
fleet-wide reading says "`cx33` is available" and answers the wrong question. (An earlier draft of
this very paragraph made that mistake, which is why it is called out here.)

So the risk is two distinct things, not one. For the `cx33` pair it is the original: the type is
not orderable where they run. For `soleur-registry` it is now sharper — the type IS orderable
today and was not two days ago, so **availability is not a property of the type**; it is a
point-in-time reading of vendor supply that moves in both directions, and a dated tick is not a
capacity reservation. Each rebuild is a type decision and a cost change either way.
`soleur-registry` was repinned to `cpx22` on that basis (#7309, row above) — `cpx22` was available
at every recorded probe. The two `cx33` hosts are unrepinned. Tracked in #6460, which must sample
**repeatedly and per datacenter**: a single fleet-wide probe would have certified any of the
contradictory answers above.

## One-Time

| Service | Provider | Category | Amount | Status | Date | Notes |
|---------|----------|----------|--------|--------|------|-------|
| soleur.ai registration | Cloudflare | domain | 140.00 | active | 2026-02-16 | 2-year initial registration |
| Sentry credit (jikigai org cancellation) | Sentry | observability | -29.00 | credit | 2026-05-21 | $29 transferred from canceled `jikigai` duplicate org to `jikigai-eu`; applied on next jikigai-eu invoice (2026-06-17). Vendor confirmation: Sentry support (Joe) reply 2026-05-21. Context: #3861 residency reframe, #3962 PIR Phase 8 Gate 3b |

## Downstream Consumers

- **Finance:** [finance/cost-model.md](../finance/cost-model.md) — derived monthly burn and break-even model (R&D / product-COGS split). Refresh on every category subtotal shift >10 % per cost-model.md `review_cadence`.
