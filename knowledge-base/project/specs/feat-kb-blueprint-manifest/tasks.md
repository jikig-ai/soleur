---
feature: feat-kb-blueprint-manifest
issue: "#7332"
pr: 7336
lane: cross-domain
brand_survival_threshold: single-user incident
plan: knowledge-base/project/plans/2026-08-06-feat-kb-blueprint-manifest-plan.md
scope: PR 1 (CLI producers). PR 2 (manifest + dashboard) is deferred and scoped in the plan.
---

# Tasks — KB producers (PR 1)

## Phase 0 — Preconditions (resolve before writing code)

- [x] **0.1** Verify `likec4@1.50.0` is reachable from the headless agent process
      (`npx -y likec4@1.50.0 --version`). If npm is unreachable in the sandbox,
      Phase 1 render must degrade cleanly rather than error. Record the result.
- [x] **0.2** Better Stack ingest path **measured and rejected for PR 1**. The path
      exists (in-sandbox stdout → `git-lock-marker-telemetry.ts` `MARKER_RE` at `:93`
      → pino WARN+ → vector Source 3 → `[sinks.betterstack]`) but is unreachable
      here: `MARKER_RE` is an exact-sentinel allowlist, the hook is registered only
      on the hosted path (`agent-runner-query-options.ts:38`), `.claude/settings.json`
      registers no PostToolUse `Bash` matcher, and a customer's self-hosted CLI has
      no route to Soleur infra — nor should it, absent consent. Liveness signal is
      **layer 7 (`cli-stdout-artifact`)**. Ruled by the `cto` agent. See plan
      §Observability and ADR-171 §Observability boundary.
- [x] **0.3** ADR-171 confirmed free against freshly-fetched `origin/main`
      (highest existing: ADR-170).
- [x] **0.4** **(blocking — do before Phase 3)** Add layer 7 to
      `plugins/soleur/agents/engineering/review/observability-coverage-reviewer.md`:
      new numbered entry `cli-stdout-artifact`, added to the Step-2 accepted-substring
      list, section heading corrected to seven, and the stale "five observability
      layers" / "not a seventh layer" / "The six layers above are all server/host-side"
      clauses swept. Without this no honest layer citation is expressible and the
      reviewer agent P1s the plan. **This edit lives under `plugins/` and therefore
      does not cross the boundary ADR-171 defines.**

## Phase 1 — C4 producer

### 1.1 Specify the dependency contract upstream
- [x] Edit `plugins/soleur/skills/spec-templates/SKILL.md:133` — component template
      emits `dependencies: [<component-ids>]` frontmatter alongside the prose
      `**Internal**:` line.
- [x] Edit `plugins/soleur/commands/sync.md` Project Analysis — instruct the same
      frontmatter emission.

### 1.2 Parser (TDD — failing tests first)
- [x] Write `plugins/soleur/test/fixtures/component-docs/` — **synthesized only**
      (`cq-test-fixtures-synthesized-only`), three variants: frontmatter form, link
      form, and Soleur's own prose form (`**Internal**: None (agents are standalone)`).
- [x] Write `plugins/soleur/test/c4-from-components.test.sh` (RED).
- [x] Implement `plugins/soleur/lib/c4-from-components.ts`: read `dependencies:`
      frontmatter; fall back to `**Internal**: [name](name.md)` link extraction.
- [x] Skip docs whose frontmatter carries `status: deprecated` (`sync.md:151` never
      deletes removed components — without this the element set accretes forever
      and deprecated docs create dangling edges).

### 1.3 Validation gates — all three
- [x] Diagnostic-stream grep using `DIAG_RE` from `scripts/regenerate-c4-model.sh:94`.
      **Do NOT mirror `c4-render.ts`** — `:192-195` explicitly rejects stderr gating.
- [x] `.elements | length > 0`.
- [x] **Relationship count > 0** — report *degraded*, not failure, when zero.
- [x] Assert the `likec4@1.50.0` pin matches `regenerate-c4-model.sh` and
      `c4-render.ts` (both-gates is only safe while the pin holds).

### 1.4 Non-destructive write contract
- [x] Emit to `generated-components.c4`, not the canonical three filenames
      (`/soleur:architecture` writes those cwd-relative — two writers, one path).
- [x] Every generated file carries `// GENERATED by /soleur:sync c4 — hand edits will be overwritten`.
- [x] Producer **refuses to overwrite** any file lacking that header.

### 1.5 Full artifact set
- [x] Emit `model.likec4.json` (the viewer returns `MODEL_NOT_BUILT` without it —
      `api/kb/c4/project/route.ts:174`).
- [x] Emit `c4-model.md` carrying a ` ```likec4-view ` fence (the KB viewer renders
      diagrams only from a markdown page with an embed).

### 1.6 Wire into sync
- [x] Add the `c4` area to `plugins/soleur/commands/sync.md` and to `all` dispatch.

## Phase 2 — domain-model promotion

### 2.1 Register bootstrap
- [x] Write `plugins/soleur/test/domain-model-init.test.sh` (RED).
- [x] Add `init --register <path>` to `scripts/domain-model-drift.sh`: seeds both
      canonical headings (`## Business Rules`, `## Auto-inferred (unreviewed)`);
      idempotent no-op exit 0 when the file exists.
- [x] **Leave the `realpath -e` guards at `:161` and `:240` unchanged** — `drift`
      and `write-row` must still die on an unresolvable path. Only `init` creates.

### 2.2 Headless append
- [x] Write `plugins/soleur/test/domain-model-headless-append.test.sh` (RED).
- [x] Add a headless branch in `sync.md` (bypassing the per-row `AskUserQuestion` at
      `:196-207`) that appends every candidate via the existing `write-row` primitive.
- [x] Regression-test each preserved safety property, red-when-broken:
      secret-shaped content refused fail-closed; duplicate content anchor is a
      no-op; write is atomic; no `BR-*` id minted; curated `## Business Rules` table
      byte-identical before and after.

### 2.3 Reconcile the terminal-area contract
- [x] Explicit `/soleur:sync domain-model` still terminates after the drift report
      (`sync.md:212`).
- [x] New `all`-dispatch path: `init` → `drift` → headless append → feed row counts
      into the Phase 3 coverage summary. Both behaviours asserted.

## Phase 3 — Coverage summary

- [x] Implement `plugins/soleur/lib/kb-coverage.ts`. Expected-set is a static list
      in PR 1 (PR 2 replaces it with the manifest — the only coupling).
- [x] Emit `knowledge-base/project/kb-coverage.md` **deterministically**: stable
      ordering, no embedded timestamp. Two consecutive runs on an unchanged KB
      produce a byte-identical file.
- [x] Wording: expectation-vs-presence only. "no `privacy-policy.md` present in this
      knowledge base", never "missing: privacy policy".
- [x] Print the same summary to stdout at end of sync.
- [x] Emit the `SOLEUR_KB_SYNC_PRODUCERS` marker with
      `{c4_elements, c4_relationships, domain_model_rows, coverage_present, coverage_expected}`
      **twice per run**: on sync stdout AND as a literal line inside the committed
      `knowledge-base/project/kb-coverage.md`. Identical token, identical five fields.
      Layer 7 (`cli-stdout-artifact`) — stdout alone does not survive the session.
- [x] Emit `SOLEUR_KB_SYNC_ERROR` on failure — **stdout marker, not
      `reportSilentFallback`** (that lives in `apps/web-platform/server/`; nothing
      under `plugins/` imports Sentry) — **plus a degraded-state row in
      `kb-coverage.md`**, so a failed run survives the session.
- [x] Determinism assertion covers the counts line: two consecutive runs on an
      unchanged KB produce a byte-identical file, counts line included. (Counts are
      a pure function of KB state, so this holds — assert it rather than assume it.)
- [x] **Drift guard:** a plugin-side test asserting the stdout marker and the
      artifact's marker line carry **byte-identical field sets**. This is the
      in-scope analogue of `git-lock-marker-telemetry.test.ts`'s sentinel pinning,
      and it is what keeps the `discoverability_test` non-vacuous.
- [x] **Negative AC (consent):** assert `kb-coverage.md` and the stdout markers
      contain no repository-identifying data beyond counts — no paths, no file names,
      no repo URL. Keeps ADR-171 claim 3 true in code, not just prose, and forecloses
      the consent problem if a future PR ever routes these markers anywhere.

## Phase 4 — ADR-171 + Soleur's own C4

- [x] Author `ADR-171` via `/soleur:architecture`, `status: adopting`. Record: the
      Docker-build-context fact as the reason a cross-boundary import is impossible;
      the two-writer precedence rule from 1.4; runtime-read vs build-time-import.
- [x] ADR-171 `## Observability boundary` — four claims (CTO ruling, Phase 0.2):
      (1) the build-time boundary generalizes — plugin code cannot import Soleur's
      Sentry client, logger, or any sink, by construction; (2) string coupling via
      `MARKER_RE` is a distinct and weaker crossing than an import and was **still
      rejected**, admissible only for sentinels with a real hosted producer, since a
      dead allowlist entry corrupts a file read as an inventory of live channels;
      (3) the **consent** boundary is stricter than the build boundary and is the
      operative one — any future self-hosted telemetry must be customer-owned and
      opt-in, never a Soleur-owned default; (4) therefore plugin-emitted observability
      is layer 7, with the re-entry condition that `MARKER_RE` may be widened only
      when a hosted producer actually ships, and the drift guard at
      `git-lock-marker-telemetry.test.ts:177-212` must be extended in the same change.
- [x] `model.c4` — add `connectedRepoKb` (`#external` database, trust-boundary
      description in the style of `connectedRepoPlugin:320`).
- [x] `model.c4` — add `sync -> connectedRepoKb` (`sync` currently has **no** edge
      at all, unlike every sibling skill at `:625-629`).
- [x] `model.c4` — add `architecture -> connectedRepoKb` (1.4's collision, in
      diagram form).
- [x] `model.c4:87` — update the `kb` description (omits diagrams and the register).
- [x] `views.c4:26-37` — add `connectedRepoKb` to `containers` or it will not render.
- [x] `bash scripts/regenerate-c4-model.sh` and commit `model.likec4.json` (lockfile).

## Phase 5 — Verification

- [x] Typecheck — **but not via this AC's literal command, and the reason matters.**
      The diff touches **0 files under `apps/web-platform/`**, and no tsconfig covers
      `plugins/` (there is no root or plugin tsconfig), so `tsc --noEmit` from
      `apps/web-platform` would never have typechecked any TypeScript this PR adds.
      Worse, in a worktree with no `node_modules`, bare `npx tsc` resolves to an
      unrelated registry package `tsc@2.0.4` ("This is not the tsc command you are
      looking for") — CI only gets the right compiler because `npm ci` runs first.
      Ran the project's pinned compiler (typescript@5.7.2, matching
      `apps/web-platform/package.json` `^5.7.0`) with `--strict` against the three
      new modules instead: **clean, exit 0**. CI still runs the repo-convention
      command unchanged.
- [ ] `bash scripts/test-all.sh` — **required**, not optional:
      `c4-model-freshness.test.sh` is an orphan suite the touched-file loop misses.
- [ ] Walk every AC in the plan's Acceptance Criteria section.
- [x] AC2 specifically: prove the relationship-count gate goes **red** against the
      prose-form fixture. A gate never seen red is not a gate.
- [ ] PR body: `## Changelog` section + `semver:minor` label
      (`plugins/soleur/AGENTS.md` pre-commit checklist).

## Phase 6 — Post-merge verification (agent-run, NOT operator)

Both steps below are `gh api` reads with no human gate — they are executed by
`/soleur:postmerge` after this PR merges, not handed to the operator and not
pre-filed as an issue (that would be phantom backlog for work the pipeline
already performs). They are listed here as the verification contract, not as a
checklist for a person.

**Precondition, stated rather than assumed:** step 1 needs a sync run against the
dogfood repo *after* this merges, and step 2 can only pass once that run's PR is
pushed and merged — the KB viewer reads the GitHub source of truth, not the
on-disk clone. If the dogfood sync has not happened yet, these stay open rather
than being reported green.

- [ ] Re-sync `2my8r9ry2t-wq/Skouer`. Assert elements match the component-doc set
      and relationships match declared dependencies. **No literal count** —
      `ci-guards.md` has no `**Internal**` line, so 7 docs ≠ 7 connected elements.
- [ ] Confirm the generated `.c4` and `model.likec4.json` reached **GitHub** (the
      viewer's source of truth), not just the local clone:
      `gh api repos/2my8r9ry2t-wq/Skouer/contents/knowledge-base/engineering/architecture/diagrams`.
