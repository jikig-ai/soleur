# Tasks: fix(inngest cutover) — execute → quiesce-web → execute → arm loop drivable end to end

Plan: `knowledge-base/project/plans/2026-09-14-fix-inngest-cutover-loop-drivable-plan.md` (closes #6921, #8077). **Deepen 2026-09-14:** the plan's `## Deepen-Plan Corrections` (C1–C13) supersede conflicting plan text; tasks below are updated to match. Every phase is RED → GREEN: write the rows first, observe them red, then implement. No `cutover-inngest.yml` op and no `op=quiesce-web` is dispatched in this work item.

## Phase 0: Preconditions (measured, no writes)

- [ ] 0.1 Confirm `is-active`/`is-enabled` are used without `sudo` in `apps/web-platform/infra/ci-deploy.sh` (`verify_inngest_quiesced`, `inngest_unit_enabled`) — no sudoers change.
- [ ] 0.2 Confirm `hooks.json.tmpl` `include-command-output-in-response-on-error: true` on `inngest-inventory` (:161) and `inngest-liveness` (:181).
- [ ] 0.3 Confirm `vector.toml` Source 4 allowlists `ci-deploy`, `inngest-rearm-reminders`, `inngest-inventory` (:178-182) — no vector change.
- [ ] 0.4 Confirm the `ci-deploy.test.sh` mock `systemctl` default is the quiesced shape (`is-active → inactive`, `is-enabled → disabled`, :125-160) and inventory the four `restart inngest …` rows that must be armed with `MOCK_SYSTEMCTL_ENABLED_STATE=enabled`.
- [ ] 0.5 Confirm the rearm script's capture block (:197-219) reads `ENUMERATE_CMD` (:173)/`MODE`/`CAPTURE_FILE` (:194-195) defined AFTER the `SECRET=` block (:161-166) — the relocation moves the `SECRET=` block down, never the capture block up.
- [ ] 0.7 Inventory the capture-mode invocations in `apps/web-platform/infra/webhook-doppler-token-reread.test.sh` (`run_rearm_capture()` :171-182, inline F and C24) — ~25 assertions use capture mode as the Doppler-read vehicle (C2).
- [ ] 0.6 Confirm `webhook.service` `User=deploy` (`cloud-init.yml:250`), `ReadWritePaths … -/var/lib/inngest` (:267), `chown deploy:deploy /var/lib/inngest` (`inngest-bootstrap.sh:142-143`).

## Phase 1: D1 / D1b / D1c — capture resume, capture-at-quiesce, Σ=0 re-arm

- [ ] 1.1 `inngest-rearm-reminders.test.sh`: mock `systemctl` on `MOCKBIN` (`MOCK_SYSTEMCTL_ACTIVE`, `MOCK_SYSTEMCTL_ENABLED_STATE`, verb log; absent unit → `is-enabled` prints `not-found` rc 4; `is-active` exits 3 on inactive); extra must-PASS row `failed`+`disabled` + valid file → `source:persisted` (C1); rows `test_capture_resumes_persisted_when_quiesced`, `test_capture_quiesced_without_file_fails_with_remedy`, `test_capture_quiesced_corrupt_file_fails`, `test_capture_serving_enumerates_live`, `test_capture_health_down_but_enabled_does_not_resume`, `test_capture_needs_no_secret` (no secret, no `doppler` on PATH), `test_rearm_still_fails_closed_without_secret`, `test_rearm_empty_capture_emits_canonical_line` — observe RED.
- [ ] 1.2 `inngest-rearm-reminders.sh`: `unit_quiesced()` (`is-active ∈ {inactive, failed}` AND `is-enabled == disabled`, each read with `|| true`); capture branch: quiesced + valid array → `{captured, reminder_ids, capture_file, source:"persisted", captured_at}` + journald `resumed persisted capture …`; quiesced + no/invalid file → exit 1 `nothing to resume from … dispatch op=rollback`; otherwise live enumeration (response unchanged); move the `SECRET=` fail-closed block to just before the records-source section; `count == 0` branch emits `inngest-rearm-reminders: re-armed=0 failed=0 total=0` before `rm -f` (to stderr, as the non-empty path at :294); fix the header comment `:12-14` (dedup key is event `id` only, not `id`+`ts`) — observe GREEN.
- [ ] 1.2b `webhook-doppler-token-reread.test.sh` (C2): re-point `run_rearm_capture` and the inline F/C24 invocations to `INNGEST_REARM_MODE=rearm-from-capture` with a pre-seeded `[]` capture file; reword B4; update the header comment; add row `capture mode never invokes doppler`; run the suite — all ~82 rows green.
- [ ] 1.3 `ci-deploy.test.sh`: add `$MOCK_SYSTEMCTL_LOG` verb log now (not Phase 3); stateful mock **opt-in** via `MOCK_SYSTEMCTL_STATEFUL=1` (`stop` touches `$MOCK_STATE_DIR/stopped`; `is-active` → `inactive` once it exists) — default stays static so AC-Q5 (:3154) and AC-Q6 (:3160) keep their verdicts; new capture rows set `MOCK_CURL_INNGEST_HEALTH_FAIL=1`; `create_base_mocks` exports a default-success mock rearm via `INNGEST_REARM_CMD` (AC-Q6 now reaches capture); mock rearm logs `capture`, honours `MOCK_REARM_CAPTURE_FAIL=1`, sleeps for the bounded row (assert rc non-zero, not exactly 124); rows: active → `capture,disable,stop` + `quiesced`; capture fails → `quiesce_capture_failed`, no `disable`/`stop`, `INNGEST_QUIESCE_CAPTURE_FAILED` logged; inactive → no `capture`, `quiesced`; bounded → `quiesce_capture_failed` — observe RED.
- [ ] 1.4 `ci-deploy.sh` quiesce handler (:2682): `is-active == active` gate → `timeout --kill-after=5 "${QUIESCE_CAPTURE_TIMEOUT:-120}" env INNGEST_REARM_MODE=capture "$INNGEST_REARM_CMD" >/dev/null 2>"$cap_err" 200>&-`; on non-zero `logger -t "$LOG_TAG" "INNGEST_QUIESCE_CAPTURE_FAILED rc=$rc stderr_tail=$(_cred_err_tail …)"` (existing 200-char redactor, :1307) + `final_write_state 1 "quiesce_capture_failed"`; then swap today's stop (:2686) → disable (:2689) to `disable` BEFORE `stop`; extend the drift-guard row (`ci-deploy.test.sh:3607-3636`) by extracting `QUIESCE_CAPTURE_TIMEOUT:-120` by shape (exactly one match; add no new `${1:-N}`/`${2:-N}` defaults) and the `#6178` poll-window comment in `scripts/cutover-inngest.sh` — observe GREEN.
- [ ] 1.5 `scripts/cutover-inngest.sh` 2.1: `SOURCE=$(… '.source // "live"')`, `CAPTURED_AT=$(… '.captured_at // ""')`; notice `2.1 capture: Σcaptured=$SIGMA_CAPTURED source=$SOURCE[ captured_at=…]`; no second notice; `cutover-inngest-workflow.test.sh`: two grep rows on the notice/read + a `rearm)` P2-b row reconciling the Σ=0 body.

## Phase 2: D2 — QUIESCED verdict, classifier, watchdog

- [ ] 2.1 `scripts/inngest-liveness-classify.test.sh`: rows `500 + QUIESCED sentinel → inngest_quiesced`, `FATAL embedding QUIESCED → inngest_down (anchored)`, `QUIESCED with trailing text → inngest_quiesced`, `is_restart_family inngest_quiesced → no` — RED; implement the `^inngest-inventory: QUIESCED` arm before the FATAL arm — GREEN.
- [ ] 2.2 `inngest-inventory.test.sh`: seams `INVENTORY_UNIT_ACTIVE`/`INVENTORY_UNIT_ENABLED` plus ≥ 1 row through a PATH `systemctl` stub; rows liveness-only quiesced → QUIESCED exit 1; inactive+enabled → FATAL; full-inventory quiesced → QUIESCED; `failed`+`disabled` → QUIESCED `unit=failed`; `health=200` + shape → DEGRADED — RED; implement: HOIST the `/health` read + `health_code`/`verdict_mode` declarations (:482-489) above the `LIVENESS_ONLY` gate inside the non-array branch (full mode today goes straight to FATAL :496 and 2.2 calls the full-mode hook), then `unit_quiesced()` + the QUIESCED emit (host_id before any variable payload) in both modes — GREEN.
- [ ] 2.3 `inngest-dedicated-host-classify.test.sh`: rows (ii) dispatch `if:` has no `quiesced` and exactly one `||`; (v) every classifier `echo "<mode>"` except `healthy` has a probe `case` arm, ≥ 6 derived (excluding `healthy`), `*)` default still → `probe_unavailable`, the `healthy` `if` branch exists; declaration row (the line carrying `fail_mode=""` at :71 also carries `web_quiesced="no"`; the test never extracts the probe step, so this is structural) — RED.
- [ ] 2.4 `scheduled-inngest-health.yml` probe step: `web_quiesced="no"` on the `fail_mode=""` line (:71); post-loop line is :141; dispatch `if:` :390; `inngest_quiesced) web_quiesced="yes"; echo "::notice::expected after cutover: web scheduler QUIESCED …"; break ;;`; `[[ "$healthy" == "no" && "$web_quiesced" == "no" ]] && record_failure …`; NO new output, NO auto-close change — GREEN; `actionlint` clean.
- [ ] 2.5 `scripts/inngest-restart-age-gate.test.sh`: `resolve_effective_failure_mode inngest_quiesced …` passes through unchanged.

## Phase 3: D3 — every start writer refuses the quiesced shape

- [ ] 3.1 `ci-deploy.test.sh`: arm the four existing restart rows with `MOCK_SYSTEMCTL_ENABLED_STATE=enabled`; new rows `restart refuses a quiesced unit` (reason + no `restart` in the verb log), `restart proceeds on inactive-but-enabled → success`, `restart proceeds on active+disabled → success`, `restart refuses failed+disabled`, `is-enabled=not-found rc 4 → not quiesced`, `deploy inngest _ <tag> refuses when quiesced (inngest_quiesced_deploy_refused, no DOCKER_TRACE:pull)`, `is-enabled=enabled-runtime, inactive → success`; arm `MOCK_SYSTEMCTL_ENABLED_STATE=enabled` on `assert_inngest_docker_trace` rows (:1038/:1079) and #6512(c2) (:4200) — RED.
- [ ] 3.2 `ci-deploy.sh`: `inngest_unit_quiesced()` beside `inngest_unit_enabled()` (:2406) with the stricter-than-verify comment; call in the `restart` handler (`INNGEST_RESTART_REFUSED` logger + `inngest_quiesced_restart_refused`) and at the top of `case "$COMPONENT" in inngest)` (:3362) BEFORE `pull_image_with_fallback` (`inngest_quiesced_deploy_refused`) — GREEN.
- [ ] 3.3 `inngest-wiped-volume-verify.sh`: `abort "quiesced_refused" …` after Gate 2 (:217) and before the throwaway-marker arm (:219) when the shape holds; row in `inngest-wiped-volume-verify.test.sh` asserting no `stop`/`start` in the mock log (Test 1 :126 counts every verb) — RED → GREEN.
- [ ] 3.4 `workspaces-cutover.sh:633-636`: skip the start + `logger … SOLEUR_WORKSPACES_LUKS inngest_start_skipped reason=quiesced` when `is-enabled` prints `disabled`; AND the dead-man `systemd-run … /bin/sh -c` string (:818) — its remount-success arm runs `systemctl start inngest-server.service` unconditionally (C6): guard it inline with the escaped `is-enabled … = disabled ] ||` check; grep rows for both in `tests/scripts/test-workspaces-luks-cutover-gate.sh`.
- [ ] 3.4b Guard 2 row 6c assembly row: `git grep -nE 'systemctl (restart|start) inngest-server' apps/web-platform/infra/` with a NAMED allowlist (`ci-deploy.sh` enable handler :2745, `inngest-bootstrap.sh:1679`, `cloud-init.yml:774` first boot, `cloud-init-inngest.yml` dedicated host) — every other hit must carry the predicate.
- [ ] 3.5 Cross-file parity row: the three delivered scripts (`ci-deploy.sh`, `inngest-inventory.sh`, `inngest-rearm-reminders.sh`) each test the literals `inactive`, `failed`, `disabled`.
- [ ] 3.6 Pollers: `restart-inngest-server.yml` `terminal_fail` print and `deploy-inngest-image.yml:120` gain `case "$REASON" in inngest_quiesced_*_refused) ::error:: … deliberate; only op=rollback re-arms ;;`; both grep rows in `restart-inngest-workflow-guard.test.sh` (no deploy-inngest-image workflow test exists).

## Phase 4: D4 / D5 — 2.2 certifies the shape; text and runbook

- [ ] 4.1 `cutover-inngest-workflow.test.sh`: `render_2_2` modelled on `render_2_0` (:1953) (region `awk '/# ---- 2\.2 QUIESCE HARD GATE/{f=1} f&&/# ---- SEAM: operator maintenance-window steps/{exit} f'`, floor ≥ 35 non-comment lines (38 today), per-call `STUB_SEQ` with a FILE-backed counter under `$TMPD` (curl runs inside `$(…)`), pre-seeded `BASE`/`WEBHOOK_SECRET`/`CF_ACCESS_CLIENT_ID`/`CF_ACCESS_CLIENT_SECRET`/`SOURCE`); rows: 3× QUIESCED → PASSED; 3× FATAL + `SOURCE=live` → UNKNOWN naming `op=quiesce-web`; 3× FATAL + `SOURCE=persisted` → UNKNOWN naming "predates the QUIESCED verdict", not `op=quiesce-web`; `QUIESCED,200,QUIESCED` and `QUIESCED,QUIESCED,200` → STILL RUNNING; 3× `000` → UNREADABLE; `FATAL … not QUIESCED` → UNKNOWN; last-body excerpt printed — RED.
- [ ] 4.2 `scripts/cutover-inngest.sh` 2.2: `quiesced_seen` flag from `grep -qE '^inngest-inventory: QUIESCED' /tmp/exec-inv`; verdict tree per D4; `SOURCE`-branched UNKNOWN remedy + 120-char CR/LF-stripped body excerpt; `op=quiesce-web` secondary confirm prints whether the sentinel was present — GREEN.
- [ ] 4.3 `scripts/cutover-inngest.sh` text: rewrite the 2.2 `::warning::`, the quiesce-web `::warning::`, SEAM 2.2a, the 2.2 PASSED notice, the quiesce-web completion notice, the `verify)` arm caveats (:874, :2131) AND the 2.1/2.2 comment blocks (:1438-1440, :1470-1476, :1491) to the measured state (cattle web-2 has no scheduler; loop drivable; watchdog leaves a quiesced unit alone); `web-2 freeze/recreate` hits are :874, :1440, :1476, :1491, :1535, :1544, :1943, :2131; whole-file grep rows: `KNOWN GAP (#6921)`, `web-2 freeze/recreate`, `self-arms oneshots`, and `auto-restart the web scheduler|currently fails at 2\.1` (the quiesce-web warning :1848) → 0.
- [ ] 4.3b Deploy-status reason table `plugins/soleur/skills/postmerge/references/deploy-status-debugging.md` (near `quiesced` :66): add `quiesce_capture_failed` (remedy: Better Stack `INNGEST_QUIESCE_CAPTURE_FAILED stderr_tail`; if the unit is active but GQL-dead, dispatch `restart-inngest-server.yml` then re-dispatch `op=quiesce-web`), `inngest_quiesced_restart_refused`, `inngest_quiesced_deploy_refused` (only `op=rollback` re-arms). The runbook has no reason table — its prose at :838-841 gets a pointer.
- [ ] 4.4 Runbook `inngest-server.md` (§1 is at :862; rewrite the SUPERSEDED block :895-900 with §1a; §3b command :1042; §op=quiesce-web has no 15-min sentence to drop): §op=quiesce-web (QUIESCED verdict, capture-before-stop, `quiesce_capture_failed`, R8 expected-once tick, `FANOUT: webhook secret unavailable` before re-dispatch), §1 op=execute (2.1 `source`, 2.2 shape verdicts + `SOURCE`-branched UNKNOWN remedy), §1a cattle-web-2 statement, §3b revoke command without `--yes` (+ note `-replace` already revoked), pointer to the postmerge reason table (4.3b), watchdog section (the "expected after cutover" notice; the TRUE post-cutover paging path is the ADR-117 Better Stack consumer heartbeat), window section (2.4 PR pre-staged green before `op=quiesce-web`; announce the window; `502 Dispatch failed` not recoverable by re-arm), §op=rearm late-not-dropped line citing `inngest/inngest@v1.19.4` `pkg/execution/executor/executor.go` (`if evtTs.After(at) { at = evtTs }`), retrieved 2026-09-14.
- [ ] 4.5 FR13: RESOLVED at deepen — FIRES LATE (Inngest v1.19.4 source). Write the runbook line only; no clamp issue, no probe.
- [ ] 4.6 `model.c4` :205, :206 and :210: correct every stale "web-2 retired / single-host" clause to the two-key reality; run `apps/web-platform/test/c4-code-syntax.test.ts`, `c4-render.test.ts`, `plugins/soleur/test/c4-count-parity.test.sh`.

## Phase 5: ADR-100 amendment + follow-up issues

- [ ] 5.1 Direct append (the architecture skill has no amend sub-command): `### Amendment (2026-09-14, Ref #6921/#8077)` to ADR-100 — the quiesced shape as the signal; the start writers (restart, bootstrap deploy arm, wiped-volume verify, luks reconcile + dead-man) and their gates, the two-state `inactive|failed` ∧ `disabled` shape; watchdog non-remediable verdict; 2.2 certification; capture-before-stop; `enable || true` note; replaced-web-1 unit-less note pinned to #7674/#6178; corrected "LB-routed" clause; Alternatives Considered (flip flag, repo-var/Doppler marker, deploy-status slot, auto-close gating).
- [ ] 5.2 File the FR16 follow-up issue(s): `schedule-reminder` connection-refused → `503 + Retry-After`; (no clamp issue — FR13 resolved FIRES LATE). Reference the numbers in the runbook.

## Phase 6: Verification gates

- [ ] 6.1 Each touched suite green: `bash apps/web-platform/infra/{inngest-rearm-reminders,inngest-inventory,ci-deploy,cutover-inngest-workflow,inngest-dedicated-host-classify,inngest-wiped-volume-verify,webhook-doppler-token-reread,restart-inngest-workflow-guard}.test.sh`, `bash scripts/{inngest-liveness-classify,inngest-restart-age-gate}.test.sh`, `bash tests/scripts/test-workspaces-luks-cutover-gate.sh`.
- [ ] 6.2 `python3 scripts/lint-shell-capture-exit.py --baseline scripts/lint-shell-capture-exit.baseline.txt` — no new findings; `actionlint` on the three touched workflows; `python3 scripts/lint-guard-contract.py <plan>`.
- [ ] 6.3 Guard 1–5 first-row RED→GREEN notes + harness must-FAIL flips executed once and reverted (QG1/QG2).
- [ ] 6.4 NFR1: `git diff --stat` touches no `*.tf`, `hooks.json.tmpl`, `vector.toml`.
- [ ] 6.5 Post-merge (self-pulled): `apply-deploy-pipeline-fix.yml` run shows the three scripts delivered with matching sha256; next `scheduled-inngest-health.yml` run log has no `web scheduler QUIESCED` notice and no liveness-derived `failure_mode`; no `cutover-inngest.yml` op dispatched.
