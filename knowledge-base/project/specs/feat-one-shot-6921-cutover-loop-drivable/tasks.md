# Tasks: fix(inngest cutover) — execute → quiesce-web → execute → arm loop drivable end to end

Plan: `knowledge-base/project/plans/2026-09-14-fix-inngest-cutover-loop-drivable-plan.md` (closes #6921, #8077). Every phase is RED → GREEN: write the rows first, observe them red, then implement. No `cutover-inngest.yml` op and no `op=quiesce-web` is dispatched in this work item.

## Phase 0: Preconditions (measured, no writes)

- [ ] 0.1 Confirm `is-active`/`is-enabled` are used without `sudo` in `apps/web-platform/infra/ci-deploy.sh` (`verify_inngest_quiesced`, `inngest_unit_enabled`) — no sudoers change.
- [ ] 0.2 Confirm `hooks.json.tmpl` `include-command-output-in-response-on-error: true` on `inngest-inventory` (:161) and `inngest-liveness` (:181).
- [ ] 0.3 Confirm `vector.toml` Source 4 allowlists `ci-deploy`, `inngest-rearm-reminders`, `inngest-inventory` (:178-182) — no vector change.
- [ ] 0.4 Confirm the `ci-deploy.test.sh` mock `systemctl` default is the quiesced shape (`is-active → inactive`, `is-enabled → disabled`, :125-160) and inventory the four `restart inngest …` rows that must be armed with `MOCK_SYSTEMCTL_ENABLED_STATE=enabled`.
- [ ] 0.5 Confirm the rearm script's capture block (:198-219) reads `ENUMERATE_CMD`/`MODE`/`CAPTURE_FILE` defined AFTER `SECRET=` (:167) — the relocation moves the `SECRET=` block down, never the capture block up.
- [ ] 0.6 Confirm `webhook.service` `User=deploy` (`cloud-init.yml:250`), `ReadWritePaths … -/var/lib/inngest` (:267), `chown deploy:deploy /var/lib/inngest` (`inngest-bootstrap.sh:142-143`).

## Phase 1: D1 / D1b / D1c — capture resume, capture-at-quiesce, Σ=0 re-arm

- [ ] 1.1 `inngest-rearm-reminders.test.sh`: mock `systemctl` on `MOCKBIN` (`MOCK_SYSTEMCTL_ACTIVE`, `MOCK_SYSTEMCTL_ENABLED_STATE`, verb log); rows `test_capture_resumes_persisted_when_quiesced`, `test_capture_quiesced_without_file_fails_with_remedy`, `test_capture_quiesced_corrupt_file_fails`, `test_capture_serving_enumerates_live`, `test_capture_health_down_but_enabled_does_not_resume`, `test_capture_needs_no_secret` (no secret, no `doppler` on PATH), `test_rearm_still_fails_closed_without_secret`, `test_rearm_empty_capture_emits_canonical_line` — observe RED.
- [ ] 1.2 `inngest-rearm-reminders.sh`: `unit_quiesced()` (exact `inactive` + `disabled`); capture branch: quiesced + valid array → `{captured, reminder_ids, capture_file, source:"persisted", captured_at}` + journald `resumed persisted capture …`; quiesced + no/invalid file → exit 1 `nothing to resume from … dispatch op=rollback`; otherwise live enumeration (response unchanged); move the `SECRET=` fail-closed block to just before the records-source section; `count == 0` branch emits `inngest-rearm-reminders: re-armed=0 failed=0 total=0` before `rm -f` — observe GREEN.
- [ ] 1.3 `ci-deploy.test.sh`: stateful mock (`stop` touches `$MOCK_STATE_DIR/stopped`; `is-active` → `inactive` and curl `/health` → non-200 once it exists); mock rearm script (`INNGEST_REARM_CMD` seam, logs `capture`, honours `MOCK_REARM_CAPTURE_FAIL=1`, sleep for the bounded row); rows: active → `capture,disable,stop` + `quiesced`; capture fails → `quiesce_capture_failed`, no `disable`/`stop`, `INNGEST_QUIESCE_CAPTURE_FAILED` logged; inactive → no `capture`, `quiesced`; bounded → `quiesce_capture_failed` — observe RED.
- [ ] 1.4 `ci-deploy.sh` quiesce handler: `is-active == active` gate → `timeout "${QUIESCE_CAPTURE_TIMEOUT:-120}" env INNGEST_REARM_MODE=capture "$INNGEST_REARM_CMD" >/dev/null 2>"$cap_err"`; on failure `logger -t "$LOG_TAG" "INNGEST_QUIESCE_CAPTURE_FAILED rc=… stderr_tail=<300 chars, PAT-scrubbed>"` + `final_write_state 1 "quiesce_capture_failed"`; then `disable` BEFORE `stop`; extend the `QMAX_POLLS×QPOLL_INTERVAL` drift-guard row by 120 s and the `#6178` poll-window comment in `scripts/cutover-inngest.sh` — observe GREEN.
- [ ] 1.5 `scripts/cutover-inngest.sh` 2.1: `SOURCE=$(… '.source // "live"')`, `CAPTURED_AT=$(… '.captured_at // ""')`; notice `2.1 capture: Σcaptured=$SIGMA_CAPTURED source=$SOURCE[ captured_at=…]`; no second notice; `cutover-inngest-workflow.test.sh`: two grep rows on the notice/read + a `rearm)` P2-b row reconciling the Σ=0 body.

## Phase 2: D2 — QUIESCED verdict, classifier, watchdog

- [ ] 2.1 `scripts/inngest-liveness-classify.test.sh`: rows `500 + QUIESCED sentinel → inngest_quiesced`, `FATAL embedding QUIESCED → inngest_down (anchored)`, `QUIESCED with trailing text → inngest_quiesced`, `is_restart_family inngest_quiesced → no` — RED; implement the `^inngest-inventory: QUIESCED` arm before the FATAL arm — GREEN.
- [ ] 2.2 `inngest-inventory.test.sh`: seams `INVENTORY_UNIT_ACTIVE`/`INVENTORY_UNIT_ENABLED`; rows liveness-only quiesced → QUIESCED exit 1; inactive+enabled → FATAL; full-inventory quiesced → QUIESCED; `health=200` + shape → DEGRADED — RED; implement `unit_quiesced()` + the QUIESCED emit (set `verdict_mode=quiesced` before the EXISTING `SOLEUR_INNGEST_LIVENESS_VERDICT` logger line; host_id before any variable payload) in both modes — GREEN.
- [ ] 2.3 `inngest-dedicated-host-classify.test.sh`: rows (ii) dispatch `if:` has no `quiesced` and exactly one `||`; (v) every classifier `echo "<mode>"` except `healthy` has a probe `case` arm, ≥ 7 derived, `*)` default still → `probe_unavailable`, the `healthy` `if` branch exists; `secret_unset`-path row (probe body with `WEBHOOK_SECRET` unset under `set -u` still writes `failure_mode=secret_unset`) — RED.
- [ ] 2.4 `scheduled-inngest-health.yml` probe step: `web_quiesced="no"` on the `fail_mode=""` line (:72); `inngest_quiesced) web_quiesced="yes"; echo "::notice::expected after cutover: web scheduler QUIESCED …"; break ;;`; `[[ "$healthy" == "no" && "$web_quiesced" == "no" ]] && record_failure …`; NO new output, NO auto-close change — GREEN; `actionlint` clean.
- [ ] 2.5 `scripts/inngest-restart-age-gate.test.sh`: `resolve_effective_failure_mode inngest_quiesced …` passes through unchanged.

## Phase 3: D3 — every start writer refuses the quiesced shape

- [ ] 3.1 `ci-deploy.test.sh`: arm the four existing restart rows with `MOCK_SYSTEMCTL_ENABLED_STATE=enabled`; new rows `restart refuses a quiesced unit` (reason + no `restart` in the verb log), `restart proceeds on inactive-but-enabled → success`, `restart proceeds on active+disabled → success`, `deploy inngest _ <tag> refuses when quiesced (inngest_quiesced_deploy_refused, no docker create)`, `is-enabled=enabled-runtime, inactive → success` — RED.
- [ ] 3.2 `ci-deploy.sh`: `inngest_unit_quiesced()` beside `inngest_unit_enabled()` (:2406) with the stricter-than-verify comment; call in the `restart` handler (`INNGEST_RESTART_REFUSED` logger + `inngest_quiesced_restart_refused`) and at the top of `case "$COMPONENT" in inngest)` (`inngest_quiesced_deploy_refused`) — GREEN.
- [ ] 3.3 `inngest-wiped-volume-verify.sh:236`: `abort "quiesced_refused" …` before stop/wipe/start when the shape holds; row in `inngest-wiped-volume-verify.test.sh` — RED → GREEN.
- [ ] 3.4 `workspaces-cutover.sh:633-636`: skip the start + `logger … SOLEUR_WORKSPACES_LUKS inngest_start_skipped reason=quiesced` when `is-enabled` prints `disabled`; grep row in `tests/scripts/test-workspaces-luks-cutover-gate.sh`.
- [ ] 3.5 Cross-file parity row: the three delivered scripts (`ci-deploy.sh`, `inngest-inventory.sh`, `inngest-rearm-reminders.sh`) each test the literal pair `inactive`/`disabled`.
- [ ] 3.6 Pollers: `restart-inngest-server.yml` `terminal_fail` print and `deploy-inngest-image.yml:120` gain `case "$REASON" in inngest_quiesced_*_refused) ::error:: … deliberate; only op=rollback re-arms ;;`; one grep row each (`restart-inngest-workflow-guard.test.sh`, the deploy-inngest workflow test).

## Phase 4: D4 / D5 — 2.2 certifies the shape; text and runbook

- [ ] 4.1 `cutover-inngest-workflow.test.sh`: `render_2_2` (region `awk '/# ---- 2\.2 QUIESCE HARD GATE/{f=1} f&&/# ---- SEAM: operator maintenance-window steps/{exit} f'`, floor > 40 non-comment lines, per-call `STUB_SEQ`, exported `SOURCE`); rows: 3× QUIESCED → PASSED; 3× FATAL + `SOURCE=live` → UNKNOWN naming `op=quiesce-web`; 3× FATAL + `SOURCE=persisted` → UNKNOWN naming "predates the QUIESCED verdict", not `op=quiesce-web`; `QUIESCED,200,QUIESCED` and `QUIESCED,QUIESCED,200` → STILL RUNNING; 3× `000` → UNREADABLE; `FATAL … not QUIESCED` → UNKNOWN; last-body excerpt printed — RED.
- [ ] 4.2 `scripts/cutover-inngest.sh` 2.2: `quiesced_seen` flag from `grep -qE '^inngest-inventory: QUIESCED' /tmp/exec-inv`; verdict tree per D4; `SOURCE`-branched UNKNOWN remedy + 120-char CR/LF-stripped body excerpt; `op=quiesce-web` secondary confirm prints whether the sentinel was present — GREEN.
- [ ] 4.3 `scripts/cutover-inngest.sh` text: rewrite the 2.2 `::warning::`, the quiesce-web `::warning::`, SEAM 2.2a, the 2.2 PASSED notice, the quiesce-web completion notice, the `verify)` arm caveats (:874, :2131) AND the 2.1/2.2 comment blocks (:1438-1440, :1470-1476, :1491) to the measured state (cattle web-2 has no scheduler; loop drivable; watchdog leaves a quiesced unit alone); whole-file grep rows: `KNOWN GAP (#6921)`, `web-2 freeze/recreate`, `self-arms oneshots` → 0.
- [ ] 4.4 Runbook `inngest-server.md`: §op=quiesce-web (QUIESCED verdict, capture-before-stop, `quiesce_capture_failed`, R8 expected-once tick, `FANOUT: webhook secret unavailable` before re-dispatch), §1 op=execute (2.1 `source`, 2.2 shape verdicts + `SOURCE`-branched UNKNOWN remedy), §1a cattle-web-2 statement, §3b revoke command without `--yes` (+ note `-replace` already revoked), deploy-status reason table (`quiesce_capture_failed`, `inngest_quiesced_restart_refused`, `inngest_quiesced_deploy_refused`, `quiesced_refused`), watchdog section (the "expected after cutover" notice; the TRUE post-cutover paging path is the ADR-117 Better Stack consumer heartbeat), window section (2.4 PR pre-staged green before `op=quiesce-web`; announce the window; `502 Dispatch failed` not recoverable by re-arm), §op=rearm late-not-dropped line (FR13 verification cited).
- [ ] 4.5 FR13 verification: Inngest event `ts` past-value semantic via the vendor reference (URL + retrieved date) or a dev-project probe; write the runbook line; if DROPPED, file the clamp issue (FR16) — no re-arm behaviour change.
- [ ] 4.6 `model.c4:206`: correct "var.web_hosts is now single-host" to the two-key reality; run `apps/web-platform/test/c4-code-syntax.test.ts`, `c4-render.test.ts`, `plugins/soleur/test/c4-count-parity.test.sh`.

## Phase 5: ADR-100 amendment + follow-up issues

- [ ] 5.1 `/soleur:architecture`: append `### Amendment (2026-09-14, Ref #6921/#8077)` to ADR-100 — the quiesced shape as the signal; the five writers and their gates; watchdog non-remediable verdict; 2.2 certification; capture-before-stop; `enable || true` note; replaced-web-1 unit-less note pinned to #7674/#6178; corrected "LB-routed" clause; Alternatives Considered (flip flag, repo-var/Doppler marker, deploy-status slot, auto-close gating).
- [ ] 5.2 File the FR16 follow-up issue(s): `schedule-reminder` connection-refused → `503 + Retry-After`; the re-arm clamp only if FR13 yields DROPPED. Reference the numbers in the runbook.

## Phase 6: Verification gates

- [ ] 6.1 Each touched suite green: `bash apps/web-platform/infra/{inngest-rearm-reminders,inngest-inventory,ci-deploy,cutover-inngest-workflow,inngest-dedicated-host-classify,inngest-wiped-volume-verify}.test.sh`, `bash scripts/{inngest-liveness-classify,inngest-restart-age-gate}.test.sh`, `bash tests/scripts/test-workspaces-luks-cutover-gate.sh`.
- [ ] 6.2 `python3 scripts/lint-shell-capture-exit.py --baseline scripts/lint-shell-capture-exit.baseline.txt` — no new findings; `actionlint` on the three touched workflows; `python3 scripts/lint-guard-contract.py <plan>`.
- [ ] 6.3 Guard 1–5 first-row RED→GREEN notes + harness must-FAIL flips executed once and reverted (QG1/QG2).
- [ ] 6.4 NFR1: `git diff --stat` touches no `*.tf`, `hooks.json.tmpl`, `vector.toml`.
- [ ] 6.5 Post-merge (self-pulled): `apply-deploy-pipeline-fix.yml` run shows the three scripts delivered with matching sha256; next `scheduled-inngest-health.yml` run log has no `web scheduler QUIESCED` notice and no liveness-derived `failure_mode`; no `cutover-inngest.yml` op dispatched.
