# Decision Challenges: feat-one-shot-8774 (plan, 2026-09-25)

This is a headless one-shot plan run for #8774, so these taste calls were **not** put to the operator during planning. The plan applied a default for each. `soleur:ship` renders this file into the PR body and files it as an `action-required` issue, so the founder can reverse any default.

Plan: `knowledge-base/project/plans/2026-09-25-chore-blog-posts-target-non-technical-founders-plan.md`.

## Taste decisions (default applied, founder may reverse)

| # | Decision | Default applied | Alternative raised | Source |
|---|---|---|---|---|
| T-1 | Do comparison posts ("Soleur vs X") and developer-query posts ("best Claude Code plugins") get an exemption from the founder register? | **No register exemption.** Comparison posts keep "Soleur vs X" / "X alternative" in the title and meta description for search, but the body is written for the non-technical founder and opens with their problem (the note's `Search` bullet). | Exempt those formats entirely, or move them to programmatic comparison pages (CMO). **CPO F3:** mark the queued developer-query "Create" rows in `seo-refresh-queue.md` (Codex, Replit, Cursor, NanoCorp) for a founder keep-or-drop call instead of letting the cron re-angle them unreviewed. | CMO, growth-strategist, CPO |
| T-2 | Where does technical long-form live now that the blog is written for founders? | **GitHub (the pull request, ADR or README) and the existing docs site.** A post links it once, at the end. No new "engineering notes" section is built. | Build a dedicated home, such as a docs `engineering/` section or GitHub Discussions "Engineering notes", and point HN submissions there (CMO). | CMO |
| T-3 | Does the "beachhead audience" wording on Technical builders, and the engineering-problem thesis, change? | **Unchanged.** Only the blog's reader moves. The note's `Reader` bullet tells posts to use the general thesis and "your AI team". | **User-Challenge (CPO F1, F4).** `business-validation.md` already redefined the beachhead problem-first (2026-03-22, re-validated 2026-06-08). The brand guide and `vision.md` still name Technical builders as the primary beachhead, and after this PR that segment's only channels are HN, GitHub, Discord and docs. Proposed: a positioning revalidation, plus drawing blog topics from founder problems, with PRs used only as proof points. Today, ship Phase 5.5 proposes posts from merged PRs. | copywriter, CPO |
| T-4 | Does the #8548 rework ship in this PR? | **No.** It ships in #8548's own PR after this merges. This PR posts a short, idempotent handoff comment on #8548. It recommends a **re-angle** on the founder's own words, "I lose track of what I decided and what I just forgot", and links the plan's handoff notes, which hold the outline. | Bundle the re-angled post into this PR, or replace the topic entirely. | planner (the user's framing: "rework the #8548 post afterwards") |
| T-5 | Should the cron audit issue carry content-writer's scan residue, so the founder sees when a headless post kept flagged jargon? | **Not in this PR.** The residue reaches only the skill's in-run report. Adding it to the audit issue means editing `cron-content-generator.ts` (STEP 6), which adds a production deploy and an Observability block to a docs-and-plugin PR. | Add one clause to STEP 6 and update the prompt-canary test (CTO P1-a). | CTO |
| T-6 | Add a blog sentence to the brand guide's CaaS-framing paragraph? | **Cut.** The note's `Open` bullet already puts the founder's problem first. | Append "In blog posts, open with the pain-point or memory-first framing; bring in CaaS only after the problem" (copywriter conflict #2). | DHH and simplicity review against copywriter |
