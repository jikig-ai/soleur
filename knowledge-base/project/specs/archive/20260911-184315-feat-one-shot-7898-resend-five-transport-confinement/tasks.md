# Tasks: fix(7898 §2): transport-confine the five Resend-forwarding infra scripts

Plan: `knowledge-base/project/plans/2026-09-11-fix-resend-five-infra-scripts-transport-confinement-plan.md`.
Branch `feat-one-shot-7898-resend-five-transport-confinement`. PR body says `part of #7898`, never `Closes`.

## Phase 1: Setup and RED baseline

- [x] 1.1 `git fetch origin main && git rebase origin/main` (PR #8023 may have merged; its D-baseline hunks are at lines ~41/71, this PR's at 15-26 — clean either way)
- [x] 1.2 Record the RED state: `python3 scripts/lint-shell-trace-credential-refusal.py apps/web-platform/infra/{disk-monitor,resource-monitor,container-restart-monitor,cron-egress-alarm,resend-inbound-bootstrap}.sh` → 22 violations (5 Rule A, 8 transport, 9 destination); paste the summary into `session-state.md`
- [x] 1.3 Record the repo-wide summary line on `origin/main` (`python3 scripts/lint-shell-trace-credential-refusal.py | tail -1`, today `100 baselined (A/B/C), 66 baselined (D)`) AND the baseline file line counts (67 / 103) — both quantities are asserted, minus 5 each
- [x] 1.4 Record main's harness totals: `disk-monitor.test.sh` 11, `resource-monitor.test.sh` 11, `container-restart-monitor.test.sh` 18, `cron-egress-firewall.test.sh` (run it, note N)

## Phase 2: Failing tests first (cq-write-failing-tests-before)

- [x] 2.1 `disk-monitor.test.sh`: extend the `curl` stub — argv inspection BEFORE the `MOCK_CURL_FAIL` branch (and record `curl_args` there too); for argv containing `api.resend.com`/`.sentry.io` append `ARGV_ORDER host=…` to `curl_violations` unless argv[1..6] == `--disable --noproxy * --proto =https -g` (compare `'*'` QUOTED — use a quoted heredoc), `NOPROXY_COUNT n=k` unless exactly one `--noproxy`, increment `curl_checked`; add a PATH-stub `logger` recording argv; rows: WARNING + CRITICAL paths (`curl_violations` absent, `curl_checked ≥ 1`); `MOCK_CURL_FAIL=1` row asserts `logger` argv has `-p user.crit`, `-t disk-monitor`, `SOLEUR_DISK_MONITOR_SEND_FAILED`, `rc=`, and no token equal to the fixture key; `logger`-absent row (PATH without logger → exit per contract, stderr has `logger=absent`). Run → RED.
- [x] 2.2 `resource-monitor.test.sh`: same three rows, host-filtered so the loopback metrics curl is excluded. Run → RED.
- [x] 2.3 `container-restart-monitor.test.sh`: same stub rows with `curl_checked == 2` on alert rows; `MOCK_SENTRY_HOST`/`MOCK_SENTRY_KEY` overrides (default positive fixture `o0000000.ingest.de.sentry.io` / `0123456789abcdef0123456789abcdef`), `sentry_hit()` greps the positive SENTRY host (not `curl_args` wholesale); keep `ingest.example.test`/`pubkey_test` as negatives; rows (a) refused host → no Sentry curl + `…REFUSED channel=sentry reason=host-shape` + Resend payload contains `sentry channel refused` + exit 0 + no `logger` token contains `example.test`; (b) `O0000000.INGEST.DE.SENTRY.IO.` → Sentry curl to the folded host; (c) `…/?x=` and `evil.example%2F.ingest.de.sentry.io` → `reason=host-shape`; (d) `MOCK_SENTRY_KEY=pubkey_test` → `reason=key-shape`; (e) triple UNSET on an alert tick → no Sentry curl, Resend curl fires, exit 0; `logger`-absent row. Run → RED.
- [x] 2.4 `cron-egress-firewall.test.sh`: three static rows over `$ALARM` (`grep -cE "^[[:space:]]*curl --disable --noproxy '\*' --proto '=https' -g"` = 2; `grep -cF 'ingest\\.(de\\.|us\\.)?sentry\\.io$'` = 1; `assert_not_grep '^[[:space:]]*curl -s'`); FOUR labelled exec rows (PATH-shim curl/logger/jq; refused host → marker + one Resend curl carrying the note + exit 0; uppercase trailing-dot host → two curls, first to the folded host; cooldown stamp present → zero curls + `SEND_FAILED channel=resend reason=cooldown` logger argv; triple unset → one Resend curl + exit 0); ONE parity row diffing the predicate-only `# BEGIN sentry-dest-pin (#7898)`…`# END sentry-dest-pin (#7898)` region between `$ALARM` and `container-restart-monitor.sh`, failing if either extraction is empty. Run → RED.
- [x] 2.5 NEW `apps/web-platform/infra/resend-inbound-bootstrap.test.sh` (runs under `env -i PATH=<shims>:$PATH RESEND_API_KEY=re_test_fake_key_123`; the curl shim asserts the `--config` fd in-process and prints PASS/FAIL only): rows (a) newline-in-key → exit 2 + `reason=key-shape` on stderr + shim not invoked; (b) `resend_api GET '/domains/../webhooks'` and `GET '@evil.example/domains'` (function via `sed -n '/^resend_api()/,/^}/p'`) → exit 2 + `reason=path-shape` on stderr; (c) `GET /domains/3f1a2b3c-4d5e-4f60-8a71-b2c3d4e5f607` → four flags first, exactly one `header =` and zero `url =` lines in the fd; (d) `bash -x` → exit 78 + `…_HALT` on stdout, shim not invoked; (e) no call site inside `if`/`||`/`&&`. Register it with one `run: bash apps/web-platform/infra/resend-inbound-bootstrap.test.sh` line in `.github/workflows/infra-validation.yml` › `deploy-script-tests`; `bash scripts/lint-orphan-test-suites.sh` green. Run → RED.
- [x] 2.6 `apps/web-platform/test/infra/vector-pii-scrub.test.sh`: one Source 2 drift row (`[sources.system_journald]` keeps `include_matches.PRIORITY = ["0", "1", "2"]`, the two-unit `exclude_units`, and is in `pii_scrub_drop_userdata.inputs`) — GREEN today (it pins the sink).
- [x] 2.7 Commit the RED harnesses (`test: RED rows for #7898 §2 confinement`)

## Phase 3: Script edits (GREEN)

- [x] 3.1 `disk-monitor.sh`: after `set -euo pipefail` the `case "$-" in *x*)` halt arm (inline: printf marker stdout, printf stderr, `logger -p user.crit -t "$LOG_TAG" … 2>/dev/null || printf 'logger=absent' >&2`, `exit 78`) — note `readonly LOG_TAG="disk-monitor"` must precede it only if Rule A allows; it does NOT (`readonly X=` is a command), so the halt arm uses the literal tag and `LOG_TAG` + `emit_refusal()` are defined right after the `case`; `unset SSLKEYLOGFILE CURL_CA_BUNDLE SSL_CERT_FILE SSL_CERT_DIR CURL_HOME HOSTALIASES LOCALDOMAIN RES_OPTIONS OPENSSL_CONF OPENSSL_MODULES OPENSSL_ENGINES LD_PRELOAD LD_LIBRARY_PATH LD_AUDIT` right after `set -a; . "$ENV_FILE"; set +a`; `curl --disable --noproxy '*' --proto '=https' -g …` in `send_alert()`; `|| { rc=$?; HTTP_CODE="000"; }` and `emit_refusal "SOLEUR_DISK_MONITOR_SEND_FAILED channel=resend http_code=$HTTP_CODE rc=$rc"` in the non-2xx branch. No new `--arg from`.
- [x] 3.2 `resource-monitor.sh`: same edits (`LOG_TAG="resource-monitor"`); leave `sample_active_sessions()`'s loopback curl untouched.
- [x] 3.3 `container-restart-monitor.sh`: halt arm after `set -uo pipefail`; `emit_refusal()` (existing `LOG_TAG`); 14-name `unset` after the source; `export LC_ALL=C`; `# BEGIN sentry-dest-pin (#7898)` … `# END sentry-dest-pin (#7898)` block at the top of `sentry_event()` exactly as the plan's snippet (init `sentry_dest_ok=0; sentry_refuse_reason=""; _si_host=""` BEFORE the `if`; `_si_host` fold; the positive label-grammar regex → else `host-shape`; guarded `^[0-9]+$` → `project-shape`; guarded `^[a-f0-9]{32}$` → `key-shape`); on refusal `emit_refusal "SOLEUR_CONTAINER_RESTART_MONITOR_REFUSED channel=sentry reason=$sentry_refuse_reason"`, set `SENTRY_CHANNEL_NOTE`, `return 0`; Sentry URL uses `${_si_host}`, gains `-w '%{http_code}'` and `emit_refusal … SEND_FAILED channel=sentry http_code=$code` on non-2xx; the `jq … || return 0` early return calls `emit_refusal … reason=jq`; `resend_email()` appends `${SENTRY_CHANNEL_NOTE:+$'\n\n'$SENTRY_CHANNEL_NOTE}` to the body it RECEIVES (never at BODY build time), its unset-key / missing-jq early returns call `emit_refusal … SEND_FAILED channel=resend reason=unset|jq`, the "(Sentry still posted)" wording conditional on the note being empty, `rc` capture + `emit_refusal … SEND_FAILED channel=resend http_code=$http rc=$rc` on non-2xx; four flags on both curls. No new `op:` string.
- [x] 3.4 `cron-egress-alarm.sh`: halt arm after `set -uo pipefail`; `emit_refusal()` (existing `LOG_TAG`); 14-name `unset`; `export LC_ALL=C`; `[[ "$FAILED_UNIT" =~ ^[A-Za-z0-9@._-]+$ ]] || FAILED_UNIT=invalid-unit-name`; Channel 1 rewritten around the byte-identical `BEGIN/END sentry-dest-pin` block (URL uses `${_si_host}`; `-w '%{http_code}'` + `emit_refusal … SEND_FAILED channel=sentry http_code=$code` on non-2xx); email `--arg text` gains the note; cooldown log line conditional on the note being empty; `emit_refusal "SOLEUR_CRON_EGRESS_ALARM_SEND_FAILED channel=resend reason=<cooldown|jq|unset> unit=$FAILED_UNIT"` in the unset/suppressed branch; `rc` capture + `emit_refusal … SEND_FAILED channel=resend http_code=$HTTP_CODE rc=$rc` on POST failure; four flags on both curls.
- [x] 3.5 `resend-inbound-bootstrap.sh`: halt arm after `set -euo pipefail` (no logger); `emit_refusal()` writing stdout + stderr; 14-name `unset`; `export LC_ALL=C`; key-shape check; `resend_api()` path allowlist + four flags on both branches; `--config <(…)` untouched; a comment at the function noting every call site must stay a bare `var="$(resend_api …)"`. AMENDED at work time: the `local`s stay on ONE line — measured, splitting them disarms the Rule D path-pin requirement (session-state.md finding 2).
- [x] 3.6 `bash -n` × 5; per-file linter → `OK: 5 scanned file(s), 0 baselined (A/B/C), 0 baselined (D)`
- [x] 3.7 Run the four harnesses → GREEN with the recorded deltas (+3, +3, +7, +8); the new bootstrap suite 5/5; `bash apps/web-platform/test/infra/vector-pii-scrub.test.sh` green; `cd apps/web-platform && ./node_modules/.bin/vitest run resend-sender-domain sentry-container-restart-alert-op-contract` green
- [x] 3.8 `doppler-injection-bound.test.sh:256`: replace `container-restart-monitor.sh:61` with the content anchor (`container-restart-monitor.sh › the ENV_FILE source`); run that suite
- [x] 3.9 Scratchpad probe, exit codes and markers ONLY pasted into `session-state.md` (never fd contents): `PATH=<no-op logger dir>:$PATH bash -x disk-monitor.sh` with a fixture env file → exit 78, marker, no `RESEND_API_KEY=` in the trace (the bootstrap's equivalents are committed rows in 2.5)

## Phase 4: Baselines and mutation evidence

- [x] 4.1 `git fetch origin main && git rebase origin/main` (immediately before the baseline edit)
- [x] 4.2 Hand-delete the five `apps/web-platform/infra/{container-restart-monitor,cron-egress-alarm,disk-monitor,resend-inbound-bootstrap,resource-monitor}.sh` lines from `scripts/lint-shell-trace-credential-refusal-d.baseline.txt` AND `scripts/lint-shell-trace-credential-refusal.baseline.txt` — never `--write-baseline*`; `arm-heartbeats.sh` / `cutover-verify.sh` stay
- [x] 4.3 Repo-wide linter exits 0; summary = main's summary − 5 on both counts (AC2); `git diff` shows exactly 5 `-` lines and 0 `+` lines per baseline (AC4); classifier diff empty (AC3)
- [x] 4.4 `bash plugins/soleur/test/fixture-relative-assert.test.sh` — green, or regenerate with `--write-baseline` and name the moved row + reason in the commit body
- [x] 4.5 Mutation evidence → `session-state.md`: Guard 2 row 2 (move `--disable` after `-s` → RED), Guard 1 row 4 (delete the `sentry_dest_ok=1` assignment → must-PASS rows RED), and "delete one END sentry-dest-pin marker → parity row RED" are mandatory; run at least one more row per guard
- [ ] 4.6 `bash scripts/lint-shell-trace-credential-refusal.test.sh` results line unchanged; `bash scripts/test-all.sh --shard infra` (or the registered runner) green

## Phase 5: Ship prep

- [ ] 5.1 `decision-challenges.md` already carries entries 1-7 (merge side-effects, §1 correction, close-criterion question, fresh-host window, P9 scope add, two not-adopted/kept taste decisions) — `ship` renders them; verify nothing in the diff contradicts them
- [ ] 5.2 PR body: `part of #7898` (no `Closes`); follow-up section per AC11: (a) Vector Source 4 in-place Non-Goal and why it is not filed; (b) §1 correction (image bake, not `triggers_replace`); (c) the close-criterion question phrased for a non-technical reader with the counts and the recommended default; (d) the measured "no delivery path runs these under `-x`" grep and its empty result; (e) the Better Stack `SOLEUR_*_SEND_FAILED` alert-rule follow-up
- [x] 5.3 Untouched-file check: `git diff --stat origin/main -- scripts/lint-shell-trace-credential-refusal.py apps/web-platform/scripts/sentry-monitors-audit.sh scripts/sentry-alert-live-fidelity.sh apps/web-platform/infra/vector.toml apps/web-platform/infra/server.tf apps/web-platform/infra/cloud-init.yml '*.service'` is empty; the ONLY workflow diff is the one-line registration in `infra-validation.yml`
- [ ] 5.4 Rebase onto `origin/main` again before `gh pr ready`; `git merge-base --is-ancestor origin/main HEAD`

## Phase 6: Post-merge (postmerge — verify by OUTCOME)

- [ ] 6.1 Required checks on the merge commit all `success` (filter `check-runs` by the names in `scripts/required-checks.txt`, not this PR's own set)
- [ ] 6.2 On `main`: repo-wide linter summary = pre-merge summary − 5 on both counts
- [ ] 6.3 `apply-web-platform-infra.yml` latest main run `success`; log shows the three `list-timers …monitor.timer` lines AND `host-egress-ok`, zero `ASSERT-FAILED`
- [ ] 6.4 `web-platform-release.yml` latest main run `success`; `curl -sS --max-time 10 https://app.soleur.ai/health` → 200
- [ ] 6.5 AC15: the MERGED pin accepts the live prd triple — against `origin/main`'s bytes with the host locale: `git fetch origin main && doppler run -p soleur -c prd -- bash -c 'export LC_ALL=C; eval "$(git show origin/main:apps/web-platform/infra/cron-egress-alarm.sh | sed -n "/# BEGIN sentry-dest-pin/,/# END sentry-dest-pin/p")"; echo "sentry_dest_ok=$sentry_dest_ok reason=$sentry_refuse_reason"'` → `sentry_dest_ok=1 reason=` (no values printed, no SSH; same postmerge session)
- [ ] 6.6 AC17 deterministic sink assertions (vector.toml Source 2 PRIORITY set + scrub inputs; `grep -c 'logger -p user.crit -t "$LOG_TAG"'` = 2 per monitor); any live Better Stack read uses `--raw-only` + `jq` and is recorded as informational
- [ ] 6.7 `ship` filed the Better Stack `SOLEUR_*_SEND_FAILED` alert-rule follow-up (SKIPPED rows are a separate class, so the rule cannot page on cooldown/unset) with the user-visible consequence sentence and measured size (AC11(e)); the Vector Source 4 enrollment stays an in-place Non-Goal (not filed)
