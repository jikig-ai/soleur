M1 want=RED rc=1 fails=[Invocation axis (ADR-236) > no unacked referrer to a user-invoked skill;Invocation axis (ADR-236) > ack rows equal the distinct (file, skill) referrer pairs]
M2 want=RED rc=1 fails=[Invocation axis (ADR-236) > no unacked referrer to a user-invoked skill;Invocation axis (ADR-236) > ack rows equal the distinct (file, skill) referrer pairs]
M5 want=RED rc=1 fails=[plugin slash-name uniqueness > skills/trigger-cron/ stays model-invocable (must-stay-invocable pin)]
M6 want=RED rc=1 fails=[Invocation axis (ADR-236) > no stale ack (every ack row still has a hit);Invocation axis (ADR-236) > ack rows equal the distinct (file, skill) referrer pairs]
M7[AGENTS.md] want=RED rc=1 fails=[Invocation axis (ADR-236) > files examined >= 1 for nonexistent/zzz-8290 (always-loaded index);Invocation axis (ADR-236) > scanned file count equals git ls-files over the literal pathspec list]
M7[:(glob)plugins/soleur/skills/**/*.md] want=RED rc=1 fails=[Invocation axis (ADR-236) > files examined >= 150 for nonexistent/zzz-8290 (skill bodies and references);Invocation axis (ADR-236) > scanned file count equals git ls-files over the literal pathspec list;Invocation axis (ADR-236) > no stale ack (every ack row still has a hit);Invocation axis (ADR-236) > ack rows equal the distinct (file, skill) referrer pairs]
M7[:(glob)apps/web-platform/server/inngest/**/*.ts] want=RED rc=1 fails=[Invocation axis (ADR-236) > files examined >= 40 for nonexistent/zzz-8290 (cron agent prompts);Invocation axis (ADR-236) > scanned file count equals git ls-files over the literal pathspec list]
M7[:(glob)knowledge-base/engineering/operations/runbooks/**/*.md] want=RED rc=1 fails=[Invocation axis (ADR-236) > files examined >= 40 for nonexistent/zzz-8290 (runbooks);Invocation axis (ADR-236) > scanned file count equals git ls-files over the literal pathspec list;Invocation axis (ADR-236) > no stale ack (every ack row still has a hit);Invocation axis (ADR-236) > ack rows equal the distinct (file, skill) referrer pairs]
M8 want=RED rc=1 fails=[Skill description budget > cumulative description word count under budget]
M9 want=RED rc=1 fails=[Invocation axis (ADR-236) > no unacked referrer to a user-invoked skill;Invocation axis (ADR-236) > ack rows equal the distinct (file, skill) referrer pairs]
H2 want=RED rc=1 fails=[Invocation axis (ADR-236) > no unacked referrer to a user-invoked skill;Invocation axis (ADR-236) > ack rows equal the distinct (file, skill) referrer pairs]
H3a want=RED rc=1 fails=[Invocation axis (ADR-236) > files examined >= 1 for AGENTS.md (always-loaded index);Invocation axis (ADR-236) > files examined >= 1 for AGENTS.rules.md (always-loaded rule corpus);Invocation axis (ADR-236) > files examined >= 150 for :(glob)plugins/soleur/skills/**/*.md (skill bodies and r;Invocation axis (ADR-236) > files examined >= 40 for :(glob)plugins/soleur/agents/**/*.md (agent prompts);Invocation axis (ADR-236) > files examined >= 3 for :(glob)plugins/soleur/commands/*.md (entry commands);Invocation axis (ADR-236) > files examined >= 2 for :(glob)plugins/soleur/devin/**/*.md (Devin mirrors and ins;Invocation axis (ADR-236) > files examined >= 2 for :(glob)plugins/soleur/codex/**/*.md (Codex mirrors and ins;Invocation axis (ADR-236) > files examined >= 5 for :(glob)plugins/soleur/hooks/** (shipped SessionStart injec;Invocation axis (ADR-236) > files examined >= 6 for :(glob)plugins/soleur/lib/**/*.ts (harness routing text);Invocation axis (ADR-236) > files examined >= 40 for :(glob)apps/web-platform/server/inngest/**/*.ts (cron age;Invocation axis (ADR-236) > files examined >= 40 for :(glob).github/workflows/*.yml (claude-code-action prompt;Invocation axis (ADR-236) > files examined >= 50 for :(glob).claude/hooks/** (hook-injected context);Invocation axis (ADR-236) > files examined >= 40 for :(glob)knowledge-base/engineering/operations/runbooks/**/;Invocation axis (ADR-236) > scanned file count equals git ls-files over the literal pathspec list;Invocation axis (ADR-236) > no stale ack (every ack row still has a hit);Invocation axis (ADR-236) > ack rows equal the distinct (file, skill) referrer pairs]
H3b want=RED rc=1 fails=[Invocation axis (ADR-236) > files examined >= 40 for :(glob)plugins/soleur/agents/engineering/**/*.md (agent p;Invocation axis (ADR-236) > scanned file count equals git ls-files over the literal pathspec list]
H4 want=GREEN rc=0 fails=[]
H5 want=GREEN rc=0 fails=[]
