# Session State

## Plan Phase
- Plan file: knowledge-base/project/plans/2026-09-19-feat-reproduce-bug-red-loop-tagged-instrumentation-bite-proof-plan.md
- Status: complete
- Plan artifact: complete (selector=subagent Session Summary; plan bf18f5268, deepen 0eff413de)

### Errors
None. Two stale premises in the issue text were recorded, not errored on: PR #8284 is still OPEN (not "landed"); `bin/dev` does not exist. One background timer used a hook-blocked full-cmdline process-grep shape and was replaced.

### Decisions
- Peer source pinned at `c55ee46073ed923f86ce59a5eb3b6d895095d1b7` (the HEAD `plugins/soleur/NOTICE` already records); attribution comment verbatim in every distribution file taking peer prose. The README the scaffold emits into a founder's repo is Soleur-authored (zero 8-word shingles with the peer, AC2) and carries no credit — recorded as a user-challenge in `decision-challenges.md` for ship to surface.
- `reproduce-bug` goes 5 → 9 phases with observability kept as Phase 1; Phase 2 red-capable gate is decision-first ("no red-capable command, no Phase 5"); hypotheses carry a Discriminator column; `[DEBUG-<hex4>]` probes are a removable class with the two-class table in new ADR-230; cleanup gate is `git grep -niE --untracked '\[DEBUG-[0-9a-f]{4}\]' -- . ':!knowledge-base/**/*.md'` at three sites plus a CI suite; seam absence routes to `soleur:engineering:review:legacy-code-expert` + the existing `action-required` digest label.
- Bite-proof runs in a detached-HEAD worktree via a shared helper, asserts each rule on its own edge (direct → `no-client-to-server-secret`, one-hop → `-transitive`; depcruise 16.10.4), exits 71/72/73/74, self-cleans on failure/TERM, zero production test seams (fixture-owned stub `depcruise`, three-branch contract); README + pointer are append-once and written LAST (P0 from three reviewers: writing them first re-trips the clean-tree guard → 67).
- Review panel R1–R44 + deepen R45–R72 consolidated; two operator-scope challenges declined (minimise/perf branch stay — in the issue body; ratchets stay in Phase 5, run before the review panel, plus the `PROMOTED_FILES` edit test-design found would otherwise red CI).
- Not closing #8289/#8290/#8292; three deferrals to file at ship (generator.test.sh floors; CI-time bite; mirrored-not-paged `SOLEUR_CONSTRAINT_SCAFFOLD_HALT`).

### Components Invoked
- Skills: soleur:plan, soleur:plan-review (headless, ADR-084 routing), soleur:deepen-plan
- Research: learnings-researcher, repo-research-analyst, functional-discovery, git-history-analyzer
- Domain review: soleur:engineering:cto, soleur:product:cpo; advisor consult (fable tier)
- Plan-review panel: dhh-rails-reviewer, kieran-rails-reviewer, code-simplicity-reviewer, architecture-strategist, spec-flow-analyzer, cto (devex), cpo, cmo
- Deepen: general-purpose ×3, test-design-reviewer, security-sentinel, observability-coverage-reviewer, prompt-engineer
- Lints: lint-guard-contract.py, lint-infra-no-human-steps.py (clean); lefthook pre-commit clean

## Collision Gate
- Step 0a.5 (pre-plan) and post-plan re-probe both clean for #8288: no linked/open PRs, no open PR touching reproduce-bug/test-fix-loop/constraint-scaffold, no duplicate open issue. Draft PR: #8352.

## Work Phase
- Status: complete — commits dcdde71c0 (scaffold + suites + template + dogfood + ratchets), 4407d1e5f (Guard 4 suite), 41087135f (prose + anchoring row), 0a170491a (ADRs + NOTICE), 466b19db5 (tasks/decision-challenges); pushed.
- Tier B fan-out (4 groups, write-only; lead ran gates and committed). Integration fixes by the lead: emit-fix-constraints fixture (three dirs); P1b baseline regenerated (one row moved: constraint-scaffold.sh 10 → 21); plugin-root-anchoring EXPECTED_GATE_REFS + `reproduce-bug -> redact-engine.py`.
- Phase 2 §9 shard gate: **REFUSED by contention** — `--capacity` read CAPACITY_CONTENDED continuously 09:54–10:57 (sibling full gates in feat-one-shot-8279, then feat-one-shot-8361 ×2 and fix-apply-infra-workflow-size); the queued runner was stopped before launching to avoid a concurrent run. Substitute set run green on the committed tree: bite-proof 76/0, parity 10/0, boundary 37/0, generator 3/0, emit-fix-constraints 15/0, debug-probe-residue 3/0 (pop 5168), guard-vacuity-floor 23/0 (ledger 47), fixture-relative-assert 62/0, fixture-dir-operand-assert 71/0, trap-ownership --changed/--check-highwater 0, rule-body-lint OK, skill-body-budget OK, orphan suites none, check-adr-ordinals OK, sync-readme-counts in sync, at-mention footgun OK, harness-parity-tree + components + devin-cloud-mode 1504/0, plugin-component-test (hook) 3114/0, web-platform typecheck (hook) OK, git-lock-marker-telemetry + plugin-root-anchoring 54/0, eslint on the edited test 0. The full battery is the /ship Phase 4 checkpoint (ADR-183); CI's required `test` context runs the three shards on the PR head.
- Task 5.7 on this worktree: `constraint-scaffold: bite-proof pass -> fail(no-client-to-server-secret@direct, no-client-to-server-secret-transitive@via-hop) -> pass depcruise=16.10.4`, rc 0, tree + worktree list unchanged.
- Measured cost: runner pass 16–20 s ×3 + baseline capture + 2 worktree adds = 49.1 s per refresh run.

## Review Phase (PR #8352, base 6e688b13b)
- Classification: code; design-risk no. Deterministic gates before the panel: shellcheck -S warning ×4 clean; semgrep 1.177.0 on the .ts (79 rules, 0 findings).
- Panel, report-only, one spawn: security-sentinel, test-design-reviewer, structural-enumeration (general-purpose), user-impact-reviewer, observability-coverage-reviewer, architecture-strategist, code-quality-analyst, pattern-recognition-specialist, git-history-analyzer (9/9 returned).
- Convergent P1 (6 seats): merge-base 69 sat AFTER the five emits and BEFORE any trap — measured on fixtures without `origin/main`: five artifacts left, next run 66, `--refresh-baseline` re-hits 69; the 68/69 messages were stderr-only. Fixed: base ref (`origin/main`, else `origin/HEAD`) and TMPDIR containment resolved before the first write; cleanup handler armed before the first write and disarmed after `prove_bite`; `die` prints on stdout for every code; `_wt_cleanup` says "interrupted" only on a signal.
- P1 (test-design, SUT defect): `FORCE_COLOR=1` decorates depcruise's `error <rule>:` lines → both anchors match 0 → a correct install reports 72 and rolls back; a real leak reads 71 instead of 74. Fixed: runner exports `NO_COLOR=1` (template + parity-pinned dogfood), scaffold strips ANSI before the anchors; rows S-ansi (stub) + C1-color (real, FORCE_COLOR=1).
- P1 (test-design): the 73 and 71 arms could be deleted with 76/76 green. Fixed: stateful stub (S-73) and reachability-baseline stub (S-71, runner logic); both verified to red under the arm-deletion mutants (m5: 2 reds, m6: 1 red).
- P2 (user-impact/security): runner rc 130 (terminal Ctrl-C reaching node) was dispatched as a 72 verdict; `mktemp -d` failure was a raw `set -e` abort with residue; symlinked `server/` routed probes and the README outside the repo; 66 message gave no killed-install recovery. Fixed: runner propagates 130 as an interrupt, scaffold treats it as such (S-int130); mktemp guarded (S-mktemp); symlinked server/components refused 71 + `realpath -m` containment in append_once (S-symlink-server); 66 names the recovery; 65 names the missing dir; `ver` sanitised; exit 2 in the header matrix; REPO_ROOT realpath-normalised and re-guarded.
- P2 (4 seats): Guard 4 `P2_RE` missed the pino `log.warn({ SOLEUR_X_DEBUG: true })` form (11 of 12 real marker emits), `echo '…'`, `console.info`, format-then-marker printf. Fixed: widened to every call-form the tree uses, env reads excluded per line, 11 in-suite positive controls + 1 negative on a synthesized repo; MIN_CASES 3→6; instrument self-test.
- P2 (security): the prose cleanup grep was cwd-relative (`-- .` from `apps/web-platform` missed probes elsewhere — measured 2 of 4). Fixed: `-- ':/' ':(top,exclude)knowledge-base/**/*.md'` at reproduce-bug ×2, test-fix-loop, ship, ADR-230 (verified from a subdir: 3/3 non-md hits).
- P2 (security/user-impact): redactor ceiling (opaque bearer, non-vendor env names, hostnames, names/phones pass rc 0), screenshots outside the redactor, HAR strip list incomplete and storage unspecified, no mechanical Phase 9 artifact check; `$(mktemp)`/`$BODY` in the Phase 8 fence. Fixed in reproduce-bug: ceiling stated, structural header/query grep before the redactor, posting via the fail-closed `redact-sentinel.sh` shim (anchoring pin updated), screenshot PII rule, complete HAR list + `mktemp -d` outside the repo, Phase 9 + test-fix-loop untracked-artifact grep, Write-tool + literal-path fence.
- P2 (observability): hosted-runner transcript-only deviation recorded only in the plan. Fixed: one sentence in constraint-scaffold SKILL.md citing #8381; plan `alert_route` layer 4 → layers 2/3.
- P3/prose: Phase 8 options 2/3 destinations + headless fail-safe + rung-10 sentence + rungs 1–3 verbs; Phase 5 "checkpoint" → "notice" with the re-rank caveat; Phase 7 `Symptom:` line + honest label tail; Phase 9 boxes transfer to test-fix-loop; "can carry" + sentry-scrub pointer; `$RANDOM` mint; README template "regenerated"/"refreshes the baseline" claims corrected (dogfood regenerated, parity row 7 green); ADR-071 (69 number, hooks ×2, ~45–60 s, half-installed claim now true, review note); ADR-230 (site 1 phase, four sites, scope-outs, call-form list); ship Phase 5.4 executed gate; parity row 7 emitter-normalised, STRIP_EXPR anchored on the assignment form, template name-coupling row, emission census row (MIN_ROWS 10→12); bite-proof pins comment-stripped + arm/disarm window pin; helper self-tests in all three suites; emit-fix-constraints runs to completion against a stub (its old "assert on what a failed run left behind" was the residue class).
- Floors: bite-proof 57/74 → 73/91 (recipe re-derived: 75 above the locate block − 2 root-conditional; +18 real). RED against the pre-fix script (sandbox, GEN re-pointed): 79/14 — the 14 are exactly the new rows and changed pins.
- Green after fixes: bite-proof 93/93 (real depcruise), parity 12/12, debug-probe-residue 6/6, boundary 37/37, emit-fix-constraints 16/16, generator 3/3, guard-vacuity-floor 23/0 (ledger 47), fixture-relative 62/62 (one row moved: constraint-scaffold.sh 21→17, regenerated in this commit), fixture-dir-operand 71/71, trap-tempfile clean, rule-body-lint OK, skill-body-budget OK, components+harness-parity+devin-cloud-mode 1504/0, plugin-root-anchoring 26/26, git-lock-marker-telemetry 28/28, KB index fresh, ADR ordinals OK, shellcheck -S warning clean ×6.
- Not fixed (accepted, recorded): Guard 1's static census cannot see a seventh write site outside the six-name list (the porcelain rows on failure do); Guard 4 P2 is still a listed-form predicate (ADR-230 says so); the `prove_bite` probe set relies on three independent `components` literals (config template, runner argv, script) failing closed on divergence.
- Coverage consult (session model, ≥6 findings): named six absent classes; five verified against the script and closed in the follow-up commit — GNU-only tools (`realpath -m`, `sed -i`+`\x1b`, `1{…d}`) → POSIX spellings (stock macOS is a founder host per preflight); discarded git/depcruise stderr → carried into 68/69; shallow clone named; no run lock → `mkdir` lock with owner pid + S-lock rows (RED on the pre-lock script: 2 reds); suites not sourcing `git-fixture-env.sh` → sourced (fixture-env-adoption ceiling would have tripped at 26 > 25 for the Guard 4 control repo). Sixth (hooks firing inside `worktree add`) verified pre-existing and left: same trust domain as `git checkout`, and `core.hooksPath=/dev/null` would also disable LFS smudge; the captured stderr now names a failing hook. Floors 73/91 → 76/94. fixture-relative row 17 → 19 (same commit).

## QA Phase (step 5.5)
- AC1–AC19 walked with each AC's own command at 16a97c8c6: all PASS. Review-driven amendments to the AC text (recorded, not silently ticked): AC5 `founder checkpoint` → `founder notice` wording; AC7 the D4 command is now top-anchored (`-- ':/' ':(top,exclude)knowledge-base/**/*.md'`) at all three prose sites; AC9 `redact-engine.py` → `redact-sentinel.sh` (fail-closed shim; anchoring pin updated); AC11 shellcheck IS installed here (0.11.0) and is clean; AC12 the runner template + dogfood gained 12 lines each (NO_COLOR + rc 130), zero removed, boundary 37/37, parity row 2; AC14 parity is 12 rows; AC18 cross-ref scan: a sibling draft (#8360, `feat-one-shot-auto-inngest-pin-bump`, authored 11:08) also claims ADR-230 — ours is earlier (09:52) and origin/main is clean; whoever merges second renumbers (ship Phase 5.5 gate + Phase 7 sync).
- Dogfood refresh on this worktree: rc 0 in 76 s (siblings' batteries loading the host), verdict `constraint-scaffold: bite-proof pass -> fail(no-client-to-server-secret@direct, no-client-to-server-secret-transitive@via-hop) -> pass depcruise=16.10.4`, baseline captured against the origin/main merge-base (10 known violations, byte-identical: porcelain empty), lock released, no stale worktree.
- Integration scenario: dry `soleur:reproduce-bug` against #6501 (an OPEN issue whose fix landed in #6998 — the plan's "closed" is wrong) by a subagent under no-post / no-write constraints: Phases 1–9 followable; loop built as rung 3 (real hook + synthetic payload, red on the pre-fix hook via rung 9, green on HEAD); five falsifiable hypotheses with discriminators; founder notice rendered; Phase 9 grep silent from root and from apps/web-platform (positive control: the top anchor reached `.claude/hooks` from the subdir); nothing posted (comment count 1 → 1), worktree porcelain empty. Six prose sentences flagged and fixed inline: Phase 1 exit criterion off-ramp for local-only surfaces; Phase 1 item 4 vs Phase 2 "stop reading code" reconciled (read for the loop, not a theory); rung 3 names the hook form and scratch pointing; Phase 4 green-on-HEAD → rung 8/9; Phase 5 "second rendering"; `CLAUDE_PLUGIN_ROOT` unset → fail-closed per ADR-179. Also: the skill's Phase 2 gate and Phase 7 mechanism bullet are what steered away from the SIGPIPE story; Phase 1 is not — recorded as a compound candidate.
- AC20–AC23 are ship-time (PR body lines, `Closes #8288`, Filed: #8379 #8380 #8381, decision-challenges rendering).
