# Tasks: page on `SOLEUR_*_SEND_FAILED` / `SOLEUR_*_REFUSED` rows (Better Stack Logs alert, #8097)

Plan: `knowledge-base/project/plans/2026-09-12-feat-betterstack-send-failed-alert-rule-plan.md`
Branch: `feat-one-shot-8097-betterstack-send-failed-alert` — issue #8097 (PR body uses `Ref #8097`; closure is the follow-through's verdict).

## Phase 0: Preconditions (verify before any edit)

- [x] 0.1 `gh issue view 8097 --json state` → `OPEN`.
- [x] 0.2 Add `logtail = { source = "BetterStackHQ/logtail", version = "~> 11.2" }` + `provider "logtail" { api_token = var.betterstack_api_token }` to `apps/web-platform/infra/main.tf`; run `cd apps/web-platform/infra && terraform providers lock -platform=linux_amd64 -platform=darwin_arm64`; confirm the new `betterstackhq/logtail` lockfile block (`version = "11.2.0"`, 2 `h1:`) and that every sibling block is byte-unchanged.
- [x] 0.3 `terraform init -backend=false -input=false && terraform validate` green with the exact static `escalation_target` block written (null-sibling ternary, no `dynamic`).
- [x] 0.4 Canonical triplet plan (`export AWS_*` from Doppler `prd_terraform`; `terraform init -input=false`; `doppler run -p soleur -c prd_terraform --name-transformer tf-var -- terraform plan -target=logtail_exploration.monitor_send_failed -target=logtail_exploration_alert.monitor_send_failed`) → exactly `2 to add`. No apply from the workstation.
- [x] 0.4b `curl -s -o /dev/null -w '%{http_code}' -H "Authorization: Bearer $(doppler secrets get BETTERSTACK_API_TOKEN_READONLY -p soleur -c prd_terraform --plain)" https://telemetry.betterstack.com/api/v2/alerts` → `200` (measured 200 at plan time).
- [x] 0.5 ADR ordinal probe across ALL `origin/*` refs (plan §Phase 0) → ADR-218 provisional; re-run at /ship.
- [x] 0.6 Read `plugins/soleur/test/terraform-target-parity.test.ts` (name list + `TEST_FLOOR`), `scripts/lint-orphan-test-suites.test.sh`, `apps/web-platform/infra/web-host-provisioner-parity.test.sh` header, `.github/workflows/infra-validation.yml` `deploy-script-tests` job + `paths:` filters + the `*-mutation.test.sh` registration convention (≈:699); `apps/web-platform/infra/web-host-provisioner-parity{,-mutation}.test.sh` (`FLOOR_RESOURCES = 16`, M1 `swept only 15`); `plugins/soleur/lib/heartbeat-live-reconcile.ts` (`isAllowedHeartbeatsUrl`, `ViolationReason`).

## Phase 1: RED — drift guard first

- [x] 1.1 Write `apps/web-platform/test/infra/betterstack-send-failed-alert.test.sh` per Guard 1 (seams `GUARD_TF`/`GUARD_SCRIPTS_DIR`/`GUARD_SERVER_TF`/`GUARD_VECTOR_TOML`; string-aware comment stripping; predicate anchors after whitespace normalization; needle list parsed as a set == `{_SEND_FAILED, _REFUSED}`; emitters DISCOVERED by `emit_refusal()` definition in `infra/*.sh`, each body contains `logger -p user.crit`; every FAILED/REFUSED literal matches a needle, every `*SKIPPED*`/`*_HALT*` literal matches none; probe line in `server.tf` is `-p user.crit` + needle-matched; `local.vector_prd_source_id` == the `s<id>.` in `vector.toml`'s sink uri; `probe_rev` matches `^"[0-9]+"$`; ≥ 6 distinct-marker floor `SEND_FAILED_ALERT_MIN_CASES`; exactly one non-empty `sql_query`; distinct FAIL string per assertion; `=== Summary: N passed, N failed (N cases) ===`). Run → FAIL.
- [x] 1.1b Write `apps/web-platform/test/infra/betterstack-send-failed-alert-mutation.test.sh` (pristine-copy seams, `cmp` landed-check, unmutated control first, every RED row attributed by FAIL string — 36 rows as landed, H1a RED / H1b GREEN, H2-H5 PASS).
- [x] 1.2 Register BOTH the guard and the battery as explicit steps in `infra-validation.yml`'s `deploy-script-tests` job (next to the provisioner parity guard); add `apps/web-platform/test/infra/**` to both `paths:` filters; `bash scripts/lint-orphan-test-suites.test.sh` green.

## Phase 2: Terraform — alert

- [x] 2.1 `apps/web-platform/infra/betterstack-logs-alerts.tf`: `locals.vector_prd_source_id = "2457081"` (NO `data "logtail_source"` — its `token` attribute is not Sensitive), `locals.monitor_send_failed_sql` (heredoc, the live-probed predicate), `locals.monitor_send_failed_probe_rev = "1"`, `logtail_exploration.monitor_send_failed` (`name`, `chart line_chart`, `query sql_expression`, `variable source`, `team_name = "Your team"`), `logtail_exploration_alert.monitor_send_failed` (`exploration_id`, `name = "soleur-monitor-send-failed-prd"`, threshold `higher_than` 0, `check_period 60`, `query_period 300`, `confirmation_period 0`, `recovery_period 600`, `on_missing_data treat_as_zero`, `paused = false`, `email = true`, `incident_cause` with full GitHub runbook URL, `metadata.runbook`, static `escalation_target` ternary; NO `aggregation_interval`/`series_names*`/`source_variable`). Header comment: purpose line, `# Plan: …` citation, runbook pointer, ADR-218, SKIPPED-exclusion rationale, 5-line "to add another Logs alert" checklist, "changed the SQL? bump the rev".
- [x] 2.2 `.github/workflows/apply-web-platform-infra.yml` main plan allow-list: `-target=logtail_exploration.monitor_send_failed` + `-target=logtail_exploration_alert.monitor_send_failed` after `-target=betteruptime_team_member.ops`.
- [x] 2.3 Guard 1 → GREEN; `bun test plugins/soleur/test/terraform-target-parity.test.ts` green (#5566 coverage).

## Phase 3: Terraform — probe + parity

- [x] 3.1 `server.tf`: `terraform_data.send_failed_alert_probe` after `disk_monitor_install` (same multi-line connection block; `triggers_replace = local.monitor_send_failed_probe_rev`; `lifecycle { precondition { condition = can(regex("^[0-9]+$", local.monitor_send_failed_probe_rev)) … } }`; one `remote-exec` line: `logger -p user.crit -t disk-monitor 'SOLEUR_DISK_MONITOR_SEND_FAILED channel=resend http_code=000 rc=7 synthetic=1 probe_rev=${local.monitor_send_failed_probe_rev}'`).
- [x] 3.2 SSH apply list: `-target=terraform_data.send_failed_alert_probe` after `-target=terraform_data.inngest_consumer_probe_install`.
- [x] 3.3 `web-host-provisioner-parity.test.sh` `FLOOR_RESOURCES` 16 → 17 + header prose; `web-host-provisioner-parity-mutation.test.sh` M1 string `swept only 15` → `swept only 16`; `infra-validation.yml` step name "(#7000 all 15 SSH provisioners)" → 17; `terraform-target-parity.test.ts` needs no edit (`MIN_SSH_PROVISIONED` is `>=`); all three suites green.
- [x] 3.4 `terraform fmt -check -recursive` + `terraform validate` green.
- [x] 3.5 `.github/workflows/scheduled-terraform-drift.yml`: `terraform init -input=false` → `terraform init -input=false -lockfile=readonly`.

## Phase 4: Follow-through, self-health arm, runbook, ADR/C4

- [x] 4.1 `scripts/followthroughs/send-failed-alert-probe-8097.sh`: `set -uo pipefail` (never `-e`), explicit empty-checks on each secret, #7797 xtrace guard (exit 78), `export LC_ALL=C`, sibling header (WHAT IT PROVES / exit table 0-3-5-78 / `Test seam: SEND_FAILED_PROBE_BQ` / `# Observability layer: 6` / `# RETIREMENT:` / `# cq-test-fixtures-synthesized-only`); ordered checks: alerts GET (curl rc≠0 or non-2xx → 3) → `alert_absent`/`alert_paused` (projected `{name,paused,paused_reason}`) → web-1-scoped positive control over the same 14-day hot ∪ archive window (`control_rows_web1`, `control_min_dt`, `control_max_dt`; 0 → `channel_dark`) → readback keyed on `synthetic=1 probe_rev=<rev>` with `<rev>` grepped as `"[0-9]+"` from the checkout (empty → 3), `host` reported → incidents GET matched on `name` OR `cause` anchored on the row's `dt` − 600 s, PROJECTED list only; print `nonsynthetic_rows` and which incident fields were carried; verdict line `SOLEUR_SEND_FAILED_ALERT_PROBE verdict=… detail=…`; exit 0 / 3 / 5 only (never 1).
- [x] 4.2 `scripts/followthroughs/send-failed-alert-probe-8097.test.sh`: 75 assertions across 24 `env -i` cases (pass→0, alert_paused→5 with no warehouse call, alert_absent→5, channel_dark→3, row_absent→5 with the three candidate causes, row_present_no_incident→5, stale incident, host_mismatch reported, nonsynthetic_rows, curl rc≠0→3, HTTP 502→3, BQ transport→3, unset secrets→3, non-digit rev→3, xtrace→78, missing BQ→3). Registered in `scripts/test-all.sh` (orphan-suite lint).
- [x] 4.3 `plugins/soleur/scripts/reconcile-live-heartbeats.ts` + `lib/heartbeat-live-reconcile.ts`: one `logs_alert` arm through the injected `fetchImpl` with a second exact-host constant (`telemetry.betterstack.com`, never a suffix match); `ViolationReason` += `logs-alert-paused | logs-alert-absent` (grep every switch — `cq-union-widening-grep-three-patterns`); MISMATCH line `… live=logs_alert reason=logs-alert-paused|logs-alert-absent detail="<paused_reason>"` sets rc=2 (combine: ERROR 1 > MISMATCH 2 > OK 0); two new cases + a non-allowlisted-host refusal case in `plugins/soleur/test/heartbeat-live-reconcile.test.ts`; decode bullet in `scheduled-terraform-drift.yml`'s issue body.
- [x] 4.4 Runbook `knowledge-base/engineering/operations/runbooks/monitor-send-failed-alert.md` (YAML frontmatter with `applies_to`; `**TL;DR:**`; Alert name / Vendor / Means / Time to page table; step 0 `/soleur:go`; step 1 readback SQL; step 2 incidents via the READONLY-token curl, never the UI; "Verify off-host — no SSH"; decode table; unit table; `synthetic=1` semantics; `host` vs stale `host_name`; auto-resolve ≠ fixed; `_HALT` does not page; re-fire = `probe_rev` bump, one page; `[ack-destroy]`; `paused_reason` + the `reason=logs-alert-paused|logs-alert-absent` mismatch issue; no SSH). `python3 scripts/lint-infra-no-human-steps.py` on it.
- [x] 4.5 `betterstack-log-query.md` §"Standing alarms": native-alert row + provider-gap correction.
- [x] 4.6 ADR-218 via `/soleur:architecture` (decision, routing contract, `treat_as_zero`, `probe_rev` trigger, apply-path verification shape, `_HALT` exclusion + UC-1 pointer, opt-in policy for future PRIORITY-2 classes, one sentence on ADR-198's stale premise + deferral issue for `logtail_source`); ADR-096 amendment sentence; `model.c4` `betterstack` element one clause; C4 suites + `bash plugins/soleur/test/c4-count-parity.test.sh` green; `bash scripts/check-adr-ordinals.sh` green.
- [x] 4.7 File the `logtail_source` deferral issue and the cf-tunnel-ssh-bridge host-key-pinning deferral issue (`wg-when-deferring-a-capability-create-a`); reference the first from ADR-218 and replace the `tracking_issue` placeholder in the plan's Encryption Posture `exception` block with the second.
- [x] 4.8 Issue #8097 body: `<!-- soleur:followthrough script=scripts/followthroughs/send-failed-alert-probe-8097.sh earliest=<merge + 30 min> secrets=BETTERSTACK_QUERY_HOST,BETTERSTACK_QUERY_USERNAME,BETTERSTACK_QUERY_PASSWORD,BETTERSTACK_API_TOKEN -->` + `follow-through` label.

## Phase 5: Verification (pre-merge ACs)

- [x] 5.1 AC1 guard green with `SEND_FAILED_ALERT_MIN_CASES=6`; the registered mutation battery green (36 RED rows by FAIL string, H1a RED, H1b GREEN, H2-H5 PASS).
- [ ] 5.2 AC2-AC8a per plan (`actionlint` on the two touched workflows; `python3 scripts/lint-infra-no-human-steps.py --changed --base origin/main`; `git diff --stat` on the lockfile shows additions only).
- [ ] 5.3 PR body `Ref #8097`; `decision-challenges.md` (UC-1, T-1, T-2) rendered by /ship Phase 6.

## Phase 6: Post-merge (automated)

- [ ] 6.1 AC9: apply run shows both `logtail_*` creations + the probe creation; `GET /api/v2/alerts` (READONLY token) → `paused: false`; next untargeted drift plan `0 to change`.
- [ ] 6.2 AC10: sweeper runs the follow-through after `earliest=` → `verdict=pass` → #8097 closed. Otherwise #8097 stays open with the sweeper's ACTION REQUIRED / CANNOT ESTABLISH comment.

## /work findings (2026-09-13)

- **Five `emit_refusal()` definers, not four.** `resend-inbound-bootstrap.sh` defines `emit_refusal()` without any `logger` call (CI-only, stdout/stderr). Pure definition-discovery would have red on it; the guard discriminates on whether the body calls `logger` (must be `-p user.crit`) and names the non-logger definer in `NON_CRIT_ALLOWLIST` with a reason (stale entries fail). M7 (`logger -t foo`, no `-p`) and the new M10 (no `logger`, not allowlisted) both red; H4 (compliant fifth unit) stays green.
- **`isAllowedHeartbeatsUrl` lives in the script, not the lib.** The second exact-host pin (`telemetry.betterstack.com`) was placed beside it in `reconcile-live-heartbeats.ts`; the pure `reconcileLogsAlerts` / `parseLogsAlertBlocks` and the widened `ViolationReason` union are in the lib.
- **Lockfile:** `terraform providers lock` also added two `darwin_arm64` `h1:` hashes to the pre-existing `integrations/github` block; reverted so siblings stay byte-unchanged (22 additions, one block).
- **ADR-218** authored directly in the ADR-217 shape (Status / Context / Decision / Consequences / Alternatives / References; `check-adr-ordinals.sh` green) rather than through the `/soleur:architecture` interactive prompts.
- **Follow-through `earliest`** set to `2026-09-14T17:30:00Z` (before the sweeper's 18:00 UTC run). If the merge apply lands after that, the first sweep reports `alert_absent` (ACTION REQUIRED comment, issue stays open) and the next daily run re-evaluates — no re-edit strictly required, but /ship may move `earliest` to merge + 30 min.
- **Deferrals filed:** #8124 (`logtail_source` / ADR-198 stale premise), #8125 (SSH host-key pinning on the CF Tunnel bridge). Net issue flow at the filing site: closing 0 / filing 2 / net +2, both plan-mandated (`wg-when-deferring-a-capability-create-a`), neither ≤100 lines inlineable in this PR.
