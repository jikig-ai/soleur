# Tasks: registry-host-replace dispatcher derives the delivered change and its tracker (#8279)

Plan: `knowledge-base/project/plans/2026-09-19-fix-registry-dispatcher-derive-delivery-tracker-plan.md` (deepened 2026-09-19 — this file reflects the deepened plan). No `spec.md` exists for this branch (pipeline entered at `plan`); `lane:` defaulted to `cross-domain` (fail-closed).

## Phase 1: Tests first (RED)

- [x] 1.1 Write `tests/scripts/test-registry-delivery-change.sh` in the shape of `tests/scripts/test-registry-replace-preflight.sh`, with the stub design from plan §3: `$TMP/gh.sh` dispatching on argv in order `*/pulls` → `$STUB_DIR/pulls/<sha>` file, `*compare/*` → `STUB_COMPARE_RC`/`STUB_COMPARE_JSON`, a listing whose argv contains `-f path=` → `STUB_PATH_RC`/`STUB_PATH_JSON`, `commits/<sha>` → `$STUB_DIR/commit/<sha>`, anything else / unfixtured sha → exit 64; every argv appended to a `mktemp` call log; a `$TMP/bin/gh` PATH shim exiting 99; compare fixtures oldest→newest, path-listing fixtures newest→oldest; `assert_shape` (six keys, each once) on every row; values parsed with `sed -n 's/^key=//p'`; `run_sut` with `env -u GITHUB_ACTIONS REGISTRY_DELIVERY_GH_CMD=$TMP/gh.sh`; an inline anti-vacuity floor on the assertion count.
  - [x] 1.1.1 Rows T1–T18 from the plan's `## Test Scenarios` (T1 subject has no `(#N)` suffix and carries `=`, `%`, backticks, `"`, `<!--`, `@octocat`; T2 asserts compare order against an opposite-order path listing; T3 has a decoy `/pulls` for `after`; T7b anchors the suffix; T9 non-numeric `number`; T10 asserts the compare call carried no paging param; T11/T15 assert `prs=` EMPTY on a proven range; T12 rc 1 on the seam; T13 counts `PR #9001` once and shows the newer branch subject; T14 exactly 10 `/pulls` calls and the two oldest PRs absent; T16 control chars removed; T17 path-listing rc≠0; T18 malformed SHA dropped).
  - [x] 1.1.2 Run it against a non-existent helper and confirm every row is RED.
- [x] 1.2 Register the suite in `scripts/test-all.sh` directly under `run_suite "tests/scripts/registry-replace-preflight"` with the shape `run_suite "tests/scripts/registry-delivery-change" bash tests/scripts/test-registry-delivery-change.sh` (the runner does not glob `tests/scripts/`).

## Phase 2: Helper (GREEN)

- [x] 2.1 Write `scripts/registry-delivery-change.sh` (executable, `set -uo pipefail`): args `--repo`, `--after` (required, usage → exit 2), `--before` (may be empty), `--path`; seam `REGISTRY_DELIVERY_GH_CMD` (default `gh`) refused with exit 1 + `::error::` naming it when `GITHUB_ACTIONS` is set; constant `MAX_LOOKUPS=10`; a `resolve_pr_for_sha` function.
  - [x] 2.1.1 Arms 1–7 from plan §1: no watermark → `[after]`/unproven; compare called with NO paging params, rc≠0 or status ∉ {ahead, identical} → `[after]`/unproven with note; `total_commits > 250` → `[after]`/unproven; range ∩ path listing (`gh api -X GET repos/$R/commits -f sha= -f path= -F per_page=100`, static `--jq`, intersection via `--arg`/`grep -Fxf`) → candidates oldest→newest; empty intersection on a proven range (incl. `identical`) → `prs=` EMPTY, summary names `<after7>` and `<before7>`; `resolve_pr_for_sha` = `/pulls` `.[0].number // empty` → anchored `(#N)$` subject fallback (adds `range_note` `PR #N attributed by commit subject, not by the API`) → digit validation → `unattributed`; dedupe preserving order; every SHA validated `^[0-9a-f]{40}$` before any URL path (malformed → dropped + `range_note`).
  - [x] 2.1.2 Output lines: `range=`, `range_note=`, `commits=`, `prs=`, `unattributed=`, `summary=` (one line: first message line, `[\x00-\x1f\x7f]` + U+2028/U+2029 stripped, no cap, no suffix strip, subject of the newest commit attributed to each PR). Exit 0 on every API arm.
  - [x] 2.1.3 Header comment: what it answers, the watermark/range contract, why `identical` is the proven-empty arm, why merge-commit PRs resolve via their branch commits (measured on #6326), why the subject fallback is anchored (the `reusable-release.yml` precedent is not), the seam guard.
- [x] 2.2 Run the suite to GREEN; then run the plan's mutation rows and revert each (each must red the named row).

## Phase 3: Workflow

- [x] 3.1 `.github/workflows/registry-host-replace-dispatch.yml` — `workflow_dispatch.inputs.tracker` (string, optional, default `''`, description per plan §2); move `permissions:` to `jobs.dispatch-replace.permissions:`, add `pull-requests: write` with its WHY comment (token can now approve/edit PRs; PR comment is the only intended use), rewrite the `issues: write` WHY comment to name `gh api … /issues/{n}/comments -X POST` + `gh issue create`.
- [x] 3.2 Gate step: hoist the `gh run list` watermark lookup above the `"$EVENT_NAME" == "workflow_dispatch"` early exit; emit `watermark=${BEFORE_SHA}` as soon as a SHA resolves (before the gate's own compare); manual arm still `deliver=true` unconditionally.
- [x] 3.3 New step `id: change` after the gate: `if: always()`, `continue-on-error: true`, `timeout-minutes: 5`, `env:` `GH_TOKEN`, `BEFORE`, `AFTER`, `TRACKER`; `set -euo pipefail`; runs the helper, `printf '%s\n'`s each `key=value` to `$GITHUB_OUTPUT`, strips a leading `#` from `TRACKER`, digit-validates it (`::warning::` via `gha_esc` + drop otherwise), emits `targets=` = prs ∪ tracker deduplicated, `tee`s the block to stdout AND `$GITHUB_STEP_SUMMARY`, emits `::warning::attribution unproven: …` when `range=unproven`. No in-step fallback block. Define `gha_esc()` here and in the verdict step.
- [x] 3.4 Dispatch step: bind `SUMMARY: ${{ steps.change.outputs.summary }}` via `env:` with `SUMMARY="${SUMMARY:-the cloud-init-registry.yml user_data at ${MERGE_SHA:0:7}}"`; push-arm reason `Delivering the cloud-init-registry.yml user_data merged at ${MERGE_SHA} — ${SUMMARY}. Inert until this replace runs; the store volume is preserved.`; manual arm appends ` — ${SUMMARY}`; replace the `#7556` step-summary sentence with the unmeasured wording from plan §2 (uses `SUMMARY_MD`).
- [x] 3.5 Poll step: add `id: poll`; emit `apply_run=` and `apply_conclusion=` (`<concl>` or `unverified`) on every arm; remove its `gh issue comment 7556`; keep `::error::` + `exit 1` on non-success; reword `#7556 remains the authority`.
- [x] 3.6 Verdict step (`id: verdict`, "Record the delivery verdict on its tracker(s)"): `if: always() && (job.status != 'success' || steps.poll.outputs.apply_conclusion == 'unverified')`; bind the five step outcomes (gate, preflight, dispatch, poll, change), poll outputs, change outputs, `job.status`, `TRACKER` via `env:`; consumer defaults `SUMMARY`/`TARGETS`; `SUMMARY_MD` HTML-escape (`&`, `<`, `>`, `@`) used on every markdown surface; arms in order — cancelled / `Apply FAILED` / `Dispatch FAILED` (cancelled-style "read the apply run list before re-firing") / `Delivery REFUSED by the preflight` / `Delivery UNVERIFIED` / **`else`** `Delivery UNDETERMINED` printing the four outcomes; not-live sentence only when `prs` non-empty, `(attribution unproven: …)` when `range_note` non-empty, the "unchanged since the delivery watermark" sentence when `prs` is empty on a proven range, `(attribution unavailable: derivation step ${CHANGE_OUTCOME})` + `::warning::` when `CHANGE_OUTCOME != success`; `MARKER=` defined once with `KIND` ∈ `cancelled|apply-failed|dispatch-failed|refused|unverified|undetermined`, appended LAST; re-fire instruction with `-f tracker=${FIRST_TARGET}` substituted (echoed to the step summary too); posting loop with sink-side `[[ "$n" =~ ^[0-9]{1,9}$ ]]`, `gh api -X POST "repos/${GITHUB_REPOSITORY}/issues/${n}/comments" -f body="$BODY"`, per-target `::error::`, final `exit 1` if any failed; empty targets → `gh issue create --title "registry-host-replace delivery at ${MERGE_SHA:0:7} needs an owner: ${WHAT}" --body "$BODY" --label domain/engineering --label type/chore --label priority/p1-high --label action-required` (no `SUMMARY` in the title). No comment listing, no pre-create search.
- [x] 3.7 Rewrite the poll/verdict WHY comments that name #7556; add the STEP MAP comment block at the top of `jobs:` (gate → change → preflight → dispatch → poll → verdict); keep the header provenance line.
- [x] 3.8 Append a two-line note under ADR-169's "Amendment 2026-08-16" (`knowledge-base/engineering/architecture/decisions/ADR-169-what-authorizes-destroying-the-sole-pull-path.md`) recording that the standing #7556 verifier became per-delivery: the dispatcher now records verdicts on the delivering change's own tracker and asserts no enrolment it did not measure (#8279). No decision text changes.
- [x] 3.9 Verify every `run:` body parses (AC7 one-liner) and that no `run:`/`if:`/`run-name:` contains `steps.change.outputs` or `inputs.` (AC11a).

## Phase 4: Verification

- [x] 4.1 Run AC1–AC13 (including AC5a–AC5d, AC11a–AC11e) from the plan verbatim; fix anything that does not match.
- [x] 4.2 `bash scripts/alarm-issue-filing-guard.test.sh` (expect 83 scanned, ≤ 11 violations) and `python3 scripts/lint-workflow-issue-write-scope.py` (exit 0).
- [x] 4.3 `bash scripts/test-all.sh` touched shards: `tests/scripts/registry-delivery-change`, `scripts/alarm-issue-filing-guard`, `scripts/lint-workflow-issue-write-scope`, `scripts/lint-workflow-issue-write-scope-live`, `plugins/soleur/test/c4-count-parity`.
- [x] 4.4 Confirm the diff is a subset of the AC13 file list (now including the ADR-169 note).
- [x] 4.5 Run the plan's `discoverability_test.command` (`curl` against the public runs endpoint) and confirm it names the workflow path and a conclusion.

## Phase 5: Ship

- [ ] 5.1 PR body: `Closes #8279`; render `decision-challenges.md` (T1 verdict-helper split, T2 AC trimming) per `ship` Phase 6.
- [ ] 5.2 File the deferral from the plan (`resolve_pr_for_sha` extraction to `scripts/lib/` + adoption in the three callers, noting the anchored-suffix divergence from `reusable-release.yml`) as a `deferred-scope-out` issue if not already tracked.
- [ ] 5.3 Post-merge: read the registration-path dispatcher run per AC14 (`range=proven`, `deliver=false` unless a config-touching PR merged in the range, `success`).
