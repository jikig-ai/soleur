# Session State

## Plan Phase
- Plan file: knowledge-base/project/plans/2026-09-24-fix-external-watchdog-dispatch-cadence-plan.md
- Status: complete
- Plan artifact: complete (selector=branch)

### Errors
- deepen-plan PAT gate false-positive on the Cloudflare `cf_api_token` variable name; reworded, gate passes.
- One sharp-edges reference page was first read from the bare-root mirror instead of the worktree (identical content).

### Decisions
- Trigger: in-process polling loop in the web server on both web hosts; per slot + 30-150 s jitter, dispatches the workflow via an actions:write soleur-only GitHub App token unless the slot already has a run; fail-open to dispatch. Not an Inngest cron (must fire while Inngest is down); lint forbids importing Inngest code. GitHub `schedule:` crons stay as fallback.
- Rejected: Cloudflare Worker (App key custody, hand-minted token; upgrade path tied to #7230), Inngest cron, host timer, Better Stack (PAT / copied secrets), margins-only (~7 h detection).
- Sentry: crontabs unchanged; zot margin 120 -> 30; Inngest stays 15.
- Guards: dispatch-table/workflow/Sentry parity test; tick can never crash the server; WARN-level SOLEUR_WATCHDOG_DISPATCH marker (Vector ships WARN+). ADR-248 + C4 edits ship in the same PR.
- Closes #8495 allowed (merge deploys clock + applies Sentry change); post-merge AC9-AC11 reopen on failure.

### Components Invoked
soleur:plan, soleur:plan-review, soleur:deepen-plan, repo-research-analyst, learnings-researcher, best-practices-researcher, cto, spec-flow-analyzer, dhh/kieran/simplicity reviewers, architecture-strategist, security-sentinel, observability-coverage-reviewer, test-design-reviewer

## Work Phase

### Deviations from the plan
- **ADR ordinal 246 -> 248.** Open PR #8626 already carries ADR-246 and ADR-247; every `ADR-246` in this branch was swept to `ADR-248`.
- **Import boundary enforced by a vitest walker, not `.dependency-cruiser.cjs`.** That file is generated by `soleur:constraint-scaffold` ("Do NOT hand-edit") and its gate is informational/non-blocking. `watchdog-dispatch-clock.test.ts` walks the clock's transitive value imports and fails if anything under `server/inngest/` is reachable; it runs in the required `test` context.
- **C4 `api -> supabase` "every GitHub App-token use" overclaim fixed inline** (scoped to the audited `createGitHubAppClient` factory) instead of filing a separate issue: a one-line prose fix is below the cost-of-filing bar.

### Mutation battery (committed tree 3096652862, pristine-copy restore, all files restored byte-identical)
All 21 rows RED. Guard 1: G1-1 zot margin 120, G1-3 zot eligibility "" (first mutant `"" || "x"` was EQUIVALENT and survived; re-run with a real empty string -> RED), G1-4 no workflow_dispatch, G1-5 cancel-in-progress true, G1-6 pull_request trigger, G1-7 inngest relaxed to */30 consistently (the #8495 pin), G1-8 SOLEUR_HOST_ID in a workflow env. Guard 2: G2-1 inner catch dropped, G2-2 reporter fence dropped, G2-3 inFlight reset only on success, G2-4 no clearTimeout, G2-5 first entry only, G2-6 import from server/inngest. Extra: no late cutoff, jitter redrawn per poll, slot tolerance 0, dedup read fail-closed, raw error forwarded, arm on NODE_ENV only, full-grant token, no unref.
Axes NOT mutated: the default `octokitFor` (Octokit construction) and the real `createProbeOctokit` installation lookup — both only run in production; AC10 (no `op=mint` Sentry event) covers the narrowed-token mint live.

### Exit gate (substitute — `TEST_GROUP=webplat` queued behind 3 sibling worktrees' runs on the advisory lock; killed my own queued run)
Covered commit c2ff8e70ca: 30 vitest files / 608 tests (every `*.test.ts` consumer of a changed file by basename, plus `git-lock-marker-telemetry`, `eslint-config` ratchet, `cron-main-health-monitor`, `c4-code-syntax`) RC=0; 22 consumer shell suites (infra workflow suites, c4 parity/freshness/render, zot/prod-drift scripts) all rc=0; 3 bun files (c4-canonical, c4-from-components, kb-coverage) 90/0. Not run locally: the full webplat/bun/scripts shards and `apps/web-platform/infra/run-registered-suites.sh` — CI's required `test` context + ship Phase 4 own those.
Follow-ups: code-simplicity CONCUR gate DISSENTED on the Worker deferral issue → re-evaluation note on #7230 instead; #8595 overlap comment posted. Net issue flow: Closing 1 / Filing 0.

## Review Phase

Classification: code, design-risk yes. Design pass (code-simplicity + architecture-strategist) then an 11-seat report-only panel pinned to 8d514e636e: security-sentinel, test-design-reviewer, observability-coverage-reviewer, structural-enumeration seat, pattern-recognition, code-quality-analyst, performance-oracle, git-history-analyzer, semgrep-sast (79 rules / 6 files / 0 findings), data-integrity-guardian, agent-native-reviewer; then a coverage consult. All 13 seats returned.

- **Design pass (8d514e636e):** dropped the 60-s slot tolerance + late cutoff, jitter max 150 → 120 s, trimmed timer deps; ADR-248 fleet/canary/rollback claims corrected. ci-deploy.sh deliberately NOT edited (hashed triggers_replace input).
- **P1 (code-quality, measured):** margin budget used a queue sample from OTHER workflows. Re-measured on these workflows' own jobs (job started_at − created_at, last 40 runs): inngest-health median 32 s / p75 635 s / p90 1274 s / max 2272 s; zot median 17 s / p90 1169 s / max 2438 s. Margins → inngest 45, zot 60; parity budget = clock delay + 30-min queue allowance + runtime, ceiling 60.
- **Structural-cause roll-up:** (a) "the dedup read counts runs that could not have covered the slot" (non-main ref, fork/branch pull_request copy, cancelled/startup_failure) — one gap, fixed by filtering main + covering events + executed runs; (b) "each guard's window was narrower than its name" (walker regex vs import forms; SOLEUR_HOST_ID regex/scope; raw-text cron grep) — structural seat enumerated all; (c) "the workflows are not idempotent against a second run in one slot" (search-lag duplicate trackers; stacked restarts) — fixed in the workflows.
- **Coverage consult leads:** (1) no in-slot retry → FIXED (3 attempts, 2 min backoff, re-read first); (2) create-label cross-check → test added; (3) both hosts down during deploy → refuted (no late cutoff; first poll covers the slot).
- **Pre-existing, filed:** #8707 (web-platform-release.yml deploys any dispatched ref) — Mandated-By wg-when-an-audit-identifies-pre-existing.
- **Declined with reason:** clock-only kill switch (plan decision-challenges; runbook states none exists); canary opt-in via ci-deploy.sh (hashed provisioner input); moving mint helper out of _cron-shared (import-walker guard keeps the copies from routing through Inngest; redaction literal pinned by tests).

### Mutation battery 2 (review fixes, committed db425b5084, pristine restore)
26/26 RED: S1 signal, S2 raw dedup-read error, S3 outer fence, S4 poll guard, S5 SIGTERM stop, S6/S6b late settle, S7 job-level cancel, S8 quoted host id, F1–F9 (branch, event, conclusion filters; monotonic slot; interval validation; installation cache + drop; op in error name; tags), W1–W5 (lookup back to --search; title filter dropped; restart gate removed; dedup fn event filter; fail-open), M1 margin 15, G1–G2 walker (require; unresolved). Retry: 3/3 RED (no retry; unbounded retry; no backoff). Create-label cross-check: RED when a create label diverges.

### Ship phase (2026-09-24)

- Pre-ship advisor consult (advisory) found two real gaps, both fixed inline: (1) the margin budget
  omitted the in-slot retry path (clock worst case 12 min, not 4) → inngest margin 45 → 50, parity
  test derives and pins `CLOCK_DELAY_MINUTES`; (2) the restart dedup left `RESTART_OK=true` comments
  claiming "Restart re-dispatched" → restart step emits `restart_dispatch`, claims guarded. AC11
  threshold loosened 17 → 26 min. Declined: a 60-min cap on non-completed restart runs (the 45-min
  give-up window already bounds the restart path); the zot-margin-without-clock concern (the App
  installation already grants `actions: write` — `cron-expenses-verify-by.ts` dispatches with it).
