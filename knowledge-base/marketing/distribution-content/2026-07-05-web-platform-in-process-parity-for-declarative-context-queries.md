---
title: "Your brand context now auto-loads in the web AI agent, not just the CLI"
type: feature-launch
publish_date: ""
channels: x, bluesky
status: draft
pr_reference: "#6057"
issue_reference: "#6046"
---

<!-- To publish: set BOTH publish_date AND status: scheduled -->

## X/Twitter Thread

Just shipped: the AI design agent on the web now auto-loads your brand guide into context the moment you start a design task — the same automatic context our CLI agent already had. Fewer off-brand results, zero manual setup.

2/ Why it matters: a design skill should behave the same everywhere you run it. Now your committed brand context loads itself on both surfaces, so the agent starts from your own rules instead of generic defaults.

## Bluesky

Just shipped: our web AI design agent now auto-loads your brand guide into context the moment a design task starts — the same automatic context our CLI already had. Consistent, on-brand output with zero manual setup, everywhere you work.
